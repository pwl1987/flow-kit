---
phase: 02-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-kit/commands/M-health.md
  - flow-kit/commands/I-intel-scan.md
  - flow-kit/commands/update-context.md
  - flow-kit/commands/sync-team-config.md
autonomous: true
requirements:
  - CMD-40
  - CMD-41
  - CMD-42
  - CMD-43
must_haves:
  truths:
    - All 4 lateral commands are /flow-kit:X style slash commands
    - GO.md routes all commands correctly
    - Each command file is self-contained markdown
  artifacts:
    - path: flow-kit/commands/M-health.md
      contains: M-health code health scan
    - path: flow-kit/commands/I-intel-scan.md
      contains: I-intel-scan technology stack/TODO/FIXME scan
    - path: flow-kit/commands/update-context.md
      contains: update-context incremental CONTEXT.md update
    - path: flow-kit/commands/sync-team-config.md
      contains: sync-team-config team configuration sync
  key_links:
    - from: GO.md
      to: commands/*.md
      via: levenshtein routing to command files
---

<objective>
Create all 4 lateral command files under commands/. Commands follow /flow-kit:X format per G3.
</objective>

<execution_context>
@flow-kit/GO.md
</execution_context>

<context>
Per G3: /flow-kit:health style commands. GO.md handles routing via levenshtein distance (already implemented in Phase 1).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create M-health.md and I-intel-scan.md</name>
  <files>
    flow-kit/commands/M-health.md
    flow-kit/commands/I-intel-scan.md
  </files>
  <read_first>flow-kit/GO.md</read_first>
  <action>
Create M-health.md (CMD-40):
- Command: /flow-kit:health
- Purpose: Code health scan - detect tech stack, code quality indicators
- WHAT IT SCANS:
  - package.json, go.mod, pom.xml, Cargo.toml (detect stack)
  - test coverage percentage
  - lint errors count
  - dependency vulnerabilities
- OUTPUT: Health report with scores (0-100) per category
- USAGE: @flow-kit/commands/M-health.md

Create I-intel-scan.md (CMD-41):
- Command: /flow-kit:scan
- Purpose: Technology stack, TODO, FIXME, hack detection
- WHAT IT SCANS:
  - Stack detection (exact same as M-health)
  - TODO/FIXME/HACK comments count and locations
  - Large files (>1000 lines)
  - Circular dependencies
- OUTPUT: Intel report listing findings per category
- USAGE: @flow-kit/commands/I-intel-scan.md
  </action>
  <verify>
    <automated>grep -c "tech stack\|coverage\|vulnerabilities" flow-kit/commands/M-health.md && grep -c "TODO\|FIXME\|hack" flow-kit/commands/I-intel-scan.md</automated>
  </verify>
  <acceptance_criteria>
    - M-health.md: Health categories defined, /flow-kit:health command specified
    - I-intel-scan.md: TODO/FIXME detection included, /flow-kit:scan command specified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create update-context.md and sync-team-config.md</name>
  <files>
    flow-kit/commands/update-context.md
    flow-kit/commands/sync-team-config.md
  </files>
  <read_first>flow-kit/GO.md</read_first>
  <action>
Create update-context.md (CMD-42):
- Command: /flow-kit:update-context
- Purpose: Incrementally update .planning/CONTEXT.md with new decisions, blockers, state changes
- INPUT: Current phase, what changed
- PROCESS:
  1. Read existing CONTEXT.md
  2. Merge new information (decisions append, blockers update)
  3. Preserve existing structure
  4. Write back to CONTEXT.md
- OUTPUT: Updated CONTEXT.md with change log entry
- USAGE: @flow-kit/commands/update-context.md

Create sync-team-config.md (CMD-43):
- Command: /flow-kit:sync-team
- Purpose: Sync team configuration across team members
- WHAT IT SYNCES:
  - .flow-kit/user-config.md
  - team conventions (code style, review process)
  - shared decision log
- INPUT: Team config source (git URL or local path)
- PROCESS:
  1. Fetch latest team config
  2. Merge with local user-config.md (user config wins on conflicts)
  3. Validate merged config
- OUTPUT: Merged team configuration
- USAGE: @flow-kit/commands/sync-team-config.md
  </action>
  <verify>
    <automated>grep -c "CONTEXT\|decisions\|blockers" flow-kit/commands/update-context.md && grep -c "team\|sync\|user-config" flow-kit/commands/sync-team-config.md</automated>
  </verify>
  <acceptance_criteria>
    - update-context.md: Merge logic for CONTEXT.md defined
    - sync-team-config.md: Team config sync process described
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- All 4 command files exist under commands/
- Each command has /flow-kit:X format documented
- GO.md routing handles command invocation
</success_criteria>

<output>
After completion, create .planning/phases/02-guardrails/02-03-SUMMARY.md
</output>