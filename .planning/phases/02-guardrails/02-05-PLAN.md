---
phase: 02-guardrails
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-kit/config/constitution.md
  - flow-kit/config/default-user-config.md
autonomous: true
requirements:
  - CFG-80
  - CFG-81
must_haves:
  truths:
    - Constitution.md is highest priority (safety rules CANNOT be overridden)
    - User-config wins on non-safety conflicts
    - Priority order: Constitution > user-config > defaults
  artifacts:
    - path: flow-kit/config/constitution.md
      contains: Safety rules that cannot be overridden
    - path: flow-kit/config/default-user-config.md
      contains: Default configuration with override mechanism
  key_links:
    - from: constitution.md
      to: default-user-config.md
      via: Priority order enforcement
    - from: GO.md
      to: constitution.md
      via: Loaded as first priority
---

<objective>
Create constitution.md (safety floor) and default-user-config.md (defaults). Priority: Constitution > user-config > defaults per G5.
</objective>

<execution_context>
@flow-kit/GO.md
</execution_context>

<context>
Per G5: Constitution > user-config > defaults. Safety rules CANNOT be overridden by user-config.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constitution.md (safety floor)</name>
  <files>flow-kit/config/constitution.md</files>
  <read_first>flow-kit/GO.md</read_first>
  <action>
Create constitution.md (CFG-80):
- Header with CLAUDE CODE INSTRUCTION - SAFETY FLOOR, CANNOT BE OVERRIDDEN
- UNOVERRIDABLE RULES section (safety-critical):
  Security:
  - Never commit secrets/credentials to repository
  - Always validate external input
  - Never disable security checks for convenience
  Data Integrity:
  - Never delete data without backup
  - Always use transactions for multi-step DB changes
  - Verify delete impact before execution
  Deployment:
  - Never deploy without rollback plan
  - Production credentials never in code
  - Deployment requires review approval
- OVERRIDABLE RULES section (defaults that user-config can override):
  - Workflow phases, review depth, test coverage thresholds
- Priority statement: Constitution rules CANNOT be overridden by user-config or defaults
- Format: Clear rule name, brief description, category (security/data/deployment)
  </action>
  <verify>
    <automated>grep -c "security\|data\|deployment" flow-kit/config/constitution.md && grep -c "CANNOT\|overrid" flow-kit/config/constitution.md</automated>
  </verify>
  <acceptance_criteria>
    - constitution.md: At least 3 rules per category (security, data integrity, deployment)
    - Safety rules explicitly marked as unoverridable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create default-user-config.md</name>
  <files>flow-kit/config/default-user-config.md</files>
  <read_first>flow-kit/config/constitution.md</read_first>
  <action>
Create default-user-config.md (CFG-81):
- Header with CLAUDE CODE INSTRUCTION - User can override via .flow-kit/user-config.md
- USER CONFIGURATION section:
  Workflow:
  - default_phase: 1-requirement
  - parallel_tasks_max: 4
  - context_budget_warning: 70%
  - auto_checkpoint_threshold: 5 file changes
  Team:
  - review_required: true
  - min_reviewers: 1
  - commit_style: conventional
  - branch_prefix: feat/
  Linting:
  - lint_on_save: true
  - lint_tools: [eslint, prettier]
  - auto_fix: true
- Override mechanism: User creates .flow-kit/user-config.md in project root
- Merge rules: user-config wins on conflict, constitution rules are ignored if overridden (enforcement at GO.md load)
- Reference: .flow-kit/user-config.md template
  </action>
  <verify>
    <automated>grep -c "workflow\|team\|lint" flow-kit/config/default-user-config.md && grep -c "override\|user-config" flow-kit/config/default-user-config.md</automated>
  </verify>
  <acceptance_criteria>
    - default-user-config.md: Workflow, team, and linting sections defined
    - Override mechanism clearly documented
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- Both config files exist
- constitution.md safety rules cannot be overridden
- user-config can override non-safety settings
- Priority order enforced in GO.md
</success_criteria>

<output>
After completion, create .planning/phases/02-guardrails/02-05-SUMMARY.md
</output>