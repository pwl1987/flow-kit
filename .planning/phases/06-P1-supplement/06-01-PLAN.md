---
phase: "06-P1-supplement"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified:
  - "flow-kit/phases/1-requirement/1-requirement.md"
  - "flow-kit/phases/2-design/2-design.md"
  - "flow-kit/phases/3-task/3-task.md"
  - "flow-kit/phases/4-dev/4-dev.md"
  - "flow-kit/phases/5-test/5-test.md"
  - "flow-kit/phases/6-review/6-review.md"
  - "flow-kit/GO.md"
autonomous: true
requirements:
  - "REQ-005"
  - "REQ-007"
  - "REQ-009"
user_setup: []

must_haves:
  truths:
    - "phases 文件可读取 .flow-kit/project-type 判断棕地/绿地"
    - "phases 文件支持混合模式：存在标记文件则直接读取，否则触发检测"
    - "GO.md 启动时显示 project-type 检测结果摘要和推荐 actions"
    - "GO.md 支持命令行覆盖 /flow-kit:project-type [type]"
  artifacts:
    - path: "flow-kit/.flow-kit/project-type"
      provides: "项目类型标记文件"
      contains: "brownfield|greenfield|light-brownfield"
    - path: "flow-kit/GO.md"
      provides: "智能路由入口"
      contains: "project-type detection summary"
  key_links:
    - from: "flow-kit/GO.md"
      to: ".flow-kit/project-type"
      via: "read file on startup"
      pattern: "project-type.*brownfield"
    - from: "phases files"
      to: ".flow-kit/project-type"
      via: "混合模式检测"
      pattern: "project-type.*exist.*read"
---

<objective>
实现 phases 文件棕地/绿地分支增强和 GO.md 智能路由增强。

Area A: phases 文件混合模式（检查 .flow-kit/project-type 存在则直接读取，否则触发检测）
Area C: GO.md 启动时显示 project-type 检测结果 + 推荐 actions + 快捷命令提示
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@flow-kit/GO.md
@flow-kit/phases/6-review/6-review.md
@.planning/phases/06-P1-supplement/06-CONTEXT.md
@.planning/REQUIREMENTS.md

## Key Interfaces

### .flow-kit/project-type 文件格式
```
brownfield
```
或
```
greenfield
```

### D-07 检测信号（来自 Phase 5）
- 文件指纹（package.json 存在 + lock 文件）
- Git remote（是否关联 GitHub/GitLab）
- 业务代码行数（src/ 目录 LOC）
- 绿地判定（无 git remote + LOC < 5000）

### D-08 混合模式逻辑
```
if (.flow-kit/project-type exists):
  read project-type
else:
  run detection → create .flow-kit/project-type
```

### D-11 GO.md 启动输出格式
```
[Project Type Detection]
  Type: {brownfield|greenfield}
  Confidence: {high|medium|low}
  Recommended Actions:
    - /flow-kit:guardrails (if brownfield)
    - /flow-kit:skill:code-review (if P0)
  Override: /flow-kit:project-type [type]
```

### D-12 覆盖机制
- 命令行覆盖: `/flow-kit:project-type brownfield`
- 配置文件: `.flow-kit/user-config.md` 中的 project-type 设置
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 phases 文件棕地/绿地混合模式</name>
  <files>
    - "flow-kit/phases/1-requirement/1-requirement.md"
    - "flow-kit/phases/2-design/2-design.md"
    - "flow-kit/phases/3-task/3-task.md"
    - "flow-kit/phases/4-dev/4-dev.md"
    - "flow-kit/phases/5-test/5-test.md"
    - "flow-kit/phases/6-review/6-review.md"
  </files>
  <action>
为每个 phase 文件添加棕地/绿地混合模式检测逻辑。

## 读取 first
读取以下文件获取当前内容：
- flow-kit/phases/1-requirement/1-requirement.md
- flow-kit/phases/2-design/2-design.md
- flow-kit/phases/3-task/3-task.md
- flow-kit/phases/4-dev/4-dev.md
- flow-kit/phases/5-test/5-test.md
- flow-kit/phases/6-review/6-review.md

## 实现

在每个 phase 文件的 ## 核心行为 或开头添加：

```markdown
## 项目类型检测（Phase 6 增强）

### 混合模式检测逻辑
1. 检查 `.flow-kit/project-type` 是否存在
2. 若存在：直接读取项目类型
3. 若不存在：使用以下检测信号判断

### 检测信号（D-07）
| 信号 | 棕地指标 | 绿地指标 |
|------|----------|----------|
| 文件指纹 | package.json + lock 文件存在 | 缺少锁文件 |
| Git Remote | 关联 GitHub/GitLab | 无 remote |
| 业务代码行数 | src/ 目录存在且 > 5000 LOC | LOC < 5000 |
| 历史 | 有 git history | 新项目 |

### Guardrails 联动
- 棕地项目：提示 `建议使用 /flow-kit:guardrails 启用棕地护栏`
- 绿地项目：提示 `建议使用标准开发流程`
```

## 注意事项
- 使用 Edit 工具修改每个文件
- 保持原有内容完整，仅在开头或核心行为章节前插入新增内容
- 不修改现有的模板占位符（{{}}）
</action>
  <verify>
    <automated>grep -c "project-type\|混合模式" flow-kit/phases/1-requirement/1-requirement.md flow-kit/phases/2-design/2-design.md flow-kit/phases/3-task/3-task.md flow-kit/phases/4-dev/4-dev.md flow-kit/phases/5-test/5-test.md flow-kit/phases/6-review/6-review.md</automated>
  </verify>
  <done>
每个 phase 文件包含：
- 混合模式检测逻辑（检查 .flow-kit/project-type）
- 4 个检测信号说明
- Guardrails 联动提示（棕地/绿地分支）
</done>
</task>

<task type="auto">
  <name>Task 2: GO.md 智能路由增强</name>
  <files>
    - "flow-kit/GO.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/GO.md 获取当前内容。

## 实现

### 1. 添加项目类型检测摘要显示（D-11）

在 GO.md 的 "## 启动检查" 之后添加：

```markdown
## 项目类型检测（启动时自动执行）

### 检测逻辑
读取 `.flow-kit/project-type` 文件，若不存在则提示：
  [INFO] 项目类型未检测，运行 `/flow-kit:project-type detect` 进行检测

### 检测结果输出格式
当检测到项目类型后，显示：

```
[Project Type Detection]
  Type: {brownfield|greenfield|light-brownfield}
  Confidence: {high|medium|low}
  
  Recommended Actions:
    - /flow-kit:guardrails  (棕地项目启用护栏)
    - /flow-kit:skill:code-review  (P0 变更使用深度模板)
    - /flow-kit:skill:verification  (Phase 4-5 使用全量验证)
  
  Quick Commands:
    - /flow-kit:project-type detect  (重新检测)
    - /flow-kit:project-type brownfield  (手动设为棕地)
    - /flow-kit:project-type greenfield  (手动设为绿地)
```
```

### 2. 添加 project-type 命令路由（D-12）

在 "## Available Commands" 表格中添加：

| `/flow-kit:project-type` | `@flow-kit/commands/project-type.md` |
| `/flow-kit:project-type detect` | 检测并显示项目类型 |
| `/flow-kit:project-type [type]` | 手动覆盖项目类型 |

### 3. 创建 project-type 命令文件

创建 `flow-kit/commands/project-type.md`：

```markdown
# Command: /flow-kit:project-type

## 功能
- 检测并显示项目类型
- 手动覆盖项目类型

## 用法
- `/flow-kit:project-type` — 显示当前项目类型
- `/flow-kit:project-type detect` — 重新检测项目类型
- `/flow-kit:project-type brownfield` — 设为棕地
- `/flow-kit:project-type greenfield` — 设为绿地

## 检测信号
| 信号 | 棕地 | 绿地 |
|------|------|------|
| package.json + lock | 有 | 无 |
| Git remote | 有 | 无 |
| src/ LOC | > 5000 | < 5000 |

## 输出
- 写入 `.flow-kit/project-type` 文件
- 彩色输出检测结果摘要
```

## 注意事项
- 保持 GO.md 的原有结构和优先级加载规则
- 仅添加新内容，不修改现有命令路由逻辑
- project-type.md 为新文件
</action>
  <verify>
    <automated>grep -c "Project Type Detection\|project-type\|Recommended Actions" flow-kit/GO.md && test -f flow-kit/commands/project-type.md</automated>
  </verify>
  <done>
GO.md 包含：
- 项目类型检测摘要显示（启动时）
- Recommended Actions 列表
- Quick Commands 提示
- project-type.md 命令文件存在
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GO.md | 用户输入命令，GO.md 路由到对应 handler |
| GO.md → .flow-kit/project-type | 读取项目类型标记文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | .flow-kit/project-type | mitigate | 文件由 flow-kit 管理，用户通过命令修改，无直接注入风险 |
| T-06-02 | Denial | project-type 检测 | accept | 检测失败时提示用户手动指定，不阻止执行 |
| T-06-03 | Information Disclosure | 检测结果显示路径 | accept | 仅显示项目类型，不含敏感信息 |
</threat_model>

<verification>
1. `grep -c "混合模式\|project-type" flow-kit/phases/*/*.md` 返回 >= 6（6 个 phase 文件都有）
2. `grep "Project Type Detection" flow-kit/GO.md` 返回非空
3. `test -f flow-kit/commands/project-type.md` 成立
</verification>

<success_criteria>
- [x] 6 个 phase 文件都包含棕地/绿地混合模式检测逻辑
- [x] Guardrails 联动提示（棕地提示使用 /flow-kit:guardrails）
- [x] GO.md 启动时显示项目类型检测结果摘要
- [x] GO.md 支持 /flow-kit:project-type [type] 命令覆盖
- [x] project-type.md 命令文件创建
- [x] REQ-005, REQ-007, REQ-009 覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/06-P1-supplement/06-01-SUMMARY.md`
</output>
