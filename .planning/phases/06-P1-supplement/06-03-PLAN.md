---
phase: "06-P1-supplement"
plan: "03"
type: "execute"
wave: "3"
depends_on: []
files_modified:
  - "flow-kit/skills/code-review.md"
  - "flow-kit/skills/task-master.md"
  - "flow-kit/skills/subagent-execution.md"
  - "flow-kit/skills/parallel-dispatch.md"
  - "flow-kit/skills/requirement-clarify.md"
autonomous: true
requirements:
  - "REQ-008"
user_setup: []

must_haves:
  truths:
    - "code-review.md 包含三种深度模板（简化/标准/深度）和棕地专用模板"
    - "code-review.md 每个检查项有自动化命令，区分阻断项/警告项"
    - "task-master.md 包含任务依赖关系图和 checkpoint 联动"
    - "subagent-execution.md 包含动态并发控制（80% 降为 1，90% 强制串行）"
    - "parallel-dispatch.md 包含任务分组和优先级调度策略"
  artifacts:
    - path: "flow-kit/skills/code-review.md"
      provides: "增强版 code review"
      contains: "简化模板\|标准模板\|深度模板"
    - path: "flow-kit/skills/task-master.md"
      provides: "任务依赖图 + checkpoint 联动"
      contains: "依赖关系图\|blocked.*断点续跑"
    - path: "flow-kit/skills/subagent-execution.md"
      provides: "动态并发 + 执行日志"
      contains: "80%.*1.*并发\|90%.*串行"
    - path: "flow-kit/skills/parallel-dispatch.md"
      provides: "任务分组 + 优先级调度 + 监控仪表盘"
      contains: "分组策略\|优先级调度\|监控仪表盘"
    - path: "flow-kit/skills/requirement-clarify.md"
      provides: "多需求排序 + 验收标准自动生成"
      contains: "MoSCoW\|Kano\|验收标准.*自动"
  key_links:
    - from: "task-master.md"
      to: "subagent-execution.md"
      via: "依赖图 → 调度策略"
      pattern: "依赖图.*联动"
    - from: "subagent-execution.md"
      to: "verification.md"
      via: "执行日志 → 成本报告"
      pattern: "执行日志.*归档"
---

<objective>
充实 Area D 剩余 skills：code-review、task-master、subagent-execution、parallel-dispatch、requirement-clarify。

D-13~D-18: code-review.md（三种模板 + 自动化命令 + P0 深度强制）
D-22: requirement-clarify.md（多需求排序 + 验收标准自动生成）
D-23~D-25: task-master.md（依赖关系图 + checkpoint 联动 + 看板视图）
D-26: subagent-execution.md（动态并发 + 执行日志）
D-27~D-29: parallel-dispatch.md（任务分组 + 优先级调度 + 监控仪表盘）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@flow-kit/skills/code-review.md
@flow-kit/skills/task-master.md
@flow-kit/skills/subagent-execution.md
@flow-kit/skills/parallel-dispatch.md
@flow-kit/skills/requirement-clarify.md
@.planning/phases/06-P1-supplement/06-CONTEXT.md

## D-13~D-18 code-review.md 增强
- 三种模板：简化（单文件/P2）、标准（模块/P1）、深度（微服务/P0）
- P0 强制使用深度模板
- 每个检查项自动化命令（阻断项 vs 警告项）
- 棕地专用模板（历史兼容性 + 依赖风险）
- PR 描述自动填充

## D-22 requirement-clarify.md 增强
- MoSCoW/Kano 作为可选扩展
- 多需求排序场景
- 验收标准自动生成

## D-23~D-25 task-master.md 增强
- 任务依赖关系图（自动识别 + 用户手动调整）
- 与 subagent-execution 联动
- checkpoint 同步（blocked 触发断点续跑）
- 可选看板视图

## D-26 subagent-execution.md 增强
- 动态并发控制（80% 预算降为 1，90% 强制串行）
- 执行日志（结构化记录 + 归档 + 成本报告）
- 资源限制配置

## D-27~D-29 parallel-dispatch.md 增强
- 任务分组策略（前端/后端/测试分组）
- 优先级调度（P0 高优先级，仅对已完成依赖允许插队）
- 轻量监控仪表盘（Markdown 表格）
</context>

<tasks>

<task type="auto">
  <name>Task 1: code-review.md 增强</name>
  <files>
    - "flow-kit/skills/code-review.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/code-review.md 获取当前内容。

## 实现

### 1. 在 HOW_TO_USE 开头添加执行流程和深度模板

替换现有的 "### 三层审查模型" 章节：

```markdown
### 标准化执行流程（D-14）

```
前置自动化检查 → 人工三层审查 → 补充自动化检查 → 输出分级报告
```

### 三种 Review 深度模板（D-15）

| 模板 | 适用场景 | 审查深度 | Token 消耗 |
|------|----------|----------|------------|
| **简化模板** | P2/单文件修改 | CEO 层快速检查 | 低 |
| **标准模板** | P1/模块级变更 | CEO + Engineering | 中 |
| **深度模板** | P0/微服务级变更 | CEO + Design + Engineering + 额外检查 | 高 |

**P0 强制使用深度模板**。

### 棕地专用模板（D-16）
- 额外检查项：历史逻辑兼容性、依赖风险分析
- 激活条件：`.flow-kit/project-type` 为 `brownfield`
- 输出：在标准报告末尾附加 "Brownfield Risk Assessment" 小节

### 自动化检查命令（D-13）

| 检查项 | 命令 | 类型 | 适用模板 |
|--------|------|------|----------|
| Lint | `npm run lint` | 阻断 | 全部 |
| Typecheck | `npm run typecheck` | 阻断 | 全部 |
| Build | `npm run build` | 阻断 | 标准+深度 |
| Unit Tests | `npm run test` | 阻断 | 标准+深度 |
| Security Scan | `/flow-kit:health` | 阻断 | 深度 |
| 依赖检查 | `npm audit` | 警告 | 深度 |

**阻断项**：必须通过，否则拒绝合并
**警告项**：仅提示，不阻止合并
```

### 2. 在 EXAMPLE 末尾添加棕地模板示例

```markdown
### 棕地项目 Review 示例

**场景**：棕地项目修改订单模块

```
[Brownfield Risk Assessment]
  历史逻辑兼容性：✓ 通过（保留原有折扣计算逻辑）
  依赖风险分析：
    - OrderService → PaymentService（高风险，新接口）
    - OrderService → UserService（低风险，接口未变）
  建议：单独测试 Order → Payment 集成
  
Overall: APPROVED WITH CONDITIONS
```
```

### 3. 在 NOTES 末尾添加 Token 预算预警

```markdown
### Token 预算预警（D-17）
深度模板消耗更多 token。使用前自动检测：
- 若 >= 80% 预算：提示用户归档旧 phase 文件
- 若 >= 90% 预算：建议降级到标准模板
- 命令：`/flow-kit:estimate-tokens` 查看详情

### PR 描述自动填充（D-18）
Review 完成后，结果自动填充到 PR 描述的审查结果部分。
格式：
```markdown
## Review Results
- CEO Review: ✓ PASS
- Design Review: ✓ PASS  
- Engineering Review: ✓ PASS (3 findings, all resolved)
- Brownfield Risk: LOW
```
```
</action>
  <verify>
    <automated>grep -c "简化模板\|标准模板\|深度模板\|棕地专用\|阻断项\|警告项\|Token 预算" flow-kit/skills/code-review.md</automated>
  </verify>
  <done>
code-review.md 包含：
- 标准化执行流程（A→B→C→D）
- 三种深度模板（简化/标准/深度）+ P0 强制
- 棕地专用模板（历史兼容性 + 依赖风险）
- 自动化检查命令表（阻断项/警告项区分）
- Token 预算预警
- PR 描述自动填充
</done>
</task>

<task type="auto">
  <name>Task 2: task-master.md 增强</name>
  <files>
    - "flow-kit/skills/task-master.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/task-master.md 获取当前内容。

## 实现

### 1. 在 HOW_TO_USE 添加依赖关系图章节

在 "### 两步法：parse_prd → expand_task" 之后添加：

```markdown
### D-23 任务依赖关系图

#### 自动识别依赖
```javascript
// 从 task 输入输出自动推断依赖
function inferDependencies(tasks) {
  const graph = new DirectedGraph();
  for (const task of tasks) {
    for (const dep of task.dependencies) {
      graph.addEdge(dep, task.id);
    }
  }
  return graph;
}
```

#### 依赖图输出格式
```markdown
## Task Dependency Graph

```
[T1.1] ──→ [T2.1] ──→ [T3.1]
  │            │
  ↓            ↓
[T1.2]      [T2.2] ──→ [T3.2]
                │
                ↓
             [T3.3] (blocked: waiting T2.2)
```
```

#### 用户手动调整
- 命令：`/flow-kit:task-graph adjust [task-id] --after [dep-id]`
- 调整后自动验证无循环依赖

#### 与 subagent-execution 联动
依赖图生成后，自动调用 `/flow-kit:skill:subagent-execution` 进行调度。
```

### 2. 在 NOTES 添加可选看板视图

```markdown
### D-24 可选看板视图
不强制默认开启。通过命令启用：
- `/flow-kit:kanban show` — 显示看板
- `/flow-kit:kanban hide` — 隐藏看板

看板格式：
| To Do | In Progress | Done | Blocked |
|-------|-------------|------|---------|
| T1.3 | T2.1 | T1.1 | T3.3 |
| | T2.2 | T1.2 | |
```

### 3. 在 expand_task 输出格式添加 checkpoint 联动

修改现有的 "**输出格式**" 为：

```markdown
**输出格式**：
```markdown
### [TASK-ID] Task Name
- **Phase**: X
- **Status**: todo | in-progress | done | blocked
- **Entities**: entity1, entity2
- **Actions**: action1, action2
- **Input**: source file or entity
- **Output**: target file or state
- **Dependencies**: [TASK-ID], [TASK-ID]
- **Verification**: how to verify completion

## Dependency Graph
```{mermaid}
graph LR
  T1.1 --> T2.1
  T2.1 --> T3.1
```
```

**D-25 checkpoint 同步**：
- 任务状态变更自动同步到 `.flow-kit/checkpoint-state.json`
- blocked 状态触发断点续跑提示
- 命令：`/flow-kit:checkpoint status` 查看当前任务状态
```
</action>
  <verify>
    <automated>grep -c "依赖关系图\|Dependency Graph\|checkpoint\|断点续跑\|看板视图\|kanban" flow-kit/skills/task-master.md</automated>
  </verify>
  <done>
task-master.md 包含：
- 任务依赖关系图（自动识别 + 用户手动调整）
- 与 subagent-execution 联动说明
- checkpoint 同步（blocked 触发断点续跑）
- 可选看板视图（/flow-kit:kanban show/hide）
- Dependency Graph 输出格式
</done>
</task>

<task type="auto">
  <name>Task 3: subagent-execution.md 增强</name>
  <files>
    - "flow-kit/skills/subagent-execution.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/subagent-execution.md 获取当前内容。

## 实现

### 1. 在 HOW_TO_USE 添加动态并发控制

在 "### 执行参数" 表格之后添加：

```markdown
### D-26 动态并发控制

**预算感知调度**：
| Token 预算 | 并发数 | 说明 |
|------------|--------|------|
| < 80% | maxParallel（默认 4） | 正常并发 |
| >= 80% | 1 | 降级为单并发，避免预算耗尽 |
| >= 90% | 串行 | 强制串行，逐任务完成 |

**配置**：
```javascript
Task({
  // ...
  concurrency: {
    auto: true,  // 启用动态调整
    maxParallel: 4,
    degradeAt: 0.8,   // 80% 降级
    serialAt: 0.9     // 90% 串行
  }
})
```

### D-26 执行日志

**日志格式**：
```json
{
  "timestamp": "2026-05-07T10:30:00Z",
  "taskId": "T2.1",
  "agent": "subagent-1",
  "status": "completed",
  "duration": 120000,
  "tokensUsed": 50000,
  "output": { "files": ["user.ts"], "errors": [] }
}
```

**归档**：
- 执行日志归档到 `.planning/archive/execution-{date}.json`
- 命令：`/flow-kit:cost-report` 导入成本报告

**资源限制**：
- 并发数配置：`maxParallel: N`
- 预算动态调整：自动根据 token 预算调整
```
```

### 2. 更新原有的 Subagent Task Package 格式

在 "### Subagent Task Package 格式" 中添加 `concurrency` 字段：

```javascript
Task({
  description: "Task description",
  isolation: "worktree",
  workingDirectory: "/path/to/repo",
  concurrency: {
    auto: true,
    maxParallel: 4
  },
  input: { /* 任务输入 */ },
  output: { /* 任务输出 */ },
  onResult: (result) => { /* 结果处理 */ }
})
```
</action>
  <verify>
    <automated>grep -c "80%.*1.*并发\|90%.*串行\|执行日志\|concurrency\|动态并发" flow-kit/skills/subagent-execution.md</automated>
  </verify>
  <done>
subagent-execution.md 包含：
- 动态并发控制（80% 降为 1，90% 强制串行）
- 执行日志格式（JSON 结构化）
- 日志归档到 .planning/archive/
- /flow-kit:cost-report 命令
- concurrency 配置字段
</done>
</task>

<task type="auto">
  <name>Task 4: parallel-dispatch.md 增强</name>
  <files>
    - "flow-kit/skills/parallel-dispatch.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/parallel-dispatch.md 获取当前内容。

## 实现

### 1. 在 HOW_TO_USE 开头添加任务分组策略

在 "### Conflict Detection Algorithm" 之前添加：

```markdown
### D-27 任务分组策略

**分组原则**：同一类型任务分发到同一子代理，减少上下文切换。

| 分组 | 任务类型 | 示例 |
|------|----------|------|
| frontend | UI/组件/样式 | React组件、CSS、图标 |
| backend | API/服务/业务逻辑 | Controller、Service、Model |
| test | 测试/质量 | Unit tests、Integration tests |
| infra | 配置/部署/脚本 | Docker、CI/CD、脚本 |

**分组算法**：
```javascript
function groupTasks(tasks) {
  const groups = { frontend: [], backend: [], test: [], infra: [] };
  for (const task of tasks) {
    const type = inferTaskType(task);
    groups[type].push(task);
  }
  return groups;
}
```
```

### 2. 在原有冲突检测章节后添加优先级调度

在 "### 冲突类型" 表格之后添加：

```markdown
### D-28 优先级调度

**优先级规则**：
| 优先级 | 条件 | 说明 |
|--------|------|------|
| P0 | BREAKING-CHANGE、数据库迁移 | 必须优先处理 |
| P1 | 核心业务逻辑 | 高优先级 |
| P2 | 辅助功能 | 正常优先级 |

**插队规则**：
- 仅对依赖已完成的任务允许插队
- P0 任务可打断 P1/P2 任务
- P1 任务不可打断 P0 任务

**调度算法**：
```javascript
function schedule(tasks) {
  const ready = tasks.filter(t => 
    t.dependencies.every(d => d.status === 'done')
  );
  const sorted = ready.sort((a, b) => b.priority - a.priority);
  return sorted[0];  // 返回最高优先级任务
}
```
```

### 3. 在 NOTES 末尾添加监控仪表盘

```markdown
### D-29 轻量监控仪表盘

**激活命令**：`/flow-kit:dispatch:monitor show`

**显示格式**：
```markdown
## Task Dispatch Monitor

| Task | Status | Token Used | ETA | Agent |
|------|--------|------------|-----|-------|
| T2.1 | running | 45,000 | 5min | subagent-1 |
| T2.2 | queued | - | - | - |
| T2.3 | blocked | - | - | - |
| T1.1 | done | 30,000 | - | subagent-2 |

**Budget**: 62% used (62,000 / 100,000 tokens)
**Parallelism**: 2 active agents
```

**隐藏命令**：`/flow-kit:dispatch:monitor hide`
```
</action>
  <verify>
    <automated>grep -c "分组策略\|frontend.*backend.*test\|优先级调度\|P0.*P1.*P2\|监控仪表盘\|monitor" flow-kit/skills/parallel-dispatch.md</automated>
  </verify>
  <done>
parallel-dispatch.md 包含：
- 任务分组策略（frontend/backend/test/infra）
- 优先级调度（P0/P1/P2 + 插队规则）
- 轻量监控仪表盘（Markdown 表格格式）
- /flow-kit:dispatch:monitor show/hide 命令
</done>
</task>

<task type="auto">
  <name>Task 5: requirement-clarify.md 增强</name>
  <files>
    - "flow-kit/skills/requirement-clarify.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/requirement-clarify.md 获取当前内容。

## 实现

### 在 NOTES 末尾添加多需求排序和验收标准自动生成

```markdown
## D-22 增强：多需求排序与验收标准自动生成

### MoSCoW 优先级框架（可选扩展）

| 优先级 | 定义 | 交付要求 |
|--------|------|----------|
| **Must** | 核心功能，不实现则系统不可用 | 必须交付 |
| **Should** | 重要功能，但有替代方案 | 尽量交付 |
| **Could** | 增强功能，不影响业务 | 视资源情况 |
| **Won't** | 本次不实现 | 明确排除 |

**执行**：
1. 用户提供需求列表
2. 引导用户为每个需求标注 MoSCoW 优先级
3. 按 Must → Should → Could → Won't 排序
4. 输出需求优先级列表

### Kano 模型（可选扩展）

| 类型 | 特征 | 用户满意度 |
|------|------|------------|
| Must-be | 必须有，否则不满 | 线性正相关 |
| Attractive | 惊喜功能，超出预期 | 非线性正相关 |
| One-dimensional | 有则满意，无则不满 | 线性相关 |
| Indifferent | 无关紧要 | 无相关 |
| Reverse | 反向特征 | 有则不满 |

### 验收标准自动生成

**输入**：澄清后的需求描述

**输出**：
```markdown
## 验收标准 (Auto-generated)

### AC-1: [需求项名称]
- **Given** [前置条件]
- **When** [操作]
- **Then** [预期结果]

### AC-2: [需求项名称]
- **Given** ...
- **When** ...
- **Then** ...

### Edge Cases
- [边界条件 1]
- [边界条件 2]
```

**对接**：
- 生成的验收标准 → 传递给 task-master.md
- 生成的验收标准 → 传递给 verification.md
```

## 注意事项
- MoSCoW 和 Kano 作为可选扩展，不强制使用
- 验收标准自动生成对接下游 skills（task-master + verification）
</action>
  <verify>
    <automated>grep -c "MoSCoW\|Kano\|验收标准.*自动\|Must\|Should\|Could\|Won't" flow-kit/skills/requirement-clarify.md</automated>
  </verify>
  <done>
requirement-clarify.md 包含：
- MoSCoW 优先级框架（可选扩展）
- Kano 模型（可选扩展）
- 验收标准自动生成功能
- 与 task-master + verification 的对接说明
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent → parallel-dispatch | 任务调度涉及资源分配 |
| task-master → checkpoint | 任务状态写入 checkpoint 文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Denial | 监控仪表盘 | accept | 可选功能，不影响主流程 |
| T-06-08 | Tampering | checkpoint 状态 | mitigate | 任务状态由系统管理，用户通过命令修改 |
| T-06-09 | Elevation | P0 插队 | accept | P0 变更需要明确授权，风险可控 |
</threat_model>

<verification>
1. `grep -c "简化模板\|标准模板\|深度模板" flow-kit/skills/code-review.md` >= 3
2. `grep -c "依赖关系图\|checkpoint\|看板" flow-kit/skills/task-master.md` >= 2
3. `grep -c "80%.*1.*并发\|执行日志" flow-kit/skills/subagent-execution.md` >= 2
4. `grep -c "分组策略\|优先级调度\|监控仪表盘" flow-kit/skills/parallel-dispatch.md` >= 3
5. `grep -c "MoSCoW\|Kano\|验收标准.*自动" flow-kit/skills/requirement-clarify.md` >= 2
</verification>

<success_criteria>
- [x] code-review.md 三种深度模板 + 棕地专用模板 + 自动化命令
- [x] task-master.md 依赖关系图 + checkpoint 联动 + 可选看板
- [x] subagent-execution.md 动态并发 + 执行日志
- [x] parallel-dispatch.md 分组策略 + 优先级调度 + 监控仪表盘
- [x] requirement-clarify.md MoSCoW/Kano + 验收标准自动生成
- [x] REQ-008 完全覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/06-P1-supplement/06-03-SUMMARY.md`
</output>
