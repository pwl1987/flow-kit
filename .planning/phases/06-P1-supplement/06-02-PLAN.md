---
phase: "06-P1-supplement"
plan: "02"
type: "execute"
wave: "2"
depends_on: []
files_modified:
  - "flow-kit/lib/phase-executor.md"
  - "flow-kit/skills/debugging.md"
  - "flow-kit/skills/verification.md"
autonomous: true
requirements:
  - "REQ-006"
  - "REQ-008"
user_setup: []

must_haves:
  truths:
    - "phase-executor 在 80% token 预算时输出警告"
    - "phase-executor 在 90% token 预算时提示压缩"
    - "phase-executor 在 100% token 预算时强制压缩"
    - "压缩保留核心文档（CONTEXT.md / PLAN.md），归档而非删除"
  artifacts:
    - path: "flow-kit/lib/phase-executor.md"
      provides: "自动清窗机制"
      contains: "80%.*警告\|90%.*提示\|100%.*强制压缩"
    - path: "flow-kit/skills/debugging.md"
      provides: "错误模式库 + debug-snapshot"
      contains: "常见错误模式\|debug-snapshot"
    - path: "flow-kit/skills/verification.md"
      provides: "分阶段验证门控"
      contains: "Phase1-2.*轻量\|Phase3-4.*基础\|Phase5-6.*全量"
  key_links:
    - from: "phase-executor.md"
      to: "estimate-tokens.md"
      via: "token 预算检测"
      pattern: "estimate-tokens.*80%"
    - from: "debugging.md"
      to: "phase-executor.md"
      via: "checkpoint 联动"
      pattern: "checkpoint.*resume"
---

<objective>
实现 phase-executor 自动清窗机制和部分 skills 内容充实（debugging + verification）。

Area B: phase-executor 渐进式压缩机制（80% 警告 → 90% 提示 → 100% 强制压缩）
Area D: debugging.md（错误模式库 + debug-snapshot）、verification.md（分阶段验证门控）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@flow-kit/lib/phase-executor.md
@flow-kit/skills/debugging.md
@flow-kit/skills/verification.md
@.planning/phases/06-P1-supplement/06-CONTEXT.md

## D-09 渐进式压缩机制
| 预算 | 行为 |
|------|------|
| >= 80% | 输出 `[WARNING] Approaching token budget ({pct}%)` |
| >= 90% | 输出 `[HINT] Consider running cleanup: /flow-kit:cleanup` |
| >= 100% | 强制压缩，输出 `[BLOCK] Token budget exhausted, running cleanup` |

## D-10 清理粒度
- 清理已完成 phase 的 checkpoint 文件和 logs
- 核心文档（CONTEXT.md / PLAN.md）保留
- 归档而非删除（移动到 .planning/archive/）

## D-19 debugging.md 增强
- 常见错误模式库（Phase 文件格式错误、Checkpoint 残留等）
- `/flow-kit:debug-snapshot` 命令（调试前自动保存现场）
- 与 phase-executor checkpoint 联动

## D-20 debugging.md 增强
- 棕地项目自动关联数据库状态快照
- 调试场景适配（棕地/绿地）

## D-21 verification.md 增强
- 分阶段验证门控：
  - Phase 1-2：轻量（lint + typecheck）
  - Phase 3-4：基础（+ unit tests）
  - Phase 5-6：全量（+ integration tests + security scan）
- 棕地启用历史兼容性检查
- 绿地启用标准检查
- 与 token 预算联动
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase-executor 自动清窗机制</name>
  <files>
    - "flow-kit/lib/phase-executor.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/lib/phase-executor.md 获取当前内容。

## 实现

在 "### 6. Token Estimation" 章节之后添加新章节：

```markdown
### 8. Auto Cleanup（自动清窗机制）

**D-09 渐进式压缩**：
| Token 预算 | 行为 | 输出 |
|------------|------|------|
| >= 80% | WARNING | `[WARNING] Approaching token budget ({pct}%)` |
| >= 90% | HINT | `[HINT] Consider running cleanup: /flow-kit:cleanup` |
| >= 100% | BLOCK + 强制压缩 | `[BLOCK] Token budget exhausted, running cleanup` |

**D-10 清理粒度**：
- 清理目标：已完成 phase 的 checkpoint 文件和 logs
- 保留文件：CONTEXT.md, PLAN.md, SUMMARY.md（核心文档）
- 清理操作：归档到 `.planning/archive/` 而非删除
- 归档文件名格式：`.planning/archive/{phase}-{timestamp}.json`

**Cleanup 执行逻辑**：
```
On cleanup trigger (100% budget):
  1. Scan .planning/phases/*/ for completed phases
  2. For each completed phase:
     - Archive checkpoint files to .planning/archive/
     - Archive log files to .planning/archive/
  3. Preserve: CONTEXT.md, PLAN.md, SUMMARY.md
  4. Log: "Archived {N} files, preserved {M} documents"
  5. Display: "Cleanup complete. Run /flow-kit:estimate-tokens to verify"
```

**Cleanup 命令**：
- `/flow-kit:cleanup` — 手动触发清理
- `/flow-kit:cleanup --force` — 强制清理（包括未完成 phase）
```

## 注意事项
- 复用现有的 Token Estimation 逻辑（estimate-tokens.md）
- Cleanup 是 archive，不是 delete
- 保留文件清单需与用户确认
</action>
  <verify>
    <automated>grep -c "80%\|90%\|100%\|强制压缩\|cleanup\|归档" flow-kit/lib/phase-executor.md</automated>
  </verify>
  <done>
phase-executor.md 包含：
- 渐进式压缩机制（80%/90%/100% 三档）
- 清理粒度（checkpoint + logs 归档，核心文档保留）
- Cleanup 执行逻辑
- /flow-kit:cleanup 命令说明
</done>
</task>

<task type="auto">
  <name>Task 2: debugging.md 增强</name>
  <files>
    - "flow-kit/skills/debugging.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/debugging.md 获取当前内容。

## 实现

在文件末尾（NOTES 章节之后）添加：

```markdown
## D-19 增强：错误模式库

### 常见错误模式
| 错误类型 | 症状 | 联动命令 |
|----------|------|----------|
| Phase 文件格式错误 | 解析失败、占位符未替换 | `/flow-kit:validate-phase` |
| Checkpoint 残留 | 断点续跑提示旧状态 | `/flow-kit:reset-checkpoint` |
| Context 过期 | 上下文丢失、重复加载 | `/flow-kit:check-expiry` |
| Token 预算耗尽 | 执行中断、压缩提示 | `/flow-kit:estimate-tokens` |
| 棕地检测失败 | 错误的护栏激活 | `/flow-kit:project-type detect` |

### /flow-kit:validate-phase 命令
验证 phase 文件格式：
- 检查占位符是否已替换
- 检查必需章节是否存在
- 检查 YAML frontmatter 格式

### /flow-kit:reset-checkpoint 命令
强制清理 checkpoint 状态：
- 删除 `.flow-kit/checkpoint-state.json`
- 提示用户重新开始或指定起始点

## D-20 增强：debug-snapshot

### /flow-kit:debug-snapshot 命令
调试前自动保存现场：

```markdown
# Debug Snapshot Report
Generated: {timestamp}

## 环境信息
- OS: {os}
- Working Directory: {cwd}
- Git Branch: {branch}

## 项目状态
- Project Type: {from .flow-kit/project-type}
- Context Age: {N} days
- Token Budget: {pct}%

## 最近变更
{git log -3 --oneline}

## Phase 状态
{list of phase directories and their states}

## 建议
基于以上信息，推荐以下调试步骤：
1. ...
2. ...
```

### 棕地项目适配
- 自动关联数据库状态快照
- 提示最近的数据库迁移
- 提供 rollback 建议
```

## 注意事项
- 保持原有四段式结构（WHEN_TO_USE / HOW_TO_USE / EXAMPLE / NOTES）
- 新增内容追加到 NOTES 之后
- 联动命令使用 /flow-kit:xxx 格式
</action>
  <verify>
    <automated>grep -c "错误模式\|debug-snapshot\|validate-phase\|reset-checkpoint" flow-kit/skills/debugging.md</automated>
  </verify>
  <done>
debugging.md 包含：
- 常见错误模式库（5 种错误类型 + 联动命令）
- /flow-kit:validate-phase 命令说明
- /flow-kit:reset-checkpoint 命令说明
- /flow-kit:debug-snapshot 命令说明
- 棕地项目适配说明
</done>
</task>

<task type="auto">
  <name>Task 3: verification.md 增强</name>
  <files>
    - "flow-kit/skills/verification.md"
  </files>
  <action>
## 读取 first
读取 flow-kit/skills/verification.md 获取当前内容。

## 实现

### 1. 在 HOW_TO_USE 开头添加分阶段验证门控

在 "### Self-Check Delivery Checklist" 之前添加：

```markdown
### 分阶段验证门控（D-21）

| Phase | 验证级别 | 检查项 |
|-------|----------|--------|
| Phase 1-2 | 轻量 | lint, typecheck, build |
| Phase 3-4 | 基础 | + unit tests, coverage >= 70% |
| Phase 5-6 | 全量 | + integration tests, security scan, E2E |

### 棕地/绿地差异
| 项目类型 | 额外检查 |
|----------|----------|
| 棕地 | 历史兼容性检查、依赖风险分析 |
| 绿地 | 标准检查（无历史包袱） |

### Token 预算联动
- Phase 5-6 全量检查前自动检测 token 预算
- 若 >= 80%，提示用户：`[HINT] Token budget at {pct}%, consider running /flow-kit:cleanup first`
- 用户确认后继续或降级到基础检查
```

### 2. 在 EXAMPLE 之后添加验证失败恢复指南

在 "**Verification Report**:" 之后添加：

```markdown
### 验证失败恢复指南

| 失败类型 | 原因 | 修复命令 |
|----------|------|----------|
| Lint 失败 | 代码风格问题 | `npm run lint:fix` |
| Typecheck 失败 | 类型不匹配 | `npm run typecheck:fix` |
| 测试失败 | 逻辑错误 | `npm run test:debug` |
| 构建失败 | 编译错误 | `npm run build:debug` |
| 安全扫描失败 | 漏洞检测 | `/flow-kit:security-fix` |

**一键修复流程**：
1. 识别失败类型
2. 执行对应修复命令
3. 重新运行验证
4. 若仍失败，调用 `/flow-kit:debug-snapshot` 保存现场
```

## 注意事项
- 保持原有结构，仅在适当位置插入新内容
- 分阶段验证门控放在 HOW_TO_USE 开头
- 验证失败恢复指南放在 EXAMPLE 之后
</action>
  <verify>
    <automated>grep -c "Phase1-2.*轻量\|Phase3-4.*基础\|Phase5-6.*全量\|棕地\|绿地\|Token 预算" flow-kit/skills/verification.md</automated>
  </verify>
  <done>
verification.md 包含：
- 分阶段验证门控表（Phase1-2 轻量、Phase3-4 基础、Phase5-6 全量）
- 棕地/绿地差异检查说明
- Token 预算联动机制
- 验证失败恢复指南 + 一键修复命令
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-executor → filesystem | 清理操作涉及文件删除/归档 |
| debugging → git/logs | 读取项目状态信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | archive cleanup | mitigate | 仅归档到 archive/，不删除；保留文件清单明确 |
| T-06-05 | Denial | debug-snapshot | accept | 快照失败不影响主流程，仅提供提示 |
| T-06-06 | Information Disclosure | debug-snapshot output | mitigate | 仅输出到终端，不写入敏感信息到文件 |
</threat_model>

<verification>
1. `grep -c "80%\|90%\|100%" flow-kit/lib/phase-executor.md` >= 3
2. `grep -c "错误模式\|debug-snapshot" flow-kit/skills/debugging.md` >= 2
3. `grep -c "Phase1-2.*轻量\|Phase5-6.*全量" flow-kit/skills/verification.md` >= 2
</verification>

<success_criteria>
- [x] phase-executor.md 包含渐进式压缩机制（80%/90%/100%）
- [x] 清理粒度明确（归档 checkpoint + logs，保留核心文档）
- [x] debugging.md 包含错误模式库和 debug-snapshot
- [x] verification.md 包含分阶段验证门控（Phase1-2/3-4/5-6）
- [x] 棕地/绿地差异检查说明
- [x] Token 预算联动机制
- [x] REQ-006, REQ-008 覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/06-P1-supplement/06-02-SUMMARY.md`
</output>
