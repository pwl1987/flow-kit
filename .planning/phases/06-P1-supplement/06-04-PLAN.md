---
phase: "06-P1-supplement"
plan: "04-gap"
type: "execute"
wave: "1"
gap_closure: true
depends_on: []
files_modified:
  - "flow-kit/phases/1-requirement/1-requirement.md"
  - "flow-kit/phases/2-design/2-design.md"
  - "flow-kit/phases/3-task/3-task.md"
  - "flow-kit/phases/4-dev/4-dev.md"
  - "flow-kit/phases/5-test/5-test.md"
  - "flow-kit/phases/6-review/6-review.md"
autonomous: true
source_gap: "UAT Gap 1"
requirements:
  - "REQ-005"
user_setup: []

must_haves:
  truths:
    - ".flow-kit/project-type 不存在时触发检测逻辑"
    - "检测信号包括 package.json、git remote、LOC"
    - "检测结果写入 .flow-kit/project-type 文件"
  artifacts:
    - path: "flow-kit/phases/1-requirement/1-requirement.md"
      provides: "phases 文件混合模式检测触发逻辑"
      contains: "project-type.*不存在.*检测"
    - path: ".flow-kit/project-type"
      provides: "项目类型标记文件（检测后生成）"
      contains: "brownfield|greenfield|light-brownfield"
  key_links:
    - from: "phases files"
      to: ".flow-kit/project-type"
      via: "检测触发 + 文件创建"
      pattern: "project-type.*exist.*read|project-type.*检测"
---

<objective>
修复 UAT Gap 1：为 phases 文件添加 .flow-kit/project-type 不存在时的检测触发逻辑。

当 .flow-kit/project-type 文件不存在时，phases 文件应触发检测逻辑，
检测信号包括：package.json 存在 + lock 文件、git remote、LOC 等。
检测结果写入 .flow-kit/project-type 文件供后续读取。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@flow-kit/phases/1-requirement/1-requirement.md
@flow-kit/phases/2-design/2-design.md
@flow-kit/phases/3-task/3-task.md
@flow-kit/phases/4-dev/4-dev.md
@flow-kit/phases/5-test/5-test.md
@flow-kit/phases/6-review/6-review.md
@.planning/phases/06-P1-supplement/06-CONTEXT.md
@.planning/phases/06-P1-supplement/06-UAT.md
@.planning/REQUIREMENTS.md

## D-07 检测信号（来自 Phase 5）
- 文件指纹（package.json 存在 + lock 文件）
- Git remote（是否关联 GitHub/GitLab）
- 业务代码行数（src/ 目录 LOC）
- 绿地判定（无 git remote + LOC < 5000）

## D-08 混合模式逻辑
```
if (.flow-kit/project-type exists):
  read project-type
else:
  run detection → create .flow-kit/project-type
```

## D-09 检测触发逻辑应包含
1. 检查 .flow-kit/project-type 是否存在
2. 如果不存在，运行检测：
   - 检查 package.json 和 lock 文件（brownfield 指纹）
   - 检查 git remote（brownfield 指纹）
   - 检查 src/ 目录 LOC（绿地指纹）
3. 将检测结果写入 .flow-kit/project-type
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 phases 文件添加检测触发逻辑</name>
  <files>
    - "flow-kit/phases/1-requirement/1-requirement.md"
    - "flow-kit/phases/2-design/2-design.md"
    - "flow-kit/phases/3-task/3-task.md"
    - "flow-kit/phases/4-dev/4-dev.md"
    - "flow-kit/phases/5-test/5-test.md"
    - "flow-kit/phases/6-review/6-review.md"
  </files>
  <action>
为每个 phases 文件添加检测触发逻辑。当 .flow-kit/project-type 不存在时：

1. 检测 package.json + lock 文件存在 → brownfield 信号
2. 检测 git remote 存在 → brownfield 信号
3. 检测 src/ 目录 LOC &lt; 5000 + 无 git remote → greenfield 信号
4. 将检测结果写入 .flow-kit/project-type

在每个 phases 文件的检测逻辑部分添加 else 分支：
```
if (.flow-kit/project-type exists):
  read project-type
else:
  run detection → create .flow-kit/project-type
```
</action>

<task type="auto">
  <name>Task 2: 创建 .flow-kit/project-type 示例文件</name>
  <files>
    - ".flow-kit/project-type"
  </files>
  <action>
创建示例文件 .flow-kit/project-type，内容为 flow-kit 自身的检测结果（brownfield）。
此文件应在首次检测后自动生成。
</action>
</tasks>
