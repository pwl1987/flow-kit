# Phase 7: P2 体验增强 - Plan 03

**Area:** C - 破坏性变更检测模式
**Requirements:** REQ-013

---

## Plan 03: 多语言规格破坏性变更检测模式

**Objective:** 6 个语言规格包含破坏性变更检测模式，统一规则在 `flow-kit/reference/breaking-change-rules.md`

**Purpose:** 为每种语言提供标准化的破坏性变更检测规则

**Output:**
- `flow-kit/reference/breaking-change-rules.md` — 统一破坏性变更检测规则
- 更新 6 个语言规格文件，各自引用统一规则 + 语言特定规则

---

### Task 1: 创建 breaking-change-rules.md

**Files:** `flow-kit/reference/breaking-change-rules.md`

**Action:**
创建统一破坏性变更检测规则，包含：
- Pattern-based 检测规则（文件指纹）
- AST-based 检测规则（精确 API 签名）
- 检测范围：API/DB/Config/依赖

```markdown
# Breaking Change Detection Rules

## Overview

Breaking changes are categorized into four domains:
1. **API Changes** — Interface signature modifications
2. **Database Changes** — Schema/field deletions
3. **Configuration Changes** — Config structure modifications
4. **Dependency Changes** — Incompatible version updates

## Detection Methods

### Pattern-based (Fast)

File fingerprint detection for quick scanning:
- `BREAKING-CHANGE.md` file presence
- `*.sql` files (DDL/DML)
- `migration/` directory
- Lock file changes (`package-lock.json`, `yarn.lock`, `go.sum`, `Pipfile.lock`)

### AST-based (Precise)

Abstract syntax tree analysis for API signature changes:
- Function signature changes (parameter types, return types)
- Interface removal or modification
- Public API removal
- Type definition changes

## API Breaking Changes

| Change Type | Detection Pattern | Severity |
|-------------|-------------------|----------|
| Function signature change | Parameter type/return type mismatch | HIGH |
| Interface removal | Type not exported | CRITICAL |
| Public API removal | Method deleted | CRITICAL |
| Enum value change | Enum member removed | HIGH |

## Database Breaking Changes

| Change Type | Detection Pattern | Severity |
|-------------|-------------------|----------|
| Column deletion | ALTER TABLE DROP COLUMN | CRITICAL |
| Column type change | ALTER TABLE ALTER COLUMN | HIGH |
| Index removal | DROP INDEX | MEDIUM |
| Cascade delete added | FOREIGN KEY + ON DELETE CASCADE | HIGH |

## Configuration Breaking Changes

| Change Type | Detection Pattern | Severity |
|-------------|-------------------|----------|
| Required config removed | Key not present | HIGH |
| Default value changed | Different default | MEDIUM |
| Format structure changed | JSON/YAML schema mismatch | HIGH |

## Dependency Breaking Changes

| Change Type | Detection Pattern | Severity |
|-------------|-------------------|----------|
| Major version bump | semver major increment | CRITICAL |
| Removed dependency | Package deleted | HIGH |
| Incompatible API | Lock file hash change | HIGH |

## Language-specific Rules

Each language spec (go.md, java.md, php.md, python.md, rust.md, typescript.md)
should reference this file and add language-specific patterns.
```

**Verify:** `grep -l "breaking-change-rules\|Pattern-based\|AST-based" flow-kit/reference/breaking-change-rules.md`
**Done:** breaking-change-rules.md 存在，包含 4 类检测规则

---

### Task 2: 更新 typescript.md

**Files:** `flow-kit/reference/language-specs/typescript.md`

**Action:**
在 typescript.md 末尾新增 Breaking Changes 小节：

```markdown
## Breaking Change Detection

Reference: `flow-kit/reference/breaking-change-rules.md`

### TypeScript-specific Patterns

| Pattern | Example | Severity |
|---------|---------|----------|
| Interface property removed | `interface Foo { bar: string }` → `interface Foo { }` | CRITICAL |
| Type alias removed | `type Foo = string` removed | CRITICAL |
| Function overload removed | One signature removed from union | HIGH |
| Enum member removed | `enum Foo { A, B }` → `enum Foo { A }` | HIGH |
| Export removed | `export { Foo }` → `export { }` | HIGH |

### Detection Commands

```bash
# API signature changes (requires tsc)
npx tsc --noEmit --strict

# Dependency audit
npm audit --json | jq '.metadata.total === 0'

# Lock file change detection
git diff package-lock.json | grep '"version"' | head -20
```
```

**Verify:** `grep -c "Breaking Change Detection\|breaking-change-rules" flow-kit/reference/language-specs/typescript.md`
**Done:** typescript.md 包含破坏性变更检测小节

---

### Task 3: 更新其他语言规格

**Files:** `flow-kit/reference/language-specs/go.md`, `java.md`, `php.md`, `python.md`, `rust.md`

**Action:**
为每个语言规格添加 Breaking Change Detection 小节，引用 `flow-kit/reference/breaking-change-rules.md`，包含：

| Language | Key Breaking Patterns |
|----------|----------------------|
| Go | Function signature change, interface removal, package path change |
| Java | Method signature change, annotation removal, class inheritance change |
| PHP | Method signature change, property type change, interface removal |
| Python | Function signature change, abstract method added, import path change |
| Rust | Trait removal, lifetime change, struct field type change |

**Verify:** `grep -l "Breaking Change Detection" flow-kit/reference/language-specs/*.md | wc -l` 返回 6
**Done:** 所有 6 个语言规格都包含破坏性变更检测小节

---

## Success Criteria

1. `flow-kit/reference/breaking-change-rules.md` 存在，包含 4 类检测规则
2. 所有 6 个语言规格包含破坏性变更检测小节
3. 语言规格引用统一规则文件

## Output

创建 `.planning/phases/07-P2-enhancement/07-03-SUMMARY.md`