---
phase: "12-Rollback"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "flow-kit/phases/8-rollback/8-rollback.md"
  - "flow-kit/lib/phase-executor.md"
  - "flow-kit/commands/rollback.md"
autonomous: false
requirements:
  - "RB-01"
  - "RB-02"
  - "RB-03"
  - "RB-04"
  - "RB-05"
  - "RB-06"
user_setup: []
must_haves:
  truths:
    - "用户可执行 /flow-kit:rollback 回滚已标记的 phase"
    - "回滚前显示影响范围预览和不可逆警告"
    - "回滚前自动创建 backup branch/tag"
    - "回滚后自动写入 .specs/LESSONS.md"
    - "用户可选择 --force 跳过安全护栏"
    - "用户可通过 --adjust=promote|demote|remove 调整稳定点标记"
  artifacts:
    - path: "flow-kit/commands/rollback.md"
      provides: "/flow-kit:rollback 命令规格"
    - path: "flow-kit/phases/8-rollback/8-rollback.md"
      provides: "回滚阶段完整定义"
    - path: "flow-kit/lib/phase-executor.md"
      provides: "第9集成点：Rollback Trigger"
    - path: ".planning/checkpoints/{phase}-stable.json"
      provides: "稳定点标记文件"
      create_in: "Task 3"
  key_links:
    - from: "flow-kit/commands/rollback.md"
      to: "flow-kit/lib/phase-executor.md"
      via: "集成点 #9"
    - from: "phase-executor.md"
      to: ".specs/LESSONS.md"
      via: "appendLesson()"
    - from: "flow-kit/commands/rollback.md"
      to: ".planning/checkpoints/{phase}-stable.json"
      via: "--adjust=promote|demote|remove 参数"
      description: "用户通过 --adjust 参数调整稳定点标记，影响 .planning/checkpoints/ 中的标记文件状态"
---

<objective>
实现 Phase 12 回滚工作流，包括 `/flow-kit:rollback` 命令、回滚阶段定义、phase-executor 第9集成点、稳定点标记机制。

**Purpose:** 为 flow-kit 提供完整的变更回滚能力
**Output:** 回滚命令 + 回滚阶段文档 + executor 集成点
</objective>

<execution_context>
@flow-kit/commands/offline-mode.md
@flow-kit/commands/minimal-mode.md
@flow-kit/phases/8-rollback/8-rollback.md
</execution_context>

<context>
## 6项核心决策（锁定）

| Decision | Content |
|-----------|---------|
| D-RB-01 | 回滚粒度 = Git commit + 生成文件（两者兼有） |
| D-RB-02 | 触发方式 = 手动 + 自动 + 监控 |
| D-RB-03 | 回滚深度 = 仅已标记的稳定点 |
| D-RB-04 | 稳定点标记 = 混合模式（自动为主 + 用户可调整） |
| D-RB-05 | 安全护栏 = 全启用 + `--force` 跳过机制 |
| D-RB-06 | 回滚后处理 = LESSONS 记录 + 用户选择 |

## 架构设计

```
User/监控系统
      │
      ▼
/flow-kit:rollback 命令
      │
      ▼
[稳定点检查] ──> [备份创建] ──> [安全护栏确认]
      │              │               │
      │              ▼               │
      │         backup/{phase}-{timestamp}
      │              │               │
      └──────────────┴──────────────┘
                     ▼
            [执行回滚: Git revert + 文件处理]
                     │
         ┌───────────┴───────────┐
         ▼                       ▼
   [写入 LESSONS.md]      [更新 phase 状态]
         │                       │
         └───────────┬───────────┘
                     ▼
            [用户选择后续路径]
```

## 稳定点标记机制

```javascript
// 自动标记：phase 执行完成后触发
function markStablePoint(phase, reason = "auto") {
  const checkpointDir = '.planning/checkpoints';
  const markerPath = `${checkpointDir}/${phase}-stable.json`;
  fs.writeFileSync(markerPath, JSON.stringify({
    phase,
    markedAt: new Date().toISOString(),
    reason,  // "auto" or "manual"
    canRollback: true,
    commit: getCurrentCommitHash(),
    files: getGeneratedFiles(phase)
  }));
}

// 混合模式：用户可调整标记
function adjustStablePoint(phase, level) {
  // level: "promote" | "demote" | "remove"
  // promote: 用户升級某非稳定点为稳定点
  // demote: 用户降级某稳定点（谨慎）
  // remove: 移除稳定点标记（危险）
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 填充 8-rollback.md 四章节结构</name>
  <files>flow-kit/phases/8-rollback/8-rollback.md</files>
  <action>
## 填充 flow-kit/phases/8-rollback/8-rollback.md

基于 Phase 8 建立的四章节模板，填充完整内容：

### 触发条件章节
```markdown
## 触发条件

### 手动触发
- 用户执行 `/flow-kit:rollback {phase}` 命令
- 需要确认：影响范围预览、不可逆警告

### 自动触发
- 下游阶段验证失败且满足自动回滚条件
- 条件阈值由配置决定（默认：连续3次验证失败）

### 监控触发
- 外部监控系统检测到异常指标（如错误率飙升）
- 通过 webhook 或 CLI 调用触发：`/flow-kit:rollback {phase} --trigger=monitor`
```

### 核心行为章节
```markdown
## 核心行为

1. **稳定点验证**：检查目标 phase 是否为稳定点（D-RB-03）
2. **安全护栏检查**（D-RB-05）：
   - 确认提示（除非 --force）
   - 备份创建（backup branch/tag）
   - 影响范围预览
3. **回滚执行**（D-RB-01）：
   - Git revert commit
   - 清理/标记生成文件
4. **LESSONS 记录**：写入 `.specs/LESSONS.md`（D-RB-06）
5. **状态更新**：标记 phase 状态为 Rolled-back
6. **后续选择**：用户选择重新修复/废弃/其他
```

### 用户调整标记章节（D-RB-04）
```markdown
## 用户调整标记

稳定点标记采用混合模式（自动为主 + 用户可调整）：

- **自动标记**：每个 phase 执行完成时自动标记为稳定点
- **用户调整**：用户可通过以下命令调整稳定点标记：
  - `/flow-kit:rollback {phase} --adjust=promote` — 将非稳定点升級为稳定点
  - `/flow-kit:rollback {phase} --adjust=demote` — 将稳定点降级（谨慎操作）
  - `/flow-kit:rollback {phase} --adjust=remove` — 移除稳定点标记（危险操作）

调整后更新 `.planning/checkpoints/{phase}-stable.json` 中的标记状态。
```

### 边界情况章节
```markdown
## 边界情况

- **数据一致性**：回滚时确保数据不丢失或损坏（备份先于回滚）
- **级联回滚**：多个相关变更都需要回滚时，按依赖顺序依次回滚
- **无法回滚**：系统状态不支持回滚时，提示用户手动处理
- **部分回滚失败**：回滚操作部分成功时，保留备份并提示用户
- **稳定点不存在**：目标 phase 未标记为稳定点时，拒绝回滚
```

### 输出物章节
```markdown
## 输出物

- **回滚报告**：回滚操作记录和结果
- **备份引用**：backup branch/tag 名称和位置
- **问题分析**：导致回滚的问题根因
- **恢复确认**：系统恢复正常状态的确认
- **后续行动计划**：是重新修复还是创建新变更
- **流程状态更新**：变更状态标记为 Rolled-back
```
</action>
  <verify>
    <automated>grep -c "## 触发条件\|## 核心行为\|## 用户调整标记\|## 边界情况\|## 输出物" flow-kit/phases/8-rollback/8-rollback.md</automated>
    <automated>grep -c "D-RB-0" flow-kit/phases/8-rollback/8-rollback.md</automated>
  </verify>
  <done>8-rollback.md 包含完整四章节 + 用户调整标记章节，每章节内容覆盖对应决策</done>
</task>

<task type="auto">
  <name>Task 2: 创建 /flow-kit:rollback 命令规格</name>
  <files>flow-kit/commands/rollback.md</files>
  <action>
## 创建 flow-kit/commands/rollback.md

基于四章节模板和 Phase 11 命令模式：

```markdown
# Rollback Command

## 触发条件

### 命令格式
- `/flow-kit:rollback {phase}` — 回滚指定 phase
- `/flow-kit:rollback {phase} --force` — 跳过安全护栏
- `/flow-kit:rollback {phase} --trigger=auto|monitor` — 指定触发方式
- `/flow-kit:rollback {phase} --adjust=promote|demote|remove` — 调整稳定点标记（D-RB-04）

### 前置条件
- 目标 phase 已被标记为稳定点（D-RB-03）
- 用户在 flow-kit 项目根目录执行
- Git 工作区干净（或用户确认）

## 核心行为

### 1. 解析参数
```
输入：/flow-kit:rollback 10 --force
解析：
  - targetPhase: "10"
  - force: true
  - trigger: "manual"
```
```
输入：/flow-kit:rollback 10 --adjust=promote
解析：
  - targetPhase: "10"
  - adjust: "promote"  // promote|demote|remove
```

### 2. --adjust 参数处理（D-RB-04）
当指定 --adjust 时，执行稳定点标记调整：
- **promote**：将非稳定点标记为稳定点（写入 .planning/checkpoints/{phase}-stable.json）
- **demote**：将稳定点降级（修改 canRollback: false）
- **remove**：移除稳定点标记（删除标记文件）

### 3. 稳定点验证
- 检查 `.planning/checkpoints/{phase}-stable.json` 是否存在
- 若不存在：输出错误并拒绝回滚
- 若存在：加载标记文件获取 commit 和文件列表

### 4. 安全护栏检查（除非 --force）
```
显示：
[WARNING] 回滚操作不可逆
[INFO] 目标：Phase {phase}
[INFO] 将回滚以下文件：
  - file1.ts
  - file2.md
[INFO] 备份将创建于：backup/{phase}-{timestamp}

确认？ (yes/no):
```

### 5. 备份创建
- 创建 branch：`backup/{phase}-{YYYY-MM-DD-HHMMSS}`
- 或创建 tag：`rollback-backup-{phase}-{timestamp}`
- 记录备份引用到回滚报告

### 6. 执行回滚
- Git revert commit（保留历史，添加新 revert commit）
- 清理/标记生成文件（D-RB-01）
- 更新 `.planning/phases/{phase}/STATE.md` 状态为 Rolled-back

### 7. LESSONS 记录（D-RB-06）
- 追加到 `.specs/LESSONS.md`
- 包含：回滚原因、影响文件、教训

### 8. 后续选择
```
回滚完成。请选择后续操作：
1. 重新修复（启动新 phase）
2. 废弃变更
3. 其他操作
```

## 边界情况

- **稳定点不存在**：提示"Phase X 不是稳定点，无法回滚"
- **Git 冲突**：提示用户手动解决冲突后重试
- **备份创建失败**：阻止回滚，提示权限问题
- **LESSONS 写入失败**：记录到回滚报告，不阻止回滚
- **多 phase 回滚**：不支持，需逐个回滚
- **--adjust 与回滚冲突**：--adjust 模式不执行回滚，仅调整标记

## 输出物

- **回滚报告**（控制台输出）
- **备份引用**（branch/tag 名称）
- **LESSONS 追加记录**
- **Phase 状态更新**
- **标记文件更新**（--adjust 模式）
```

**注意**：使用 Phase 11 命令模式（cross-session-search.md）的四章节结构。
</action>
  <verify>
    <automated>grep -c "## 触发条件\|## 核心行为\|## 边界情况\|## 输出物" flow-kit/commands/rollback.md</automated>
    <automated>grep -c "D-RB-0\|--adjust=" flow-kit/commands/rollback.md</automated>
  </verify>
  <done>rollback.md 命令规格完整，覆盖所有触发方式和 --adjust 参数处理</done>
</task>

<task type="auto">
  <name>Task 3: phase-executor.md 新增第9集成点 + markStablePoint 实现</name>
  <files>flow-kit/lib/phase-executor.md</files>
  <action>
## 扩展 phase-executor.md

在现有 8 个集成点之后新增第 9 个集成点，同时包含 markStablePoint() 函数实现：

### 9. Rollback Trigger（回滚触发）

**集成位置**：在 "### 8. Auto Cleanup" 之后添加

```markdown
### 9. Rollback Trigger（回滚触发）

**执行时机**：用户执行 `/flow-kit:rollback` 或自动/监控触发时

**触发条件**（D-RB-02）：
- 手动：`/flow-kit:rollback {phase}`
- 自动：下游阶段验证失败连续3次
- 监控：外部监控系统 webhook 调用

**Rollback 执行流程**：
```
On rollback triggered:
  1. 验证目标 phase 是稳定点（D-RB-03）
     - 检查 .planning/checkpoints/{phase}-stable.json
     - 若不存在：拒绝回滚，提示错误
  2. 创建备份（D-RB-05）
     - 创建 branch：backup/{phase}-{timestamp}
     - 或 tag：rollback-backup-{phase}-{timestamp}
     - 记录备份引用
  3. 安全护栏检查（除非 --force）
     - 显示影响范围预览
     - 确认提示（不可逆警告）
     - 若用户拒绝：终止回滚
  4. 执行回滚（D-RB-01）
     - Git revert commit
     - 清理/标记生成文件
  5. 写入 LESSONS（D-RB-06）
     - 追加到 .specs/LESSONS.md
     - 包含：回滚原因、影响文件、教训
  6. 更新 phase 状态
     - 设置状态为 Rolled-back
     - 记录回滚时间戳
  7. 提示用户选择后续路径
     - 重新修复 / 废弃 / 其他
```

**稳定点标记触发**（D-RB-04）：
- 每个 phase 执行完成时自动标记为稳定点
- 标记文件：`.planning/checkpoints/{phase}-stable.json`
- 用户可通过 `/flow-kit:rollback {phase} --adjust=promote|demote|remove` 调整标记
```

**同时更新**：
1. 集成点目录表格（添加第9行）
2. Constitution 安全墙之后的"正常执行路径"表格（Phase 8 后添加 Rollback 行）

### 稳定点标记函数实现

在 phase-executor.md 中添加以下函数实现（放在 Constitution 安全墙之后）：

```markdown
## 稳定点标记函数

### markStablePoint(phase, reason)

phase 执行完成后自动调用，将 phase 标记为稳定点：

```javascript
function markStablePoint(phase, reason = "auto") {
  const checkpointDir = '.planning/checkpoints';
  const markerPath = `${checkpointDir}/${phase}-stable.json`;

  // 确保目录存在
  if (!fs.existsSync(checkpointDir)) {
    fs.mkdirSync(checkpointDir, { recursive: true });
  }

  // 获取当前 commit 和生成文件
  const commit = getCurrentCommitHash();
  const files = getGeneratedFiles(phase);

  // 写入标记文件
  fs.writeFileSync(markerPath, JSON.stringify({
    phase,
    markedAt: new Date().toISOString(),
    reason,  // "auto" or "manual"
    canRollback: true,
    commit,
    files
  }, null, 2));

  console.log(`[StablePoint] Phase ${phase} marked as stable (${reason})`);
}
```

### adjustStablePoint(phase, level)

用户通过 --adjust 参数调整稳定点标记：

```javascript
function adjustStablePoint(phase, level) {
  // level: "promote" | "demote" | "remove"
  const checkpointDir = '.planning/checkpoints';
  const markerPath = `${checkpointDir}/${phase}-stable.json`;

  if (level === "remove") {
    // 移除标记：删除文件
    if (fs.existsSync(markerPath)) {
      fs.unlinkSync(markerPath);
      console.log(`[StablePoint] Phase ${phase} stable point removed`);
    }
    return;
  }

  if (!fs.existsSync(markerPath)) {
    // promote 时若文件不存在，创建新标记
    if (level === "promote") {
      markStablePoint(phase, "manual");
      console.log(`[StablePoint] Phase ${phase} promoted to stable point`);
    }
    return;
  }

  // demote 或 promote：修改现有标记
  const marker = JSON.parse(fs.readFileSync(markerPath, 'utf-8'));

  if (level === "demote") {
    marker.canRollback = false;
    marker.demotedAt = new Date().toISOString();
    marker.demotedReason = "manual";
    console.log(`[StablePoint] Phase ${phase} demoted (rollback disabled)`);
  } else if (level === "promote") {
    marker.canRollback = true;
    marker.promotedAt = new Date().toISOString();
    marker.promotedReason = "manual";
    console.log(`[StablePoint] Phase ${phase} promoted to stable point`);
  }

  fs.writeFileSync(markerPath, JSON.stringify(marker, null, 2));
}
```

### 创建标记文件步骤

在 Phase 执行完成后、checkpoint 之前：
1. 调用 `markStablePoint(phase, "auto")` 创建 `.planning/checkpoints/{phase}-stable.json`
2. 标记文件包含：phase 名、标记时间、commit hash、生成文件列表、canRollback: true
</action>
  <verify>
    <automated>grep -c "Rollback Trigger\|### 9\.\|markStablePoint\|adjustStablePoint" flow-kit/lib/phase-executor.md</automated>
  </verify>
  <done>phase-executor.md 包含第9集成点和 markStablePoint()/adjustStablePoint() 函数实现</done>
</task>

</tasks>

<verification>
1. `flow-kit/phases/8-rollback/8-rollback.md` — 四章节完整填充 + 用户调整标记章节
2. `flow-kit/commands/rollback.md` — 命令规格完整，包含 --adjust 参数处理
3. `flow-kit/lib/phase-executor.md` — 包含第9集成点 + markStablePoint()/adjustStablePoint() 实现
4. 备份机制命名格式：`backup/{phase}-{YYYY-MM-DD-HHMMSS}`
5. LESSONS 追加格式：复用 Phase 10 格式
</verification>

<success_criteria>
1. 回滚命令支持三种触发方式（手动/自动/监控）
2. 安全护栏完整：确认提示 + 备份创建 + 影响范围预览
3. `--force` 跳过所有护栏
4. 回滚粒度同时包含 Git commit 和生成文件
5. 仅支持稳定点回滚
6. 回滚后自动写入 LESSONS.md
7. 用户可通过 --adjust=promote|demote|remove 调整稳定点标记
8. markStablePoint() 函数实现并创建 .planning/checkpoints/{phase}-stable.json
</success_criteria>

<output>
After completion, create `.planning/phases/12-Rollback/12-01-SUMMARY.md`
</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> Git | git 操作在此边界执行，不可信输入（phase name）需验证 |
| phase-executor -> filesystem | 备份创建和 LESSONS 写入 |
| --adjust -> checkpoints dir | 用户通过 --adjust 修改标记文件状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-RB-01 | Tampering | rollback.md 参数 | mitigate | 验证 phase name 格式，防止路径遍历 |
| T-RB-02 | Denial of Service | 频繁回滚触发 | accept | 用户主动触发，非自动 DoS |
| T-RB-03 | Information Disclosure | 备份暴露敏感文件 | mitigate | 备份在本地 branch/tag，不推送到远程 |
| T-RB-04 | Elevation of Privilege | --force 跳过护栏 | mitigate | --force 仅限本地操作，不影响他人 |
| T-RB-05 | Tampering | --adjust 恶意调整标记 | mitigate | promote/demote/remove 需用户显式确认危险操作 |
</threat_model>
