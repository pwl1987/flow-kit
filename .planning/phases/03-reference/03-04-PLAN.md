# 03-04: Rust + PHP Language Specs

## Goal
Create rust.md and php.md language specs per R1 structure

## Context
Per R1: Language specs include Toolchain (lint/test/type check/build commands), Core Concepts, Debug Guide. No framework-specific content. No code examples.

## Tasks

### Task 1: rust.md
<read_first>
- 03-CONTEXT.md (R1 decision for exact structure)
</read_first>
<action>
Create `flow-kit/reference/language-specs/rust.md` per R1 structure:
```
## Rust Engineer Reference

### Toolchain
- Lint: `cargo clippy` or `rustfmt --check`
- Test: `cargo test` (cargo test -- --nocapture for output)
- Type check: `cargo check` (faster than build, no code generation)
- Build: `cargo build --release` (optimized binary)

### Core Concepts
- **Ownership** — Each value has single owner; prevents data races and leaks
- **Borrow checker** — Compile-time borrow validation; no GC needed

### Debug Guide
- Use `cargo build` (debug) then `rust-gdb` or `rust-lldb`
- Panic messages: `RUST_BACKTRACE=1` for full stack traces
- Miri: `cargo miri` for undefined behavior detection
```
</action>
<acceptance_criteria>
- File exists: `flow-kit/reference/language-specs/rust.md`
- grep -q "Lint:" and grep -q "Test:" and grep -q "Type check:" and grep -q "Build:"
- grep -q "Core Concepts"
- grep -q "Debug Guide"
</acceptance_criteria>

### Task 2: php.md
<read_first>
- 03-CONTEXT.md (R1 decision for exact structure)
</read_first>
<action>
Create `flow-kit/reference/language-specs/php.md` per R1 structure:
```
## PHP Engineer Reference

### Toolchain
- Lint: `php -l file.php` or `phpcs --standard=PSR12`
- Test: `phpunit` or `pest` (PHPUnit wrapper)
- Type check: `phpstan analyse` or `psalm`
- Build: `composer install` or `composer dump-autoload --optimize`

### Core Concepts
- **Weak typing** — PHP is loosely typed; strict types declare at file top
- **Request lifecycle** — Single request/response; no persistent state

### Debug Guide
- Use `var_dump()` / `print_r()` for quick debugging
- Xdebug: `php -d xdebug.mode=debug` for step debugging
- Error logging: `error_log()` or syslog for production
```
</action>
<acceptance_criteria>
- File exists: `flow-kit/reference/language-specs/php.md`
- grep -q "Lint:" and grep -q "Test:" and grep -q "Type check:" and grep -q "Build:"
- grep -q "Core Concepts"
- grep -q "Debug Guide"
</acceptance_criteria>

## Verification
- Both files exist with R1 structure (Toolchain/Core Concepts/Debug Guide)
- Each contains lint, test, type check, build commands