---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "flow-kit/GO.md"
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "GO.md loads constitution.md and user-config.md as first priority"
    - "GO.md routes /flow-kit:health to M-health.md"
    - "GO.md routes /flow-kit:scan to I-intel-scan.md"
    - "GO.md routes /flow-kit:update-context to update-context.md"
    - "GO.md routes /flow-kit:sync-config to sync-team-config.md"
    - "GO.md routes /flow-kit:archive to archive-change.md"
    - "Exact match routes directly without prompt"
    - "Fuzzy match (Levenshtein distance <= 2) prompts confirmation"
    - "Substring match suggests closest command"
    - "No match shows available commands + usage hint"
  artifacts:
    - path: "flow-kit/GO.md"
      provides: "Single entry point with command routing"
      min_lines: 80
      exports:
        - "/flow-kit:health"
        - "/flow-kit:scan"
        - "/flow-kit:update-context"
        - "/flow-kit:sync-config"
        - "/flow-kit:archive"
  key_links:
    - from: "GO.md"
      to: "config/constitution.md"
      via: "priority loading step 1"
      pattern: "> 【CLAUDE CODE INSTRUCTION 强制约束】.*constitution"
    - from: "GO.md"
      to: "config/default-user-config.md"
      via: "priority loading step 2"
      pattern: "> 【CLAUDE CODE INSTRUCTION 强制约束】.*default-user-config"
    - from: "GO.md"
      to: "commands/M-health.md"
      via: "/flow-kit:health route"
---

<objective>
Create flow-kit/GO.md with routing logic per CORE-02.

Purpose: Single entry point that loads constitution/user-config first, then routes commands.
Output: GO.md with exact-match + Levenshtein fuzzy routing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-skeleton/01-CONTEXT.md
@.planning/phases/01-core-skeleton/01-RESEARCH.md

## Routing Algorithm (from CONTEXT.md D-02)

1. Parse user input for /flow-kit:xxx command
2. EXACT MATCH: if command == known command -> route directly
3. FUZZY SEARCH:
   - Calculate Levenshtein distance to all known commands
   - If min_distance <= 2 -> prompt "Did you mean {closest}? [y/n]"
   - If y -> route to closest
4. SUBSTRING MATCH: if command is substring of any known -> suggest
5. NO MATCH: show available commands + usage hint

## Known Commands
- /flow-kit:health -> @flow-kit/commands/M-health.md
- /flow-kit:scan -> @flow-kit/commands/I-intel-scan.md
- /flow-kit:update-context -> @flow-kit/commands/update-context.md
- /flow-kit:sync-config -> @flow-kit/commands/sync-team-config.md
- /flow-kit:archive -> @flow-kit/archive/archive-change.md

## Priority Loading (per PRD Section 二.2)
> 【CLAUDE CODE INSTRUCTION 强制约束·第一步】
> 1. 立即加载 @flow-kit/config/constitution.md（全局最高优先级，任何逻辑不得违反）
> 2. 立即加载 @flow-kit/config/default-user-config.md，若存在 .flow-kit/user-config.md 则优先加载用户自定义配置
> 3. 后续所有逻辑必须遵守这两份配置
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write GO.md with priority loading and command routing</name>
  <files>/data/Code/flow-kit/flow-kit/GO.md</files>
  <action>
Create flow-kit/GO.md at /data/Code/flow-kit/flow-kit/GO.md

File format requirements (per PRD):
1. Every file MUST start with `> 【CLAUDE CODE INSTRUCTION 强制约束】` block
2. File wrapped with `--- BEGIN flow-kit/GO.md ---` / `--- END flow-kit/GO.md ---`

Content structure:
1. CLAUDE CODE INSTRUCTION block at top with:
   - Priority loading instructions (constitution.md first, user-config.md second)
   - Command routing rules

2. Main content with:
   - Header: "# flow-kit Entry Point"
   - Section: "## Available Commands" listing all 5 commands
   - Section: "## Routing Logic" explaining exact/fuzzy/substring matching
   - Section: "## Levenshtein Implementation" with inline function (threshold <= 2)
   - Section: "## Confirmation Prompt" for fuzzy matches

Routing behavior:
- Exact match: route directly to target file
- Levenshtein distance <= 2: prompt "Did you mean `{closest}`? [y/n]"
- Substring match: suggest the matched command
- No match: show "Available commands: /flow-kit:health, /flow-kit:scan, /flow-kit:update-context, /flow-kit:sync-config, /flow-kit:archive"
</action>
  <verify>
    <automated>grep -c "flow-kit:health\|flow-kit:scan\|flow-kit:update-context\|flow-kit:sync-config\|flow-kit:archive" /data/Code/flow-kit/flow-kit/GO.md && grep -c "Levenshtein" /data/Code/flow-kit/flow-kit/GO.md && grep -c "constitution" /data/Code/flow-kit/flow-kit/GO.md</automated>
  </verify>
  <done>GO.md routes all 5 commands, implements Levenshtein fuzzy matching, loads constitution/user-config as first priority</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->GO.md | Untrusted user input (command strings) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Command routing | mitigate | Exact match required before routing; fuzzy requires confirmation |
| T-01-02 | Tampering | GO.md content | accept | Markdown files are version-controlled; user reviews before use |
| T-01-03 | Repudiation | Command execution | accept | flow-kit is documentation framework; execution is user's responsibility |
| T-01-04 | Information Disclosure | Config paths | accept | Paths are documented; no secrets in flow-kit |
| T-01-05 | Denial of Service | Infinite routing loop | mitigate | Simple string matching; no loops possible |
| T-01-06 | Elevation of Privilege | Unauthorized command | mitigate | Only 5 known commands routed; no arbitrary execution |
</threat_model>

<verification>
- GO.md exists at flow-kit/GO.md
- Contains "constitution" reference (priority loading)
- Contains "flow-kit:health", "flow-kit:scan", "flow-kit:update-context", "flow-kit:sync-config", "flow-kit:archive"
- Contains "Levenshtein" implementation with threshold <= 2
- Starts with "> 【CLAUDE CODE INSTRUCTION 强制约束】"
- Has "--- BEGIN flow-kit/GO.md ---" wrapper
</verification>

<success_criteria>
- GO.md loads constitution.md and user-config.md as first priority (per success criteria #2)
- GO.md routes all commands correctly (per success criteria #3)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-skeleton/01-02-SUMMARY.md`
</output>
