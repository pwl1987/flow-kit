---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "flow-kit/ directory structure matches PRD specification"
    - "All required subdirectories exist"
    - ".specs/ directory structure created on project side"
  artifacts:
    - path: "flow-kit/"
      provides: "Root directory"
    - path: "flow-kit/phases/"
      provides: "8 phase workflow directories"
    - path: "flow-kit/templates/"
      provides: "Template files directory"
    - path: "flow-kit/commands/"
      provides: "Lateral commands directory"
    - path: "flow-kit/guardrails/"
      provides: "Brownfield guardrails directory"
    - path: "flow-kit/skills/"
      provides: "Skill packages directory"
    - path: "flow-kit/mcp/"
      provides: "MCP adaptation directory"
    - path: "flow-kit/reference/"
      provides: "Engineering reference directory"
    - path: "flow-kit/reference/language-specs/"
      provides: "Language specifications directory"
    - path: "flow-kit/config/"
      provides: "Configuration directory"
    - path: "flow-kit/archive/"
      provides: "Archive directory"
    - path: ".specs/"
      provides: "Project-side specs directory"
  key_links: []
---

<objective>
Create complete flow-kit/ directory structure per CORE-01 and PRD specification.

Purpose: Establish the foundational directory layout that all subsequent phase files will be created within.
Output: All required directories created under flow-kit/ and .specs/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

## Directory Structure (per CONTEXT.md locked decision D-01)

```
flow-kit/
├── GO.md                  # Entry point (Wave 2)
├── README.md              # Quick start (Wave 1)
├── CONSTITUTION.md        # (Phase 2)
├── phases/
│   ├── 01-core-skeleton/  # Phase spec directory
│   ├── 02-guardrails/     # Phase spec directory
│   ├── 03-reference/       # Phase spec directory
│   ├── 04-integration/     # Phase spec directory
│   ├── 0-change/          # Phase file directory
│   ├── 1-requirement/     # Phase file directory
│   ├── 2-design/          # Phase file directory
│   ├── 3-task/           # Phase file directory
│   ├── 4-dev/            # Phase file directory
│   ├── 5-test/          # Phase file directory
│   ├── 6-review/         # Phase file directory
│   ├── 7-integration/    # Phase file directory
│   └── 8-rollback/       # Phase file directory
├── templates/
├── commands/
├── guardrails/
├── skills/
├── mcp/
├── reference/
│   └── language-specs/
├── config/
└── archive/

.specs/                    # PROJECT-SIDE: Auto-created
├── CONTEXT.md
├── LESSONS.md
├── COST-REPORT.md
├── pending-approvals/
└── archive/
```

Note: PRD says `core/` but CONTEXT.md locked decision uses `phases/`. Use `phases/` per D-01.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flow-kit root directory structure</name>
  <files>/data/Code/flow-kit/flow-kit</files>
  <action>
Create the following directories under /data/Code/flow-kit/flow-kit/:
- phases/ (with 01-core-skeleton/, 02-guardrails/, 03-reference/, 04-integration/ subdirs for phase specs)
- phases/ (with 0-change/, 1-requirement/, 2-design/, 3-task/, 4-dev/, 5-test/, 6-review/, 7-integration/, 8-rollback/ subdirs for phase files)
- templates/
- commands/
- guardrails/
- skills/
- mcp/
- reference/
- reference/language-specs/
- config/
- archive/

Use: mkdir -p for each directory path. Create parent first, then children.

Do NOT create files - only directories in this task.
  </action>
  <verify>
    <automated>find /data/Code/flow-kit/flow-kit -type d | sort</automated>
  </verify>
  <done>All 12 directories exist under flow-kit/</done>
</task>

<task type="auto">
  <name>Task 2: Create .specs/ directory structure (project-side)</name>
  <files>/data/Code/flow-kit/.specs</files>
  <action>
Create the following directories under /data/Code/flow-kit/.specs/:
- pending-approvals/
- archive/

Note: .specs/CONTEXT.md, .specs/LESSONS.md, .specs/COST-REPORT.md are files (created in later phases), not directories.
  </action>
  <verify>
    <automated>find /data/Code/flow-kit/.specs -type d | sort</automated>
  </verify>
  <done>All 2 directories exist under .specs/</done>
</task>

</tasks>

<verification>
- `find /data/Code/flow-kit/flow-kit -type d | wc -l` returns 22 (12 + 10 subdirs)
- `find /data/Code/flow-kit/.specs -type d | wc -l` returns 3 (root + 2 subdirs)
- Directory names match exactly: phases/, templates/, commands/, guardrails/, skills/, mcp/, reference/, reference/language-specs/, config/, archive/
</verification>

<success_criteria>
- All directories created per CORE-01 specification
- Directory names match PRD/CONTEXT.md exactly (using `phases/` not `core/`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-skeleton/01-01-SUMMARY.md`
</output>
