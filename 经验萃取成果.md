# 经验萃取成果 — flow-kit v3.6.0

> 来源：Y:\pwl\code\plugins\Clippings 全部 122 篇文件分析
> 提取维度：技术最佳实践、完整技术方案、设计模式与架构思想
> 版本：v3.0 | 日期：2026-05-16 | 状态：全部完成（优化合并版）

---

## 目录

- [一、AI 编程工作流框架对比与选型](#一ai-编程工作流框架对比与选型)
- [二、Spec 驱动开发（SDD）方法论](#二spec-驱动开发sdd方法论)
- [三、Skill 设计与上下文工程](#三skill-设计与上下文工程)
- [四、多代理编排与团队协作](#四多代理编排与团队协作)
- [五、自主运行与持续执行](#五自主运行与持续执行)
- [六、CLAUDE.md 配置与行为塑造](#六claudemd-配置与行为塑造)
- [七、Token 优化与成本控制](#七token-优化与成本控制)
- [八、代码审查与质量保障](#八代码审查与质量保障)
- [九、TDD 与测试策略](#九tdd-与测试策略)
- [十、自进化与知识复利](#十自进化与知识复利)
- [十一、flow-kit 可借鉴的具体改进点](#十一flow-kit-可借鉴的具体改进点)
- [十二、Context Engineering 与上下文管理](#十二context-engineering-与上下文管理)
- [十三、全局路由协议与多框架协作](#十三全局路由协议与多框架协作)
- [十四、生产级交付实践](#十四生产级交付实践)
- [十五、团队工程化落地](#十五团队工程化落地)
- [十六、AutoResearch 方法论](#十六autoresearch-方法论)
- [十七、DESIGN.md 设计系统与 grill-me 需求访谈](#十七designmd-设计系统与-grill-me-需求访谈)
- [十八、web-access 浏览器直连](#十八web-access-浏览器直连)
- [十九、Google Agent Skills](#十九google-agent-skills)
- [二十、Command Center 目标管理](#二十command-center-目标管理)

---

## 一、AI 编程工作流框架对比与选型

### 1.1 六大框架核心定位

| 框架 | 核心定位 | 适用场景 | Star |
|------|----------|----------|------|
| **Superpowers** | 技能驱动工作流，14 个可组合 Skill | 个人开发、质量纪律 | 115K+ |
| **OpenSpec** | 轻量 Spec 驱动，变更管理 | 已有项目迭代、需求对齐 | 34.5K |
| **Spec-Kit** | GitHub 官方，规范可执行化 | 企业级、跨平台 | 82.5K |
| **GSD** | 上下文隔离 + 阶段化执行 | 长任务、复杂重构 | - |
| **BMAD** | AI 敏捷开发全框架，21 Agent | 企业级、0→1 产品 | 34.2K |
| **GStack** | YC 出品，6 大专家工作流 | Claude 专属、生产发布 | 82K+ |

### 1.2 关键洞察

- **Superpowers 管"怎么做"**（执行纪律），**OpenSpec 管"做什么"**（需求规范），两者互补
- **GSD 解决上下文腐烂**：波浪式并行，无依赖任务同时执行，每个子代理独立上下文
- **BMAD 是完整方法论**：product-brief → prd → architecture → dev-story → review 全流程
- **GStack 模式切换**：CEO 视角（产品方向）→ Eng 视角（技术严谨）→ QA 视角（浏览器自动化测试）

### 1.3 对 flow-kit 的启示

- flow-kit 的 Phase 体系可借鉴 OpenSpec 的 propose → apply → archive 三阶段模型
- 可引入 GSD 的"上下文隔离"思想，为每个 Phase 提供独立上下文
- Superpowers 的"技能自动触发"机制可集成到 Phase 工作流中

---

## 二、Spec 驱动开发（SDD）方法论

### 2.1 OpenSpec 核心工作流

```
/opsx:new → /opsx:continue → /opsx:ff → /opsx:apply → /opsx:verify → /opsx:archive
```

**关键设计**：
- **变更驱动**：每个功能变更是独立目录（proposal.md + specs/ + design.md + tasks.md）
- **增量规范**：通过 delta spec 描述变更，而非重写整个规范
- **制品依赖图（DAG）**：按依赖关系逐步生成制品
- **Expanded 模式**：7 个命令提供细粒度控制

### 2.2 四层防护 TDD 模型（OpenSpec + Superpowers）

| 层级 | 机制 | 解决的问题 |
|------|------|------------|
| 第一层 | 原子化任务 | 任务内部混合 RED+GREEN |
| 第二层 | Subagent 隔离 | AI 跨任务批量执行 |
| 第三层 | 两阶段审查 | Subagent 过度执行 |
| 第四层 | 验证证据 | 无法确认 TDD 顺序 |

**核心发现**：失败根因不在 instruction 措辞，在**任务粒度**。每个 task 只包含一个 TDD 阶段。

### 2.3 对 flow-kit 的启示

- Phase 工作流可引入"制品依赖图"概念，明确每个 Phase 的输入/输出
- 可借鉴"增量规范"思想，Phase 间传递变更描述而非完整文档
- 四层防护模型可应用于 Phase 4（开发执行）的质量保障

---

## 三、Skill 设计与上下文工程

### 3.1 Skill 三层渐进式披露架构

```
metadata 层 → 只加载名称和描述（判断是否激活）
instructions 层 → Skill 激活时载入（核心指令）
resources 层 → 按需调用（references/scripts/templates）
```

### 3.2 Skill 设计原则

1. **职责单一**：一个 Skill 只做一件事
2. **边界清晰**：明确输入/输出/触发条件
3. **可组合**：Skill 之间可串联形成工作流
4. **可移植**：不依赖特定项目环境

### 3.3 Skill 质量评估六维度（skills-refiner）

| 维度 | 评估内容 |
|------|----------|
| 定位 | Skill 真正解决什么问题，边界在哪 |
| 机制 | 哪些设计选择真正驱动了行为 |
| 价值 | 什么是真正强的和可复用的 |
| 风险 | 什么是脆弱的或难以维护的 |
| 改进 | 具体的提升方向 |
| 集成 | 可移植性评估 |

### 3.4 Skill 协议设计关键

- **MANDATORY / RECOMMENDED / ON ERROR** 三级关键词让 Agent 明确优先级
- 状态管理应分层：必须执行（看板追踪）vs 可选增强（持久化脚本）
- 减少 Agent 执行负担：从 6 次脚本调用降到 2 次，负担降低 67%

### 3.5 Skill-creator 评估体系

**四大新能力**：
1. 评估系统：自动体检报告，触发率、问题定位
2. 基准测试：通过率、耗时、token 用量全量化
3. 多代理并行测试：每个代理在干净环境独立运行，零交叉污染
4. 描述调优：自动优化触发条件

### 3.6 对 flow-kit 的启示

- Phase 工作流文件可改造为 Skill 格式，利用渐进式披露减少 token 消耗
- 引入 MANDATORY/RECOMMENDED/OPTIONAL 三级指令标记
- 建立 Skill 质量评估机制

---

## 四、多代理编排与团队协作

### 4.1 OMC（Oh My ClaudeCode）多代理系统

- 多 Agent 分工：规划 / 编码 / 验证
- 自动 pipeline：plan → execute → verify
- 工具链：AST / LSP / Python REPL
- 状态管理 + workflow

### 4.2 GStack 六角色虚拟团队

| 命令 | 角色 | 职责 |
|------|------|------|
| `/plan-ceo-review` | CEO | 重新思考产品方向 |
| `/plan-eng-review` | 工程经理 | 架构、数据流、边界 |
| `/review` | 代码审查员 | 自动修复、阻塞严重 bug |
| `/qa` | QA 工程师 | 浏览器自动化测试 |
| `/ship` | 发布工程师 | 测试、PR、部署 |
| `/retro` | 复盘教练 | 工程复盘 |

### 4.3 团队配置七大核心构件

从强约束到弱约束的七层配置体系：

| 构件 | 职责 | 约束强度 |
|------|------|----------|
| CLAUDE.md | 项目级"记忆与约定" | 中 |
| rules/ | 团队必须遵守的"底线规则" | 强 |
| agents/ | 专用"子代理分工" | 中 |
| commands/ | 高频操作"一键化流程" | 中 |
| skills/ | 可复用的"方法论与领域知识" | 弱 |
| hooks/ | 关键节点"自动化护栏" | 强 |
| MCP 配置 | 外部工具"接入层" | 弱 |

**落地三原则**：
1. 按"项目独有/全局通用"划分存储位置：团队共享进仓库，个人专属放本地
2. 用 rules 固化底线，用 commands 固化流程：底线和流程分开
3. 把"需要换脑子"的任务交给 Agent：减少主对话上下文污染

### 4.4 四层配置体系（Best Practice）

| 层级 | 机制 | 定位 |
|------|------|------|
| L1 | CLAUDE.md | 项目记忆（建议性） |
| L2 | Skills | 可复用工作流 |
| L3 | Hooks | 强制执行规则 |
| L4 | Subagents | 分工协作 |

**核心洞察**：CLAUDE.md 控制在 150 条指令以内，详细流程放 Skills，安全规则放 Hooks，复杂任务交给 Subagents。

### 4.5 对 flow-kit 的启示

- flow-kit 可引入"虚拟团队"概念，不同 Phase 分配不同角色
- 借鉴 GStack 的模式切换思想，Phase 间切换审查视角
- 七大构件体系可指导 flow-kit 的配置文件结构设计
- Phase 工作流文件应按层级拆分，避免单文件过大

---

## 五、自主运行与持续执行

### 5.1 Claude Code /loop 命令

**两种模式**：
- **定时调度（Cron 模式）**：`/loop 30m /review`，支持秒/分/时/天级别间隔
- **自主迭代（Agentic Loop）**：自动试错引擎，规划→执行→验证→修正→循环

**三种调度方案对比**：

| 方案 | 运行位置 | 需要开机 | 访问本地文件 |
|------|----------|----------|-------------|
| Cloud 任务 | Anthropic 云端 | 不需要 | 不能 |
| Desktop 任务 | 你的机器 | 需要 | 能 |
| /loop | 你的机器 | 需要（会话级） | 能 |

### 5.2 Ralph Wiggum 循环（16.5K Stars）

**核心逻辑**：PRD → prd.json → 循环执行 → 全部完成 → 输出 `<promise>COMPLETE</promise>`

每次循环：
1. 从 prd.json 挑出优先级最高的 `passes: false` 的用户故事
2. 启动全新 AI 实例，带项目上下文
3. AI 完成这一项 story
4. 运行质量检查（typecheck、测试）
5. 检查通过 → 提交 commit → 标记 `passes: true`
6. 把学习到的上下文追加到 progress.txt 和 AGENTS.md
7. 重复，直到所有 story 完成

**四种安全 Hook**：
1. No-Ask-Human：防止 AI 向用户提问
2. Context Monitor：监控上下文使用率
3. Syntax Check：代码语法检查
4. Decision Warn：高风险决策警告

**三级反馈闭环**：

| 层级 | 机制 | 作用 |
|------|------|------|
| Typecheck | 编译/类型检查 | 拦截类型错误 |
| 测试 | 单元/集成测试 | 验证行为正确性 |
| Git history | 版本历史 | 让下一个 AI 看到上一个 AI 写了什么 |

### 5.3 Claude Code Task 系统

- 内置任务管理：TaskCreate / TaskList / TaskUpdate / TaskGet
- 三种依赖模式：线性流水线 / 并行任务池 / 菱形依赖
- 触发方式：提示词要求 / CLAUDE.md 配置 / 中途切换
- 任务粒度：5-30 分钟执行量最佳

### 5.4 claude-task-master：PRD 驱动的任务自动拆解

**核心机制**：
- 输入 PRD 文档 → 自动解析为结构化任务列表（tasks.json）
- 自动分析任务依赖关系，按依赖顺序逐个执行
- 三种模型分工：主模型（编码）、研究模型（搜索最新信息）、兜底模型（容灾）

**关键设计**：
- 36 个工具分级加载：core（7个/~5000 token）、standard（15个/~10000 token）、all（36个/~21000 token）
- 复杂度分析：动手前先评估项目复杂度、关键路径、预估工时
- 动态任务插入：需求变更时可直接插入新任务到依赖图正确位置

### 5.5 对 flow-kit 的启示

- 集成 /loop 或 Ralph 循环实现 Phase 自动推进
- 利用 Task 系统管理 Phase 内部任务
- 建立安全 Hook 机制防止自主运行失控
- Ralph 的"每次迭代全新 AI 实例"设计可直接应用于 Phase 4（开发执行）
- prd.json 的 passes/fail 状态追踪可集成到 session-state.json
- 工具分级加载机制可应用于 flow-kit 的 Token 优化

---

## 六、CLAUDE.md 配置与行为塑造

### 6.1 Karpathy 四原则

| 原则 | 内容 | 解决的问题 |
|------|------|-----------|
| Think Before Coding | 先描述打算做什么、为什么 | 自信地犯蠢 |
| Simplicity First | 3 行能解决的别写 30 行 | 过度工程 |
| Surgical Changes | 只改需要改的，不顺手重构 | 范围蔓延 |
| Goal-Driven Execution | 持续对齐目标，偏离就停 | 跑偏 |

### 6.2 Mnilax 12 条规则（错误率从 41% 降到 3%）

核心扩展：在 Karpathy 4 条基础上增加项目特定规则、安全规则、API 设计规则、性能规则

### 6.3 8 行规则（输出 token 降 63%）

1. Think before acting
2. Be concise in output but thorough in reasoning
3. Prefer editing over rewriting whole files
4. Do not re-read files you have already read
5. Test your code before declaring done
6. No sycophantic openers or closing fluff
7. Keep solutions simple and direct
8. User instructions always override this file

### 6.4 双 CLAUDE.md 策略

```
CLAUDE.md = Karpathy（行为层）+ 项目规范（约束层）+ OMC（执行层）
```

### 6.5 五个神级 Skill

| Skill | 功能 | 效果 |
|-------|------|------|
| Caveman | 输出极简化 | Token 节省 75% |
| Memory Compiler | Hook 自动捕获→提取知识→编译知识库 | 每天省 1 小时 |
| Paper2Code | arxiv URL → citation-anchored 代码 | 论文→可运行代码 |
| CLAUDE.md Optimizer | 自动分析项目→生成上下文 | 比手写更精准 |
| TestDrift | 检测代码变更→自动生成测试 | TDD 流程不中断 |

### 6.6 对 flow-kit 的启示

- flow-kit 的 CLAUDE.md 应分层设计：行为层 + 约束层 + 执行层
- 引入 Karpathy 四原则作为基础行为约束
- 建立项目级 rules/ 目录
- Caveman 压缩模式可应用于 Phase 执行输出
- Memory Compiler 的 Hook 自动捕获可集成到 session-state

---

## 七、Token 优化与成本控制

### 7.1 五大优化策略

| 策略 | 方法 | 节省效果 |
|------|------|----------|
| Prompt Caching | system prompt/tool定义/长文档末尾放 cache_control 断点 | cache hit 仅 0.1× 基础价 |
| 模型分级 | Haiku 干杂活、Sonnet 干活、Opus 留给硬骨头 | Haiku 比 Opus 便宜 5× |
| 上下文卫生 | CLAUDE.md ≤200行、任务切换用 /clear、/compact 带焦点 | 每轮节省 $0.025+ |
| Thinking 预算 | MAX_THINKING_TOKENS=8000-10000，简单任务 /effort low | 账单再降 20-30% |
| Hooks预处理+子智能体+Batch | 日志过滤、子智能体外包、批量任务走 Batch API | Batch 50% 折扣 |

**叠加效果**：$19.50 → $8.22（节省 58%），加 Batch 可压到 30-40%

### 7.2 OpenSpace 自进化降本

- 同样任务：token 消耗减少 46%
- 任务收益：4.2 倍
- 机制：自动捕获成功模式 → 封装为 Skill → 下次复用

### 7.3 Caveman：输出压缩 75%

- 4 种强度模式：Lite（轻度）、Full（标准）、Ultra（极限）、文言文
- 技术准确性不变，只去掉废话
- 多 Agent 通用，Auto-activation 自动生效

### 7.4 RTK：终端输出净化，Token 节省 89%

**四层净化策略**：

| 层级 | 策略 | 效果 |
|------|------|------|
| 智能过滤 | 剔除注释、空行、ANSI 颜色码、进度条、无关警告 | 从源头减少 Token |
| 分组聚合 | 同类输出合并展示（搜索结果按文件分组、错误按类型归类） | 避免重复铺开 |
| 智能截断 | 基于启发式保留最有价值上下文，砍掉重复/长尾片段 | 去除冗余 |
| 去重合并 | 反复出现的相同行合并并附带次数（"连接超时（10次）"） | 大幅缩短输出 |

**实测数据**：30 分钟开发会话从 15 万 Token 压至 1.7 万（节省 88.9%），部分命令压缩率高达 99%。零侵入设计：单二进制文件、不改代码、`rtk init --global` 一键启用全局 Hook。

### 7.5 OpenWolf：Claude Code 的第二大脑，Token 节省 80%

**三文件记忆系统**：

| 文件 | 作用 | 机制 |
|------|------|------|
| anatomy.md | 项目地图 | 每个文件一行描述 + Token 估算，Claude 读文件前先看描述 |
| cerebrum.md | 学习记忆 | 记录每次纠正和偏好，Do-Not-Repeat 列表防止重复犯错 |
| buglog.json | Bug 账本 | 错误信息、根因和修复方案自动归档，下次直接套用 |

**实测数据**：相同任务裸 Claude CLI 消耗 250 万 Token，OpenWolf + Claude CLI 仅 42.5 万（节省约 80%）。20 个项目、132+ 次会话统计平均节省 65.8%，重复文件读取拦截率 71%。通过 6 个 Hook 脚本在 Claude 读文件、写代码的关键节点上做手脚。

### 7.6 对 flow-kit 的启示

- Phase 工作流文件采用渐进式披露设计
- 建立 token 使用监控机制
- 考虑引入"经验复用"减少重复推理
- RTK 的终端输出净化可集成到 Phase 4（开发执行）的工具调用层
- OpenWolf 的 anatomy.md 项目地图可替代重复的文件探索
- cerebrum.md 的 Do-Not-Repeat 列表可集成到 session-state.json
- buglog.json 的 Bug 自动归档可集成到 F5（自我进化机制）

---

## 八、代码审查与质量保障

### 8.1 大模型代码审核的三种核心风险

| 风险类型 | 表现 | 危害 |
|----------|------|------|
| 漏报 | 返回值语义变了但调用方未更新、新增提前返回未释放资源、字段改名但序列化未同步 | 最危险，可能进入生产环境 |
| 误报 | 假设函数返回 null（实际保证不会）、假设字段来自用户输入（实际来自常量）、把风格偏好说成正确性问题 | 打断开发者思路，可能接受错误修复 |
| 不确定性伪装成结论 | 没看到完整上下文却推断完整调用链、没运行测试却断言一定失败、证据不足但结论很硬 | 降低工具可信度 |

### 8.2 为什么单个 Prompt 不够

- **不能保证独立性**：模型先看到某个结论后，后续容易围绕该结论继续分析，形成上下文惯性
- **不能保证覆盖率**：模型注意力偏向更显眼、更靠前的片段，大 diff 时后半部分被粗略带过
- **不能自动区分共识和猜测**：强证据问题、推断问题、建议混在一起，开发者难以判断优先级

### 8.3 Multi-Pass Review 核心设计

```
准备审查材料 → 执行独立审核轮次 → 聚合并生成报告
```

**关键原则**：
- 每轮输入一致、输出结构一致、审查规则一致
- 每轮结果独立保存，最终由聚合规则处理
- 多轮一致且证据充分的问题优先处理
- 单轮发现但证据充分的问题人工确认后处理
- 单轮发现且证据不足的作为建议或待验证项

### 8.4 Schema 设计原则

| 字段 | 要求 |
|------|------|
| severity | 只保留 HIGH/MEDIUM/LOW 三级，定义清晰（HIGH=崩溃/数据损坏/安全风险，MEDIUM=逻辑错误/边界缺失，LOW=风格/可读性） |
| category | 短而稳定：logic/security/error-handling/compatibility/performance/concurrency/resource-lifetime/test-coverage |
| description | 必须写证据链：改动做了什么 → 为什么产生风险 → 什么条件下触发 → 缺少什么保护 |
| suggestion | 必须可执行、最小化：只修当前问题，不引入无关重构，不改变公共契约 |

### 8.5 聚合规则

- 共识问题优先进入主报告
- 单轮问题不直接丢弃，放入"待确认问题/可选建议/低置信发现"区域
- LOW 问题默认不打扰主流程
- 聚合必须保留原始证据（哪些轮次发现、各轮次严重程度、代表性描述）

### 8.6 六大反模式

1. 把 Skill 写成长篇教程而非执行规则
2. 没有脚本，全部靠模型自觉
3. 没有输出 schema，无法可靠聚合
4. 把所有问题都输出到主报告（噪声大）
5. 复审时只看旧问题（修复本身可能引入新问题）
6. 把模型建议当成自动修复

### 8.7 code-review-graph 知识图谱

- 减少 6.8 倍代码审查 Token
- 只读关键代码，不加载全量上下文
- 本地知识图谱构建，2900 个文件重新索引仅需不到 2 秒
- 爆炸半径分析：文件变更时追踪每个调用者、依赖项和受影响的测试，100% 召回率

### 8.8 Claude Code /simplify 命令

- 三个并行代理：安全性、性能、规范性
- 取代过时的 /review

### 8.9 对 flow-kit 的启示

- Phase 6（代码审查）应引入 Multi-Pass 机制：多轮独立审查 + 结构化输出 + 聚合报告
- 建立审查 Schema 标准（severity/category/description/suggestion）
- 聚合脚本化，不让模型审自己的报告
- 区分"审核"和"修复"两个阶段
- 考虑引入知识图谱减少审查 token

---

## 九、TDD 与测试策略

### 9.1 Superpowers TDD 强制执行

- Iron Law：没有失败测试，不能写功能代码
- 红-绿-重构循环不可跳过
- 反合理化表格：列出 AI 可能找的借口并预先反驳

### 9.2 四层防护 TDD 模型

详见第二章 2.2 节。核心发现：失败根因不在 instruction 措辞，在**任务粒度**。

### 9.3 验证证据原则

- RED 阶段：报告必须显示测试失败
- GREEN 阶段：报告必须显示测试通过
- REFACTOR 阶段：全量测试输出
- 不看 AI 怎么说，看测试输出怎么说

### 9.4 对 flow-kit 的启示

- Phase 4（开发执行）强制引入 TDD 循环
- 建立"验证证据"机制，Phase 完成需附带测试输出
- 引入反合理化表格防止 AI 跳过测试

---

## 十、自进化与知识复利

### 10.1 自进化引擎失效分析：五大断裂点

self-improving-agent 被安装 43 万次，但真正形成完整进化闭环的用户不足 30%。五个关键节点在实践中全部断裂：

| 断裂点 | 现象 | 根因 |
|--------|------|------|
| Hook 提醒软化 | 提醒每次都出现，但从未转化为实际写入 | Agent 忙完后不被明确要求就不会主动写 |
| 纠正未记录 | 用户说"用 pnpm 不是 npm"，Agent 回"好的记住了"但 LEARNINGS.md 为空 | 没有 WAL 协议捕获修正 |
| 错误未分类 | Docker build 失败写入了每日日志，但 ERRORS.md 为空 | 每日日志与 .learnings/ 未打通 |
| 重复计数失效 | 同一个错误遇到 4 次，Recurrence-Count 始终为 1 | 前三个断裂点导致每次都被当作"第一次" |
| 知识映射缺失 | VFM 通过后 Agent 不知道该写入哪个文件 | SKILL.md 有分类规则但没有映射表 |

### 10.2 完整闭环搭建方法

**第一步：修正状态元数据** — Hook 显示 ✓ ready 但 SESSION-STATE.md 标记"未启用"，这种不一致会误导后续所有判断。

**第二步：建立硬触发规则（WAL 协议）** — 先写入，再回复。六大触发场景：
1. 命令执行失败 → ERRORS.md
2. 用户纠正行为/输出 → LEARNINGS.md
3. 发现非显而易见的解决方案 → LEARNINGS.md
4. 用户提出功能需求 → FEATURE_REQUESTS.md
5. 识别到重复模式 → 更新 Recurrence-Count
6. 任务完成复盘 → 评估是否有可提炼知识

**第三步：增强状态文件** — heartbeat-state.json 增加 learnings 统计字段（lastLearningsReview、pendingLearningsCount、promotedLearningsCount、learningsStatus），让微观引擎和宏观引擎共享状态。

**第四步：Cron 读取 .learnings/** — 每日复盘 Cron 增加 11 个步骤，包含读取 .learnings/ 三文件、统计 pending 条目、识别 Recurrence-Count ≥ 3 的条目、执行 VFM 评分、promote 到 SOUL/AGENTS/TOOLS。

**第五步：配置月度人工复盘** — 处理 Cron 无法覆盖的灰度区域：低频高价值模式、用户反馈驱动的临时需求、跨时间段趋势判断。

### 10.3 VFM 评分模型

VFM（Value-Frequency-Maintainability）三维评分决定是否将学习条目提升为永久规则：
- Value：这个教训能避免多大损失
- Frequency：这个场景出现的频率
- Maintainability：规则本身的维护成本

**关键经验**：对 infra 类型错误需要偏向性加权（高频度和失败减少各上浮 1 分），因为换一台机器、换一个项目几乎必现。

### 10.4 OpenSpace 自进化引擎

**三个自动机制**：
- AUTO-FIX：技能失败 → 自动分析原因 → 自动修复
- AUTO-IMPROVE：执行成功 → 分析模式 → 生成更好版本
- AUTO-LEARN：发现新工作流 → 自动捕获 → 封装为新技能

**效果**：第 1 次 50 步骤 → 第 2 次 5 步骤，token 降 80%；任务收益 4.2 倍；Token 消耗减少 46%。

### 10.5 Compound Engineering（CE）知识复利系统

**核心定位**：Superpowers 管纪律，CE 管能力。层次不同，不构成替代。

**知识沉淀机制**：`/ce:compound` 启动 5 个子代理，自动提取本轮踩过的坑、用到的模式、review 的关键发现，结构化存入 `docs/solutions/` 目录。下一轮 plan 时，learnings-researcher 子代理自动搜索并引用。

**验证效果**：第二轮 plan 自动引用第一轮经验，设计方案主动避开已知坑，review 发现数明显下降。

**12 子代理并行审查**：从安全、性能、架构、可维护性等维度并行审查代码。

### 10.6 Claude Code 自进化系统四层架构

```
第一层：认知核心（CLAUDE.md）
第二层：专用子智能体（subagents）
第三层：路径作用域规则（path-scoped rules）
第四层：进化引擎（evolution engine）
```

### 10.7 对 flow-kit 的启示

- F5（自我进化机制）必须解决五个断裂点，不能只靠 Hook 提醒
- 引入 WAL 协议：检测到触发条件 → 立即写入 → 再响应用户
- session-state.json 需增加学习统计字段
- Phase 执行中的错误和纠正应自动触发经验记录
- 建立 VFM 评分机制决定经验是否提升为永久规则
- CE 的知识沉淀机制可集成到 F5
- 12 子代理并行审查可应用于 Phase 6（代码审查）
- 建立四层架构：CLAUDE.md → Phase 工作流 → 护栏规则 → 进化引擎

---

## 十一、flow-kit 可借鉴的具体改进点

### 11.1 架构层面

| 改进点 | 借鉴来源 | 优先级 |
|--------|----------|--------|
| Phase 制品依赖图（DAG） | OpenSpec | P0 |
| 增量规范传递（Delta Spec） | OpenSpec | P0 |
| 三层记忆架构 | Claude Code 记忆体系 | P0 |
| 四层自进化架构 | 自进化系统指南 | P1 |
| 虚拟团队角色分工 | GStack/OMC | P1 |

### 11.2 执行层面

| 改进点 | 借鉴来源 | 优先级 |
|--------|----------|--------|
| 原子化任务拆分 | OpenSpec+Superpowers TDD v2 | P0 |
| Subagent 上下文隔离 | GSD/Superpowers | P0 |
| 四层防护 TDD 模型 | OpenSpec+Superpowers | P1 |
| /loop 自主迭代集成 | Claude Code /loop | P0 |
| Task 系统集成 | Claude Code Task | P1 |

### 11.3 质量层面

| 改进点 | 借鉴来源 | 优先级 |
|--------|----------|--------|
| 多维度并行审查 | /simplify 命令 | P1 |
| 验证证据机制 | Superpowers TDD | P0 |
| 独立 reviewer 上下文 | GStack | P1 |
| 知识图谱审查 | code-review-graph | P2 |

### 11.4 效率层面

| 改进点 | 借鉴来源 | 优先级 |
|--------|----------|--------|
| Skill 渐进式披露 | Agent Skills 架构 | P0 |
| Token 优化五策略 | Token 优化指南 | P1 |
| 8 行规则精简输出 | 8 行规则 | P1 |
| 经验复用降本 | OpenSpace | P2 |

### 11.5 配置层面

| 改进点 | 借鉴来源 | 优先级 |
|--------|----------|--------|
| 双 CLAUDE.md 策略 | OMC+Karpathy | P0 |
| MANDATORY/RECOMMENDED/OPTIONAL 三级指令 | Skill 协议设计 | P0 |
| settings.json 权限精细配置 | settings.json 神配置 | P1 |
| 安全 Hook 机制 | Ralph Wiggum | P0 |

---

## 十二、Context Engineering 与上下文管理

### 12.1 /rewind 的深层价值：不只是撤销

**核心发现**：`/rewind` 不仅仅是撤销操作，它同时回滚 context window。这是 Context Engineering 的关键工具。

**工作原理**：
- rewind 前 context 用量 35.6k → rewind 后回到 8.1k
- 模型的高质量处理区间在总 context window 的 40% 左右
- 错误对话会产生"噪音"，影响模型后续判断

**最佳实践**：
- 当 AI 在某段对话中出现偏差后，最好的方式是 `/rewind` 完全抹除这段信息
- 确保只把真正有价值、准确的信息放进 context 里

### 12.2 Claude Code 三层记忆架构

| 层级 | 文件位置 | 谁来写 | 作用域 | 加载方式 |
|------|----------|--------|--------|----------|
| 用户级 | `~/.claude/CLAUDE.md` | 用户 | 所有项目 | 启动时加载 |
| 项目级 | 项目根 `CLAUDE.md` + `.claude/rules/*.md` | 用户/团队 | 当前项目 | 启动时 + glob 匹配 |
| 自动记忆 | `~/.claude/projects/<project>/memory/` | Claude 自己 | 当前项目 | 自动注入 |

**Rules 的 Glob 精准加载**：
```yaml
---
paths:
  - "src/components/**/*.tsx"
---
```
不同场景加载不同规则，既精准又不浪费上下文窗口。

### 12.3 Memory Compiler：知识库持久化

**核心机制**：
- Hook 自动捕获对话 → 提取知识 → 存入每日日志 → 编译成知识库
- 知识库结构：concepts（概念）、connections（关联）、qa（问答）
- No RAG 设计：个人规模 50-500 篇文章，LLM 直接读 index.md 效果更好
- 健康检查：lint.py 7 项检查（broken links、orphaned pages、contradictions、staleness）

### 12.4 claude-mem：长期记忆系统

**核心机制**：
- 语义压缩：不存原始对话，用 Claude Agent SDK 压缩成结构化观察记录
- 三层检索：会话摘要层（自动注入）→ 语义搜索层（MCP 按需检索）→ 文件夹上下文层
- 双标签隐私系统：敏感内容标记为私密，不注入未来会话
- 每次会话节省约 2250 token

### 12.5 Subagent Fork 机制

**Subagent 核心价值**：运行在独立上下文窗口，有独立的 system prompt、工具权限和执行边界。工具调用（grep/find/ls/glob）的噪音不会污染主上下文。

**Fork 模式（CLAUDE_CODE_FORK_SUBAGENT=1）**：
- 继承父代理在 fork 那一刻的完整对话上下文
- 与父代理共享 prompt cache 前缀，第 2 到 N 个子代理输入成本明显更低
- 仍在隔离环境中运行，不会把工具调用污染父上下文
- 最终只把结论性摘要返回主会话

**context-timeline Hook**：实时可视化主代理上下文消耗、并行 Subagent 数量、每个 Subagent 回传信息量。

### 12.6 对 flow-kit 的启示

- Phase 4（开发执行）可集成代码知识图谱，减少上下文浪费
- flow-kit 的 session-state.json 可升级为三层记忆架构
- Phase 工作流文件可使用 glob 匹配实现按场景加载
- Auto Memory 机制可集成到 flow-kit 的自我进化系统
- flow-kit 可借鉴"知识编译"机制，将 Phase 执行经验自动沉淀为知识库
- Phase 4（开发执行）的 Subagent 应默认启用 Fork 模式
- context-timeline 可集成到 flow-kit 的上下文监控

---

## 十三、全局路由协议与多框架协作

### 13.1 Superpowers 与 gstack 的全局路由协议

**核心设计原则**：
1. 用户明确指令 > 项目 AGENTS.md > 全局 AGENTS.md > 插件默认说明
2. 默认一回合只允许一个"主流程框架"接管当前任务
3. Superpowers = 默认通用研发流程框架（怎么做、怎么拆、怎么改、怎么查）
4. gstack = 专项能力框架（验证、运行态、页面、上线、巡检）

**Superpowers 内部路由细分**：
- brainstorming：需求模糊、方向未定
- writing-plans：方案已定，需拆文件落点
- systematic-debugging：bug 排查、状态漂移
- test-driven-development：新增功能、逻辑重写
- verification-before-completion：收尾前完整验证
- subagent-driven-development：大任务并行推进

**防止双主流程的三类问题**：
1. 重复启动：两个框架同时起手
2. 默认控制权冲突：双方都试图当默认入口
3. 用户心智被打散：需要理解为什么流程被改造

### 13.2 SDD 三阶段工作流（gstack + superpowers）

**规划阶段（gstack）**：
- office-hours：YC 六步框架验证需求真实性
- plan-ceo-review：战略评审（市场、竞争、商业模式）
- plan-eng-review：架构评审（数据流、性能、测试）
- plan-design-review：设计评审（七维度评分）

**开发阶段（superpowers）**：
- brainstorming → write-a-prd → writing-plans → subagent-driven-development
- 每个任务 2-5 分钟，独立 subagent 执行
- 强制 TDD，两阶段评审

**发布阶段（gstack）**：
- review：代码评审
- qa：真实浏览器测试
- ship：同步主分支、运行测试、推送代码、创建 PR

### 13.3 superpowers + gstack 双插件分工

**核心设计**：
- superpowers（大脑）：brainstorming → writing-plans → TDD → systematic-debugging → verification → code-review
- gstack（手脚）：/browse → /qa → /ship → /land-and-deploy → /canary → /careful
- 标准开发闭环：想法 → brainstorming → plans → (plan-review) → executing + TDD → /browse → verification → code-review → /ship → /canary

**五大核心原则**：
1. 流程归 superpowers
2. 执行归 gstack
3. 独立 reviewer 通道（作者和审查者不在同一上下文）
4. 证据优先（没有证据的完成 = 没完成）
5. 遇到歧义先 brainstorm

### 13.4 对 flow-kit 的启示

- Phase 0-3 可借鉴 gstack 的评审体系
- Phase 4-7 可借鉴 superpowers 的执行体系
- 三阶段划分与 flow-kit 的思考/执行分离理念一致
- flow-kit 的 Phase 体系可借鉴"思考+执行"分离设计
- 独立 reviewer 通道可应用于 Phase 的代码审查环节

---

## 十四、生产级交付实践

### 14.1 渐进式上下文加载

**核心方法**：
- 需求文档分层：项目总需求 → 模块级 → 菜单级 → 页面级 → 按钮级
- CLAUDE.md 只做需求入口，详细需求放 `doc/需求文档/` 目录
- 需求文档与代码一致性维护：文档滞后时以代码为准

### 14.2 前后端分离开发流程

**前端原型阶段**：
1. 反向输出技术架构和开发规范 → CLAUDE.md
2. 补充项目背景、用户故事、业务结构
3. 生成框架代码（mock 数据方案）
4. 渐进式完成各菜单功能设计

**后端开发阶段**：
1. 修改工程目录结构（frontend/backend 分离）
2. 生成后端技术约束 → backend/CLAUDE.md
3. 生成后端框架代码并验证启动
4. 按领域模型完善子模块接口服务

**本地调试原则**：
- 前后端日志写入文件供 Agent 自行 debug
- 约束模型不主动管理服务启停
- 日志文件加入 .gitignore

### 14.3 Agentic Coding 工作流设计

**核心理念**：AI 是思维的拓展，不是现成框架的替代品。

**Spec-Driven Incremental Deploy 方法论**：
- 设计阶段：Generator-Evaluator 收敛循环，产出 5 个 checkpoint YAML
- 实现阶段：RED → GREEN → REVIEW → DEPLOY → COMMIT
- 变更阶段：先影响分析，再动手实现

**TOC 约束理论在 Agentic Coding 中的应用**：
- 识别约束：LLM 几乎不是主要约束，真正的约束是验证、集成、部署、回滚
- 充分利用约束：不要一次性堆积大量未验证的实现
- 其他环节服从约束：每个功能走完整闭环

**Skill 设计原则**：
- Skill 不是 prompt，而是知识包（SKILL.md + knowledge/ + roles/ + templates/ + references/）
- 知识先行，角色后置：角色不是人格扮演，而是视角过滤器

### 14.4 OMC vs Karpathy：硬控制 vs 软约束

| 维度 | OMC（硬控制） | Karpathy（软约束） |
|------|-------------|-------------------|
| 控制方式 | 系统流程 | Prompt 规则 |
| 控制强度 | 极强（不可绕） | 较弱（可能失效） |
| 作用层 | 执行层 | 思考层 |

**融合策略**：CLAUDE.md = Karpathy（行为层）+ 项目规范（约束层）+ OMC（执行层）

### 14.5 Claude Code auto mode 安全机制

**两层过滤器**：
- 输入层：注入检测，文件内容含劫持指令时加警告
- 输出层：Sonnet 4.6 分类器检查操作合理性，单 token 快判 + 完整推理

**拦截场景**：范围扩大、翻找凭证、猜参数、往外发数据、跳安全检查

### 14.6 对 flow-kit 的启示

- Phase 1（需求分析）可借鉴渐进式文档加载
- flow-kit 可内置前后端分离的项目模板
- 日志自动采集和 debug 能力可增强 Phase 的自动化程度
- flow-kit 的自动化执行可借鉴两层安全过滤器
- Phase 边界定义 = 硬控制（不可绕），CLAUDE.md 行为引导 = 软约束（思考层）

---

## 十五、团队工程化落地

### 15.1 七大核心构件体系

详见第四章 4.3 节。从强约束到弱约束的七层配置体系，覆盖 CLAUDE.md、rules/、agents/、commands/、skills/、hooks/、MCP 配置。

### 15.2 7 天极简落地路线

| 天数 | 配置项 | 目标 |
|------|--------|------|
| Day1 | CLAUDE.md | 项目记忆与约定 |
| Day2 | rules/ | 团队底线规则 |
| Day3 | /plan 命令 | 规划流程一键化 |
| Day4 | /code-review 命令 | 审查流程一键化 |
| Day5 | 提醒 Hook | 关键节点提醒 |
| Day6 | 一致性 Hook | 代码风格一致性 |
| Day7 | Agent 分工 | 子代理任务分配 |

### 15.3 flow-kit 社区融合实践（rihebty/flow-kit）

该项目融合了 7 个主流框架的核心能力：

| 来源 | 借鉴内容 | 落地位置 |
|------|----------|----------|
| spec-kit | 8 阶段骨架、[P] 并行标记、constitution 约束 | 整体架构 |
| OpenSpec | CHANGE 一等公民、三段式 change 流、context hygiene | 0-change.md / 7-integration.md |
| GSD | XML 任务模板、fresh subagent、plan-checker 循环 | 4-dev.md |
| claude-task-master | parse_prd→expand_task、工具分层加载 | 3-task.md |
| gstack | 三连审（CEO/Design/Eng）、跨模型二意见 | 6-review.md |
| superpowers | 元技能、原子任务、code review 分层 | 全流程 |
| skills (anthropic) | SKILL.md 渐进披露、grill-me、caveman 压缩 | prompt 写作风格 |

**关键创新点**：
- **四闸门 + 第五闸门**：路由声明 → 用户回复 → 加载 prompt 并确认 → prompt 第一步反问并等回复。阶段切换时再加一道闸门
- **@GO.md 零例外路由**：只要消息里出现 @GO.md，AI 必须先给路由声明或选项菜单，禁止直接动手
- **H-hotfix.md 三档响应**：P0（先回滚再诊断）、P1（3 问澄清后直接修复）、P2（建议走正常流程）
- **R-recall.md 项目记忆恢复**：触发后 AI 输出"5 分钟项目摘要 + 推荐下一步"
- **Secrets 保护**：强制 .env.example + .gitignore，提交前自动扫描敏感信息

### 15.4 对 flow-kit 的启示

- 7 天落地路线可适配为 flow-kit 的团队 onboarding 流程
- Hooks 的"提醒型→一致性型→阻断型"渐进式推进可应用于 Phase 护栏
- 四闸门机制可直接应用于 F1（Phase 边界重构）
- H-hotfix 可集成到 Phase 工作流作为异常分支
- R-recall 可集成到 session-state 恢复机制
- Secrets 保护可集成到 F3（自动化审计）

---

## 十六、AutoResearch 方法论

### 16.1 Karpathy AutoResearch 核心设计（71K Stars）

**三个文件就是全部**：
- `prepare.py` — 数据准备（AI 不可修改）
- `train.py` — AI 的"实验室"（AI 可修改一切）
- `program.md` — 人类写的"研究纲领"（Markdown 编排 AI 行为）

**实验循环**：读取 program.md → 查看 git 历史 → 提出假设 → 编辑 train.py → 训练 5 分钟 → 检查 val_bpb → 改善则保留，否则 git revert

**核心洞察**：没有框架！编排逻辑就是一个 Markdown 文件。智能体的上下文窗口就是状态机，Markdown 文档就是工作流定义，Git 就是版本控制和回滚机制。

### 16.2 软件开发领域迁移：三大关键改进

| 改进 | Karpathy 原版 | 软件开发版 |
|------|--------------|-----------|
| 审核机制 | 单 Agent 自审 | 多 Agent 交叉审核（Codex + Claude 轮流） |
| 评分体系 | 单一 metric（val loss） | 5 维度加权评分（正确性35%+测试25%+代码质量20%+安全10%+性能10%） |
| 反馈机制 | 独立循环 | 审核反馈驱动下一轮实现 |

**六条核心原则**：
1. program.md 是唯一规则来源
2. 多 Agent 对抗审核（不同模型盲区互补）
3. 量化评分门槛（总分 ≥ 9.0 才通过）
4. 审核反馈驱动迭代（不是盲循环）
5. 只保留改进（退化就回滚）
6. 全量记录可追溯

### 16.3 对 flow-kit 的启示

- program.md 模式可替代复杂的 YAML 工作流定义
- 多 Agent 交叉审核可集成到 Phase 6（代码审查）
- 5 维度加权评分可应用于 Phase 5（测试验证）
- "只保留改进"原则可集成到自我进化机制（F5）

---

## 十七、DESIGN.md 设计系统与 grill-me 需求访谈

### 17.1 DESIGN.md：68.4K Star 的前端设计约束系统

**核心价值**：让 AI 生成前端从"凭感觉"到"遵循规范"。

**结构化设计规则**：
- 颜色系统：主色/辅色/中性色/语义色的具体色值
- 间距系统：基于 4px 网格的间距层级（xs/sm/md/lg/xl/2xl）
- 圆角系统：sm(4px)/md(8px)/lg(12px)/full(9999px)
- 字体层级：标题/正文/辅助文字的 size/weight/line-height
- 组件规范：按钮/输入框/卡片等常用组件的具体约束

**关键洞察**：AI 不是不会写前端，而是缺少具体的设计约束。有了 DESIGN.md，AI 生成的 UI 从"每次都不一样"变成"风格统一、可直接使用"。

### 17.2 grill-me：78K Star 的 10 行需求访谈

**核心机制**：10 行 Markdown 实现需求澄清，解决 AI 理解与用户表达偏差问题。

```
Interview me relentlessly about every aspect of this plan
until we reach a shared understanding. Walk down each branch
of the design tree, resolving dependencies between decisions
one-by-one. For each question, provide your recommended answer.
Ask the questions one at a time.
```

**与 Karpathy 的互补关系**：
- Karpathy：治理 AI（约束 AI 的行为）
- grill-me：治理人（帮人想清楚自己要什么）

**RTF 框架（Role-Task-Format）**：
- Role：明确 AI 的角色定位
- Task：定义具体任务
- Format：规定输出格式

**Matt Pocock 14 个 Skill 分类**：
- 工程类（10 个）：grill-with-docs、diagnose、tdd、improve-codebase-architecture、triage、to-prd、to-issues、zoom-out、prototype、setup
- 效率类（4 个）：grill-me、caveman、handoff、write-a-skill

### 17.3 对 flow-kit 的启示

- Phase 1（需求分析）可集成 grill-me 机制：AI 主动追问直到双方理解一致
- Phase 4（开发执行）可引入 DESIGN.md 风格的设计约束文件
- grill-me 的"每次只问一个问题"原则可应用于 Phase 0（路由判断）
- Matt Pocock 的 Skill 分类思路可指导 flow-kit 的 Skill 体系设计

---

## 十八、web-access 浏览器直连

### 18.1 核心架构：CDP 协议 + 浏览器直连

web-access 是 Skill 而非 MCP——不只给工具，还给"怎么用工具"的完整方法论。

**三层能力**：

| 层级 | 能力 | 说明 |
|------|------|------|
| 基础层 | WebSearch/WebFetch/curl/Jina | 传统抓取，加了调度决策（先用哪个、失败降级、放弃换路） |
| 核心层 | CDP 浏览器直连 | 接入日常使用的 Chrome，登录态天然携带，动态渲染页面正常访问 |
| 效率层 | 多任务并行分治 | 多个子 Agent 同时调研不同目标，结果汇总给主 Agent |

### 18.2 关键设计

- CDP Proxy 常驻后台，无需每次手动启动
- 接入日常 Chrome（非无头浏览器），已登录网站直接可用
- 自动环境检查：Node.js ≥ 22、CDP 端口、Proxy 进程
- 目标导向：接到任务先理解目标，找最短路径，每步根据结果动态调整

### 18.3 实战效果

- 竞品调研：三个子 Agent 并行调研 Dify/Coze/FastGPT，时间压缩为原来的 1/3
- 小红书抓取：CDP 模式绕过反爬，登录态保持，搜索结果与手动一致
- 子 Agent 调研深度：进入文档站逐条提取节点类型和模型列表

### 18.4 对 flow-kit 的启示

- Phase 0（研究）可集成 web-access 的并行调研能力
- CDP 浏览器直连可应用于 Phase 5（测试验证）的真实浏览器测试
- "目标导向"的调度决策可应用于 Phase 工作流的动态路由

---

## 十九、Google Agent Skills

### 19.1 核心定位

Google Gemini 团队主管 Addy Osmani 开源，23000+ GitHub Star。将《Software Engineering at Google》中的工程方法论封装为 20 个 Skill + 7 个 Slash 命令 + 3 个 Agent 人设。

**六阶段开发链路**：`/spec` → `/plan` → `/build` → `/test` → `/review` → `/ship`

**三个预设 Agent**：code-reviewer、test-engineer、security-auditor，在 `/ship` 时并行出具三份报告。

### 19.2 三大框架对比

| 框架 | 思路 | 核心机制 |
|------|------|----------|
| Spec Kit | 用文档定 AI | 先把需求/计划/任务写成规范文档，AI 按文档办事 |
| Superpowers | 用流程带 AI | 把聊需求→写计划→写测试→代码互查自动串起来 |
| Agent Skills | 用纪律管 AI | 把资深工程师的工作习惯拆成 20 个可组合 Skill |

### 19.3 对 flow-kit 的启示

- flow-kit 的 Phase 体系与 Agent Skills 的六阶段链路高度契合
- 可借鉴"三个预设 Agent 并行审查"机制增强 Phase 6（代码审查）
- Agent Skills 的"纪律约束"理念与 flow-kit 的护栏机制一致
- 跨平台支持（Claude Code/Cursor/Gemini CLI/Windsurf/Codex）的设计思路值得参考

---

## 二十、Command Center 目标管理

### 20.1 核心洞察：瓶颈从 AI 能力转移到人类管理界面

当 Agent 足够强大时，真正的瓶颈不再是 AI 能不能完成任务，而是人类能不能高效管理多个并行任务。五个终端窗口是人脑同时管理的上限。

### 20.2 五大方案对比

| 方案 | 定位 | 核心问题 |
|------|------|----------|
| Tmux | 终端分屏 | 还是在终端里面，看不到全局图景 |
| Anthropic 桌面应用 | 官方 GUI | 本质上还是一次管一个对话 |
| Vibe Kanban | 编码 Agent 看板 | 为开发者设计，语言是 commit/PR/branch |
| Paperclip | 自治公司 OS | 概念超前但太重，需要组织架构图 |
| Command Center | 业务目标指挥中心 | 自顶向下，从目标出发自动决定执行策略 |

### 20.3 Command Center 核心设计

**迭代式 Kanban**：任务在"Your Turn"和"Claude's Turn"之间反复弹跳，而非线性流转。

**智能上下文感知**：Agent 自动回顾历史，基于昨天的成果继续构建。

**四大核心模块**：
- 定时任务：学习健康检查、每周摘要、每日 Skill 更新
- Skill 管理：可视化浏览/搜索/编辑所有 Skill，内置 Meta Skill Creator
- 文档管理：CLAUDE.md/品牌上下文按客户隔离
- 活动 Feed：所有任务状态、产出文件、历史记录一览

**三种任务深度**：Quick Task（简单执行）、Campaign（自动拆分子任务）、Deep Build（分阶段执行）

### 20.4 对 flow-kit 的启示

- flow-kit 的 Phase 管理可借鉴"Your Turn / Claude's Turn"的迭代模式
- 任务深度分级（Quick/Campaign/Deep Build）可应用于 Phase 路由判断
- 智能上下文感知（自动回顾历史）可集成到 session-state 恢复机制
- "管目标不管终端"的理念与 flow-kit 的 Markdown 驱动架构一致

---

## 附录：已分析文档清单（122/122）

> 版本：v3.0 | 日期：2026-05-16 | 状态：全部完成

1. Superpowers 实战指南：7 步流程 + 14 个技能 + 3 条铁律
2. Claude 插件新组合：Superpowers + Ralph-Loop
3. 让 Claude Code 在你睡觉时持续运行：完整实战指南
4. 如何把 Claude Code 改造成一个自进化系统：完整指南
5. 深度拆解 Superpowers 和 gstack
6. Ralph Wiggum 插件 技术文档
7. Claude Code 钩子 技术文档
8. 子代理 技术文档
9. 技术文档.md（Agent Skills）
10. Claude Code 双插件最佳搭配
11. Claude Code Token 优化 2026
12. Claude Code 工作流选型指南：5 套主流方案
13. Skill 语法完全手册
14. Claude Code 的记忆体系
15. Claude Code auto mode 实战
16. 20K+ Star！Claude Code 多智能体编排神器
17. Claude Code 最佳实践全攻略
18. 8 行代码让 Claude Code 闭嘴
19. 忘掉 brainstorming：grill-me + trellis
20. 揭秘让 Claude 代码效率飙升 10 倍的 settings.json 神配置
21. Claude Code 团队落地指南
22. Claude Code 越用越卡？原因拆解与实战修复指南
23. 从阶段到行动：OpenSpec OPSX 完整指南
24. Mnilax：CLAUDE.md 规则从 4 条到 12 条
25. GitHub 10 万星 Claude Code 配置揭秘
26. 如何用 CLAUDE.md 把 Claude Code 调教成靠谱队友
27. 我在 Prompt 里加了四个字
28. AI 编程工作流选型：Spec-Kit、OpenSpec、Superpowers 深度对比
29. AI 编程五把利器：GSD、Superpowers、Skills、OpenSpec、BMAD
30. 6 个代表性 AI 编程 Harness 工程化框架拆解
31. 6 大 Spec 驱动规范实测
32. OpenSpec + Superpowers + gstack 组合拳
33. OpenSpec + Superpowers TDD v2：4 层防护
34. OpenSpec 进阶：从 Core 到 Expanded
35. OpenSpec 进阶：自研 AI coder + openspec
36. Superpowers + GSD：双模式工作流实战
37. Superpowers+OpenSpec 两个框架一起用踩了 7 个坑
38. 实测对比 3 个让 Agent 质变的 Skill
39. 不可避免的未来：设计自己的 Agentic Coding 工作流
40. Claude Code Task 系统
41. Claude Code 进阶指南：双 CLAUDE.md
42. Claude Code 创始人力荐 /loop 命令
43. Claude Code 拥有 50 多个命令
44. Claude Code 提效的插件
45. Claude Code 第二个神器 OpenSpace
46. Claude 悄悄史诗级更新 Skill-creator
47. Skills 赏析：skills-refiner
48. Claude的/rewind功能 — Context Engineering 关键工具
49. code-review-graph — 知识图谱减少 6.8 倍审查 Token
50. Alan 全局路由协议 — Superpowers 与 gstack 分工
51. 放弃 Vibe Coding — SDD 三阶段工作流实战
52. Claude Code + MiniMax 2.7 — 生产系统交付全流程
53. Agentic Coding 工作流设计 — TOC 约束理论应用
54. OMC vs Karpathy — 硬控制与软约束融合
55. /loop 命令 — 定时调度 + 自主迭代双模式
56. Claude Code 50+ 命令全解析
57. Claude Code 提效插件推荐
58. OpenSpace 自进化引擎
59. Skill-creator 评估体系
60. Claude Code Task 系统深度解析
61. Claude Code 进阶指南：双 CLAUDE.md 策略
62. Claude Code 创始人力荐 /loop 命令
63. impeccable — 一站式前端开发全流程增强 Skill（6大模块18命令）
64. claude-task-master — PRD驱动任务自动拆解与依赖管理
65. Claude Code 记忆体系 — 三层架构（CLAUDE.md+Rules+Auto Memory）
66. 5个Claude Code神级Skill — Caveman/Memory Compiler/Paper2Code/CLAUDE.md Optimizer/TestDrift
67. Claude Code 团队落地指南 — 七大构件+7天落地路线
68. Claude Code 最佳实践三件套 — Karpathy+claude-mem+Best Practice
69. Claude Code 越用越卡修复指南 — Context Window过载+Tool Call重试+GEP协议
70. Claude Code Hooks技术文档 — 9个钩子事件+退出代码控制
71. Claude Code 50+命令全解析 — CLI/斜杠/快捷键三类指令
72. Claude Code 提效插件推荐 — superpowers+claude-hud+feature-dev
73. Claude Code auto mode — 两层安全过滤器+拦截场景分类
74. Claude Code Task系统 — 四种工具+三种依赖模式
75. superpowers+gstack双插件 — 大脑+手脚分工+标准开发闭环
76. Claude Code Token优化5策略 — prompt caching+模型分级+上下文卫生+thinking预算+Hooks
77. Source-Verifier信源验证 — NATO Admiralty Code双维度评估
78. Claude Code生产系统交付 — 渐进式上下文加载+前后端分离+本地调试原则
79. Ralph 自主 Agent 循环 — PRD→prd.json→循环→完成，每次迭代全新 AI 实例
80. Karpathy AutoResearch — 三个文件就是全部，Markdown 编排 AI 行为
81. AutoResearch 软件开发迁移 — 多 Agent 交叉审核+5 维度评分+反馈驱动迭代
82. CE + Superpowers 三模型实测 — 知识沉淀是团队复利，12 子代理并行审查
83. Superpowers 185K 星深度拆解 — brainstorming 9 步 HARD-GATE，systematic-debugging 四阶段
84. superpowers-zh 20 个超能力 — 三类 20 个 skill，17 款 AI 工具通用
85. flow-kit 融合升级版 — 四闸门+@GO.md 路由+H-hotfix+R-recall+Secrets 保护
86. flow-kit 多 IDE 通用版 — 融合 7 框架，8 阶段工作流，外部工具检测回退
87. pm-skills — 65 技能+36 工作流，顶级 PM 方法论编码进 AI
88. Claude Code 最佳实践三件套 — Karpathy 四原则+claude-mem+Best Practice
89. 5 个 Claude Code 神级 Skill — Caveman/Memory Compiler/Paper2Code/CLAUDE.md Optimizer/TestDrift
90. Claude Code 团队落地指南 — 七大构件+配置即代码+7 天落地路线
91. everything-claude-code 84 条规则 — 编码风格+测试规范+安全清单+10 种 Agent
92. Claude Code 50+命令全解析 — CLI/斜杠/快捷键三类指令
93. Claude Code /loop 命令 — 定时调度+自主迭代双模式
94. Claude Code 创始人力荐 /loop — 三种调度方式对比，五个实际场景
95. Claude Code auto mode — 两层安全过滤器+拦截场景分类
96. Claude Code Hooks 技术文档 — 9 个钩子事件+退出代码控制
97. Claude Code Task 系统 — 四种工具+三种依赖模式
98. superpowers+gstack 双插件 — 大脑+手脚分工+标准开发闭环
99. 为什么有人开始用 CE 替代 Superpowers？关键不只是流程，而是"记忆" — CE 记忆系统+Multi-Pass Review
100. 为什么需要一个 Multi-Pass Review Skill — 多轮独立审查+Schema设计+聚合规则+六大反模式
101. 被安装43万次的"自我进化"skill，为什么在你的Agent身上毫无作用？ — 五大断裂点+WAL协议+VFM评分
102. 68.4K Star！DESIGN.md 让 AI 生成的前端从"凭感觉"到"遵循规范" — 结构化设计约束系统
103. 78K Star！grill-me 用 10 行 Markdown 解决 AI 理解与用户表达偏差 — RTF框架+需求访谈
104. 谷歌开源 Agent Skills — 20个Skill+7个Slash命令+3个Agent人设
105. 让 Claude Code 联网调研的终极方案：web-access Skill — CDP浏览器直连+并行子Agent
106. 让 Claude Code 成本爆降 80%，这个开源工具有点猛 — OpenWolf三文件记忆系统
107. 别再把 Skill 当插件装了！Claude Code 的 Subagent 才是真正的"分身术" — Fork模式+context-timeline
108. 我把 Claude Code 的两个神器叠在一起：150K star 管思维，90K star 管执行 — Karpathy+OpenSpec组合
109. GitHub 暴涨 70k Star！这个开源 AI 程序员同事 — OpenSpec深度解析
110. 一人一城：gstack+Superpowers — 双插件协同实战
111. 上班族必装！7个让Claude Code原地起飞的神级Skill — 效率工具合集
112. 让 Claude Code 成本爆降 80%，这个开源工具有点猛 — OpenWolf深度解析
113. 别再把 Skill 当插件装了！Claude Code 的 Subagent 才是真正的"分身术" — Subagent深度解析
114. 我把 Claude Code 的两个神器叠在一起 — Karpathy+OpenSpec组合实战
115. GitHub 暴涨 70k Star！这个开源 AI 程序员同事 — OpenSpec实战
116. 一人一城：gstack+Superpowers — 双插件协同
117. 上班族必装！7个让Claude Code原地起飞的神级Skill — 效率工具
118. 让 Claude Code 联网调研的终极方案：web-access Skill — CDP浏览器直连
119. 谷歌开源 Agent Skills — 用纪律管AI
120. 68.4K Star！DESIGN.md — 前端设计约束系统
121. 78K Star！grill-me — 10行Markdown需求访谈
122. 被安装43万次的"自我进化"skill — 五大断裂点分析