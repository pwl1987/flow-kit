# flow-kit v3.6.0 产品需求文档（PRD）

| 字段 | 值 |
|------|-----|
| 版本 | v3.6.0 |
| 基线版本 | v3.5.0 |
| 日期 | 2026-05-16 |
| 状态 | 草案（基于 grill-me 审查重写） |

---

## 1. 产品概述

### 1.1 产品定位

flow-kit 是基于 Claude Code 的结构化开发流程工具包，提供 9 阶段（Phase 0-8）工作流。v3.6.0 聚焦两个核心方向：**Phase 职责边界约束**和**半自动化执行**。

### 1.2 版本目标

| 目标 | 描述 | 验证方式 |
|------|------|----------|
| Phase 边界清晰化 | 每个 phase 有明确的 allowed/forbidden 声明 | 所有 9 个 phase 文件含 YAML front matter |
| 操作越界警告 | 编辑超出当前 phase 范围的文件时发出警告 | hooks 输出警告信息 |
| 半自动执行 | 一键执行当前 phase + 推荐下一步 | `/flow-kit:auto-pilot` 命令可用 |
| 产出物验证 | phase 执行后自动检查预期产出物 | validate-phase.sh 输出验证证据 |
| PRD 预处理 | Markdown/JSON 格式的 PRD 自动提取结构 | prd-parser.sh 输出结构化数据 |
| 增强回滚 | phase-8 增加三档分级和 RCA 模板 | phase-8 工作流包含分级流程 |
| Secrets 检测 | 提交前扫描密钥泄露 | security-scanner.sh 检测 18 种模式 |

### 1.3 目标用户

| 角色 | 场景 | 核心需求 |
|------|------|----------|
| 独立开发者 | 个人项目 | 自动执行、减少手动操作 |
| 小团队（2-5 人） | 协作开发 | 状态透明、产出物验证 |

---

## 2. 问题分析

### 问题 1：Phase 命令越界执行

**现象**：`/flow-kit:phase-N` 执行时会超出职责范围（如 phase-0 开始架构设计，phase-4 自行执行测试）。

**根因**：
1. phase 工作流文件无结构化的边界声明（无 YAML front matter）
2. hooks 不检查操作范围
3. AI 倾向"主动多做"

**解决方案**：F1（边界声明 + hooks 警告）

### 问题 2：缺少自动化执行能力

**现象**：用户需手动逐个执行 phase 命令。

**根因**：
1. 缺少 phase 间状态编排
2. 缺少产出物自动验证
3. 缺少基于 DAG 的制品依赖追踪

**解决方案**：F2（半自动引擎）+ F3（产出物验证）

### 问题 3：缺少产出物验证

**现象**：phase 执行后无法自动确认产出物是否符合预期。

**根因**：
1. phase 文件未声明预期产出物
2. 无验证证据记录机制

**解决方案**：F3（产出物模板 + 验证证据）

### 问题 4：回滚流程粗粒度

**现象**：phase-8（回滚）只有简单的回滚操作，缺少分级响应和事后分析。

**根因**：phase-8 工作流缺少 P0/P1/P2 分级和 RCA 模板。

**解决方案**：F8（增强回滚阶段）

---

## 3. 功能需求

### 3.1 F1：Phase 职责边界重构

**做什么**：为每个 phase 工作流文件添加 YAML front matter，声明 allowed/forbidden 操作；增强 hooks 进行边界警告。

**为什么**：当前 phase 文件无结构化边界声明，AI 无法区分哪些操作属于当前 phase。

#### F1.1 YAML Front Matter 格式

每个 phase 文件头部添加：

```yaml
---
phase: 0
name: 变更立项
stage: thinking  # thinking | execution
allowed_operations:
  - 影响范围评估
  - 变更提议生成
forbidden_operations:
  - 代码修改
  - 测试执行
  - 架构设计
expected_artifacts:
  - .flow-kit/change-proposal.md
next_phase: 1
---
```

每个 phase 文件（brownfield.md + greenfield.md，共 19 个文件）都需添加 front matter。

#### F1.2 Hooks 边界警告

**post-edit-format.sh 增强**：
- 读取当前 phase 的 `allowed_operations` / `forbidden_operations`
- 检查编辑的文件路径是否在预期范围内
- 如果超出范围，输出警告：`[boundary] 警告：当前 Phase N 不建议修改此路径`

**stop-quality-gate.sh 增强**：
- phase 结束时检查 `expected_artifacts` 是否存在
- 输出检查结果作为质量门禁的一部分

**注意**：hooks 只做警告，不做硬拦截。真正的约束靠 front matter 提示词。

#### F1.3 验收标准

- [ ] 所有 19 个 phase 文件（9 brownfield + 9 greenfield + 1 phase-2a）含 YAML front matter
- [ ] front matter 包含 phase、name、stage、allowed_operations、forbidden_operations、expected_artifacts 字段
- [ ] post-edit-format.sh 能读取当前 phase 的边界声明并输出警告
- [ ] stop-quality-gate.sh 能检查 expected_artifacts 是否存在
- [ ] 向后兼容：无 front matter 的旧 phase 文件仍可正常加载

---

### 3.2 F2：半自动执行引擎

**做什么**：auto-pilot.sh 编排现有脚本，实现"执行当前 phase → 验证产出物 → 推荐下一步"的半自动流程。人控制推进节奏。

**为什么**：用户需手动逐个执行 phase 命令，缺少状态编排和产出物追踪。

#### F2.1 状态机

auto-pilot.sh 编排现有脚本：

```
读取 session-state.json（当前 phase）
  → 生成/更新 auto-plan.json（含 DAG 制品依赖）
  → 调用 phase-executor.sh 执行当前 phase
  → 调用 validate-phase.sh 检查产出物
  → 输出验证证据
  → 推荐下一步（不自动推进，需人确认）
```

#### F2.2 auto-plan.json 结构

融入 DAG 制品依赖（经验萃取 P0-1）：

```json
{
  "description": "变更描述",
  "task_depth": "campaign",
  "current_phase": 2,
  "phases": [
    {
      "id": 0,
      "name": "变更立项",
      "status": "completed",
      "inputs": [],
      "outputs": [".flow-kit/change-proposal.md"],
      "evidence": [{"check": "file_exists", "result": "pass", "ts": "..."}]
    },
    {
      "id": 1,
      "name": "需求澄清",
      "status": "completed",
      "inputs": [".flow-kit/change-proposal.md"],
      "outputs": [".flow-kit/requirements.md"],
      "evidence": []
    }
  ]
}
```

#### F2.3 任务深度分级

保留三种深度（经验萃取 P0-3 原子化任务拆分）：

| 深度 | 触发条件 | 行为 |
|------|----------|------|
| quick | 单文件修改、明确 bug | 跳过 Phase 0-3，直接 Phase 4 |
| campaign | 多文件但边界清晰 | 正常流程，每 phase 确认 |
| deep | 跨模块、新架构 | 完整流程 + 额外审查点 |

#### F2.4 验收标准

- [ ] auto-pilot.sh 能读取 session-state.json 并生成 auto-plan.json
- [ ] auto-plan.json 包含每个 phase 的 inputs/outputs（DAG 依赖）
- [ ] 支持三种任务深度（quick/campaign/deep）
- [ ] phase 执行后调用 validate-phase.sh 并记录验证证据
- [ ] 推荐下一步但不自动推进
- [ ] auto-plan.json 与 session-state.json 职责分离（auto-plan 管执行计划，session-state 管全局状态）

---

### 3.3 F3：产出物检查增强

**做什么**：validate-phase.sh 增加 phase 级别的产出物模板检查和验证证据记录。

**为什么**：当前 validate-phase.sh 不读取 phase 的 expected_artifacts 声明，无法验证产出物完整性。

#### F3.1 产出物模板检查

融入 Delta Spec 增量规范（经验萃取 P0-2）：

- 读取每个 phase 的 `expected_artifacts` 声明（来自 YAML front matter）
- 检查产出物文件是否存在
- 可选：检查产出物内容是否包含预期关键词
- 输出结构化检查结果

#### F3.2 验证证据记录

融入验证证据机制（经验萃取 P0-5）：

```json
{
  "phase": 2,
  "timestamp": "2026-05-16T10:00:00Z",
  "checks": [
    {"artifact": ".flow-kit/design.md", "type": "file_exists", "result": "pass"},
    {"artifact": ".flow-kit/design.md", "type": "contains_keyword", "keyword": "ADR", "result": "pass"}
  ],
  "verdict": "pass",
  "missing": []
}
```

原则：**没有证据的完成 = 没完成**。

#### F3.3 验收标准

- [ ] validate-phase.sh 能读取 phase front matter 的 expected_artifacts
- [ ] 对每个 expected_artifact 执行存在性检查
- [ ] 验证结果以 JSON 格式记录到 auto-plan.json 的 evidence 字段
- [ ] 验证失败时输出明确的缺失列表
- [ ] 不做越界检测（归 F1）、不做自动修正（归下版本）

---

### 3.4 F4：PRD 预处理

**做什么**：prd-parser.sh 对 Markdown/JSON 格式的 PRD 进行预处理（格式检测、结构提取），为 AI 解析做准备。

**为什么**：Shell 无法理解自然语言，但可以做结构提取，减少 AI 的预处理工作。

#### F4.1 支持格式

| 格式 | 提取方式 | 产出 |
|------|----------|------|
| Markdown | 正则提取标题、章节、列表项 | JSON 结构 |
| JSON | jq 直接提取 | 原始 JSON |

不支持 plain-text（无结构可抓）。

#### F4.2 处理流程

```
检测文件格式
  → Markdown: 提取 H1-H4 标题树 + 列表项 + 表格
  → JSON: jq 提取顶级键和嵌套结构
  → 输出结构化 JSON
  → 传递给 AI 做实际理解和需求提取
```

#### F4.3 验收标准

- [ ] prd-parser.sh 能区分 Markdown 和 JSON 格式
- [ ] Markdown 提取：标题树、列表项、表格内容
- [ ] JSON 提取：顶级键、嵌套结构
- [ ] 输出为 JSON 格式，可被后续流程消费
- [ ] 不做自然语言理解（由 AI 完成）

---

### 3.5 F8：增强回滚阶段

**做什么**：phases/8-rollback/ 工作流 Markdown 增加三档分级判定流程和 RCA 模板。

**为什么**：当前 phase-8 只有简单回滚操作，缺少分级响应和事后分析。

#### F8.1 三档分级

| 级别 | 场景 | 响应 |
|------|------|------|
| P0 | 生产故障、数据丢失 | 先回滚 → 再诊断 → 事后 5 件事 |
| P1 | 功能异常但可绕过 | 3 问澄清 → 快速修复 → 事后 5 件事 |
| P2 | 体验问题、非阻塞 | 走正常修复流程 |

#### F8.2 RCA 模板

phase-8 工作流中嵌入事故报告模板：

```markdown
## 事故报告

### 基本信息
- 时间：
- 影响范围：
- 分级：P0/P1/P2

### 根因分析（5 Whys）
1. 为什么出现这个问题？
2. 为什么这个原因没被发现？
3. 为什么没有预防措施？
4. 为什么预防措施没生效？
5. 根本原因是什么？

### 修复方案
- 短期：
- 长期：

### 事后 5 件事
- [ ] 1. 根因分析（RCA）
- [ ] 2. 修复方案文档
- [ ] 3. 测试补充（防止复发）
- [ ] 4. 监控告警规则更新
- [ ] 5. 文档更新
```

#### F8.3 验收标准

- [ ] phases/8-rollback/brownfield.md 和 greenfield.md 包含三档分级判定流程
- [ ] 包含 RCA 事故报告模板
- [ ] 包含事后 5 件事检查清单
- [ ] P0 级别明确"先回滚再诊断"原则
- [ ] 不新建独立 Shell 脚本

---

### 3.6 Secrets 检测增强

**做什么**：lib/security-scanner.sh 增加 18 种密钥模式检测。

**为什么**：当前 security-scanner.sh 无密钥泄露检测能力。

#### 检测模式

GitHub Token (`ghp_`/`gho_`/`ghu_`/`ghs_`/`ghr_`)、OpenAI Key (`sk-`)、AWS Key (`AKIA`)、JWT (`eyJ...`)、SSH Private Key、API Key/Secret/Token/Password 通用模式、Slack Token (`xox[bpras]-`)。

#### 验收标准

- [ ] security-scanner.sh 新增 `scan_secrets` 函数
- [ ] 支持 18 种密钥模式正则匹配
- [ ] 支持 full/staged/diff 三种扫描范围
- [ ] 检测到密钥时输出警告并返回非零退出码

---

## 4. 优先级与依赖

### 4.1 MoSCoW 优先级

| 优先级 | 功能 | 工作量 | 依赖 |
|--------|------|--------|------|
| Must Have | F1 边界重构 | 2 天 | 无 |
| Must Have | F2 半自动引擎 | 3 天 | F1（front matter） |
| Must Have | F3 产出物增强 | 2 天 | F1（front matter） |
| Should Have | F4 PRD 预处理 | 1.5 天 | 无 |
| Should Have | F8 增强回滚 | 1 天 | 无 |
| Could Have | Secrets 增强 | 0.5 天 | 无 |

### 4.2 依赖关系

```
F1 (front matter) ──→ F2 (auto-pilot 读 front matter)
                  ──→ F3 (validate 读 expected_artifacts)

F2 (auto-pilot) ──→ F3 (调用 validate-phase.sh)

F4, F8, Secrets 独立，无前置依赖
```

### 4.3 与现有能力的关系

| 新功能 | 复用的现有能力 | 不新建的 |
|--------|----------------|----------|
| F1 边界 | post-edit-format.sh + stop-quality-gate.sh | boundary-enforcer.sh |
| F2 执行 | phase-executor.sh + next-phase.sh + plan-generate.sh | 全新引擎 |
| F3 验证 | validate-phase.sh | execution-auditor.sh |
| F8 回滚 | phases/8-rollback/ 工作流 | hotfix-executor.sh |
| Secrets | lib/security-scanner.sh | secrets-guard.sh |

### 4.4 v3.7.0 规划（不在本版本）

| 功能 | 说明 |
|------|------|
| Ralph 集成 | 生成任务文件 + 官方 ralph-loop 插件对接 |
| /recall 命令 | 简化项目摘要（读取 session-state + git log） |
| 自我进化指标 | 简化版执行指标收集 |

---

## 5. 实施计划

### 5.1 总体时间线：6 周，14 人天

| 阶段 | 时间 | 内容 | 工作量 |
|------|------|------|--------|
| 迭代 1 | 第 1-2 周 | F1 边界重构 + Secrets 增强 | 2.5 人天 |
| 迭代 2 | 第 3-4 周 | F2 半自动引擎 + F3 产出物增强 | 5 人天 |
| 迭代 3 | 第 4-5 周 | F4 PRD 预处理 + F8 增强回滚 | 2.5 人天 |
| 迭代 4 | 第 5-6 周 | 集成测试 + 文档 | 4 人天 |

### 5.2 测试策略：TDD

每个功能先写测试，再写实现：

1. F1：测试 front matter 解析、边界警告输出
2. F2：测试 auto-plan.json 生成、状态机推进、DAG 依赖
3. F3：测试产出物检查、验证证据记录
4. F4：测试 Markdown/JSON 格式检测和结构提取
5. F8：测试三档分级判定流程（工作流 review）
6. Secrets：测试 18 种模式匹配

使用现有 vitest 测试框架（flow-kit/tests/）。

### 5.3 关键文件清单

| 文件 | 操作 | 功能 |
|------|------|------|
| `phases/*/brownfield.md` + `greenfield.md` | 修改（加 front matter） | F1 |
| `hooks/post-edit-format.sh` | 修改（加边界警告） | F1 |
| `hooks/stop-quality-gate.sh` | 修改（加产出物检查） | F1/F3 |
| `scripts/auto-pilot.sh` | 新建 | F2 |
| `scripts/validate-phase.sh` | 修改（加产出物模板+证据） | F3 |
| `scripts/prd-parser.sh` | 新建 | F4 |
| `phases/8-rollback/brownfield.md` + `greenfield.md` | 修改（加三档+RCA） | F8 |
| `lib/security-scanner.sh` | 修改（加密钥检测） | Secrets |

---

## 6. 约束与风险

### 6.1 技术约束

| 约束 | 说明 |
|------|------|
| Shell 脚本 | 所有脚本使用 bash，不引入新语言 |
| 无新外部依赖 | 仅使用 jq/yq/git 等已有工具 |
| 向后兼容 | 无 front matter 的旧 phase 文件仍可正常加载 |
| AI 执行边界 | Shell 做状态管理和预处理，AI 做理解和生成 |

### 6.2 Hooks 冲突分析

当前已注册的 hooks：

| Hook 类型 | 来源 | 数量 |
|-----------|------|------|
| PreToolUse | flow-kit | 1 |
| PostToolUse | flow-kit | 1 |
| Stop | flow-kit + ralph-loop | 2（可能冲突） |
| SessionStart | flow-kit | 1 |
| Notification | flow-kit | 1 |

**风险**：flow-kit 的 stop-quality-gate.sh 和 ralph-loop 的 stop-hook.sh 都注册了 Stop hook。Claude Code 的 hooks 串联机制需验证。

**缓解**：重写前先测试 Claude Code 多插件 Stop hook 的行为。如果冲突，考虑将 flow-kit 的 stop 检查移到 post-edit-format。

### 6.3 风险矩阵

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| front matter 格式不被 AI 遵守 | 中 | 中 | hooks 做补充警告 |
| auto-pilot 与现有脚本不兼容 | 低 | 高 | 增量增强现有脚本，不替换 |
| Hooks 冲突导致功能异常 | 中 | 高 | 先验证再集成 |
| 向后兼容性问题 | 低 | 中 | front matter 解析做容错 |

---

## 7. 验收标准汇总

### 功能验收

- [ ] F1：19 个 phase 文件含完整 YAML front matter
- [ ] F1：post-edit-format.sh 能输出边界警告
- [ ] F1：stop-quality-gate.sh 能检查 expected_artifacts
- [ ] F1：无 front matter 的旧文件仍可正常加载
- [ ] F2：auto-pilot.sh 能生成 auto-plan.json（含 DAG 依赖）
- [ ] F2：支持三种任务深度
- [ ] F2：phase 执行后记录验证证据
- [ ] F2：不自动推进到下一 phase
- [ ] F3：validate-phase.sh 能读取 expected_artifacts 并检查
- [ ] F3：验证结果以 JSON 格式记录
- [ ] F3：验证失败输出缺失列表
- [ ] F4：prd-parser.sh 支持 Markdown + JSON 格式
- [ ] F4：输出为可消费的 JSON
- [ ] F8：phase-8 含三档分级流程
- [ ] F8：phase-8 含 RCA 事故报告模板
- [ ] F8：phase-8 含事后 5 件事检查清单
- [ ] Secrets：security-scanner.sh 检测 18 种密钥模式

### 非功能验收

- [ ] 所有新增/修改的 Shell 脚本通过 ShellCheck
- [ ] 所有功能有对应的 vitest 测试（TDD）
- [ ] 向后兼容 v3.5.0 现有工作流
- [ ] 无功能间职责重叠

### 参考文件

- **借鉴分析**：`3.6.0-references.md`（从原 PRD 第 15 章拆出）
- **经验萃取**：`经验萃取成果.md`
- **原 PRD**：`3.6.0.md`（归档）、`3.6.0bak.md`（归档）
- **审查报告**：`/home/lytv/.claude/plans/rippling-honking-puppy.md`
