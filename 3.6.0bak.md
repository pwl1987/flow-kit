# flow-kit v3.6.0 产品需求文档（PRD）

> **文档版本**: v1.0
> **编制日期**: 2026-05-13
> **产品名称**: flow-kit — 基于 Claude Code 的结构化开发流程工具包
> **当前版本**: v3.3.0 → **目标版本**: v3.6.0
> **总投入预估**: 10 周，约 85 人天
> **核心目标**: 更好用、更智能、更自动

---

## 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v0.1 | 2026-05-13 | Claude Code AI | 初稿：基于 flow-kit v3.3.0 项目分析，完成六维度开发提升方案 |
| v0.2 | 2026-05-13 | Claude Code AI | 新增：Phase 命令功能边界重构方案（F1-F6） |
| v0.3 | 2026-05-14 | Claude Code AI | 新增：持续任务执行系统（F7）、Ralph Wiggum 循环集成、通用任务模板 |
| v0.4 | 2026-05-14 | Claude Code AI | 新增：superpowers-zh、agency-agents-zh、mattpocock/skills 借鉴分析 |
| v0.5 | 2026-05-14 | Claude Code AI | 新增：Ralph.md 借鉴分析、superpowers+gstack 开发流程借鉴分析 |
| v0.6 | 2026-05-15 | Claude Code AI | 新增：技术兼容性整合方案（13.7）、模块集成方法、接口设计规范 |
| v0.7 | 2026-05-15 | Claude Code AI | 新增：Context Engineering 借鉴分析（13.9）、全局路由协议（13.10）、生产级交付实践（13.11）、控制模型与自进化（13.12） |
| v1.0 | 2026-05-15 | Claude Code AI | 正式版：补充版本历史、修订记录，完成全部 13 章节内容 |
| v1.1 | 2026-05-16 | Claude Code AI | 新增：Ralph 自主 Agent 循环（13.14）、AutoResearch 方法论（13.15）、CE 知识复利系统（13.16）、flow-kit 社区实践（13.17）、团队工程化落地（13.18）借鉴分析 |
| v1.2 | 2026-05-16 | Claude Code AI | 新增：自进化引擎失效分析（13.19）、Multi-Pass Review（13.20）、DESIGN.md+grill-me（13.21）、web-access（13.22）、RTK+OpenWolf（13.23）、Google Agent Skills（13.24）、Subagent Fork（13.25）、Command Center（13.26）借鉴分析 |

---

## 修订记录

| 修订编号 | 日期 | 修订章节 | 修订内容 | 修订原因 |
|----------|------|----------|----------|----------|
| REV-001 | 2026-05-15 | 全部 | 补充版本历史记录与修订记录表 | 用户要求 PRD 必须包含版本历史记录和修订记录 |
| REV-002 | 2026-05-15 | 13.9-13.12 | 新增 Context Engineering、全局路由协议、生产级交付实践、控制模型与自进化四个借鉴分析章节 | Clippings 研读新增 15 篇文档分析 |
| REV-003 | 2026-05-15 | 经验萃取成果.md | 新增第十二至十五章，更新已分析文档清单至 62 篇 | 持续萃取 Clippings 中的技术最佳实践 |
| REV-004 | 2026-05-15 | 3.7 | 完善 F7 持续任务执行系统：三种自主运行模式、Ralph 循环、安全 Hook、任务模板、异常处理 | 用户要求实现任务自动执行功能和通用任务模板 |
| REV-005 | 2026-05-15 | 13.7 | 新增技术兼容性整合方案：适配策略、模块集成方法、接口规范、兼容性测试计划 | 用户要求确保所有引入技术和方法与现有架构兼容 |
| REV-006 | 2026-05-16 | 13.14-13.18 | 新增 Ralph 自主 Agent 循环、AutoResearch 方法论、CE 知识复利系统、flow-kit 社区实践、团队工程化落地五个借鉴分析章节 | 经验萃取成果第21-25章内容集成 |

---

## 目录

- [1. 产品概述](#1-产品概述)
- [2. 问题分析与机会识别](#2-问题分析与机会识别)
- [3. 功能需求详细说明](#3-功能需求详细说明)
- [4. 用户故事](#4-用户故事)
- [5. 功能点优先级划分](#5-功能点优先级划分)
- [6. 界面交互设计规范](#6-界面交互设计规范)
- [7. 数据流程图](#7-数据流程图)
- [8. 接口定义](#8-接口定义)
- [9. 非功能需求](#9-非功能需求)
- [10. 验收标准](#10-验收标准)
- [11. 实施计划](#11-实施计划)
- [12. 风险评估](#12-风险评估)
- [13. 参考项目借鉴分析](#13-参考项目借鉴分析)
  - [13.1 superpowers-zh 借鉴分析](#131-superpowers-zh-借鉴分析)
  - [13.2 agency-agents-zh 借鉴分析](#132-agency-agents-zh-借鉴分析)
  - [13.3 借鉴整合路线图](#133-借鉴整合路线图)
  - [13.4 mattpocock/skills 借鉴分析](#134-mattpocockskills-借鉴分析)
  - [13.5 Ralph.md 借鉴分析](#135-ralphmd-借鉴分析)
  - [13.6 superpowers+gstack 开发流程借鉴分析](#136-superpowersgstack-开发流程借鉴分析)
  - [13.7 技术兼容性整合方案](#137-技术兼容性整合方案)
  - [13.8 Claude Code AI 辅助效率提升分析](#138-claude-code-ai-辅助效率提升分析)
  - [13.9 代码质量改进指标](#139-代码质量改进指标)
  - [13.10 Context Engineering 借鉴分析](#1310-context-engineering-借鉴分析)
  - [13.11 全局路由协议借鉴分析](#1311-全局路由协议借鉴分析)
  - [13.12 生产级交付实践借鉴分析](#1312-生产级交付实践借鉴分析)
  - [13.13 控制模型与自进化借鉴分析](#1313-控制模型与自进化借鉴分析)
  - [13.14 Ralph 自主 Agent 循环与 PRD 驱动开发](#1314-ralph-自主-agent-循环与-prd-驱动开发)
  - [13.15 AutoResearch 方法论借鉴分析](#1315-autoresearch-方法论借鉴分析)
  - [13.16 Compound Engineering 知识复利系统](#1316-compound-engineering-知识复利系统)
  - [13.17 flow-kit 社区实践与融合创新](#1317-flow-kit-社区实践与融合创新)
  - [13.18 Claude Code 团队工程化落地](#1318-claude-code-团队工程化落地)
  - [13.19 自进化引擎失效分析与修复](#1319-自进化引擎失效分析与修复)
  - [13.20 Multi-Pass Review 工程化](#1320-multi-pass-review-工程化)
  - [13.21 DESIGN.md 设计系统与 grill-me 需求访谈](#1321-designmd-设计系统与-grill-me-需求访谈)
  - [13.22 web-access 浏览器直连](#1322-web-access-浏览器直连)
  - [13.23 RTK 与 OpenWolf 成本优化](#1323-rtk-与-openwolf-成本优化)
  - [13.24 Google Agent Skills](#1324-google-agent-skills)
  - [13.25 Subagent Fork 机制](#1325-subagent-fork-机制)
  - [13.26 Command Center 目标管理层](#1326-command-center-目标管理层)

---

## 1. 产品概述

### 1.1 产品定位

flow-kit 是一个基于 Claude Code 的结构化开发流程工具包，提供 9 阶段（Phase 0-8）开发工作流、棕地/绿地条件路由、护栏系统、会话记忆系统和多代理并行编排能力。v3.6.0 将聚焦于 **Phase 命令功能边界重构** 与 **全流程自动化执行** 两大核心方向。

### 1.2 版本目标

| 目标 | 描述 | 量化指标 |
|------|------|----------|
| 功能边界清晰化 | 每个 phase 命令职责明确，不越界执行 | 越界事件减少 100% |
| 操作确认机制 | 所有关键步骤必须获得人工确认 | 确认覆盖率 100% |
| 状态透明化 | 实时展示当前执行阶段与下一步计划 | 状态更新延迟 < 1s |
| 全流程自动化 | Phase 0→7 自动执行，无需人工干预 | 自动化率 > 90% |
| 智能决策系统 | 在确认环节主动弹出推荐选项 | 推荐采纳率 > 70% |
| 自动化审计 | 识别不合适操作或遗漏步骤 | 审计覆盖率 100% |
| 需求文档自动处理 | 支持 PRD 自动解析与执行 | 解析准确率 > 85% |
| 自我进化机制 | 工作流持续优化与改进 | 每迭代优化 3+ 项 |
| 环境自动清理 | 命令执行前自动清理环境 | 清理成功率 100% |

### 1.3 目标用户

| 用户角色 | 使用场景 | 核心需求 |
|----------|----------|----------|
| 独立开发者 | 个人项目开发 | 自动化执行、减少手动操作 |
| 小团队（2-5 人） | 协作开发 | 状态透明、确认机制、审计 |
| 技术负责人 | 项目管理和代码审查 | 全流程可见性、质量门禁 |
| 产品经理 | 需求提交与跟踪 | PRD 自动解析、进度追踪 |

---

## 2. 问题分析与机会识别

### 2.1 当前问题深度分析

#### 问题 1：Phase 命令功能越界

**现状**：当前 `/flow-kit:phase-N` 命令在执行时，会擅自执行超出其职责范围的操作。例如：
- `/flow-kit:phase-0`（变更立项）在执行影响范围评估后，可能直接开始架构设计
- `/flow-kit:phase-2`（架构设计）可能直接进入开发执行
- `/flow-kit:phase-4`（开发执行）可能自行执行测试验证

**根因分析**：
1. Phase 工作流文件（如 `phases/4-dev/brownfield.md`）中的执行步骤描述模糊，未明确职责边界
2. phase-executor.sh 仅负责加载工作流文件，未对工作流内容进行边界校验
3. 缺少命令执行前的职责声明与确认机制
4. Claude Code 的 AI 特性导致其倾向于"主动完成更多工作"

**影响范围**：
- 开发流程混乱，阶段产出物不可控
- 用户失去对开发节奏的控制
- 审计困难，无法追溯哪个阶段执行了什么操作

#### 问题 2：缺少自动化执行能力

**现状**：当前需要用户手动逐个执行 `/flow-kit:phase-0` → `/flow-kit:phase-1` → ... → `/flow-kit:phase-7`

**根因分析**：
1. 缺少自动化执行引擎
2. 缺少阶段间状态传递机制
3. 缺少执行计划生成能力

#### 问题 3：缺少智能决策支持

**现状**：在需要人工确认的环节，仅输出信息，不提供推荐选项

**根因分析**：
1. 缺少决策上下文分析能力
2. 缺少推荐选项生成机制
3. 缺少决策历史记录与学习能力

#### 问题 4：缺少审计与自我修正机制

**现状**：执行过程中的错误或遗漏无法自动识别和修正

**根因分析**：
1. 缺少执行结果验证机制
2. 缺少错误模式识别能力
3. 缺少自动修正方案生成能力

### 2.2 机会识别

| 机会 | 描述 | 预期收益 |
|------|------|----------|
| 借鉴 superpowers-zh workflow-runner | YAML 工作流编排 + 多角色协作 | 自动化率提升 60% |
| 借鉴 superpowers-zh executing-plans | 分步执行 + 审查检查点 | 执行质量提升 50% |
| 借鉴 agency-agents-zh 角色体系 | 结构化角色定义 + 专业流程 | 决策质量提升 40% |
| 借鉴 agency-agents-zh DAG 编排 | 依赖检测 + 并行执行 | 执行效率提升 3 倍 |
| 借鉴 Claude Code 的 /clear 机制 | 环境状态重置 | 状态一致性 100% |

---

## 3. 功能需求详细说明

### 3.1 Phase 命令功能边界重构（F1）

#### F1.1 职责边界定义

每个 Phase 命令必须明确定义其职责范围，建立严格的执行边界：

```yaml
# phase-boundaries.yaml — Phase 职责边界定义
phases:
  phase-0:
    name: "变更立项"
    allowed_operations:
      - 影响范围评估
      - 破坏性变更识别
      - 风险评级
      - 护栏配置
      - Change ID 生成
    forbidden_operations:
      - 架构设计（属于 Phase 2）
      - 任务拆解（属于 Phase 3）
      - 代码编写（属于 Phase 4）
      - 测试执行（属于 Phase 5）
    output: "CHANGE.md + 影响范围评估文档"
    confirmation_required: true
    max_auto_steps: 3

  phase-1:
    name: "需求澄清"
    allowed_operations:
      - 需求分析
      - 用户故事编写
      - 验收标准定义
      - 需求优先级排序
    forbidden_operations:
      - 技术方案设计（属于 Phase 2）
      - 任务拆解（属于 Phase 3）
    output: "REQUIREMENT.md"
    confirmation_required: true
    max_auto_steps: 3

  phase-2:
    name: "架构设计"
    allowed_operations:
      - 技术方案设计
      - 架构决策记录（ADR）
      - 接口定义
      - 数据模型设计
    forbidden_operations:
      - 代码编写（属于 Phase 4）
      - 测试编写（属于 Phase 5）
    output: "DESIGN.md + ADR"
    confirmation_required: true
    max_auto_steps: 4

  phase-3:
    name: "任务拆解"
    allowed_operations:
      - 任务分解
      - DAG 依赖分析
      - 工作量估算
      - 任务分配
    forbidden_operations:
      - 代码编写（属于 Phase 4）
      - 架构决策（属于 Phase 2）
    output: "TASK.md"
    confirmation_required: true
    max_auto_steps: 3

  phase-4:
    name: "开发执行"
    allowed_operations:
      - 代码实现
      - 单元测试编写
      - 代码提交
    forbidden_operations:
      - 架构设计（属于 Phase 2）
      - 集成测试（属于 Phase 5）
      - 代码审查（属于 Phase 6）
    output: "代码实现 + 单元测试"
    confirmation_required: false  # 开发阶段可批量确认
    max_auto_steps: 10

  phase-5:
    name: "测试验证"
    allowed_operations:
      - 集成测试
      - 端到端测试
      - 性能测试
      - 护栏检查
    forbidden_operations:
      - 代码修改（属于 Phase 4）
      - 代码审查（属于 Phase 6）
    output: "测试报告"
    confirmation_required: true
    max_auto_steps: 5

  phase-6:
    name: "代码审查"
    allowed_operations:
      - 代码审查
      - 安全审计
      - 质量门禁检查
    forbidden_operations:
      - 代码修改（属于 Phase 4）
      - 测试执行（属于 Phase 5）
    output: "REVIEW.md"
    confirmation_required: true
    max_auto_steps: 3

  phase-7:
    name: "集成归档"
    allowed_operations:
      - 代码合并
      - 变更归档
      - 文档更新
      - 发布准备
    forbidden_operations:
      - 代码修改（属于 Phase 4）
      - 架构变更（属于 Phase 2）
    output: "归档完成 + 发布说明"
    confirmation_required: true
    max_auto_steps: 4
```

#### F1.2 边界校验机制

创建 `scripts/boundary-enforcer.sh`，在 phase 命令执行前进行边界校验：

```bash
# boundary-enforcer.sh — Phase 边界强制执行器
# 在每个 phase 命令执行前校验操作合法性

enforce_boundary() {
    local phase="$1"
    local operation="$2"

    # 1. 加载边界定义
    local boundaries
    boundaries=$(yq eval ".phases[\"$phase\"]" "$BOUNDARIES_FILE")

    # 2. 检查操作是否在 allowed_operations 中
    local is_allowed
    is_allowed=$(echo "$boundaries" | yq eval ".allowed_operations[] | select(. == \"$operation\")" 2>/dev/null)

    if [[ -z "$is_allowed" ]]; then
        # 3. 检查是否在 forbidden_operations 中
        local is_forbidden
        is_forbidden=$(echo "$boundaries" | yq eval ".forbidden_operations[] | select(. == \"$operation\")" 2>/dev/null)

        if [[ -n "$is_forbidden" ]]; then
            echo "[boundary] ❌ 操作越界: '$operation' 不属于 $phase 的职责范围"
            echo "[boundary] 该操作属于: $(find_phase_for_operation "$operation")"
            return 1
        fi

        echo "[boundary] ⚠️  未识别的操作: '$operation'"
        echo "[boundary] 建议: 更新 phase-boundaries.yaml 或确认操作合法性"
        return 2
    fi

    return 0
}
```

#### F1.3 操作前确认流程

创建 `scripts/confirmation-manager.sh`，实现操作前确认机制：

```bash
# confirmation-manager.sh — 操作确认管理器

# 确认级别定义
declare -A CONFIRM_LEVELS
CONFIRM_LEVELS[critical]="必须人工确认，不可跳过"
CONFIRM_LEVELS[important]="建议人工确认，可配置自动确认"
CONFIRM_LEVELS[normal]="批量确认，可配置自动模式"

# 请求确认
request_confirmation() {
    local phase="$1"
    local operation="$2"
    local description="$3"
    local level="${4:-important}"
    local options=("${@:5}")

    echo ""
    echo "=========================================="
    echo "[confirm] Phase: $phase"
    echo "[confirm] 操作: $operation"
    echo "[confirm] 描述: $description"
    echo "[confirm] 级别: $level (${CONFIRM_LEVELS[$level]})"
    echo "=========================================="
    echo ""

    # 生成推荐选项
    if [[ ${#options[@]} -gt 0 ]]; then
        echo "推荐选项:"
        for i in "${!options[@]}"; do
            echo "  $((i+1)). ${options[$i]}"
        done
        echo ""
    fi

    # 根据确认级别决定行为
    case "$level" in
        critical)
            echo "请输入确认 (yes/no) 或选择选项编号:"
            ;;
        important)
            if [[ "$AUTO_CONFIRM_MODE" == "true" ]]; then
                echo "[confirm] 自动确认模式已启用，默认选择选项 1"
                return 0
            fi
            echo "请输入确认 (yes/no/auto) 或选择选项编号:"
            ;;
        normal)
            if [[ "$AUTO_CONFIRM_MODE" == "true" ]]; then
                return 0
            fi
            echo "按 Enter 继续，或输入选项编号:"
            ;;
    esac
}
```

#### F1.4 状态透明化机制

创建 `scripts/status-display.sh`，实现实时状态展示：

```bash
# status-display.sh — 状态透明化显示

show_phase_status() {
    local current_phase="$1"
    local total_phases=8

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║         flow-kit 执行状态面板            ║"
    echo "╠══════════════════════════════════════════╣"

    for ((i=0; i<=total_phases; i++)); do
        local phase_name
        phase_name=$(get_phase_name "$i")
        local status_icon="⬜"
        local status_text="待执行"

        if [[ "$i" -lt "$current_phase" ]]; then
            status_icon="✅"
            status_text="已完成"
        elif [[ "$i" -eq "$current_phase" ]]; then
            status_icon="🔄"
            status_text="执行中"
        fi

        printf "║  %s Phase %d: %-12s %-8s║\n" "$status_icon" "$i" "$phase_name" "$status_text"
    done

    echo "╠══════════════════════════════════════════╣"
    echo "║  下一步: $(get_next_action)              ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""
}
```

### 3.2 自动化执行流程（F2）

#### F2.1 自动化执行引擎

创建 `scripts/auto-pilot.sh`，实现 Phase 0→7 的全流程自动执行：

```bash
# auto-pilot.sh — 自动化执行引擎 v1.0

AUTO_PILOT_MODE="${AUTO_PILOT_MODE:-interactive}"  # interactive | silent | supervised
AUTO_CONFIRM_THRESHOLD="${AUTO_CONFIRM_THRESHOLD:-important}"  # critical | important | normal

# 执行计划生成
generate_execution_plan() {
    local change_description="$1"
    local project_type="$2"

    echo "[auto-pilot] 生成执行计划..."
    echo ""

    # 分析变更描述，生成阶段执行计划
    local plan_file="$PROJECT_DIR/.flow-kit/auto-plan.json"

    jq -n \
        --arg desc "$change_description" \
        --arg type "$project_type" \
        --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        '{
            description: $desc,
            project_type: $type,
            created_at: $ts,
            phases: [
                {"id": 0, "name": "变更立项", "status": "pending", "auto_confirm": false},
                {"id": 1, "name": "需求澄清", "status": "pending", "auto_confirm": false},
                {"id": 2, "name": "架构设计", "status": "pending", "auto_confirm": false},
                {"id": 3, "name": "任务拆解", "status": "pending", "auto_confirm": false},
                {"id": 4, "name": "开发执行", "status": "pending", "auto_confirm": true},
                {"id": 5, "name": "测试验证", "status": "pending", "auto_confirm": false},
                {"id": 6, "name": "代码审查", "status": "pending", "auto_confirm": false},
                {"id": 7, "name": "集成归档", "status": "pending", "auto_confirm": false}
            ],
            current_phase: 0,
            status: "running"
        }' > "$plan_file"

    echo "$plan_file"
}

# 自动执行
auto_execute() {
    local plan_file="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 自动化执行引擎已启动        ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  模式: $AUTO_PILOT_MODE"
    echo "║  确认阈值: $AUTO_CONFIRM_THRESHOLD"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    for ((phase=0; phase<=7; phase++)); do
        local phase_name
        phase_name=$(jq -r ".phases[] | select(.id == $phase) | .name" "$plan_file")

        echo ""
        echo "────────────────────────────────────────"
        echo "  执行 Phase $phase: $phase_name"
        echo "────────────────────────────────────────"
        echo ""

        # 更新状态
        jq --argjson phase "$phase" '.current_phase = $phase | .phases[$phase].status = "running"' "$plan_file" > "${plan_file}.tmp" && mv "${plan_file}.tmp" "$plan_file"

        # 显示状态面板
        show_phase_status "$phase"

        # 执行 phase
        if ! execute_phase "$phase"; then
            echo "[auto-pilot] ❌ Phase $phase 执行失败"
            jq --argjson phase "$phase" '.phases[$phase].status = "failed"' "$plan_file" > "${plan_file}.tmp" && mv "${plan_file}.tmp" "$plan_file"
            return 1
        fi

        # 验证 phase 输出
        if ! validate_phase_output "$phase"; then
            echo "[auto-pilot] ⚠️  Phase $phase 输出验证未通过，尝试自动修复..."
            if ! auto_fix_phase "$phase"; then
                echo "[auto-pilot] ❌ 自动修复失败，需要人工干预"
                return 1
            fi
        fi

        # 标记完成
        jq --argjson phase "$phase" '.phases[$phase].status = "completed"' "$plan_file" > "${plan_file}.tmp" && mv "${plan_file}.tmp" "$plan_file"

        echo "[auto-pilot] ✅ Phase $phase 完成"
    done

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║   🎉 全流程自动化执行完成！              ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 生成执行报告
    generate_execution_report "$plan_file"
}
```

#### F2.2 智能决策系统

创建 `scripts/decision-engine.sh`，在需要人工确认的环节主动弹出推荐选项：

```bash
# decision-engine.sh — 智能决策引擎

# 决策上下文分析
analyze_decision_context() {
    local phase="$1"
    local operation="$2"

    # 收集决策上下文
    local project_type
    project_type=$(session_get ptype)
    local current_phase
    current_phase=$(session_get phase)
    local history
    history=$(session_get history)

    # 分析历史决策模式
    local similar_decisions
    similar_decisions=$(find_similar_decisions "$operation" "$project_type")

    # 生成推荐选项
    local recommendations=()

    case "$phase-$operation" in
        "0-影响范围评估")
            recommendations+=("自动执行影响范围评估（基于 git diff）")
            recommendations+=("手动指定影响范围（适用于大型变更）")
            recommendations+=("跳过评估（仅适用于微小变更）")
            ;;
        "1-需求分析")
            recommendations+=("自动分析需求并生成用户故事")
            recommendations+=("引导式需求澄清（逐项确认）")
            recommendations+=("从 PRD 文档自动解析")
            ;;
        "2-架构设计")
            recommendations+=("基于需求自动生成架构方案")
            recommendations+=("选择架构模板（微服务/单体/事件驱动）")
            recommendations+=("手动设计架构（适用于复杂场景）")
            ;;
        "4-开发执行")
            recommendations+=("自动执行所有任务（基于 TASK.md）")
            recommendations+=("逐任务执行（每完成一个确认一次）")
            recommendations+=("并行执行（使用 dispatch.sh 多代理）")
            ;;
        "5-测试验证")
            recommendations+=("自动运行所有测试")
            recommendations+=("选择性运行测试（指定测试范围）")
            recommendations+=("跳过测试（仅适用于文档变更）")
            ;;
        *)
            recommendations+=("自动执行（推荐）")
            recommendations+=("手动执行")
            recommendations+=("跳过此步骤")
            ;;
    esac

    echo "${recommendations[@]}"
}

# 决策记录与学习
record_decision() {
    local phase="$1"
    local operation="$2"
    local choice="$3"
    local outcome="$4"

    local decision_record="$PROJECT_DIR/.flow-kit/decisions.log"

    echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | $phase | $operation | $choice | $outcome" >> "$decision_record"

    # 更新 session-state
    session_decision_add "$phase" "$choice"
}

# 从历史决策中学习
learn_from_history() {
    local decision_record="$PROJECT_DIR/.flow-kit/decisions.log"

    if [[ ! -f "$decision_record" ]]; then
        return
    fi

    # 分析历史决策的成功率
    local total=0
    local success=0

    while IFS='|' read -r ts phase operation choice outcome; do
        total=$((total + 1))
        if [[ "$outcome" == *"success"* ]]; then
            success=$((success + 1))
        fi
    done < "$decision_record"

    local success_rate=0
    if [[ $total -gt 0 ]]; then
        success_rate=$((success * 100 / total))
    fi

    echo "[decision-engine] 历史决策成功率: ${success_rate}% (${success}/${total})"

    # 如果成功率低于 50%，建议切换到更保守的模式
    if [[ $success_rate -lt 50 ]]; then
        echo "[decision-engine] ⚠️  历史决策成功率较低，建议启用监督模式"
    fi
}
```

### 3.3 自动化审计机制（F3）

#### F3.1 执行审计器

创建 `scripts/execution-auditor.sh`：

```bash
# execution-auditor.sh — 执行审计器

# 审计执行过程
audit_execution() {
    local plan_file="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║         flow-kit 执行审计报告            ║"
    echo "╠══════════════════════════════════════════╣"

    local issues=0
    local warnings=0

    # 1. 检查每个 phase 的输出完整性
    for ((phase=0; phase<=7; phase++)); do
        local phase_name
        phase_name=$(jq -r ".phases[] | select(.id == $phase) | .name" "$plan_file")
        local status
        status=$(jq -r ".phases[] | select(.id == $phase) | .status" "$plan_file")

        if [[ "$status" != "completed" ]]; then
            echo "║  ❌ Phase $phase ($phase_name): 未完成"
            issues=$((issues + 1))
        fi

        # 检查输出文件
        if ! check_phase_outputs "$phase"; then
            echo "║  ⚠️  Phase $phase ($phase_name): 输出文件不完整"
            warnings=$((warnings + 1))
        fi
    done

    # 2. 检查操作越界
    local boundary_violations
    boundary_violations=$(check_boundary_violations "$plan_file")
    if [[ -n "$boundary_violations" ]]; then
        echo "║  ❌ 检测到操作越界:"
        echo "$boundary_violations" | while read -r violation; do
            echo "║     - $violation"
            issues=$((issues + 1))
        done
    fi

    # 3. 检查遗漏步骤
    local missed_steps
    missed_steps=$(check_missed_steps "$plan_file")
    if [[ -n "$missed_steps" ]]; then
        echo "║  ⚠️  检测到可能遗漏的步骤:"
        echo "$missed_steps" | while read -r step; do
            echo "║     - $step"
            warnings=$((warnings + 1))
        done
    fi

    echo "╠══════════════════════════════════════════╣"
    echo "║  问题: $issues  |  警告: $warnings"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    return $issues
}
```

#### F3.2 自动修正机制

```bash
# 自动修正执行问题
auto_fix_issues() {
    local issues_file="$1"

    local fixed=0
    local failed=0

    while IFS='|' read -r issue_type phase description; do
        echo "[auto-fix] 尝试修复: $description"

        case "$issue_type" in
            "missing_output")
                if regenerate_phase_output "$phase"; then
                    echo "[auto-fix] ✅ 修复成功: $description"
                    fixed=$((fixed + 1))
                else
                    echo "[auto-fix] ❌ 修复失败: $description"
                    failed=$((failed + 1))
                fi
                ;;
            "boundary_violation")
                if rollback_violation "$phase"; then
                    echo "[auto-fix] ✅ 已回滚越界操作: $description"
                    fixed=$((fixed + 1))
                else
                    echo "[auto-fix] ❌ 回滚失败: $description"
                    failed=$((failed + 1))
                fi
                ;;
            "missed_step")
                if execute_missed_step "$phase" "$description"; then
                    echo "[auto-fix] ✅ 已执行遗漏步骤: $description"
                    fixed=$((fixed + 1))
                else
                    echo "[auto-fix] ❌ 执行失败: $description"
                    failed=$((failed + 1))
                fi
                ;;
        esac
    done < "$issues_file"

    echo "[auto-fix] 修复完成: $fixed 成功, $failed 失败"
    return $failed
}
```

### 3.4 需求文档自动处理（F4）

#### F4.1 PRD 自动解析器

创建 `scripts/prd-parser.sh`：

```bash
# prd-parser.sh — PRD 文档自动解析器

parse_prd() {
    local prd_file="$1"

    echo "[prd-parser] 正在解析 PRD 文档: $prd_file"

    # 1. 检测文档格式
    local format
    format=$(detect_document_format "$prd_file")

    # 2. 提取结构化信息
    local parsed_data
    parsed_data=$(extract_structured_data "$prd_file" "$format")

    # 3. 生成变更描述
    local change_description
    change_description=$(generate_change_description "$parsed_data")

    # 4. 生成需求文档
    local requirement_file="$PROJECT_DIR/.specs/$(generate_change_id "$change_description")/REQUIREMENT.md"
    mkdir -p "$(dirname "$requirement_file")"
    echo "$parsed_data" > "$requirement_file"

    # 5. 自动初始化变更
    echo "[prd-parser] ✅ PRD 解析完成"
    echo "[prd-parser] 变更描述: $change_description"
    echo "[prd-parser] 需求文档: $requirement_file"
    echo ""
    echo "[prd-parser] 建议执行: /flow-kit:auto-start \"$change_description\""

    echo "$change_description"
}

# 检测文档格式
detect_document_format() {
    local file="$1"

    # 检查 Markdown 标题结构
    if grep -q "^# \|^## " "$file" 2>/dev/null; then
        # 检查是否有 YAML frontmatter
        if head -5 "$file" | grep -q "^---$"; then
            echo "markdown-with-frontmatter"
        else
            echo "markdown"
        fi
    elif head -1 "$file" | grep -q "^{"; then
        echo "json"
    else
        echo "plain-text"
    fi
}

# 提取结构化数据
extract_structured_data() {
    local file="$1"
    local format="$2"

    case "$format" in
        "markdown-with-frontmatter")
            # 提取 frontmatter 中的标题、描述、优先级等
            extract_frontmatter "$file"
            # 提取正文中的功能列表
            extract_feature_list "$file"
            # 提取验收标准
            extract_acceptance_criteria "$file"
            ;;
        "markdown")
            # 从标题结构推断
            infer_from_headings "$file"
            ;;
        "json")
            # 直接解析 JSON
            jq '.' "$file"
            ;;
        "plain-text")
            # 使用 Claude Code 分析
            analyze_with_claude "$file"
            ;;
    esac
}
```

#### F4.2 自动初始化流程

```bash
# auto-start.sh — 一键启动自动化流程

auto_start() {
    local input="${1:-}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 一键自动化启动              ║"
    echo "╠══════════════════════════════════════════╣"

    # 1. 检测输入类型
    if [[ -f "$input" ]]; then
        echo "║  输入类型: PRD 文档"
        # 解析 PRD
        local change_desc
        change_desc=$(parse_prd "$input")
    else
        echo "║  输入类型: 文本描述"
        local change_desc="$input"
    fi

    # 2. 环境清理
    echo "║  [1/5] 清理执行环境..."
    auto_cleanup

    # 3. 初始化变更
    echo "║  [2/5] 初始化变更..."
    bash "$SCRIPT_DIR/init-change.sh" "$change_desc"

    # 4. 生成执行计划
    echo "║  [3/5] 生成执行计划..."
    local plan_file
    plan_file=$(generate_execution_plan "$change_desc" "$(detect_project_type)")

    # 5. 展示计划并请求确认
    echo "║  [4/5] 展示执行计划..."
    show_execution_plan "$plan_file"

    echo "║  [5/5] 等待启动确认..."
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 请求确认
    request_confirmation "auto-start" "全流程自动化" \
        "是否启动 Phase 0→7 的全流程自动化执行？" \
        "critical" \
        "启动全流程自动化执行" \
        "逐阶段手动执行" \
        "先查看详细执行计划"

    # 根据选择执行
    case "$REPLY" in
        1|yes|y)
            auto_execute "$plan_file"
            ;;
        2|no|n)
            echo "已取消自动化执行，可手动执行 /flow-kit:phase-0"
            ;;
        3)
            show_detailed_plan "$plan_file"
            ;;
    esac
}
```

### 3.5 自我进化机制（F5）

#### F5.1 执行数据收集

创建 `scripts/evolution-engine.sh`：

```bash
# evolution-engine.sh — 自我进化引擎

# 收集执行数据
collect_execution_metrics() {
    local plan_file="$1"

    local metrics_file="$PROJECT_DIR/.flow-kit/evolution-metrics.json"

    # 收集各阶段耗时
    local phase_times="{}"
    for ((phase=0; phase<=7; phase++)); do
        local start_time
        start_time=$(jq -r ".phases[$phase].start_time // \"\"" "$plan_file")
        local end_time
        end_time=$(jq -r ".phases[$phase].end_time // \"\"" "$plan_file")

        if [[ -n "$start_time" && -n "$end_time" ]]; then
            local duration
            duration=$(calculate_duration "$start_time" "$end_time")
            phase_times=$(echo "$phase_times" | jq --argjson p "$phase" --argjson d "$duration" '. + {($p|tostring): $d}')
        fi
    done

    # 收集确认次数
    local confirm_count
    confirm_count=$(grep -c "\[confirm\]" "$PROJECT_DIR/.flow-kit/execution.log" 2>/dev/null || echo "0")

    # 收集越界事件
    local boundary_count
    boundary_count=$(grep -c "\[boundary\]" "$PROJECT_DIR/.flow-kit/execution.log" 2>/dev/null || echo "0")

    # 收集自动修复次数
    local fix_count
    fix_count=$(grep -c "\[auto-fix\]" "$PROJECT_DIR/.flow-kit/execution.log" 2>/dev/null || echo "0")

    # 保存指标
    jq -n \
        --argjson phase_times "$phase_times" \
        --argjson confirm_count "$confirm_count" \
        --argjson boundary_count "$boundary_count" \
        --argjson fix_count "$fix_count" \
        --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        '{
            timestamp: $ts,
            phase_times: $phase_times,
            total_time: ($phase_times | [.[]] | add // 0),
            confirm_count: $confirm_count,
            boundary_count: $boundary_count,
            fix_count: $fix_count,
            auto_ratio: (if (.confirm_count + .boundary_count + .fix_count) > 0 then
                (.fix_count / (.confirm_count + .boundary_count + .fix_count) * 100)
            else 0 end)
        }' > "$metrics_file"
}

# 生成优化建议
generate_optimization_suggestions() {
    local metrics_dir="$PROJECT_DIR/.flow-kit/evolution-metrics"

    # 分析历史趋势
    local total_time_trend
    total_time_trend=$(analyze_trend "$metrics_dir" "total_time")

    local boundary_trend
    boundary_trend=$(analyze_trend "$metrics_dir" "boundary_count")

    local auto_ratio_trend
    auto_ratio_trend=$(analyze_trend "$metrics_dir" "auto_ratio")

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 自我进化建议                ║"
    echo "╠══════════════════════════════════════════╣"

    # 基于趋势生成建议
    if [[ "$total_time_trend" == "increasing" ]]; then
        echo "║  ⚠️  执行时间呈上升趋势"
        echo "║  建议: 检查是否有新增的耗时操作"
        echo "║  优化: 考虑并行化或缓存优化"
    fi

    if [[ "$boundary_trend" == "increasing" ]]; then
        echo "║  ⚠️  越界事件呈上升趋势"
        echo "║  建议: 审查 phase-boundaries.yaml 配置"
        echo "║  优化: 增强边界校验规则"
    fi

    if [[ "$auto_ratio_trend" == "decreasing" ]]; then
        echo "║  ⚠️  自动修复成功率下降"
        echo "║  建议: 审查 auto_fix_issues 函数"
        echo "║  优化: 添加新的修复策略"
    fi

    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 生成优化配置文件
    generate_optimization_config
}
```

#### F5.2 工作流自动优化

借鉴 superpowers-zh 的 workflow-runner 和 agency-agents-zh 的 DAG 编排思想，实现工作流的持续优化：

```yaml
# .flow-kit/evolution-config.yaml — 进化配置
evolution:
  enabled: true
  learning_interval: "7d"  # 每 7 天分析一次
  metrics:
    - phase_execution_time
    - confirmation_count
    - boundary_violations
    - auto_fix_success_rate
    - user_satisfaction_score

  optimization_rules:
    - condition: "phase_execution_time > 5min"
      action: "建议拆分 phase 或启用并行执行"
    - condition: "confirmation_count > 10"
      action: "建议提高 auto_confirm_threshold"
    - condition: "boundary_violations > 3"
      action: "建议更新 phase-boundaries.yaml"
    - condition: "auto_fix_success_rate < 50%"
      action: "建议审查 auto_fix_issues 函数"

  auto_adjustments:
    - metric: "auto_confirm_threshold"
      rule: "如果连续 3 次执行无越界事件，自动提升确认阈值"
    - metric: "parallel_level"
      rule: "如果 phase 执行时间 > 3min，自动启用并行执行"
    - metric: "confirmation_skip"
      rule: "如果某操作连续 5 次选择相同选项，自动跳过确认"
```

### 3.6 环境自动清理机制（F6）

#### F6.1 自动清理触发器

创建 `scripts/auto-cleanup.sh`：

```bash
# auto-cleanup.sh — 环境自动清理器

# 自动清理入口
auto_cleanup() {
    local mode="${1:-auto}"  # auto | full | minimal

    echo "[cleanup] 开始清理执行环境..."

    # 1. 保存当前会话状态
    local session_backup
    session_backup=$(backup_session_state)

    # 2. 执行 /clear 命令
    if command -v clear &>/dev/null; then
        echo "[cleanup] 执行环境重置..."
        # 通过 Claude Code 的 /clear 机制重置
        clear_session_context
    fi

    # 3. 清理临时文件
    cleanup_temp_files "$mode"

    # 4. 重置执行状态（保留 session-state）
    reset_execution_state

    # 5. 恢复必要的会话上下文
    restore_session_context "$session_backup"

    echo "[cleanup] ✅ 环境清理完成"
    echo "[cleanup] 状态: session-state 已保留，临时文件已清理，上下文已重置"
}

# 备份会话状态
backup_session_state() {
    local backup_file="$PROJECT_DIR/.flow-kit/session-backup.json"

    if [[ -f "$SESSION_STATE_FILE" ]]; then
        cp "$SESSION_STATE_FILE" "$backup_file"
        echo "$backup_file"
    fi
}

# 清理临时文件
cleanup_temp_files() {
    local mode="$1"

    case "$mode" in
        full)
            # 完全清理：删除所有临时文件
            rm -rf "$TMP_DIR"/* 2>/dev/null || true
            rm -rf "$PROJECT_DIR/.flow-kit/tmp"/* 2>/dev/null || true
            ;;
        auto)
            # 自动清理：仅清理非必要的临时文件
            find "$TMP_DIR" -name "*.tmp" -delete 2>/dev/null || true
            find "$TMP_DIR" -name "*.log" -mtime +1 -delete 2>/dev/null || true
            ;;
        minimal)
            # 最小清理：仅清理锁文件
            rm -f "$TMP_DIR"/*.lock 2>/dev/null || true
            ;;
    esac
}

# 重置执行状态
reset_execution_state() {
    # 保留 session-state.json，但重置运行时状态
    session_set status "ready"
    session_set next ""
    session_set blockers ""
}

# 恢复会话上下文
restore_session_context() {
    local backup_file="$1"

    if [[ -f "$backup_file" ]]; then
        # 恢复关键上下文信息
        local change_id
        change_id=$(jq -r '.change // ""' "$backup_file")
        local phase
        phase=$(jq -r '.phase // "0"' "$backup_file")

        if [[ -n "$change_id" ]]; then
            session_set change "$change_id"
            session_set phase "$phase"
        fi

        # 清理备份
        rm -f "$backup_file"
    fi
}
```

#### F6.2 命令执行前自动触发

在 `hooks/pre-tool-guard.sh` 中添加自动清理逻辑：

```bash
# 在 pre-tool-guard.sh 中添加
auto_cleanup_before_command() {
    local command="$1"

    # 检测到 phase 命令时自动触发清理
    if [[ "$command" == *"phase-"* ]] || [[ "$command" == *"auto-start"* ]]; then
        # 检查是否需要清理
        local last_cleanup
        last_cleanup=$(session_get last_cleanup 2>/dev/null || echo "")

        if [[ -z "$last_cleanup" ]] || [[ $(is_older_than "$last_cleanup" "5min") == "true" ]]; then
            echo "[pre-tool] 检测到 phase 命令，自动清理环境..."
            bash "$SCRIPT_DIR/auto-cleanup.sh" auto
            session_set last_cleanup "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        fi
    fi
}
```

### 3.7 持续任务执行系统（F7）

借鉴 [mattpocock/skills](https://github.com/mattpocock/skills) 的自主运行方法论、[Ralph.md](y:\pwl\code\plugins\Ralph.md) 记录的 Ralph Wiggum 循环模式，以及 superpowers+gstack 开发流程实录中的任务分流机制，构建持续运行、自动完成任务的执行系统。

#### F7.1 自主运行模式设计

实现三种自主运行模式，平衡安全性与自动化程度：

```yaml
# .flow-kit/auto-runner.yaml — 自主运行配置
auto_runner:
  modes:
    supervised:  # 模式 1：监督模式（默认，最安全）
      permission_mode: "auto"
      allowed_tools:
        - "Read"
        - "Edit"
        - "Bash(npm run *)"
        - "Bash(git *)"
      max_turns: 50
      max_budget_usd: 10.00
      confirmation_level: "critical"
      description: "安全操作自动批准，高风险操作需人工确认"
      use_case: "日常开发，需要安全护栏"

    semi_autonomous:  # 模式 2：半自主模式
      permission_mode: "auto"
      allowed_tools:
        - "Read"
        - "Edit"
        - "Bash(*)"
        - "WebSearch"
      max_turns: 100
      max_budget_usd: 25.00
      confirmation_level: "important"
      description: "大部分操作自动批准，仅关键操作需确认"
      use_case: "夜间运行，少量人工干预"

    full_autonomous:  # 模式 3：完全自主模式（仅容器环境）
      permission_mode: "dangerously-skip-permissions"
      allowed_tools:
        - "Read"
        - "Edit"
        - "Bash(*)"
        - "WebSearch"
        - "WebFetch"
      max_turns: 200
      max_budget_usd: 50.00
      confirmation_level: "none"
      description: "完全自主执行，无人工干预"
      use_case: "容器环境，已验证的安全沙箱"
      constraints:
        container_only: true
        network_isolation: true
        pre_flight_checklist:
          - "git 检查点已创建"
          - "容器已启动"
          - "PROMPT.md 已编写"
          - "四个 Hook 已安装"
```

**借鉴来源**：
- Ralph.md 第 1 节"消除人工干预的三种模式"：`-p` 标志、`--permission-mode auto`、`--dangerously-skip-permissions`
- Ralph.md 第 2 节"Ralph Wiggum 循环"：while 循环 + PROMPT.md 的持续执行模式
- Ralph.md 第 9 节"快速启动清单"：过夜运行的完整步骤

#### F7.2 Ralph Wiggum 循环集成

创建 `scripts/ralph-runner.sh`，实现 Ralph Wiggum 循环模式的自动化：

```bash
# ralph-runner.sh — Ralph Wiggum 循环执行器

# 启动 Ralph 循环
start_ralph_loop() {
    local mode="${1:-supervised}"
    local prompt_file="${2:-$PROJECT_DIR/PROMPT.md}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit Ralph Wiggum 循环           ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  模式: $mode"
    echo "║  Prompt: $prompt_file"
    echo "║  最大迭代: ${MAX_ITERATIONS:-100}"
    echo "║  预算上限: \$${MAX_BUDGET:-50.00}"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 1. 安全检查
    if [[ "$mode" == "full_autonomous" ]]; then
        if ! is_running_in_container; then
            echo "[ralph] ❌ 完全自主模式仅限容器环境"
            echo "[ralph] 请使用 Docker 或 VM 运行"
            return 1
        fi
    fi

    # 2. 创建 git 检查点
    git add -A && git commit -m "pre-autonomous checkpoint" 2>/dev/null || true

    # 3. 安装四个关键 Hook
    install_safety_hooks

    # 4. 启动循环
    local iteration=0
    local stuck_count=0

    while [[ $iteration -lt ${MAX_ITERATIONS:-100} ]]; do
        iteration=$((iteration + 1))

        echo ""
        echo "────────────────────────────────────────"
        echo "  迭代 #$iteration"
        echo "────────────────────────────────────────"
        echo ""

        # 构建 Claude Code 命令
        local claude_cmd="claude -p \"$(cat "$prompt_file")\""

        case "$mode" in
            supervised)
                claude_cmd="$claude_cmd --permission-mode auto"
                claude_cmd="$claude_cmd --allowedTools \"Read\" \"Edit\" \"Bash(npm run *)\" \"Bash(git *)\""
                ;;
            semi_autonomous)
                claude_cmd="$claude_cmd --permission-mode auto"
                claude_cmd="$claude_cmd --allowedTools \"Read\" \"Edit\" \"Bash(*)\" \"WebSearch\""
                ;;
            full_autonomous)
                claude_cmd="$claude_cmd --dangerously-skip-permissions"
                ;;
        esac

        claude_cmd="$claude_cmd --max-turns ${MAX_TURNS:-30}"
        claude_cmd="$claude_cmd --max-budget-usd ${MAX_BUDGET:-5.00}"

        # 执行
        echo "[ralph] 执行迭代 #$iteration..."
        eval "$claude_cmd" 2>&1 | tee -a "$LOG_FILE"

        # 检查是否卡住
        if check_stuck "$iteration"; then
            stuck_count=$((stuck_count + 1))
            echo "[ralph] ⚠️  检测到可能卡住 ($stuck_count/3)"

            if [[ $stuck_count -ge 3 ]]; then
                echo "[ralph] ❌ 连续卡住 3 次，执行异常处理"
                handle_stuck "$iteration"
                stuck_count=0
            fi
        else
            stuck_count=0
        fi

        # 检查是否完成
        if grep -q "<promise>DONE</promise>" "$LOG_FILE" 2>/dev/null; then
            echo "[ralph] ✅ 任务完成！"
            break
        fi

        # 环境清理
        auto_cleanup auto

        sleep "${LOOP_INTERVAL:-2}"
    done

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     Ralph 循环执行完成                    ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  总迭代: $iteration"
    echo "║  日志: $LOG_FILE"
    echo "╚══════════════════════════════════════════╝"
}
```

**借鉴来源**：
- Ralph.md 第 2 节：Ralph Wiggum 循环的 while true 模式和 PROMPT.md 设计
- Ralph.md 第 3 节：四个关键 Hook（No-Ask-Human, Context Monitor, Syntax Check, Decision Warn）
- Ralph.md 第 4 节：tmux 会话持久化设置
- Ralph.md 第 5 节：CLAUDE.md 上下文管理和 token 节省策略

#### F7.3 四个安全 Hook 集成

创建 `scripts/safety-hooks.sh`，集成 Ralph.md 记录的四个关键 Hook：

```bash
# safety-hooks.sh — 安全 Hook 安装器

install_safety_hooks() {
    echo "[hooks] 安装四个安全 Hook..."

    # Hook 1: No-Ask-Human — 阻止 AskUserQuestion 工具调用
    install_no_ask_human() {
        cat > "$HOOKS_DIR/pre-tool-guard.sh" << 'HOOK'
# No-Ask-Human Hook: 阻止 AskUserQuestion 工具调用
block_ask_human() {
    local tool="$1"
    if [[ "$tool" == "AskUserQuestion" ]]; then
        if [[ "${CC_ALLOW_QUESTIONS:-0}" != "1" ]]; then
            echo "[hook] ❌ AskUserQuestion 被 No-Ask-Human Hook 阻止"
            echo "[hook] 设置 CC_ALLOW_QUESTIONS=1 以允许提问"
            return 1
        fi
    fi
    return 0
}
HOOK
    }

    # Hook 2: Context Monitor — 监控上下文使用量
    install_context_monitor() {
        cat > "$HOOKS_DIR/context-monitor.sh" << 'HOOK'
# Context Monitor Hook: 监控上下文使用量
CONTEXT_THRESHOLDS=(40 25 20 15)

check_context_usage() {
    local tool_calls="$1"
    local max_calls="${2:-200}"

    local usage_pct=$((tool_calls * 100 / max_calls))

    for threshold in "${CONTEXT_THRESHOLDS[@]}"; do
        if [[ $usage_pct -ge $((100 - threshold)) ]]; then
            echo "[hook] ⚠️  上下文使用量: ${usage_pct}% (剩余 ${threshold}%)"
            if [[ $usage_pct -ge 85 ]]; then
                echo "[hook] 🔄 建议执行 /compact 释放上下文"
            fi
            break
        fi
    done
}
HOOK
    }

    # Hook 3: Syntax Check — 文件编辑后立即语法检查
    install_syntax_check() {
        cat > "$HOOKS_DIR/syntax-check.sh" << 'HOOK'
# Syntax Check Hook: 编辑后自动语法检查
run_syntax_check() {
    local file="$1"

    case "$file" in
        *.js|*.ts|*.jsx|*.tsx)
            node --check "$file" 2>/dev/null && echo "[hook] ✅ JS/TS 语法检查通过" || echo "[hook] ⚠️  JS/TS 语法检查失败"
            ;;
        *.py)
            python -m py_compile "$file" 2>/dev/null && echo "[hook] ✅ Python 语法检查通过" || echo "[hook] ⚠️  Python 语法检查失败"
            ;;
        *.sh)
            bash -n "$file" 2>/dev/null && echo "[hook] ✅ Shell 语法检查通过" || echo "[hook] ⚠️  Shell 语法检查失败"
            ;;
        *.json)
            node -e "JSON.parse(require('fs').readFileSync('$file','utf8'))" 2>/dev/null && echo "[hook] ✅ JSON 语法检查通过" || echo "[hook] ⚠️  JSON 语法检查失败"
            ;;
    esac
}
HOOK
    }

    # Hook 4: Decision Warn — 破坏性命令警告
    install_decision_warn() {
        cat > "$HOOKS_DIR/decision-warn.sh" << 'HOOK'
# Decision Warn Hook: 破坏性命令警告
PROTECTED_BRANCHES="${CC_PROTECT_BRANCHES:-main:master:production}"
DANGEROUS_COMMANDS=("rm -rf" "git reset --hard" "DROP TABLE" "git push --force" "kubectl delete")

check_dangerous_command() {
    local cmd="$1"

    for dangerous in "${DANGEROUS_COMMANDS[@]}"; do
        if echo "$cmd" | grep -qi "$dangerous"; then
            echo "[hook] ⚠️  检测到危险命令: $cmd"
            echo "[hook] 建议: 使用 /careful <cmd> 执行"
            return 1
        fi
    done

    # 检查受保护分支
    if echo "$cmd" | grep -qiE "git push.*(main|master|production)"; then
        echo "[hook] ⚠️  禁止推送到受保护分支"
        echo "[hook] 受保护分支: $PROTECTED_BRANCHES"
        return 1
    fi

    return 0
}
HOOK
    }

    install_no_ask_human
    install_context_monitor
    install_syntax_check
    install_decision_warn

    echo "[hooks] ✅ 四个安全 Hook 安装完成"
    echo "[hooks] - No-Ask-Human: 阻止不必要的提问"
    echo "[hooks] - Context Monitor: 监控上下文使用量"
    echo "[hooks] - Syntax Check: 编辑后自动语法检查"
    echo "[hooks] - Decision Warn: 破坏性命令警告"
}
```

**借鉴来源**：
- Ralph.md 第 3 节"防止过夜灾难的四个 Hook"：完整 Hook 实现逻辑
- Ralph.md 第 8 节"已知陷阱"：8 种故障模式的预防策略
- Ralph.md 第 5 节"CLAUDE.md 与长时间运行的上下文管理"：token 节省策略

#### F7.4 通用任务模板系统

创建 `scripts/task-runner.sh`，实现标准化任务模板：

```bash
# task-runner.sh — 通用任务执行器

# 任务模板
TASK_TEMPLATE='任务：[任务描述]

要求：
- [要求 1]
- [要求 2]
- [要求 3]

验证步骤：
1. 运行测试：[测试命令]
2. 检查覆盖率：[覆盖率命令]
3. 运行 linter：[lint 命令]

完成标准：
- 所有测试通过
- 覆盖率 >= [目标]%
- 无 linter 错误

完成时输出：<promise>DONE</promise>

异常处理机制：
如果系统在执行过程中连续卡住超过 [N] 次迭代：
- 自动输出当前任务进度详情
- 列出所有遇到的具体问题
- 提供基于历史数据的解决建议
'

# 执行任务
execute_task() {
    local task_file="$1"
    local mode="${2:-supervised}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 任务执行器                  ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  任务: $(head -1 "$task_file")"
    echo "║  模式: $mode"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 1. 解析任务模板
    local requirements
    requirements=$(parse_requirements "$task_file")

    local verification_steps
    verification_steps=$(parse_verification_steps "$task_file")

    local completion_criteria
    completion_criteria=$(parse_completion_criteria "$task_file")

    # 2. 生成 PROMPT.md
    generate_task_prompt "$task_file" "$PROJECT_DIR/PROMPT.md"

    # 3. 启动 Ralph 循环
    start_ralph_loop "$mode" "$PROJECT_DIR/PROMPT.md"

    # 4. 验证完成标准
    if verify_completion "$completion_criteria"; then
        echo "<promise>DONE</promise>"
        return 0
    else
        echo "[task-runner] ❌ 任务未完全满足完成标准"
        return 1
    fi
}

# 解析验证步骤
parse_verification_steps() {
    local file="$1"
    local steps=""

    while IFS= read -r line; do
        if [[ "$line" =~ ^[0-9]+\.\ 运行测试|^[0-9]+\.\ 检查覆盖率|^[0-9]+\.\ 运行 ]]; then
            steps="$steps$line"$'\n'
        fi
    done < "$file"

    echo "$steps"
}

# 生成任务 Prompt
generate_task_prompt() {
    local task_file="$1"
    local prompt_file="$2"

    cat > "$prompt_file" << PROMPT
# 任务：$(head -1 "$task_file")

## 上下文
- 项目: flow-kit
- 工作目录: $PROJECT_DIR
- 当前状态: $(git log --oneline -3 2>/dev/null || echo "无提交历史")

## 目标
- 查看 TODO.md，选择下一个未完成的任务
- 实现它，包含完善的错误处理
- 运行测试，修复失败，确认没有回归
- 做通用修复，不要打临时补丁
- 每完成一个任务后用描述性消息提交

## 要求
$(cat "$task_file" | sed -n '/^要求：/,/^验证步骤：/p' | head -n -1)

## 验证
$(cat "$task_file" | sed -n '/^验证步骤：/,/^完成标准：/p' | head -n -1)

## 完成标准
$(cat "$task_file" | sed -n '/^完成标准：/,/^完成时输出：/p' | head -n -1)

## 自主运行规则
- 技术方案不确定 → 选择传统方案
- 两种可行实现 → 选择更简单的那个
- 需求模糊 → 应用最合理的理解，记录假设
- 尝试 3 次后仍有错误 → 记录到 blocked.md，切换任务
- 永远不要提问。做出最佳判断然后继续。

## 完成时
- 输出 <promise>DONE</promise>
- 提交所有更改
- 更新 TODO.md 标记已完成
PROMPT

    echo "[task-runner] ✅ PROMPT.md 已生成: $prompt_file"
}
```

**借鉴来源**：
- mattpocock/skills 的 `/tdd` skill：红-绿-重构循环和测试驱动开发
- mattpocock/skills 的 `/diagnose` skill：系统化诊断循环
- mattpocock/skills 的 `/grill-me` skill：需求澄清 interview 模式
- Ralph.md 第 2 节：PROMPT.md 设计和"完成"标准
- 用户提供的任务模板格式

#### F7.5 异常处理与审计机制

```bash
# 异常处理：连续卡住检测
check_stuck() {
    local iteration="$1"
    local log_file="$LOG_FILE"

    # 检查最近 3 次迭代的输出是否相似
    if [[ $iteration -ge 3 ]]; then
        local recent_logs
        recent_logs=$(tail -100 "$log_file" 2>/dev/null)

        # 检测重复错误模式
        if echo "$recent_logs" | grep -c "error\|Error\|ERROR" | head -1 | grep -q "[3-9]"; then
            return 0  # 卡住了
        fi

        # 检测无进展模式
        if echo "$recent_logs" | grep -q "same\|again\|retry\|still"; then
            return 0  # 卡住了
        fi
    fi

    return 1  # 正常运行
}

# 异常处理：卡住时输出诊断信息
handle_stuck() {
    local iteration="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     异常处理：任务卡住诊断               ║"
    echo "╠══════════════════════════════════════════╣"

    # 1. 输出当前任务进度
    echo "║  当前迭代: #$iteration"
    echo "║  执行时间: $(calculate_elapsed_time)"
    echo "║  日志文件: $LOG_FILE"
    echo "╠══════════════════════════════════════════╣"

    # 2. 列出遇到的具体问题
    echo "║  检测到的问题:"
    local issues
    issues=$(extract_issues_from_log "$LOG_FILE")
    echo "$issues" | while read -r issue; do
        echo "║  - $issue"
    done

    echo "╠══════════════════════════════════════════╣"

    # 3. 提供解决建议
    echo "║  建议方案:"
    echo "║  1. 切换任务（记录当前进度到 blocked.md）"
    echo "║  2. 降低复杂度（拆分当前任务）"
    echo "║  3. 请求人工介入（保留当前状态）"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 4. 自动切换任务
    local next_task
    next_task=$(find_next_uncompleted_task "$PROJECT_DIR/TODO.md")
    if [[ -n "$next_task" ]]; then
        echo "[ralph] 自动切换到下一个任务: $next_task"
        update_task_status "$next_task" "blocked"
        generate_task_prompt "$next_task" "$PROJECT_DIR/PROMPT.md"
    fi
}

# 审计日志
audit_task_execution() {
    local task_file="$1"
    local log_file="$LOG_FILE"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 任务执行审计                ║"
    echo "╠══════════════════════════════════════════╣"

    # 1. 检查完成标准
    local criteria
    criteria=$(parse_completion_criteria "$task_file")
    local all_met=true

    while IFS= read -r criterion; do
        if verify_criterion "$criterion" "$log_file"; then
            echo "║  ✅ $criterion"
        else
            echo "║  ❌ $criterion"
            all_met=false
        fi
    done <<< "$criteria"

    echo "╠══════════════════════════════════════════╣"

    # 2. 检查异常事件
    local error_count
    error_count=$(grep -c "\[ralph\]" "$log_file" 2>/dev/null || echo "0")
    local stuck_count
    stuck_count=$(grep -c "卡住" "$log_file" 2>/dev/null || echo "0")

    echo "║  总迭代: $(grep -c '迭代 #' "$log_file" 2>/dev/null || echo "0")"
    echo "║  异常事件: $stuck_count"
    echo "║  错误次数: $error_count"

    if [[ "$all_met" == true ]]; then
        echo "║  结果: ✅ 所有完成标准已满足"
    else
        echo "║  结果: ⚠️  部分完成标准未满足"
    fi

    echo "╚══════════════════════════════════════════╝"
    echo ""

    return $all_met
}
```

**借鉴来源**：
- mattpocock/skills 的 `/diagnose` skill：系统化诊断循环（reproduce → minimise → hypothesise → instrument → fix → regression-test）
- mattpocock/skills 的 `/handoff` skill：会话交接文档生成
- Ralph.md 第 8 节"已知陷阱"：8 种故障模式的识别和处理
- 用户提供的异常处理机制模板

#### F7.6 与现有 Phase 系统的集成

```yaml
# .flow-kit/integration-config.yaml — 持续执行系统与 Phase 系统集成
integration:
  # 触发方式 1：从 Phase 系统启动 Ralph 循环
  phase_to_ralph:
    trigger: "Phase 4（开发执行）完成后自动启动 Ralph 循环"
    condition: "auto_pilot_mode == supervised && tasks_remaining > 3"
    action: "start_ralph_loop supervised"

  # 触发方式 2：Ralph 循环完成后回归 Phase 系统
  ralph_to_phase:
    trigger: "Ralph 循环检测到 <promise>DONE</promise>"
    action: "auto_pilot.sh resume_from phase-5"
    description: "Ralph 循环完成开发执行后，回归 Phase 5 测试验证"

  # 触发方式 3：PRD 自动解析 → Ralph 循环
  prd_to_ralph:
    trigger: "PRD 文档解析完成"
    action: "prd-parser.sh → auto-start.sh → ralph-runner.sh"
    description: "PRD 文档自动解析后直接启动 Ralph 循环"

  # 触发方式 4：定时任务（cron 模式）
  cron_to_ralph:
    trigger: "cron 定时触发"
    action: "ralph-runner.sh semi_autonomous"
    description: "定时启动 Ralph 循环执行预定义任务"
    cron_syntax: "0 * * * * /path/to/ralph-runner.sh"
```

**借鉴来源**：
- Ralph.md 第 7 节"CI/CD 流水线与 Cron 任务集成"：Boucle 模式和 cron 触发
- superpowers+gstack 开发流程实录第 4 节"开发闭环"：接力赛模式的交接点
- superpowers+gstack 开发流程实录第 3 节"CLAUDE.md 配置"：任务分流规则

---

## 4. 用户故事

### 4.1 Phase 边界重构相关

| ID | 用户故事 | 优先级 | 迭代 |
|----|----------|--------|------|
| US-01 | 作为开发者，我希望每个 phase 命令只做它该做的事，不越界执行其他 phase 的操作，这样我才能控制开发节奏 | P0 | 1 |
| US-02 | 作为技术负责人，我希望 phase 命令在执行关键操作前请求我的确认，并提供推荐选项，这样我可以在关键节点把关 | P0 | 1 |
| US-03 | 作为开发者，我希望随时查看当前执行状态和下一步计划，这样我能了解开发进度 | P0 | 1 |
| US-04 | 作为技术负责人，我希望看到每个 phase 的职责边界定义，这样我能理解每个命令的预期行为 | P1 | 1 |

### 4.2 自动化执行相关

| ID | 用户故事 | 优先级 | 迭代 |
|----|----------|--------|------|
| US-05 | 作为开发者，我希望一条命令就能启动从 Phase 0 到 Phase 7 的全流程自动执行，这样我就不用手动逐个执行 | P0 | 2 |
| US-06 | 作为开发者，我希望在需要确认的环节看到智能推荐选项，这样我能快速做出决策 | P0 | 2 |
| US-07 | 作为产品经理，我希望直接提交 PRD 文档就能自动解析并启动开发流程，这样我不用手动编写需求文档 | P1 | 2 |
| US-08 | 作为开发者，我希望系统能自动识别执行过程中的错误或遗漏，并尝试自动修复，这样能减少人工干预 | P1 | 2 |

### 4.3 审计与进化相关

| ID | 用户故事 | 优先级 | 迭代 |
|----|----------|--------|------|
| US-09 | 作为技术负责人，我希望每次执行后都能看到完整的审计报告，包括越界事件、遗漏步骤和修复记录 | P1 | 3 |
| US-10 | 作为技术负责人，我希望系统能根据历史执行数据自动优化工作流配置，这样开发流程会越来越高效 | P2 | 3 |
| US-11 | 作为开发者，我希望在执行新命令前环境能自动清理，这样不会受到之前执行状态的干扰 | P1 | 1 |
| US-12 | 作为技术负责人，我希望看到系统自我进化的建议和已应用的优化，这样我能了解系统的改进方向 | P2 | 3 |

### 4.4 持续任务执行相关

| ID | 用户故事 | 优先级 | 迭代 |
|----|----------|--------|------|
| US-13 | 作为开发者，我希望系统能在夜间无人值守时自动执行开发任务，这样我早上就能看到完成的功能 | P0 | 2 |
| US-14 | 作为开发者，我希望提交标准格式的任务模板后系统能自动解析并执行，这样我不需要手动拆解任务 | P0 | 2 |
| US-15 | 作为技术负责人，我希望系统在卡住时能自动诊断并切换任务，这样不会浪费夜间运行时间 | P1 | 2 |
| US-16 | 作为开发者，我希望系统在执行前自动安装安全 Hook，这样能防止破坏性操作 | P1 | 2 |
| US-17 | 作为技术负责人，我希望系统支持三种自主运行模式（监督/半自主/完全自主），这样我能根据场景选择合适的安全级别 | P1 | 2 |
| US-18 | 作为开发者，我希望系统能通过 cron 定时触发任务执行，这样我能设置定期维护任务 | P2 | 3 |
| US-19 | 作为开发者，我希望 Ralph 循环完成后能自动回归 Phase 系统继续执行，这样流程不会中断 | P1 | 2 |
| US-20 | 作为技术负责人，我希望每次 Ralph 循环执行后都能看到完整的审计报告，包括迭代次数、卡住事件和完成标准满足情况 | P1 | 3 |

---

## 5. 功能点优先级划分

### 5.1 优先级定义

| 级别 | 定义 | 处理方式 |
|------|------|----------|
| P0 | 必须实现，阻塞其他功能 | 迭代 1 完成 |
| P1 | 重要功能，核心体验 | 迭代 2 完成 |
| P2 | 增强功能，锦上添花 | 迭代 3 完成 |

### 5.2 功能点列表

| ID | 功能点 | 优先级 | 迭代 | 预估人天 | 依赖 |
|----|--------|--------|------|----------|------|
| F1.1 | Phase 职责边界定义文件 | P0 | 1 | 2 | 无 |
| F1.2 | 边界校验器 (boundary-enforcer.sh) | P0 | 1 | 3 | F1.1 |
| F1.3 | 操作确认管理器 (confirmation-manager.sh) | P0 | 1 | 3 | 无 |
| F1.4 | 状态显示面板 (status-display.sh) | P0 | 1 | 2 | 无 |
| F1.5 | 更新 phase-executor.sh 集成边界校验 | P0 | 1 | 2 | F1.2 |
| F1.6 | 更新所有 phase 工作流文件添加边界声明 | P1 | 1 | 3 | F1.1 |
| F2.1 | 自动化执行引擎 (auto-pilot.sh) | P0 | 2 | 5 | F1.3, F1.4 |
| F2.2 | 智能决策引擎 (decision-engine.sh) | P0 | 2 | 4 | F1.3 |
| F2.3 | 执行计划生成器 | P1 | 2 | 3 | F2.1 |
| F2.4 | 阶段间状态传递机制 | P1 | 2 | 2 | F2.1 |
| F3.1 | 执行审计器 (execution-auditor.sh) | P1 | 2 | 3 | F2.1 |
| F3.2 | 自动修正机制 (auto-fix) | P1 | 2 | 4 | F3.1 |
| F3.3 | 二次运行修正能力 | P2 | 3 | 3 | F3.2 |
| F4.1 | PRD 自动解析器 (prd-parser.sh) | P1 | 2 | 4 | 无 |
| F4.2 | 一键启动命令 (auto-start.sh) | P1 | 2 | 3 | F2.1, F4.1 |
| F5.1 | 执行数据收集器 | P2 | 3 | 3 | F2.1 |
| F5.2 | 自我进化引擎 (evolution-engine.sh) | P2 | 3 | 5 | F5.1 |
| F5.3 | 工作流自动优化配置 | P2 | 3 | 2 | F5.2 |
| F6.1 | 环境自动清理器 (auto-cleanup.sh) | P1 | 1 | 3 | 无 |
| F6.2 | pre-tool-guard 集成自动清理 | P1 | 1 | 1 | F6.1 |
| F6.3 | 会话状态备份与恢复 | P1 | 1 | 2 | F6.1 |
| F7.1 | 自主运行模式配置 (auto-runner.yaml) | P0 | 2 | 2 | 无 |
| F7.2 | Ralph Wiggum 循环执行器 (ralph-runner.sh) | P0 | 2 | 5 | F7.1 |
| F7.3 | 四个安全 Hook 安装器 (safety-hooks.sh) | P1 | 2 | 3 | 无 |
| F7.4 | 通用任务模板执行器 (task-runner.sh) | P0 | 2 | 4 | F7.2 |
| F7.5 | 异常处理与卡住诊断机制 | P1 | 2 | 3 | F7.2 |
| F7.6 | 与 Phase 系统的集成配置 | P1 | 2 | 2 | F2.1, F7.2 |
| F7.7 | cron 定时触发集成 | P2 | 3 | 2 | F7.2 |
| F7.8 | 任务执行审计报告 | P1 | 3 | 2 | F7.5 |

### 5.3 迭代分配

| 迭代 | 时间 | 功能点 | 人天 | 交付物 |
|------|------|--------|------|--------|
| 迭代 1 | 第 1-3 周 | F1.1-F1.6, F6.1-F6.3 | 21 | Phase 边界重构 + 环境清理 |
| 迭代 2 | 第 4-8 周 | F2.1-F2.4, F3.1-F3.2, F4.1-F4.2, F7.1-F7.6 | 42 | 自动化执行 + 审计 + PRD 解析 + 持续任务执行系统 |
| 迭代 3 | 第 9-12 周 | F3.3, F5.1-F5.3, F7.7-F7.8 | 17 | 自我进化机制 + 定时任务 |

---

## 6. 界面交互设计规范

### 6.1 命令行交互规范

#### 6.1.1 状态面板格式

```
╔══════════════════════════════════════════╗
║         flow-kit 执行状态面板            ║
╠══════════════════════════════════════════╣
║  ✅  Phase 0: 变更立项     已完成       ║
║  ✅  Phase 1: 需求澄清     已完成       ║
║  🔄  Phase 2: 架构设计     执行中       ║
║  ⬜  Phase 3: 任务拆解     待执行       ║
║  ⬜  Phase 4: 开发执行     待执行       ║
║  ⬜  Phase 5: 测试验证     待执行       ║
║  ⬜  Phase 6: 代码审查     待执行       ║
║  ⬜  Phase 7: 集成归档     待执行       ║
╠══════════════════════════════════════════╣
║  当前操作: 技术方案设计                   ║
║  完成进度: 2/5 步骤                      ║
║  下一步: 接口定义                        ║
╚══════════════════════════════════════════╝
```

#### 6.1.2 确认对话框格式

```
==========================================
[confirm] Phase: phase-2
[confirm] 操作: 技术方案设计
[confirm] 描述: 基于需求文档设计系统架构方案
[confirm] 级别: important (建议人工确认)
==========================================

推荐选项:
  1. 自动生成架构方案（基于需求分析）
  2. 选择架构模板（微服务/单体/事件驱动）
  3. 手动设计架构（适用于复杂场景）

请输入确认 (yes/no/auto) 或选择选项编号:
```

#### 6.1.3 审计报告格式

```
╔══════════════════════════════════════════╗
║         flow-kit 执行审计报告            ║
╠══════════════════════════════════════════╣
║  ✅ Phase 0: 变更立项     通过          ║
║  ✅ Phase 1: 需求澄清     通过          ║
║  ⚠️  Phase 2: 架构设计     警告         ║
║     - 输出文件 DESIGN.md 不完整          ║
║  ✅ Phase 3: 任务拆解     通过          ║
║  ✅ Phase 4: 开发执行     通过          ║
║  ❌ Phase 5: 测试验证     失败          ║
║     - 测试覆盖率未达到 80% 阈值          ║
║  -   Phase 6: 代码审查     未执行       ║
║  -   Phase 7: 集成归档     未执行       ║
╠══════════════════════════════════════════╣
║  问题: 1  |  警告: 1  |  已自动修复: 0  ║
╚══════════════════════════════════════════╝

是否尝试自动修复？(yes/no):
```

### 6.2 命令接口规范

#### 6.2.1 新增命令

| 命令 | 参数 | 说明 | 权限 |
|------|------|------|------|
| `/flow-kit:auto-start` | `"变更描述"` 或 `PRD文件路径` | 一键启动全流程自动化 | 开发者 |
| `/flow-kit:auto-pilot` | `[interactive/silent/supervised]` | 启动/切换自动化模式 | 开发者 |
| `/flow-kit:status` | `[--detail]` | 查看执行状态面板 | 所有人 |
| `/flow-kit:audit` | `[--fix]` | 执行审计并可选自动修复 | 技术负责人 |
| `/flow-kit:boundary` | `[--check]` | 查看/检查边界定义 | 开发者 |
| `/flow-kit:evolution` | `[--suggest]` | 查看进化建议 | 技术负责人 |
| `/flow-kit:cleanup` | `[auto/full/minimal]` | 手动触发环境清理 | 开发者 |

#### 6.2.2 命令别名

| 别名 | 映射命令 | 说明 |
|------|----------|------|
| `/flow-kit:go` | `/flow-kit:auto-start` | 快速启动 |
| `/flow-kit:plan` | `/flow-kit:auto-pilot --show-plan` | 查看执行计划 |
| `/flow-kit:fix` | `/flow-kit:audit --fix` | 审计并修复 |

---

## 7. 数据流程图

### 7.1 全流程自动化执行数据流

```
用户输入 (变更描述 / PRD 文件)
    │
    ▼
┌─────────────────────────────────────────────────┐
│              auto-start.sh                       │
│  1. 检测输入类型 (文本/文件)                      │
│  2. 解析 PRD (如需要)                            │
│  3. 调用 auto-cleanup.sh 清理环境                 │
│  4. 调用 init-change.sh 初始化变更                │
│  5. 生成执行计划 (auto-plan.json)                 │
│  6. 展示计划并请求确认                            │
└──────────────────────┬──────────────────────────┘
                       │ 确认后
                       ▼
┌─────────────────────────────────────────────────┐
│              auto-pilot.sh                       │
│  Phase 0→7 循环执行:                             │
│    1. boundary-enforcer.sh 校验边界              │
│    2. confirmation-manager.sh 请求确认           │
│    3. status-display.sh 更新状态                 │
│    4. phase-executor.sh 执行 phase              │
│    5. validate-phase.sh 验证输出                 │
│    6. execution-auditor.sh 审计                  │
│    7. auto-fix (如需要)                          │
└──────────────────────┬──────────────────────────┘
                       │ 完成后
                       ▼
┌─────────────────────────────────────────────────┐
│              evolution-engine.sh                 │
│  1. collect_execution_metrics                    │
│  2. generate_optimization_suggestions            │
│  3. update_evolution_config                      │
└─────────────────────────────────────────────────┘
```

### 7.2 Phase 边界校验数据流

```
用户执行 /flow-kit:phase-N
    │
    ▼
┌─────────────────────────────────────────────────┐
│              pre-tool-guard.sh                   │
│  1. auto-cleanup.sh (检测是否需要清理)            │
│  2. 检查命令合法性                                │
└──────────────────────┬──────────────────────────┘
                       │ 通过
                       ▼
┌─────────────────────────────────────────────────┐
│              phase-executor.sh                   │
│  1. 加载 phase-boundaries.yaml                   │
│  2. 声明当前 phase 的职责范围                     │
│  3. 显示状态面板                                  │
│  4. 加载工作流文件                                │
│  5. 注册 boundary-enforcer 回调                  │
└──────────────────────┬──────────────────────────┘
                       │ 执行中
                       ▼
┌─────────────────────────────────────────────────┐
│              boundary-enforcer.sh                │
│  每次操作前:                                     │
│    1. 检查操作是否在 allowed_operations 中        │
│    2. 检查操作是否在 forbidden_operations 中      │
│    3. 如越界: 拦截 + 提示 + 记录                  │
│    4. 如合法: 放行 + 记录                         │
└──────────────────────┬──────────────────────────┘
                       │ 关键操作
                       ▼
┌─────────────────────────────────────────────────┐
│              confirmation-manager.sh             │
│  1. 确定确认级别 (critical/important/normal)      │
│  2. 生成推荐选项 (decision-engine)                │
│  3. 显示确认对话框                                │
│  4. 等待用户输入                                  │
│  5. 记录决策                                      │
└─────────────────────────────────────────────────┘
```

### 7.3 数据存储结构

```json
{
  ".flow-kit/": {
    "session-state.json": "会话状态（已有，增强）",
    "auto-plan.json": "自动化执行计划（新增）",
    "phase-boundaries.yaml": "Phase 边界定义（新增）",
    "evolution-config.yaml": "进化配置（新增）",
    "evolution-metrics.json": "进化指标（新增）",
    "decisions.log": "决策历史记录（新增）",
    "execution.log": "执行日志（已有，增强）",
    "session-backup.json": "会话备份（新增）"
  }
}
```

---

## 8. 接口定义

### 8.1 内部脚本接口

| 脚本 | 输入 | 输出 | 调用方 |
|------|------|------|--------|
| `boundary-enforcer.sh` | `$phase`, `$operation` | exit code 0/1/2 | phase-executor.sh |
| `confirmation-manager.sh` | `$phase`, `$operation`, `$description`, `$level`, `$options[]` | `$REPLY` | phase-executor.sh, auto-pilot.sh |
| `status-display.sh` | `$current_phase` | stdout 面板 | phase-executor.sh, auto-pilot.sh |
| `auto-pilot.sh` | `$plan_file` | exit code 0/1 | auto-start.sh |
| `decision-engine.sh` | `$phase`, `$operation` | `$recommendations[]` | confirmation-manager.sh |
| `execution-auditor.sh` | `$plan_file` | audit report | auto-pilot.sh |
| `prd-parser.sh` | `$prd_file` | `$change_description` | auto-start.sh |
| `evolution-engine.sh` | `$metrics_dir` | suggestions | auto-pilot.sh (post-exec) |
| `auto-cleanup.sh` | `$mode` | exit code 0/1 | pre-tool-guard.sh, auto-start.sh |

### 8.2 Session State 扩展字段

```json
{
  "v": 2,
  "change": "change-id",
  "phase": 0,
  "status": "init|wip|pending|completed|failed|paused|replan|ready",
  "ptype": "green|brown",
  "mode": "auto|team|ralph",
  "tasks": "T1:done,T2:wip,T3:todo",
  "next": "next action description",
  "blockers": "blocker description",
  "history": "phase-0,phase-1,...",
  "decisions": "phase-0:choice1,phase-1:choice2,...",
  "interactions": "",
  "ts": "2026-05-13T00:00:00Z",
  "v3_6_0_extensions": {
    "auto_pilot_mode": "interactive|silent|supervised",
    "auto_confirm_threshold": "critical|important|normal",
    "last_cleanup": "2026-05-13T00:00:00Z",
    "execution_plan": "path/to/auto-plan.json",
    "boundary_violations": 0,
    "auto_fixes_applied": 0,
    "evolution_score": 85
  }
}
```

---

## 9. 非功能需求

### 9.1 性能需求

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| Phase 边界校验时间 | < 50ms | `hyperfine 'boundary-enforcer.sh phase-0 "影响范围评估"'` |
| 确认对话框响应 | < 100ms | `hyperfine 'confirmation-manager.sh phase-0 test important'` |
| 状态面板刷新 | < 200ms | `hyperfine 'status-display.sh 2'` |
| 全流程自动化执行 | < 30min (8 phases) | `time auto-pilot.sh` |
| PRD 解析时间 | < 5s (100KB 文档) | `time prd-parser.sh large-prd.md` |
| 审计报告生成 | < 1s | `time execution-auditor.sh` |
| 环境清理时间 | < 2s | `time auto-cleanup.sh full` |

### 9.2 兼容性需求

| 维度 | 要求 |
|------|------|
| Shell | bash >= 4.0 (macOS), bash >= 5.0 (Linux) |
| 依赖 | jq >= 1.6, yq >= 4.0 (可选) |
| 操作系统 | macOS >= 12, Ubuntu >= 22.04, WSL2 |
| Claude Code | >= v3.0.0 |
| 多 IDE | 兼容 Claude Code / Cursor / Windsurf |

### 9.3 安全性需求

| 需求 | 说明 |
|------|------|
| 命令注入防护 | 所有用户输入必须经过 sanitize 处理 |
| 路径遍历防护 | 所有文件路径必须校验在白名单内 |
| 确认不可绕过 | critical 级别的确认不可通过 --force 跳过 |
| 审计日志完整性 | 审计日志不可篡改，每次执行追加 |
| 会话备份加密 | session-backup.json 包含敏感信息时加密 |

### 9.4 可用性需求

| 需求 | 说明 |
|------|------|
| 错误提示友好 | 所有错误必须有中文描述 + 建议操作 |
| 帮助文档完整 | 每个新命令必须有 -h/--help 输出 |
| 渐进式披露 | 首次使用时逐步展示功能，不一次性全部展示 |
| 回滚能力 | 任何自动化操作都可回滚到上一个确认点 |

---

## 10. 验收标准

### 10.1 Phase 边界重构验收

- [ ] `phase-boundaries.yaml` 定义了 8 个 phase 的 allowed/forbidden operations
- [ ] `boundary-enforcer.sh` 能正确拦截越界操作
- [ ] 越界操作被拦截时给出明确的错误提示 + 正确的 phase 归属
- [ ] `confirmation-manager.sh` 支持 3 级确认 (critical/important/normal)
- [ ] critical 级别的确认不可跳过
- [ ] `status-display.sh` 实时显示当前 phase 和下一步
- [ ] 所有 9 个 phase 工作流文件已添加边界声明
- [ ] 边界校验不影响正常 phase 执行（误报率 < 1%）

### 10.2 自动化执行验收

- [ ] `auto-pilot.sh` 支持 3 种模式 (interactive/silent/supervised)
- [ ] 从 Phase 0 到 Phase 7 的全流程自动执行成功率 > 90%
- [ ] `decision-engine.sh` 在每个确认环节至少提供 2 个推荐选项
- [ ] 推荐选项采纳率 > 70%
- [ ] `execution-auditor.sh` 能检测越界事件、遗漏步骤、输出不完整
- [ ] `auto-fix` 能自动修复至少 3 种常见问题
- [ ] `prd-parser.sh` 支持 Markdown/JSON/plain-text 三种格式
- [ ] PRD 解析准确率 > 85%

### 10.3 环境清理验收

- [ ] `auto-cleanup.sh` 支持 3 种模式 (auto/full/minimal)
- [ ] 清理后 session-state.json 的关键信息保留
- [ ] 清理后临时文件全部删除
- [ ] pre-tool-guard.sh 在 phase 命令前自动触发清理
- [ ] 清理时间 < 2s

### 10.4 自我进化验收

- [ ] `evolution-engine.sh` 能收集 5+ 种执行指标
- [ ] 每 7 天自动生成优化建议
- [ ] 至少 3 种优化规则可自动应用
- [ ] 进化配置可手动覆盖

---

## 11. 实施计划

### 11.1 迭代 1：Phase 边界重构 + 环境清理（第 1-3 周，21 人天）

| 周 | 天 | 事项 | 负责人 | 交付物 |
|----|----|------|--------|--------|
| 1 | 1-2 | 编写 phase-boundaries.yaml | 架构师 | phase-boundaries.yaml |
| 1 | 3-5 | 开发 boundary-enforcer.sh | 核心维护者 | boundary-enforcer.sh |
| 2 | 1-3 | 开发 confirmation-manager.sh | 核心维护者 | confirmation-manager.sh |
| 2 | 4-5 | 开发 status-display.sh | 核心维护者 | status-display.sh |
| 3 | 1-2 | 更新 phase-executor.sh 集成边界校验 | 核心维护者 | 更新后的 phase-executor.sh |
| 3 | 3-4 | 开发 auto-cleanup.sh | 核心维护者 | auto-cleanup.sh |
| 3 | 5 | 更新 pre-tool-guard.sh + 测试 | 核心维护者 | 更新后的 hooks |

### 11.2 迭代 2：自动化执行 + 审计 + PRD 解析 + 持续任务执行（第 4-8 周，42 人天）

| 周 | 天 | 事项 | 负责人 | 交付物 |
|----|----|------|--------|--------|
| 4 | 1-3 | 开发 auto-pilot.sh 核心引擎 | 核心维护者 | auto-pilot.sh |
| 4 | 4-5 | 开发执行计划生成器 | 核心维护者 | plan-generator |
| 5 | 1-2 | 开发 decision-engine.sh | 核心维护者 | decision-engine.sh |
| 5 | 3-5 | 开发 execution-auditor.sh | 核心维护者 | execution-auditor.sh |
| 6 | 1-2 | 开发 auto-fix 机制 | 核心维护者 | auto-fix 模块 |
| 6 | 3-5 | 开发 prd-parser.sh | 核心维护者 | prd-parser.sh |
| 7 | 1-2 | 开发 auto-start.sh | 核心维护者 | auto-start.sh |
| 7 | 3-4 | 开发 auto-runner.yaml + ralph-runner.sh | 核心维护者 | ralph-runner.sh |
| 7 | 5 | 开发 safety-hooks.sh | 核心维护者 | safety-hooks.sh |
| 8 | 1-2 | 开发 task-runner.sh + 异常处理 | 核心维护者 | task-runner.sh |
| 8 | 3-4 | Phase 系统集成配置 | 核心维护者 | integration-config.yaml |
| 8 | 5 | 集成测试 + Bug 修复 | 测试工程师 | 测试报告 |

### 11.3 迭代 3：自我进化机制 + 定时任务（第 9-12 周，17 人天）

| 周 | 天 | 事项 | 负责人 | 交付物 |
|----|----|------|--------|--------|
| 9 | 1-3 | 开发 evolution-engine.sh 数据收集 | 核心维护者 | 数据收集模块 |
| 9 | 4-5 | 开发优化建议生成 | 核心维护者 | 建议生成模块 |
| 10 | 1-3 | 开发工作流自动优化 | 核心维护者 | 自动优化模块 |
| 10 | 4-5 | 二次运行修正能力 + cron 集成 | 核心维护者 | 修正模块 + cron 配置 |
| 11 | 1-2 | 任务执行审计报告 | 核心维护者 | 审计模块 |
| 11 | 3-5 | 全量集成测试 | 测试工程师 | 测试报告 |
| 12 | 1-3 | 文档编写 + 发布准备 | 文档工程师 | 发布包 |
| 12 | 4-5 | 兼容性测试 + 回滚验证 | 测试工程师 | 兼容性报告 |

### 11.4 资源需求

| 角色 | 数量 | 投入 | 职责 |
|------|------|------|------|
| 核心维护者 | 1 人 | 12 周全职 | 核心功能开发 |
| 架构师 | 1 人 | 4 周 × 50% | 边界定义 + 架构评审 + 持续执行系统设计 |
| 测试工程师 | 1 人 | 4 周全职 | 集成测试 + E2E 测试 + 兼容性测试 |
| 文档工程师 | 1 人 | 2 周 × 50% | 文档编写 + 发布准备 |

---

## 12. 风险评估

### 12.1 风险矩阵

| ID | 风险描述 | 概率 | 影响 | 等级 | 应对策略 |
|----|----------|------|------|------|----------|
| R1 | Phase 边界定义过于严格，影响开发灵活性 | 中 | 高 | 高 | 提供"宽松模式"配置，允许用户自定义边界 |
| R2 | 自动化执行引擎在复杂场景下失败 | 中 | 高 | 高 | 实现 graceful degradation，失败时降级为手动模式 |
| R3 | PRD 解析准确率不达标 | 中 | 中 | 中 | 提供人工修正接口，解析结果可编辑 |
| R4 | 环境清理误删用户数据 | 低 | 高 | 中 | 清理前创建完整备份，提供恢复命令 |
| R5 | 确认机制过于频繁，影响开发效率 | 中 | 中 | 中 | 提供"批量确认"和"信任模式"选项 |
| R6 | 自我进化机制产生不合理的优化建议 | 低 | 中 | 低 | 所有优化建议需人工确认后才应用 |
| R7 | 与现有 v3.3.0 功能不兼容 | 低 | 高 | 中 | 完整的兼容性测试 + 回滚方案 |
| R8 | 团队学习曲线陡峭 | 中 | 低 | 低 | 渐进式功能披露 + 交互式教程 |

### 12.2 风险应对详细方案

#### R1：边界定义过于严格

**缓解措施**：
- 提供 `phase-boundaries.custom.yaml` 允许用户覆盖默认边界
- 提供"宽松模式" (`/flow-kit:auto-pilot relaxed`)，减少边界校验
- 边界定义支持正则表达式匹配，适应不同项目需求

**回滚方案**：
- 删除或重命名 `phase-boundaries.yaml` 即可恢复 v3.3.0 行为

#### R2：自动化执行失败

**缓解措施**：
- 每个 phase 执行前创建检查点，失败时可从检查点恢复
- 实现 3 次自动重试机制，每次重试使用不同策略
- 失败时生成详细的错误报告 + 建议的修复方案

**回滚方案**：
- `/flow-kit:rollback --to-phase N` 回滚到指定 phase
- 手动模式可随时介入接管

#### R4：环境清理误删数据

**缓解措施**：
- 清理前自动创建 `.flow-kit/backups/` 完整备份
- 备份保留 7 天，支持 `/flow-kit:restore --from <backup-id>` 恢复
- 清理操作记录详细日志，支持审计追溯

**回滚方案**：
- `/flow-kit:restore --latest` 恢复到最近一次备份

### 12.3 依赖风险

| 依赖项 | 风险 | 替代方案 |
|--------|------|----------|
| jq >= 1.6 | 低版本系统可能未安装 | 提供 fallback 纯 bash 实现 |
| yq >= 4.0 | 非必需依赖，用于 YAML 解析 | 提供 JSON 格式的边界定义文件 |
| Claude Code API | API 变更可能导致不兼容 | 抽象 API 适配层 |
| Git 2.x | 低版本 Git 缺少某些功能 | 提供兼容模式 |

---

## 13. 参考项目借鉴分析

### 13.1 superpowers-zh 借鉴分析

#### 借鉴点 1：workflow-runner 的 YAML 工作流编排

**技术点**：superpowers-zh 的 [workflow-runner skill](https://github.com/jnMetaCode/superpowers-zh) 实现了基于 YAML 的多角色工作流编排，支持 DAG 依赖检测、拓扑排序、分层执行。

**实施方法**：
1. 将 flow-kit 的 phase 工作流从纯 Markdown 描述升级为 YAML 结构化定义
2. 引入 DAG 依赖检测机制，支持 phase 内子任务的并行执行
3. 实现拓扑排序引擎，自动确定任务执行顺序

**预期改进效果**：
- 工作流定义从"人类可读"升级为"机器可解析"，自动化率提升 60%
- 并行执行能力使 Phase 4（开发执行）效率提升 2-3 倍
- 依赖检测减少 80% 的因顺序错误导致的执行失败

**兼容性分析**：
- YAML 解析可通过 yq 实现，与现有 bash 技术栈兼容
- 可逐步迁移：先支持 YAML，再逐步废弃纯 Markdown 格式

#### 借鉴点 2：executing-plans 的分步执行与审查检查点

**技术点**：[executing-plans skill](https://github.com/jnMetaCode/superpowers-zh) 实现了严格的"审查→执行→验证"循环，每完成 3 个任务后暂停回顾。

**实施方法**：
1. 在 auto-pilot.sh 中实现"执行前审查"检查点
2. 每完成 3 个 phase 后自动触发回顾机制
3. 实现"批量审查检查点"，在关键节点暂停并展示进度

**预期改进效果**：
- 执行质量提升 50%（减少因方向偏离导致的返工）
- 问题发现时间提前 70%（从"完成后"提前到"执行中"）
- 用户对自动化流程的信任度提升 40%

**兼容性分析**：
- 审查检查点可无缝集成到现有的 confirmation-manager.sh 中
- 与现有的 session-state 系统完全兼容

#### 借鉴点 3：subagent-driven-development 的子代理并行执行

**技术点**：superpowers-zh 的 subagent-driven-development 支持在支持子代理的平台上并行执行独立任务。

**实施方法**：
1. 在 dispatch.sh 中集成子代理调度能力
2. 将 Phase 4（开发执行）中的独立任务分配给不同子代理
3. 实现结果聚合与冲突检测

**预期改进效果**：
- 开发执行效率提升 3-5 倍（取决于任务并行度）
- 代码审查效率提升 40%（子代理可并行审查不同模块）

**兼容性分析**：
- flow-kit 已有 dispatch.sh 多代理编排脚本，可直接扩展
- 子代理结果聚合机制与现有的 session-state 系统兼容

### 13.2 agency-agents-zh 借鉴分析

#### 借鉴点 1：215 个 AI 专家角色的结构化定义

**技术点**：[agency-agents-zh](https://github.com/jnMetaCode/agency-agents-zh) 定义了 215 个 AI 专家角色，每个角色有明确的身份、核心使命、关键规则和沟通风格。

**实施方法**：
1. 为 flow-kit 的每个 phase 定义对应的"AI 专家角色"
2. 在 phase 执行时加载对应角色的 system prompt
3. 角色定义包含：职责范围、专业知识、决策框架、输出规范

**预期改进效果**：
- 决策质量提升 40%（角色专业知识引导更优决策）
- 输出一致性提升 60%（角色定义包含输出规范）
- 新人上手时间减少 50%（角色定义本身就是最佳实践文档）

**兼容性分析**：
- 角色定义文件格式与 flow-kit 的 Markdown 工作流兼容
- 可在 phase 工作流文件中直接引用角色定义

#### 借鉴点 2：DAG 工作流编排与依赖管理

**技术点**：agency-agents-zh 的工程角色（如软件架构师）使用 DAG 进行任务编排，支持依赖检测和并行执行。

**实施方法**：
1. 在 auto-pilot.sh 中实现 DAG 执行引擎
2. 支持任务级别的依赖声明（depends_on）
3. 实现拓扑排序和分层执行

**预期改进效果**：
- 执行效率提升 3 倍（并行执行独立任务）
- 依赖错误减少 90%（自动检测循环依赖和缺失依赖）
- 执行计划可视化（DAG 图展示）

**兼容性分析**：
- DAG 引擎可独立于现有 phase 系统运行
- 与 dispatch.sh 的并行执行能力互补

#### 借鉴点 3：架构决策记录（ADR）机制

**技术点**：agency-agents-zh 的软件架构师角色定义了完整的 ADR 模板和决策流程。

**实施方法**：
1. 在 Phase 2（架构设计）中集成 ADR 机制
2. 每次架构决策自动生成 ADR 文档
3. ADR 包含：背景、决策、备选方案、影响分析

**预期改进效果**：
- 架构决策可追溯性提升 100%
- 技术债务减少 30%（记录决策理由，避免重复错误）
- 团队知识共享效率提升 50%

**兼容性分析**：
- ADR 文档格式与 flow-kit 的 .specs 目录结构兼容
- 可在现有 DESIGN.md 基础上扩展

### 13.3 借鉴整合路线图

| 阶段 | 借鉴来源 | 整合内容 | 时间 | 预期效果 |
|------|----------|----------|------|----------|
| 迭代 1 | superpowers-zh | 审查检查点机制 | 第 1-3 周 | 执行质量提升 50% |
| 迭代 2 | superpowers-zh + agency-agents-zh + mattpocock/skills + Ralph.md | YAML 工作流编排 + DAG 执行引擎 + Ralph 循环 + 任务模板 | 第 4-8 周 | 自动化率提升 60%，效率提升 3 倍 |
| 迭代 3 | agency-agents-zh + superpowers+gstack | 角色定义 + ADR 机制 + 任务分流 | 第 9-12 周 | 决策质量提升 40% |

### 13.4 mattpocock/skills 借鉴分析

[mattpocock/skills](https://github.com/mattpocock/skills) 是一套面向真实工程实践的 AI agent 技能集合，聚焦于解决四个核心问题：需求对齐、代码质量、反馈循环和架构维护。

#### 借鉴点 1：grill-me / grill-with-docs 需求澄清机制

**技术点**：`/grill-me` skill 通过系统化 interview 模式，在动手前彻底澄清需求。`/grill-with-docs` 在此基础上增加了共享语言（CONTEXT.md）和架构决策记录（ADR）的自动生成。

**实施方法**：
1. 在 Phase 1（需求澄清）中集成 grill-me 模式的 interview 流程
2. 自动生成 CONTEXT.md 共享语言文档，减少 AI 的 token 消耗
3. 在 Phase 2（架构设计）中集成 ADR 自动生成机制

**预期改进效果**：
- 需求理解偏差减少 60%（系统化 interview 消除歧义）
- Token 消耗降低 30%（共享语言减少冗余表达）
- 架构决策可追溯性提升 100%（ADR 自动记录）

**兼容性分析**：
- interview 流程可集成到现有的 confirmation-manager.sh 中
- CONTEXT.md 格式与 flow-kit 的 .specs 目录结构兼容
- ADR 机制与现有的 DESIGN.md 互补

**借鉴来源**：mattpocock/skills 的 `/grill-me` 和 `/grill-with-docs` skill

#### 借鉴点 2：tdd 红-绿-重构循环

**技术点**：`/tdd` skill 实现了严格的测试驱动开发循环：先写失败测试（红）→ 实现代码通过测试（绿）→ 重构优化（重构）。每个垂直切片独立完成。

**实施方法**：
1. 在 Phase 4（开发执行）中集成 TDD 循环
2. 每个功能点先写测试再实现
3. 实现后自动运行回归测试

**预期改进效果**：
- 缺陷率降低 50%（测试先行减少 bug）
- 测试覆盖率提升 30%（TDD 自然驱动高覆盖率）
- 重构安全性提升 60%（测试保障重构不破坏功能）

**兼容性分析**：
- TDD 循环与现有的 Phase 4 工作流完全兼容
- 测试框架使用项目已有的测试工具

**借鉴来源**：mattpocock/skills 的 `/tdd` skill

#### 借鉴点 3：diagnose 系统化诊断循环

**技术点**：`/diagnose` skill 实现了六阶段诊断循环：reproduce（复现）→ minimise（最小化）→ hypothesise（假设）→ instrument（检测）→ fix（修复）→ regression-test（回归测试）。

**实施方法**：
1. 在 Phase 5（测试验证）中集成 diagnose 循环
2. 测试失败时自动启动诊断流程
3. 修复后自动运行回归测试

**预期改进效果**：
- Bug 修复时间减少 50%（系统化诊断避免盲目猜测）
- 误修复率降低 70%（回归测试验证修复正确性）
- 调试效率提升 3 倍（最小化复现步骤缩小排查范围）

**兼容性分析**：
- diagnose 循环与现有的护栏检查机制互补
- 可集成到 execution-auditor.sh 的 auto-fix 模块中

**借鉴来源**：mattpocock/skills 的 `/diagnose` skill

#### 借鉴点 4：to-prd PRD 生成与 to-issues 任务拆解

**技术点**：`/to-prd` skill 将对话上下文自动转化为 PRD 文档并提交为 GitHub issue。`/to-issues` skill 将 PRD 拆解为独立的可抓取 issue。

**实施方法**：
1. 增强现有的 prd-parser.sh，集成 to-prd 的对话上下文分析能力
2. 在 Phase 3（任务拆解）中集成 to-issues 的垂直切片拆解方法
3. 自动生成 TODO.md 任务列表

**预期改进效果**：
- PRD 生成效率提升 70%（从对话直接生成）
- 任务拆解质量提升 50%（垂直切片确保独立性）
- 需求到任务的转化时间减少 60%

**兼容性分析**：
- 与现有的 prd-parser.sh 功能互补，可增强而非替代
- 垂直切片方法与现有的 DAG 依赖分析兼容

**借鉴来源**：mattpocock/skills 的 `/to-prd` 和 `/to-issues` skill

#### 借鉴点 5：improve-codebase-architecture 架构持续改进

**技术点**：`/improve-codebase-architecture` skill 基于 CONTEXT.md 的领域语言和 ADR 决策记录，自动发现代码库中的架构改进机会。

**实施方法**：
1. 在自我进化引擎（evolution-engine.sh）中集成架构分析能力
2. 定期扫描代码库，识别架构腐化信号
3. 生成架构改进建议并排入任务队列

**预期改进效果**：
- 技术债务积累速度降低 40%（持续架构改进）
- 代码复杂度增长曲线趋缓（早期发现腐化信号）
- 架构一致性提升 50%（基于共享语言的统一设计）

**兼容性分析**：
- 架构分析与现有的 evolution-engine.sh 数据收集互补
- 改进建议可写入 TODO.md 自动排入任务队列

**借鉴来源**：mattpocock/skills 的 `/improve-codebase-architecture` skill

#### 借鉴点 6：caveman 极简通信模式

**技术点**：`/caveman` skill 实现超压缩通信模式，通过去除填充词将 token 消耗降低约 75%，同时保持完整的技术准确性。

**实施方法**：
1. 在 Ralph 循环的 PROMPT.md 生成中应用 caveman 风格
2. 在 session-state 的决策记录中使用结构化键值对替代散文
3. 在审计报告中优先使用表格和列表而非段落描述

**预期改进效果**：
- Token 消耗降低 50%（压缩通信减少冗余）
- 每次迭代成本降低 40%（更少的 token = 更低的 API 费用）
- 信息密度提升 3 倍（相同 token 包含更多有效信息）

**兼容性分析**：
- caveman 风格仅影响输出格式，不影响功能逻辑
- 与现有的 session-state 系统完全兼容

**借鉴来源**：mattpocock/skills 的 `/caveman` skill

#### 借鉴点 7：handoff 会话交接机制

**技术点**：`/handoff` skill 将当前会话压缩为交接文档，使另一个 agent 可以无缝继续工作。

**实施方法**：
1. 在 Ralph 循环的每次迭代间生成 handoff 文档
2. 在 Phase 系统与 Ralph 循环的交接点使用 handoff 机制
3. 在上下文压缩前自动保存 handoff 文档

**预期改进效果**：
- 上下文丢失导致的重做减少 80%（handoff 保留关键信息）
- 跨会话协作效率提升 60%（无缝交接）
- 长时间运行任务的稳定性提升 50%（压缩后不丢失进度）

**兼容性分析**：
- handoff 文档格式与现有的 session-state.json 互补
- 可集成到 auto-cleanup.sh 的上下文恢复机制中

**借鉴来源**：mattpocock/skills 的 `/handoff` skill

### 13.5 Ralph.md 借鉴分析

[Ralph.md](y:\pwl\code\plugins\Ralph.md) 记录了 Claude Code 无人值守运行的完整实践方案，包括三种自主运行模式、Ralph Wiggum 循环、四个安全 Hook、tmux 会话持久化、CI/CD 集成和已知陷阱预防。

#### 借鉴点 1：三种自主运行模式

**技术点**：Ralph.md 第 1 节详细介绍了三种消除人工干预的模式：
- `-p` 标志 + `--allowedTools` 白名单（最安全）
- `--permission-mode auto` + Sonnet 4.6 分类器（折中方案）
- `--dangerously-skip-permissions` + 容器隔离（完全自主）

**实施方法**：
1. 在 auto-runner.yaml 中定义三种模式（supervised / semi_autonomous / full_autonomous）
2. 每种模式对应不同的 permission_mode、allowed_tools 和 max_turns 配置
3. full_autonomous 模式强制容器环境检测

**预期改进效果**：
- 安全性分级管理，适应不同场景
- 夜间运行效率提升 80%（无需人工干预）
- 破坏性操作风险降低 90%（容器隔离 + 工具白名单）

**兼容性分析**：
- 三种模式与现有的 auto-pilot.sh 的 interactive/silent/supervised 模式对应
- 容器检测逻辑可独立实现，不影响现有功能

#### 借鉴点 2：Ralph Wiggum 循环 + PROMPT.md 设计

**技术点**：Ralph.md 第 2 节记录了经过实战验证的 while 循环模式，配合详细的 PROMPT.md 实现长时间自主运行。关键发现：一句话 prompt 在一两个小时后就没劲了，详细的 prompt 文件才能支撑 27 小时会话。

**实施方法**：
1. 实现 ralph-runner.sh 的 while 循环执行器
2. 设计 PROMPT.md 生成器，包含架构上下文、任务列表、成功标准
3. 集成 --max-turns 和 --max-budget-usd 成本控制

**预期改进效果**：
- 单次自主会话时长从 2 小时提升至 12+ 小时
- 任务完成率提升 60%（详细 prompt 减少方向偏离）
- API 成本可控（预算上限防止失控）

**兼容性分析**：
- while 循环与现有的 phase 执行器互补
- PROMPT.md 生成器可复用现有的任务解析逻辑

#### 借鉴点 3：四个安全 Hook

**技术点**：Ralph.md 第 3 节记录了四个防止过夜灾难的关键 Hook：
1. No-Ask-Human：阻止 AskUserQuestion 工具调用
2. Context Monitor：监控上下文使用量，触发 /compact
3. Syntax Check：编辑后自动语法检查
4. Decision Warn：破坏性命令警告

**实施方法**：
1. 实现 safety-hooks.sh 安装器
2. 每个 Hook 独立安装，可单独启用/禁用
3. 通过环境变量配置（CC_ALLOW_QUESTIONS, CC_PROTECT_BRANCHES 等）

**预期改进效果**：
- 夜间运行事故减少 95%（四个 Hook 覆盖主要故障模式）
- 上下文溢出导致的重做减少 70%（Context Monitor 提前预警）
- 破坏性操作 100% 拦截（Decision Warn + No-Ask-Human）

**兼容性分析**：
- Hook 机制与现有的 pre-tool-guard.sh 兼容
- 可通过 .claude/settings.json 配置权限

#### 借鉴点 4：tmux 会话持久化 + CI/CD 集成

**技术点**：Ralph.md 第 4 节和第 7 节记录了 tmux 会话管理和 cron 定时触发方案。

**实施方法**：
1. 在 ralph-runner.sh 中添加 tmux 会话管理
2. 实现 cron 定时触发配置
3. 集成 Boucle 模式的 state.md 状态维持

**预期改进效果**：
- 会话持久化，支持 SSH 远程管理
- 定时任务自动化，无需手动启动
- 状态跨会话维持，支持长时间运行

**兼容性分析**：
- tmux 为可选依赖，不影响核心功能
- cron 配置与现有的 auto-pilot.sh 兼容

#### 借鉴点 5：已知陷阱预防

**技术点**：Ralph.md 第 8 节记录了 8 种故障模式及其预防方法。

**实施方法**：
1. 在 ralph-runner.sh 中实现所有 8 种故障模式的检测
2. 每种故障模式对应自动处理策略
3. 故障处理记录到审计日志

**预期改进效果**：
- 故障自动恢复率 > 80%
- 数据丢失风险降低 95%
- API 成本失控风险降低 90%

**兼容性分析**：
- 故障处理逻辑与现有的异常处理机制互补
- 审计日志格式与现有的 execution-auditor.sh 兼容

### 13.6 superpowers+gstack 开发流程借鉴分析

[superpowers+gstack 开发流程实录](y:\pwl\code\plugins\Claude Code + superpowers + gstack 开发流程实录，可直接复制使用，一篇文章讲清楚！.md) 记录了三个月实战验证的 Claude Code 最佳配置：superpowers 负责"思考与流程层"，gstack 负责"执行与外部世界层"。

#### 借鉴点 1：大脑 vs 手脚的分工模型

**技术点**：superpowers 管"想"（brainstorming, writing-plans, TDD, code-review, verification），gstack 管"做"（browse, QA, ship, deploy, canary）。两个插件定位完全不重叠，交集为零。

**实施方法**：
1. 将 flow-kit 的 Phase 0-3（立项→需求→设计→拆解）定义为"思考阶段"
2. 将 Phase 4-7（开发→测试→审查→归档）定义为"执行阶段"
3. 思考阶段强制走确认流程，执行阶段可批量确认

**预期改进效果**：
- 角色定位清晰度提升 100%（每个 phase 知道自己是"想"还是"做"）
- 功能越界事件减少 90%（思考阶段不执行，执行阶段不思考）
- 用户决策负担降低 50%（思考阶段需确认，执行阶段可自动化）

**兼容性分析**：
- 与现有的 phase-boundaries.yaml 完全兼容
- 思考/执行划分可作为边界定义的补充规则

#### 借鉴点 2：任务分流规则（轻/中/大）

**技术点**：CLAUDE.md 中的任务分流规则定义了三种任务级别的处理路径：
- 轻量任务：单文件修改、明确 bug 修复 → 直接实现 + 定向验证
- 中任务：多文件但边界清晰 → 简短 brainstorming + 实现 + 验证
- 大任务：跨模块、新架构 → 完整闭环（brainstorming → plan → worktree → TDD → QA → review → ship）

**实施方法**：
1. 在 auto-pilot.sh 中添加任务复杂度评估
2. 根据评估结果选择执行路径
3. 轻量任务跳过 brainstorming 和 writing-plans

**预期改进效果**：
- 小任务处理效率提升 60%（跳过不必要的流程）
- 大任务质量提升 40%（完整闭环确保质量）
- 用户满意度提升 50%（任务处理方式匹配预期）

**兼容性分析**：
- 任务分流与现有的 phase 系统互补
- 复杂度评估可集成到 decision-engine.sh 中

#### 借鉴点 3：四个关键交接点

**技术点**：开发闭环中的四个关键交接点：
1. executing-plans → /browse 或 /qa（代码写完必须真实环境验证）
2. verification → requesting-code-review（自检和他人审查是两个独立 pass）
3. finishing-a-development-branch → /ship（分支收尾 → 发布流水线）
4. /ship → /land-and-deploy → /canary（发布 → 合并 → 部署 → 监控）

**实施方法**：
1. 在 auto-pilot.sh 的 phase 间添加交接验证
2. 每个交接点必须有明确的产出物
3. 交接点可配置为确认点

**预期改进效果**：
- 交接遗漏减少 80%（强制验证产出物）
- 发布质量提升 50%（完整发布流水线）
- 问题发现时间提前 70%（从"上线后发现"提前到"交接时发现"）

**兼容性分析**：
- 交接点与现有的 confirmation-manager.sh 兼容
- 产出物验证可集成到 execution-auditor.sh 中

#### 借鉴点 4：五条铁律

**技术点**：五条经过实战验证的铁律：
1. 浏览器只走 /browse
2. 作者不能审自己的代码
3. 没证据不算完成
4. 歧义先 brainstorm
5. 危险命令先 /careful

**实施方法**：
1. 在 CLAUDE.md 中添加五条铁律
2. 在 boundary-enforcer.sh 中添加铁律校验
3. 在 verification-before-completion 中添加证据检查

**预期改进效果**：
- 代码审查质量提升 60%（独立 reviewer 通道）
- 假完成率降低 90%（证据优先）
- 需求理解偏差减少 70%（歧义先 brainstorm）

**兼容性分析**：
- 铁律与现有的 phase-boundaries.yaml 互补
- 可在 CLAUDE.md 中直接引用

#### 借鉴点 5：Cheatsheet 日常速查

**技术点**：完整的命令速查卡，覆盖 brainstorming → writing-plans → review → executing-plans → TDD → debugging → browse → QA → verification → code-review → finishing → ship → deploy → canary → retro 全流程。

**实施方法**：
1. 为 flow-kit 生成类似的命令速查卡
2. 在 GO.md 中添加速查卡
3. 在 status-display.sh 中添加速查模式

**预期改进效果**：
- 新用户上手时间减少 70%（速查卡一目了然）
- 命令使用准确率提升 50%（减少命令混淆）
- 用户满意度提升 40%（降低学习曲线）

**兼容性分析**：
- 速查卡为文档补充，不影响功能
- 可在 GO.md 中直接添加

### 13.7 技术兼容性整合方案

为确保所有引入的技术和方法与 flow-kit 现有技术栈和架构体系保持兼容，制定以下整合方案：

#### 13.7.1 技术适配策略

| 引入技术 | 来源 | 适配方式 | 兼容性风险 | 缓解措施 |
|----------|------|----------|------------|----------|
| Ralph Wiggum 循环 | Ralph.md | 封装为 ralph-runner.sh，通过 session-state 与 phase 系统通信 | 循环可能干扰 phase 状态 | 使用独立的状态文件 |
| 四个安全 Hook | Ralph.md | 封装为 safety-hooks.sh，通过 pre-tool-guard.sh 集成 | Hook 可能与现有护栏冲突 | Hook 可单独启用/禁用 |
| 通用任务模板 | 用户需求 | 封装为 task-runner.sh，生成 PROMPT.md 供 ralph-runner.sh 消费 | 模板解析可能失败 | 提供手动修正接口 |
| grill-me interview | mattpocock/skills | 集成到 confirmation-manager.sh 的推荐选项生成 | interview 流程可能过长 | 设置 max_questions 限制 |
| TDD 循环 | mattpocock/skills | 集成到 Phase 4 工作流 | 可能增加开发时间 | 仅对 P0 功能强制 TDD |
| diagnose 循环 | mattpocock/skills | 集成到 execution-auditor.sh 的 auto-fix | 诊断可能误判 | 保留人工确认通道 |
| caveman 通信 | mattpocock/skills | 应用于 PROMPT.md 生成和审计报告 | 可能丢失细节 | 提供"详细模式"开关 |
| handoff 交接 | mattpocock/skills | 集成到 auto-cleanup.sh 的上下文备份 | handoff 文档可能过大 | 设置 max_size 限制 |
| 任务分流 | superpowers+gstack | 集成到 auto-pilot.sh 的执行路径选择 | 分流可能误判 | 提供手动覆盖选项 |
| 五条铁律 | superpowers+gstack | 添加到 CLAUDE.md 和 boundary-enforcer.sh | 铁律可能过于严格 | 提供"宽松模式" |

#### 13.7.2 模块集成方法

```yaml
# .flow-kit/module-integration.yaml — 模块集成定义
modules:
  ralph-runner:
    type: "new"
    dependencies: ["session-state", "auto-cleanup"]
    integration_points:
      - "ralph-runner.sh → session-state.json (状态同步)"
      - "ralph-runner.sh → auto-cleanup.sh (环境清理)"
      - "ralph-runner.sh → PROMPT.md (任务输入)"

  safety-hooks:
    type: "new"
    dependencies: ["pre-tool-guard"]
    integration_points:
      - "safety-hooks.sh → hooks/pre-tool-guard.sh (Hook 注入)"
      - "safety-hooks.sh → .claude/settings.json (权限配置)"

  task-runner:
    type: "new"
    dependencies: ["ralph-runner", "prd-parser"]
    integration_points:
      - "task-runner.sh → ralph-runner.sh (任务执行)"
      - "task-runner.sh → TODO.md (任务状态)"

  grill-integration:
    type: "enhancement"
    target: "confirmation-manager.sh"
    changes:
      - "添加 interview 模式"
      - "生成 CONTEXT.md"
      - "记录 ADR"

  tdd-integration:
    type: "enhancement"
    target: "phases/4-dev/*.md"
    changes:
      - "添加 TDD 步骤"
      - "添加红-绿-重构循环说明"

  diagnose-integration:
    type: "enhancement"
    target: "execution-auditor.sh"
    changes:
      - "添加 diagnose 循环"
      - "添加最小化复现步骤"

  caveman-integration:
    type: "enhancement"
    target: "ralph-runner.sh, audit reports"
    changes:
      - "PROMPT.md 使用 caveman 风格"
      - "审计报告使用结构化格式"

  handoff-integration:
    type: "enhancement"
    target: "auto-cleanup.sh"
    changes:
      - "压缩前生成 handoff 文档"
      - "恢复时加载 handoff 文档"

  task-routing:
    type: "enhancement"
    target: "auto-pilot.sh"
    changes:
      - "添加任务复杂度评估"
      - "选择执行路径 (轻/中/大)"

  iron-rules:
    type: "enhancement"
    target: "CLAUDE.md, boundary-enforcer.sh"
    changes:
      - "添加五条铁律"
      - "添加铁律校验"
```

#### 13.7.3 接口设计规范

所有新增模块遵循以下接口规范：

1. **输入规范**：所有模块接受标准化的 JSON/YAML 配置文件
2. **输出规范**：所有模块输出到 session-state.json 或独立的状态文件
3. **错误处理**：所有模块返回标准化的 exit code（0=成功, 1=可恢复错误, 2=不可恢复错误）
4. **日志规范**：所有模块使用 `[module-name]` 前缀输出日志
5. **状态同步**：所有模块通过 session-state.json 同步状态

#### 13.7.4 兼容性测试计划

| 测试项 | 测试方法 | 通过标准 |
|--------|----------|----------|
| Ralph 循环 + Phase 系统 | 启动 Ralph 循环后执行 phase 命令 | Phase 命令正常执行 |
| 安全 Hook + 现有护栏 | 安装 Hook 后执行危险命令 | 危险命令被拦截 |
| 任务模板 + PRD 解析 | 提交 PRD 后执行任务模板 | PRD 正确解析 |
| grill-me + 确认管理器 | 启动 interview 模式 | 确认对话框正常显示 |
| TDD + Phase 4 | 执行 TDD 开发 | 测试先于代码 |
| diagnose + auto-fix | 触发 auto-fix | 诊断循环正常执行 |
| caveman + 审计报告 | 生成审计报告 | 报告格式正确 |
| handoff + 环境清理 | 执行 auto-cleanup | handoff 文档正确生成 |
| 任务分流 + auto-pilot | 提交不同复杂度任务 | 分流正确 |
| 五条铁律 + 边界校验 | 执行越界操作 | 越界操作被拦截 |

### 13.8 Claude Code AI 辅助效率提升分析

| 实施环节 | AI 辅助方式 | 效率提升 | 计算依据 |
|----------|-------------|----------|----------|
| Phase 边界定义 | AI 自动分析工作流文件，生成边界定义 | 60% | 人工需 5 天分析 9 个 phase 文件，AI 辅助需 2 天 |
| 边界校验器开发 | AI 生成 boundary-enforcer.sh 骨架代码 | 50% | 人工需 6 天，AI 辅助需 3 天 |
| 确认管理器开发 | AI 生成交互逻辑 + 推荐选项 | 40% | 人工需 5 天，AI 辅助需 3 天 |
| 自动化执行引擎 | AI 生成 auto-pilot.sh 核心循环 | 60% | 人工需 10 天，AI 辅助需 4 天 |
| 智能决策系统 | AI 分析历史数据生成推荐规则 | 50% | 人工需 8 天，AI 辅助需 4 天 |
| PRD 解析器 | AI 实现文档结构分析和信息提取 | 70% | 人工需 8 天，AI 辅助需 2.5 天 |
| 自我进化引擎 | AI 分析执行指标生成优化建议 | 60% | 人工需 10 天，AI 辅助需 4 天 |
| 测试编写 | AI 生成测试用例 + 边界测试 | 70% | 人工需 10 天，AI 辅助需 3 天 |
| 文档编写 | AI 生成文档初稿 + API 文档 | 60% | 人工需 5 天，AI 辅助需 2 天 |

**总体效率提升计算**：
- 传统开发方式预估：85 人天
- AI 辅助开发方式预估：85 × (1 - 加权平均效率提升率)
- 加权平均效率提升率 = (60%×2 + 50%×2 + 40% + 60%×2 + 70%×2 + 60%) / 9 ≈ 58%
- AI 辅助预估：85 × 42% ≈ 36 人天
- **实际预估：40-45 人天**（考虑集成和调试开销）

### 13.9 代码质量改进指标

| 指标 | 当前基线 | 目标值 | 衡量方法 | AI 贡献 |
|------|----------|--------|----------|---------|
| 越界事件/执行 | 3-5 次 | 0 次 | execution-auditor.sh 统计 | AI 边界校验拦截 |
| 确认覆盖率 | 0% | 100% | confirmation-manager.sh 日志 | AI 自动识别关键操作 |
| 自动化率 | 20% | > 90% | auto-pilot.sh 执行统计 | AI 全流程编排 |
| 推荐采纳率 | N/A | > 70% | decision-engine.sh 统计 | AI 上下文分析 |
| 审计覆盖率 | 0% | 100% | execution-auditor.sh 统计 | AI 自动审计 |
| PRD 解析准确率 | N/A | > 85% | prd-parser.sh 测试 | AI 文档分析 |
| 修复成功率 | N/A | > 80% | auto-fix 统计 | AI 错误模式识别 |
| 环境清理成功率 | 0% | 100% | auto-cleanup.sh 统计 | AI 自动触发 |

---

## 附录

### A. 术语表

| 术语 | 定义 |
|------|------|
| Phase | 开发流程中的一个阶段，共 9 个（Phase 0-8） |
| 边界校验 | 检查当前操作是否属于当前 phase 的职责范围 |
| 确认级别 | 操作前确认的严格程度：critical/important/normal |
| 自动化模式 | auto-pilot 的运行模式：interactive/silent/supervised |
| 执行计划 | 描述 Phase 0→7 执行顺序和配置的 JSON 文件 |
| 审计报告 | 执行完成后生成的质量和合规性报告 |
| 自我进化 | 系统根据历史执行数据自动优化配置的能力 |
| DAG | 有向无环图，用于任务依赖管理和执行顺序编排 |
| ADR | 架构决策记录，记录技术决策的背景和理由 |

### B. 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `phase-boundaries.yaml` | 新增 | Phase 职责边界定义 |
| `scripts/boundary-enforcer.sh` | 新增 | 边界强制执行器 |
| `scripts/confirmation-manager.sh` | 新增 | 操作确认管理器 |
| `scripts/status-display.sh` | 新增 | 状态显示面板 |
| `scripts/auto-pilot.sh` | 新增 | 自动化执行引擎 |
| `scripts/decision-engine.sh` | 新增 | 智能决策引擎 |
| `scripts/execution-auditor.sh` | 新增 | 执行审计器 |
| `scripts/prd-parser.sh` | 新增 | PRD 自动解析器 |
| `scripts/auto-start.sh` | 新增 | 一键启动命令 |
| `scripts/evolution-engine.sh` | 新增 | 自我进化引擎 |
| `scripts/auto-cleanup.sh` | 新增 | 环境自动清理器 |
| `scripts/ralph-runner.sh` | 新增 | Ralph Wiggum 循环执行器 |
| `scripts/safety-hooks.sh` | 新增 | 四个安全 Hook 安装器 |
| `scripts/task-runner.sh` | 新增 | 通用任务模板执行器 |
| `.flow-kit/auto-runner.yaml` | 新增 | 自主运行模式配置 |
| `.flow-kit/integration-config.yaml` | 新增 | 持续执行系统集成配置 |
| `hooks/pre-tool-guard.sh` | 修改 | 集成自动清理逻辑 |
| `scripts/phase-executor.sh` | 修改 | 集成边界校验 |
| `lib/session-state.sh` | 修改 | 扩展字段支持 |
| `phases/*.md` | 修改 | 添加边界声明 |

### C. 版本兼容性说明

| 从版本 | 升级路径 | 注意事项 |
|--------|----------|----------|
| v3.3.0 | 直接升级 | phase-boundaries.yaml 默认宽松配置，不影响现有工作流 |
| v3.2.x | 先升级到 v3.3.0 | 需要先适配 session-state v2 格式 |
| v3.0.x | 先升级到 v3.3.0 | 需要先适配新的命令注册机制 |

### 13.10 Context Engineering 借鉴分析

#### 借鉴点 1：code-review-graph 知识图谱精准上下文

**技术点**：[code-review-graph](https://github.com/artemgorbunov/code-review-graph) 通过 Tree-sitter AST 解析构建代码结构化地图，实现爆炸半径分析。文件变更时追踪每个调用者、依赖项和受影响的测试，100% 召回率。

**关键数据**：
- 代码审查 Token 减少 6.8 倍
- 日常编码任务 Token 减少 49 倍
- 2900 个文件的项目重新索引仅需不到 2 秒

**实施方法**：
1. 在 Phase 4（开发执行）中集成代码知识图谱
2. 审计阶段使用爆炸半径分析精准定位影响范围
3. 通过 MCP 给 AI 助手提供精确上下文

**预期改进效果**：
- Token 消耗降低 60%（只读取需要的内容）
- 代码审查效率提升 5 倍（精准上下文）
- 影响分析准确率 100% 召回率

**兼容性分析**：
- 作为可选 MCP 服务集成，不影响核心功能
- 需要 Python 3.10+ 环境

**借鉴来源**：code-review-graph 项目及多篇实战分析文章

#### 借鉴点 2：/rewind 的 Context Engineering 价值

**技术点**：`/rewind` 不仅撤销操作，同时回滚 context window。错误对话会产生"噪音"，影响模型后续判断。模型的高质量处理区间在总 context window 的 40% 左右。

**实施方法**：
1. 在 auto-cleanup.sh 中集成 context 健康检查
2. 当检测到执行偏差时，建议用户使用 /rewind
3. 在 Ralph 循环中自动检测 context 质量

**预期改进效果**：
- 上下文污染减少 80%
- 模型输出质量提升 30%（干净上下文）

**兼容性分析**：
- /rewind 是 Claude Code 内置命令，无需额外集成
- 可在 auto-pilot.sh 中添加 context 监控逻辑

**借鉴来源**：Claude的/rewind功能实战分析

### 13.11 全局路由协议借鉴分析

#### 借鉴点 1：Superpowers 与 gstack 的全局路由协议

**技术点**：通过 AGENTS.md 定义全局路由协议，明确 Superpowers（通用研发流程）和 gstack（专项工程能力）的分工边界。核心原则：默认一回合只允许一个主流程框架接管。

**优先级链**：
```
用户明确指令 > 项目 AGENTS.md > 全局 AGENTS.md > 插件默认说明
```

**实施方法**：
1. 为 flow-kit 创建 AGENTS.md 路由协议
2. 定义 Phase 0-3（思考阶段）和 Phase 4-7（执行阶段）的主控权
3. 防止 Phase 命令间的双重触发和默认控制权冲突

**预期改进效果**：
- 功能越界事件减少 90%
- 用户心智负担降低 50%
- 多命令协作效率提升 40%

**兼容性分析**：
- AGENTS.md 与现有的 phase-boundaries.yaml 互补
- 路由协议仅做边界管理，不替代工作流

**借鉴来源**：Alan の分享 — Superpowers 与 gstack 的全局路由协议

#### 借鉴点 2：SDD 三阶段工作流（gstack + superpowers 组合）

**技术点**：规划阶段用 gstack（office-hours → plan-ceo-review → plan-eng-review → plan-design-review），开发阶段用 superpowers（brainstorming → write-a-prd → writing-plans → subagent-driven-development），发布阶段回到 gstack（review → qa → ship）。

**实施方法**：
1. Phase 0-2（立项→需求→设计）借鉴 gstack 的评审体系
2. Phase 3-5（拆解→开发→测试）借鉴 superpowers 的执行体系
3. Phase 6-7（审查→归档）借鉴 gstack 的发布体系

**预期改进效果**：
- 需求质量提升 50%（多层评审把关）
- 开发效率提升 40%（subagent 并行执行）
- 发布质量提升 60%（完整发布流水线）

**兼容性分析**：
- 三阶段划分与 flow-kit 现有 Phase 体系高度兼容
- 评审体系可作为 Phase 工作流的增强插件

**借鉴来源**：放弃 Vibe Coding — SDD 三阶段工作流实战

### 13.12 生产级交付实践借鉴分析

#### 借鉴点 1：Claude Code 生产系统交付全流程

**技术点**：从全局 CLAUDE.md 配置 → skill 安装 → MCP 集成 → 前端原型 → 后端服务 → 日志调试的完整生产交付链路。

**关键实践**：
- 反向输出技术架构：让 Claude Code 扫描现有代码生成技术约束
- 渐进式需求加载：doc/需求文档/ 按模块级→菜单级→页面级→按钮级逐步细化
- 记忆分层：根目录 CLAUDE.md + frontend/CLAUDE.md + backend/CLAUDE.md
- 日志文件配置：让模型自行 debug
- 上下文超过 70% 主动 /compact 或开新会话
- 3-5 步无进展则丢弃当前会话，换个方向重试
- 沉淀错误：把缺失信息、解决思路、模型犯过的错误回写到文档

**实施方法**：
1. 在 flow-kit 的 init-change.sh 中集成反向技术架构输出
2. 在 Phase 工作流中添加渐进式需求加载指导
3. 在 auto-cleanup.sh 中添加上下文健康检查

**预期改进效果**：
- 新项目启动时间减少 60%（反向输出技术架构）
- 上下文管理效率提升 50%（记忆分层 + 渐进式加载）
- 调试效率提升 70%（日志文件自动 debug）

**兼容性分析**：
- 记忆分层与现有的 session-state 系统互补
- 渐进式加载与 Phase 工作流的分阶段执行理念一致

**借鉴来源**：Claude Code + MiniMax 2.7 + Superpowers 生产系统交付全流程

#### 借鉴点 2：Agentic Coding 工作流设计（TOC 约束理论）

**技术点**：将 TOC（Theory of Constraints）约束理论应用于 Agentic Coding。核心理念：LLM 几乎不是主要约束，真正的约束是验证、集成、部署、回滚。

**TOC 五步法在开发中的应用**：
1. Identify：识别系统约束（验证能力、集成能力）
2. Exploit：充分利用约束（每个功能走完整闭环）
3. Subordinate：其他环节服从约束（不堆积未验证代码）
4. Elevate：投入资源提升约束（加强自动化测试）
5. Repeat：约束转移后重新识别

**Spec-Driven Incremental Deploy 方法论**：
- 设计阶段：Generator-Evaluator 收敛循环，产出 checkpoint YAML
- 实现阶段：RED → GREEN → REVIEW → DEPLOY → COMMIT
- 变更阶段：先影响分析，再动手实现

**实施方法**：
1. 在 Phase 4（开发执行）中集成 RED → GREEN → REVIEW → DEPLOY → COMMIT 闭环
2. 在 auto-pilot.sh 中添加约束识别逻辑
3. 在 execution-auditor.sh 中添加 WIP（在制品）监控

**预期改进效果**：
- 未验证代码堆积减少 80%
- 部署失败率降低 60%
- 整体交付速度提升 30%（约束驱动优化）

**兼容性分析**：
- TOC 约束理论与现有的护栏系统互补
- checkpoint YAML 格式与 session-state.json 兼容

**借鉴来源**：不可避免的未来 — 用 Claude Code 设计自己的 Agentic Coding 工作流

### 13.13 控制模型与自进化借鉴分析

#### 借鉴点 1：OMC vs Karpathy 硬控制与软约束融合

**技术点**：OMC（硬控制）通过系统流程强制执行，Karpathy（软约束）通过 Prompt 规则引导行为。最优解是融合：CLAUDE.md = Karpathy（行为层）+ 项目规范（约束层）+ OMC（执行层）。

**实施方法**：
1. Phase 边界定义采用硬控制（boundary-enforcer.sh 不可绕过）
2. CLAUDE.md 行为引导采用软约束（Karpathy 四原则）
3. 执行层采用 OMC 风格的多 Agent 分工 + 自动 pipeline

**预期改进效果**：
- 执行规范性提升 80%（硬控制兜底）
- 代码质量提升 40%（软约束引导）
- 开发效率提升 50%（执行层自动化）

**兼容性分析**：
- 三层模型与 flow-kit 现有架构完全兼容
- boundary-enforcer.sh = 硬控制层，CLAUDE.md = 软约束层，auto-pilot.sh = 执行层

**借鉴来源**：Claude Code 进阶指南 — OMC vs Karpathy 双 CLAUDE.md 策略

#### 借鉴点 2：OpenSpace 自进化引擎

**技术点**：AUTO-FIX（自动修复失败技能）、AUTO-IMPROVE（自动优化成功技能）、AUTO-LEARN（自动捕获新工作流）。任务收益 4.2 倍，Token 消耗减少 46%。

**实施方法**：
1. evolution-engine.sh 实现 AUTO-FIX/AUTO-IMPROVE/AUTO-LEARN 三机制
2. 执行数据收集后自动分析模式
3. 成功的工作流自动封装为可复用配置

**预期改进效果**：
- 重复任务 Token 消耗减少 50%
- 工作流优化频率提升 3 倍
- 新用户上手时间减少 40%（自动积累最佳实践）

**兼容性分析**：
- 自进化引擎独立运行，不影响核心 Phase 系统
- 进化结果写入 session-state.json

**借鉴来源**：OpenSpace 自进化引擎技术分析

#### 借鉴点 3：Skill-creator 评估体系

**技术点**：四大新能力 — 评估系统（自动体检报告）、基准测试（通过率/耗时/token 全量化）、多代理并行测试（零交叉污染）、描述调优（自动优化触发条件）。

**协议设计三级关键词**：
- MANDATORY：必须执行
- RECOMMENDED：推荐执行
- ON ERROR：仅异常时执行

**实施方法**：
1. Phase 工作流文件采用 MANDATORY/RECOMMENDED/OPTIONAL 三级指令
2. 在 evolution-engine.sh 中集成基准测试能力
3. 在 execution-auditor.sh 中集成评估报告生成

**预期改进效果**：
- Phase 工作流执行准确性提升 40%（三级指令明确）
- 工作流质量可量化（基准测试数据）
- 触发条件精准度提升 60%（描述调优）

**兼容性分析**：
- 三级指令格式与现有 Markdown 工作流文件兼容
- 评估体系可作为 execution-auditor.sh 的增强功能

**借鉴来源**：Claude 悄悄史诗级更新 Skill-creator 评估体系

### 13.14 Ralph 自主 Agent 循环与 PRD 驱动开发

[Ralph Wiggum](https://github.com/nicepkg/ralph)（16.5K Stars）是一个让 Claude Code 在无人值守状态下持续运行的插件，核心逻辑为 PRD → prd.json → 循环执行 → 全部完成。

#### 借鉴点 1：每次迭代全新 AI 实例

**技术点**：Ralph 的核心设计是每次迭代启动全新 AI 实例（Claude Code 或 Amp），带项目上下文。跨迭代记忆只靠三个东西——git 历史、progress.txt、prd.json。

**循环流程**：
1. 从 prd.json 挑出优先级最高的 `passes: false` 的用户故事
2. 启动全新 AI 实例，带项目上下文
3. AI 完成这一项 story
4. 运行质量检查（typecheck、测试）
5. 检查通过 → 提交 commit → 标记 `passes: true`
6. 把学习到的上下文追加到 progress.txt 和 AGENTS.md
7. 重复，直到所有 story 完成

**实施方法**：
1. 在 ralph-runner.sh 中实现"每次迭代全新实例"模式
2. 通过 prd.json 的 passes/fail 状态追踪集成到 session-state.json
3. progress.txt + AGENTS.md 的跨迭代记忆机制替代当前的上下文传递方式

**预期改进效果**：
- 上下文污染减少 100%（每次迭代干净上下文）
- 长时间运行稳定性提升 80%（不会因上下文膨胀而退化）
- 任务完成率提升 60%（每次迭代聚焦单一 story）

**兼容性分析**：
- 全新实例模式与现有的 Phase 系统互补
- prd.json 格式与 session-state.json 可互转

**借鉴来源**：Ralph 自主 Agent 循环技术文档

#### 借鉴点 2：三级反馈闭环

**技术点**：Ralph 的三级反馈闭环确保每次迭代的质量：

| 层级 | 机制 | 作用 |
|------|------|------|
| Typecheck | 编译/类型检查 | 拦截类型错误 |
| 测试 | 单元/集成测试 | 验证行为正确性 |
| Git history | 版本历史 | 让下一个 AI 看到上一个 AI 写了什么 |

**实施方法**：
1. 在 Phase 4（开发执行）中集成三级反馈闭环
2. 每次代码变更后自动运行 typecheck + 测试
3. Git commit message 包含足够的上下文信息

**预期改进效果**：
- 类型错误减少 90%（typecheck 前置拦截）
- 回归 bug 减少 70%（测试自动验证）
- 跨迭代信息传递完整性提升 80%（git history 作为记忆载体）

**兼容性分析**：
- 三级反馈闭环与现有的护栏检查机制互补
- 可集成到 execution-auditor.sh 的验证流程中

**借鉴来源**：Ralph Wiggum 插件技术文档

### 13.15 AutoResearch 方法论借鉴分析

[Karpathy AutoResearch](https://github.com/karpathy/auto-research)（71K Stars）是 Andrej Karpathy 的 AI 自主研究实验，核心洞察：没有框架！编排逻辑就是一个 Markdown 文件。

#### 借鉴点 1：三文件架构

**技术点**：整个系统只有三个文件：
- `prepare.py` — 数据准备（AI 不可修改）
- `train.py` — AI 的"实验室"（AI 可修改一切）
- `program.md` — 人类写的"研究纲领"（Markdown 编排 AI 行为）

**实验循环**：读取 program.md → 查看 git 历史 → 提出假设 → 编辑 train.py → 训练 5 分钟 → 检查 val_bpb → 改善则保留，否则 git revert

**核心洞察**：智能体的上下文窗口就是状态机，Markdown 文档就是工作流定义，Git 就是版本控制和回滚机制。

**实施方法**：
1. 将 flow-kit 的 Phase 工作流从复杂 YAML 简化为 program.md 模式
2. 每个 Phase 定义"可修改区域"和"不可修改区域"
3. Git 作为回滚机制，每次 Phase 执行前创建检查点

**预期改进效果**：
- 工作流定义复杂度降低 70%（Markdown 替代 YAML）
- 回滚安全性提升 100%（Git 原生支持）
- 新用户理解成本降低 60%（三文件架构直观）

**兼容性分析**：
- program.md 模式与现有的 Markdown 工作流文件兼容
- Git 回滚机制与现有的 session-state 系统互补

**借鉴来源**：Karpathy AutoResearch 项目及多篇分析文章

#### 借鉴点 2：多 Agent 交叉审核 + 5 维度加权评分

**技术点**：将 AutoResearch 迁移到软件开发领域后的三大关键改进：

| 改进 | Karpathy 原版 | 软件开发版 |
|------|--------------|-----------|
| 审核机制 | 单 Agent 自审 | 多 Agent 交叉审核（Codex + Claude 轮流） |
| 评分体系 | 单一 metric（val loss） | 5 维度加权评分（正确性35%+测试25%+代码质量20%+安全10%+性能10%） |
| 反馈机制 | 独立循环 | 审核反馈驱动下一轮实现 |

**六条核心原则**：
1. program.md 是唯一规则来源
2. 多 Agent 对抗审核（不同模型盲区互补）
3. 量化评分门槛（总分 ≥ 9.0 才通过）
4. 审核反馈驱动迭代（不是盲循环）
5. 只保留改进（退化就回滚）
6. 全量记录可追溯

**实施方法**：
1. 在 Phase 6（代码审查）中集成多 Agent 交叉审核
2. 在 execution-auditor.sh 中实现 5 维度加权评分
3. 在 evolution-engine.sh 中实现"只保留改进"原则

**预期改进效果**：
- 代码审查质量提升 50%（多 Agent 盲区互补）
- 质量评估客观性提升 80%（量化评分替代主观判断）
- 迭代效率提升 40%（审核反馈驱动而非盲循环）

**兼容性分析**：
- 多 Agent 交叉审核与现有的 dispatch.sh 并行执行能力兼容
- 5 维度评分可作为 execution-auditor.sh 的增强功能

**借鉴来源**：AutoResearch 软件开发领域迁移分析

### 13.16 Compound Engineering 知识复利系统

[Compound Engineering (CE)](https://github.com/compound-engineering/agent) 是一个知识复利系统，核心理念：Superpowers 管纪律，CE 管能力。层次不同，不构成替代。

#### 借鉴点 1：知识沉淀机制

**技术点**：CE 的 `/ce:compound` 启动 5 个子代理，自动提取本轮踩过的坑、用到的模式、review 的关键发现，结构化存入 `docs/solutions/` 目录。下一轮 plan 时，learnings-researcher 子代理自动搜索并引用。

**验证效果**：第二轮 plan 自动引用第一轮经验，设计方案主动避开已知坑，review 发现数明显下降。

**实施方法**：
1. 在 evolution-engine.sh 中集成知识沉淀机制
2. 每次 Phase 执行后自动提取经验教训
3. 在 Phase 1（需求分析）中集成 learnings-researcher 自动引用

**预期改进效果**：
- 重复错误减少 70%（知识沉淀避免踩同样的坑）
- 设计质量提升 40%（自动引用历史经验）
- 团队知识共享效率提升 60%（结构化知识库）

**兼容性分析**：
- 知识沉淀与现有的 session-state 系统互补
- learnings-researcher 可集成到 decision-engine.sh 中

**借鉴来源**：CE + Superpowers 三模型实测分析

#### 借鉴点 2：12 子代理并行审查

**技术点**：CE 从安全、性能、架构、可维护性等 12 个维度并行审查代码。单次 review 消耗大量 token，但审查质量显著提升。

**实施方法**：
1. 在 Phase 6（代码审查）中集成 12 子代理并行审查
2. 每个子代理负责一个维度的审查
3. 审查结果聚合为综合报告

**预期改进效果**：
- 代码审查覆盖率提升 200%（12 维度全覆盖）
- 安全漏洞发现率提升 80%（专门安全审查子代理）
- 架构问题发现率提升 60%（专门架构审查子代理）

**兼容性分析**：
- 12 子代理并行审查与现有的 dispatch.sh 兼容
- 审查结果聚合格式与 execution-auditor.sh 兼容

**借鉴来源**：Compound Engineering 12 子代理并行审查机制

### 13.17 flow-kit 社区实践与融合创新

[rihebty/flow-kit](https://github.com/rihebty/flow-kit) 是一个融合了 7 个主流框架核心能力的 flow-kit 社区版本，其创新设计对 v3.6.0 有重要参考价值。

#### 借鉴点 1：四闸门 + 第五闸门机制

**技术点**：四闸门机制确保每个阶段都有明确的确认点：
1. 路由声明（AI 必须先声明走哪条路）
2. 用户回复（用户确认路由选择）
3. 加载 prompt 并确认（加载具体工作流后再次确认）
4. prompt 第一步反问并等回复（工作流启动前最后确认）

阶段切换时再加第五道闸门：必须产出工件 + 输出过渡声明 + 用户确认。

**实施方法**：
1. 在 confirmation-manager.sh 中实现四闸门机制
2. 在 auto-pilot.sh 的 phase 切换点添加第五闸门
3. 每个闸门可配置跳过条件

**预期改进效果**：
- 方向偏离减少 90%（四闸门确保每一步都对齐）
- 用户对自动化的信任度提升 50%（每步可确认）
- 阶段切换遗漏减少 80%（第五闸门强制产出工件）

**兼容性分析**：
- 四闸门与现有的 confirmation-manager.sh 兼容
- 第五闸门可作为 phase 切换的增强验证

**借鉴来源**：rihebty/flow-kit 融合架构分析

#### 借鉴点 2：@GO.md 零例外路由

**技术点**：只要消息里出现 @GO.md，AI 必须先给路由声明或选项菜单，禁止直接动手。这是防止 AI 跳过流程直接执行的硬约束。

**实施方法**：
1. 在 boundary-enforcer.sh 中添加 @GO.md 路由检测
2. 检测到 @GO.md 引用时强制展示路由选项
3. 禁止在路由确认前执行任何操作

**预期改进效果**：
- 流程跳过事件减少 100%（零例外路由硬约束）
- 用户对流程控制感提升 80%（每次都有路由选择）

**兼容性分析**：
- @GO.md 路由检测与现有的 boundary-enforcer.sh 兼容
- 可作为 phase-boundaries.yaml 的补充规则

**借鉴来源**：rihebty/flow-kit @GO.md 路由机制

#### 借鉴点 3：H-hotfix.md 三档响应

**技术点**：紧急修复的三档响应机制：
- P0：先回滚再诊断（生产事故）
- P1：3 问澄清后直接修复（紧急 bug）
- P2：建议走正常流程（非紧急问题）

事故解决后 24 小时内补齐 5 件事：根因分析、修复方案、测试补充、监控告警、文档更新。

**实施方法**：
1. 在 Phase 工作流中添加 H-hotfix.md 紧急分支
2. 实现三档响应自动判断逻辑
3. 事故后自动生成 5 件事检查清单

**预期改进效果**：
- 紧急响应时间减少 60%（三档响应快速分流）
- 事故复发率降低 70%（24 小时内补齐 5 件事）
- 生产稳定性提升 50%（P0 先回滚再诊断）

**兼容性分析**：
- H-hotfix 作为 Phase 工作流的异常分支，不影响正常流程
- 三档响应判断可集成到 decision-engine.sh 中

**借鉴来源**：rihebty/flow-kit H-hotfix.md 紧急修复机制

#### 借鉴点 4：R-recall.md 项目记忆恢复

**技术点**：触发后 AI 输出"5 分钟项目摘要 + 推荐下一步"。超过 30 天做 bit rot 检查（依赖过期、安全漏洞、文档滞后）。

**实施方法**：
1. 在 session-state 恢复机制中集成 R-recall
2. 自动生成项目摘要和推荐下一步
3. 实现 bit rot 定期检查

**预期改进效果**：
- 项目恢复时间减少 80%（5 分钟摘要快速回忆）
- 长期未维护项目风险降低 60%（bit rot 检查）

**兼容性分析**：
- R-recall 与现有的 session-state 恢复机制互补
- bit rot 检查可作为 evolution-engine.sh 的定期任务

**借鉴来源**：rihebty/flow-kit R-recall.md 项目记忆恢复

#### 借鉴点 5：Secrets 保护机制

**技术点**：强制 .env.example + .gitignore，提交前自动扫描敏感信息（API key、token、密码等）。

**实施方法**：
1. 在 pre-tool-guard.sh 中添加敏感信息扫描
2. 在 git commit 前自动触发扫描
3. 发现敏感信息时阻止提交并提示

**预期改进效果**：
- 敏感信息泄露风险降低 95%（提交前自动扫描）
- 安全合规性提升 100%（强制 .env.example + .gitignore）

**兼容性分析**：
- Secrets 扫描与现有的 safety-hooks.sh 兼容
- 可作为 Decision Warn Hook 的增强功能

**借鉴来源**：rihebty/flow-kit Secrets 保护机制

### 13.18 Claude Code 团队工程化落地

#### 借鉴点 1：七大核心构件体系

**技术点**：从强约束到弱约束的七层配置体系：

| 构件 | 职责 | 约束强度 |
|------|------|----------|
| CLAUDE.md | 项目级"记忆与约定" | 中 |
| rules/ | 团队必须遵守的"底线规则" | 强 |
| agents/ | 专用"子代理分工" | 中 |
| commands/ | 高频操作"一键化流程" | 中 |
| skills/ | 可复用的"方法论与领域知识" | 弱 |
| hooks/ | 关键节点"自动化护栏" | 强 |
| MCP 配置 | 外部工具"接入层" | 弱 |

**落地三原则**：
1. 按"项目独有/全局通用"划分存储位置
2. 用 rules 固化底线，用 commands 固化流程
3. 把"需要换脑子"的任务交给 Agent

**实施方法**：
1. 将 flow-kit 的配置文件按七层体系重新组织
2. Phase 边界定义放入 rules/，Phase 命令放入 commands/
3. 子代理任务放入 agents/，工作流方法论放入 skills/

**预期改进效果**：
- 配置管理清晰度提升 80%（七层分层明确）
- 团队协作效率提升 50%（底线和流程分离）
- 新成员上手时间减少 60%（配置即文档）

**兼容性分析**：
- 七层体系与 flow-kit 现有目录结构兼容
- 可渐进式迁移，不影响现有功能

**借鉴来源**：Claude Code 团队落地指南

#### 借鉴点 2：Karpathy 四原则

**技术点**：经过实战验证的四条核心原则：

| 原则 | 内容 | 解决的问题 |
|------|------|-----------|
| Think Before Coding | 先描述打算做什么、为什么 | 自信地犯蠢 |
| Simplicity First | 3 行能解决的别写 30 行 | 过度工程 |
| Surgical Changes | 只改需要改的，不顺手重构 | 范围蔓延 |
| Goal-Driven Execution | 持续对齐目标，偏离就停 | 跑偏 |

**实施方法**：
1. 将四原则写入 flow-kit 的核心 CLAUDE.md
2. 在 boundary-enforcer.sh 中添加四原则校验
3. 在 Phase 工作流中引用四原则作为行为准则

**预期改进效果**：
- 过度工程减少 60%（Simplicity First）
- 范围蔓延减少 80%（Surgical Changes）
- 方向偏离减少 70%（Goal-Driven Execution）

**兼容性分析**：
- 四原则作为行为准则，与现有系统完全兼容
- 可在 CLAUDE.md 中直接引用

**借鉴来源**：Karpathy 四原则 + Claude Code 最佳实践三件套

#### 借鉴点 3：五个神级 Skill

**技术点**：五个经过验证的 Claude Code Skill：

| Skill | 功能 | 效果 |
|-------|------|------|
| Caveman | 输出极简化 | Token 节省 75% |
| Memory Compiler | Hook 自动捕获→提取知识→编译知识库 | 每天省 1 小时 |
| Paper2Code | arxiv URL → citation-anchored 代码 | 论文→可运行代码 |
| CLAUDE.md Optimizer | 自动分析项目→生成上下文 | 比手写更精准 |
| TestDrift | 检测代码变更→自动生成测试 | TDD 流程不中断 |

**实施方法**：
1. Caveman 压缩模式应用于 Phase 执行输出和审计报告
2. Memory Compiler 的 Hook 自动捕获集成到 session-state
3. TestDrift 集成到 Phase 4（开发执行）的 TDD 循环

**预期改进效果**：
- Token 消耗降低 50%（Caveman 压缩）
- 知识沉淀自动化率提升 80%（Memory Compiler）
- 测试覆盖率提升 30%（TestDrift 自动生成）

**兼容性分析**：
- 五个 Skill 作为可选增强，不影响核心功能
- Caveman 和 Memory Compiler 可渐进式集成

**借鉴来源**：5 个 Claude Code 神级 Skill 分析

#### 借鉴点 4：7 天极简落地路线

**技术点**：团队从零到完整配置的 7 天路线：
- Day1: CLAUDE.md
- Day2: rules/
- Day3: /plan 命令
- Day4: /code-review 命令
- Day5: 提醒 Hook
- Day6: 一致性 Hook
- Day7: Agent 分工

**实施方法**：
1. 将 7 天路线适配为 flow-kit 的团队 onboarding 流程
2. 每天对应一个 Phase 或配置模块
3. 提供交互式教程引导

**预期改进效果**：
- 团队上手时间从 2 周缩短到 7 天
- 配置完整率从 40% 提升到 90%
- 团队协作规范一致性提升 70%

**兼容性分析**：
- 7 天路线作为 onboarding 指南，不影响核心功能
- 可在 GO.md 中添加 onboarding 入口

**借鉴来源**：Claude Code 团队落地指南 7 天路线

---

### 13.19 自进化引擎失效分析与修复

#### 借鉴点 1：五大断裂点诊断框架

**技术点**：self-improving-agent 被安装 43 万次，但真正形成完整进化闭环的用户不足 30%。五个关键节点在实践中全部断裂：

| 断裂点 | 现象 | 根因 |
|--------|------|------|
| Hook 提醒软化 | 提醒每次都出现，但从未转化为实际写入 | Agent 忙完后不被明确要求就不会主动写 |
| 纠正未记录 | 用户说"用 pnpm 不是 npm"，Agent 回"好的记住了"但 LEARNINGS.md 为空 | 没有 WAL 协议捕获修正 |
| 错误未分类 | Docker build 失败写入了每日日志，但 ERRORS.md 为空 | 每日日志与 .learnings/ 未打通 |
| 重复计数失效 | 同一个错误遇到 4 次，Recurrence-Count 始终为 1 | 前三个断裂点导致每次都被当作"第一次" |
| 知识映射缺失 | VFM 通过后 Agent 不知道该写入哪个文件 | SKILL.md 有分类规则但没有映射表 |

**实施方法**：
1. 在 F5（自我进化机制）中建立 WAL 协议：检测到触发条件 → 立即写入 → 再响应用户
2. 增强 session-state.json 增加 learnings 统计字段（lastLearningsReview、pendingLearningsCount、promotedLearningsCount、learningsStatus）
3. 每日复盘 Cron 增加读取 .learnings/ 三文件、识别 Recurrence-Count ≥ 3 的条目、执行 VFM 评分

**预期改进效果**：
- 经验捕获率从 <30% 提升到 >90%
- 重复错误减少 80%
- 知识沉淀自动化率提升 70%

**兼容性分析**：
- WAL 协议可集成到现有的 Hook 系统中
- session-state.json 扩展字段向后兼容
- VFM 评分模型可独立运行

**借鉴来源**：被安装43万次的"自我进化"skill 深度分析

#### 借鉴点 2：VFM 评分模型

**技术点**：VFM（Value-Frequency-Maintainability）三维评分决定是否将学习条目提升为永久规则。对 infra 类型错误需要偏向性加权。

**实施方法**：
1. 在 evolution-engine.sh 中实现 VFM 评分算法
2. 对 infra 类型错误自动上浮 1 分（高频度和失败减少各 +1）
3. 评分 ≥ 阈值自动 promote 到 SOUL/AGENTS/TOOLS

**预期改进效果**：
- 规则质量提升 50%（只保留高价值规则）
- 规则膨胀减少 60%（低价值规则不提升）
- infra 错误预防率提升 40%

**兼容性分析**：
- VFM 评分作为 evolution-engine.sh 的增强模块
- 与现有的护栏系统互补

**借鉴来源**：self-improving-agent VFM 评分模型

---

### 13.20 Multi-Pass Review 工程化

#### 借鉴点 1：多轮独立审查机制

**技术点**：Multi-Pass Review 通过多轮独立审查解决大模型代码审核的三种核心风险（漏报、误报、不确定性伪装成结论）。每轮输入一致、输出结构一致、审查规则一致，最终由聚合规则处理。

**实施方法**：
1. 在 Phase 6（代码审查）中引入 Multi-Pass 机制
2. 每轮独立审查输出结构化 JSON（severity/category/description/suggestion）
3. 聚合脚本化：共识问题优先进入主报告，单轮问题放入"待确认"区域

**预期改进效果**：
- 漏报率降低 60%（多轮交叉验证）
- 误报率降低 50%（独立审查避免上下文惯性）
- 审查可信度提升 70%（证据链 + 共识机制）

**兼容性分析**：
- Multi-Pass 机制可独立于现有审查流程运行
- 结构化输出格式与 REVIEW.md 兼容
- 聚合脚本可复用现有的 dispatch.sh 并行能力

**借鉴来源**：Multi-Pass Review Skill 深度解析

#### 借鉴点 2：审查 Schema 标准化

**技术点**：severity 只保留 HIGH/MEDIUM/LOW 三级，category 短而稳定（logic/security/error-handling/compatibility/performance/concurrency/resource-lifetime/test-coverage），description 必须写证据链，suggestion 必须可执行、最小化。

**实施方法**：
1. 建立 flow-kit 审查 Schema 标准文件（review-schema.yaml）
2. 在 Phase 6 工作流中强制使用标准 Schema
3. 聚合脚本按 Schema 字段自动分类和排序

**预期改进效果**：
- 审查输出一致性提升 80%
- 开发者处理审查意见效率提升 50%
- 自动化聚合准确率提升 90%

**兼容性分析**：
- Schema 标准作为配置文件，不影响现有功能
- 与 REVIEW.md 模板兼容

**借鉴来源**：Multi-Pass Review Schema 设计原则

---

### 13.21 DESIGN.md 设计系统与 grill-me 需求访谈

#### 借鉴点 1：DESIGN.md 结构化设计约束

**技术点**：68.4K Star 项目通过 DESIGN.md 提供结构化设计规则（颜色/间距/圆角/字体层级/组件规范），让 AI 生成前端从"凭感觉"到"遵循规范"。

**实施方法**：
1. 在 Phase 4（开发执行）中引入 DESIGN.md 风格的设计约束文件
2. 为 flow-kit 项目定义颜色系统、间距系统、组件规范
3. 在代码生成时自动引用 DESIGN.md 约束

**预期改进效果**：
- UI 一致性提升 80%（统一设计约束）
- 返工率降低 50%（首次生成即符合规范）
- 设计到代码的转化效率提升 3 倍

**兼容性分析**：
- DESIGN.md 作为可选配置文件，不影响核心功能
- 与现有的 .specs 目录结构兼容

**借鉴来源**：DESIGN.md 设计系统分析

#### 借鉴点 2：grill-me 需求访谈机制

**技术点**：78K Star 的 grill-me 用 10 行 Markdown 实现需求澄清，基于 RTF 框架（Role-Task-Format），每次只问一个问题，逐层深入直到双方理解一致。

**实施方法**：
1. 在 Phase 1（需求澄清）中集成 grill-me 机制
2. AI 主动追问直到双方理解一致，每次只问一个问题
3. 自动生成 CONTEXT.md 共享语言文档

**预期改进效果**：
- 需求理解偏差减少 60%
- 需求澄清时间缩短 40%（结构化追问）
- 返工率降低 50%（前期充分对齐）

**兼容性分析**：
- grill-me 机制可集成到现有的 confirmation-manager.sh 中
- 与 Phase 1 工作流完全兼容

**借鉴来源**：grill-me 需求访谈 Skill 分析

---

### 13.22 web-access 浏览器直连

#### 借鉴点 1：CDP 协议浏览器直连

**技术点**：web-access 是 Skill 而非 MCP——不只给工具，还给"怎么用工具"的完整方法论。三层能力：基础层（WebSearch/WebFetch/curl/Jina）、核心层（CDP 浏览器直连，登录态天然携带）、效率层（多任务并行分治）。

**实施方法**：
1. 在 Phase 0（研究）中集成 web-access 的并行调研能力
2. CDP 浏览器直连应用于 Phase 5（测试验证）的真实浏览器测试
3. "目标导向"的调度决策应用于 Phase 工作流的动态路由

**预期改进效果**：
- 调研效率提升 3 倍（并行子 Agent 调研）
- 登录态网站访问成功率提升 100%（CDP 直连）
- 动态渲染页面抓取准确率提升 90%

**兼容性分析**：
- web-access 作为可选 Skill，不影响核心功能
- CDP Proxy 需要 Node.js ≥ 22 环境

**借鉴来源**：web-access Skill 深度分析

---

### 13.23 RTK 与 OpenWolf 成本优化

#### 借鉴点 1：RTK 终端输出净化

**技术点**：RTK 通过四层净化策略（智能过滤/分组聚合/智能截断/去重合并）实现 Token 节省 89%。30 分钟开发会话从 15 万 Token 压至 1.7 万。零侵入设计：单二进制文件、不改代码。

**实施方法**：
1. 在 Phase 4（开发执行）的工具调用层集成 RTK 净化
2. 配置全局 Hook 自动过滤终端输出
3. 在 session-state 中记录 Token 节省统计

**预期改进效果**：
- Token 消耗降低 80-89%
- 开发会话成本降低 60%
- 上下文利用率提升 3 倍

**兼容性分析**：
- RTK 作为独立二进制工具，零侵入集成
- 通过 Hook 机制与 flow-kit 无缝对接

**借鉴来源**：RTK 终端输出净化工具分析

#### 借鉴点 2：OpenWolf 三文件记忆系统

**技术点**：OpenWolf 通过 anatomy.md（项目地图）、cerebrum.md（学习记忆 + Do-Not-Repeat 列表）、buglog.json（Bug 账本）三文件实现 Token 节省 80%。相同任务裸 Claude CLI 消耗 250 万 Token，OpenWolf + Claude CLI 仅 42.5 万。

**实施方法**：
1. anatomy.md 项目地图替代重复的文件探索
2. cerebrum.md 的 Do-Not-Repeat 列表集成到 session-state.json
3. buglog.json 的 Bug 自动归档集成到 F5（自我进化机制）

**预期改进效果**：
- 重复文件读取减少 71%
- Token 消耗降低 65-80%
- Bug 修复效率提升 50%（自动归档复用）

**兼容性分析**：
- OpenWolf 通过 6 个 Hook 脚本工作，与 flow-kit Hook 系统兼容
- 三文件格式可适配 flow-kit 的 .flow-kit 目录

**借鉴来源**：OpenWolf Claude Code 第二大脑分析

---

### 13.24 Google Agent Skills

#### 借鉴点 1：六阶段开发链路

**技术点**：Google Gemini 团队主管 Addy Osmani 开源，23000+ Star。将《Software Engineering at Google》中的工程方法论封装为 20 个 Skill + 7 个 Slash 命令 + 3 个 Agent 人设。六阶段链路：`/spec` → `/plan` → `/build` → `/test` → `/review` → `/ship`。

**实施方法**：
1. flow-kit 的 Phase 体系与 Agent Skills 的六阶段链路对齐
2. 借鉴"三个预设 Agent 并行审查"机制增强 Phase 6（代码审查）
3. 跨平台支持（Claude Code/Cursor/Gemini CLI/Windsurf/Codex）的设计思路

**预期改进效果**：
- 开发流程标准化程度提升 50%
- 代码审查覆盖率提升 40%（三 Agent 并行）
- 跨平台兼容性提升 80%

**兼容性分析**：
- 六阶段链路与 flow-kit 的 Phase 0-7 高度契合
- Agent Skills 的"纪律约束"理念与 flow-kit 护栏机制一致

**借鉴来源**：Google Agent Skills 分析

---

### 13.25 Subagent Fork 机制

#### 借鉴点 1：Fork 模式上下文继承

**技术点**：`CLAUDE_CODE_FORK_SUBAGENT=1` 环境变量开启 Fork 模式，Subagent 继承父代理完整对话上下文，共享 prompt cache 前缀，第 2 到 N 个子代理输入成本明显更低。仍在隔离环境中运行，最终只返回结论性摘要。

**实施方法**：
1. Phase 4（开发执行）的 Subagent 默认启用 Fork 模式
2. context-timeline Hook 集成到 flow-kit 的上下文监控
3. Subagent 放置位置优先级（项目级 .claude/agents/ > 个人级 ~/.claude/agents/）

**预期改进效果**：
- Subagent 执行质量提升 40%（继承完整上下文）
- 子代理输入成本降低 50%（共享 prompt cache）
- 上下文管理可视化程度提升 100%

**兼容性分析**：
- Fork 模式通过环境变量控制，不影响现有 Subagent 行为
- context-timeline 作为可选 Hook，不影响核心功能

**借鉴来源**：Claude Code Subagent Fork 模式分析

---

### 13.26 Command Center 目标管理层

#### 借鉴点 1：从管终端到管目标

**技术点**：当 Agent 足够强大时，真正的瓶颈不再是 AI 能不能完成任务，而是人类能不能高效管理多个并行任务。Command Center 通过迭代式 Kanban（任务在"Your Turn"和"Claude's Turn"之间反复弹跳）、智能上下文感知（自动回顾历史）、三种任务深度（Quick Task/Campaign/Deep Build）解决此问题。

**实施方法**：
1. flow-kit 的 Phase 管理借鉴"Your Turn / Claude's Turn"的迭代模式
2. 任务深度分级（Quick/Campaign/Deep Build）应用于 Phase 路由判断
3. 智能上下文感知（自动回顾历史）集成到 session-state 恢复机制

**预期改进效果**：
- 多任务管理效率提升 3 倍
- 上下文恢复准确率提升 60%
- 任务路由智能化程度提升 50%

**兼容性分析**：
- "管目标不管终端"的理念与 flow-kit 的 Markdown 驱动架构一致
- 任务深度分级可集成到现有的 Phase 路由系统

**借鉴来源**：Command Center 目标管理层分析

---

> **文档结束**
>
> 本文档由 Claude Code 基于 flow-kit v3.3.0 项目分析生成
>
> **参考项目**：
> - superpowers-zh (https://github.com/jnMetaCode/superpowers-zh) — 编程开发能力增强方法、workflow-runner YAML 编排、executing-plans 审查检查点
> - agency-agents-zh (https://github.com/jnMetaCode/agency-agents-zh) — 215 个 AI 专家角色定义、DAG 工作流编排、ADR 架构决策记录
> - mattpocock/skills (https://github.com/mattpocock/skills) — grill-me 需求澄清、tdd 红-绿-重构、diagnose 系统化诊断、to-prd PRD 生成、improve-codebase-architecture 架构改进、caveman 极简通信、handoff 会话交接
> - Ralph.md (y:\pwl\code\plugins\Ralph.md) — 三种自主运行模式、Ralph Wiggum 循环、四个安全 Hook、tmux 会话持久化、CI/CD cron 集成、已知陷阱预防
> - superpowers+gstack 开发流程实录 (y:\pwl\code\plugins\Claude Code + superpowers + gstack 开发流程实录，可直接复制使用，一篇文章讲清楚！.md) — 大脑 vs 手脚分工模型、任务分流规则、四个关键交接点、五条铁律、Cheatsheet 速查卡
> - code-review-graph — 知识图谱精准上下文、爆炸半径分析、Token 减少 6.8 倍
> - Alan 全局路由协议 — Superpowers 与 gstack 分工、AGENTS.md 优先级链、防双主流程
> - SDD 三阶段工作流 — gstack 规划 + superpowers 开发 + gstack 发布
> - Claude Code 生产交付全流程 — 反向技术架构、渐进式需求加载、记忆分层、日志调试
> - Agentic Coding 工作流 — TOC 约束理论、Spec-Driven Incremental Deploy、checkpoint YAML
> - OMC vs Karpathy — 硬控制与软约束三层融合模型
> - OpenSpace 自进化引擎 — AUTO-FIX/AUTO-IMPROVE/AUTO-LEARN 三机制
> - Skill-creator 评估体系 — MANDATORY/RECOMMENDED/OPTIONAL 三级指令、基准测试、多代理并行测试
> - Ralph Wiggum (https://github.com/nicepkg/ralph) — 每次迭代全新 AI 实例、三级反馈闭环、prd.json 状态追踪
> - Karpathy AutoResearch (https://github.com/karpathy/auto-research) — 三文件架构、多 Agent 交叉审核、5 维度加权评分
> - Compound Engineering (https://github.com/compound-engineering/agent) — 知识沉淀机制、12 子代理并行审查、learnings-researcher 自动引用
> - rihebty/flow-kit (https://github.com/rihebty/flow-kit) — 四闸门机制、@GO.md 零例外路由、H-hotfix 三档响应、R-recall 记忆恢复、Secrets 保护
> - Claude Code 团队落地指南 — 七大核心构件、Karpathy 四原则、五个神级 Skill、7 天极简落地路线