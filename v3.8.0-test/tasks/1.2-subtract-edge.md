# 任务 1.2：添加 subtract 边界测试

## 依赖
1.1

## 目标
添加边界条件测试。

## 实现步骤

### RED
1. 在 `tests/subtract.test.mjs` 添加测试：
   - `subtract(0, 0) === 0`
   - `subtract(-1, -1) === 0`
   - `subtract(5.5, 0.5) === 5`
2. 运行 → 全部通过（已有实现）

### GREEN
无新代码（测试通过即完成）

## 推荐技能
- RED 阶段：`/test-driven-development`

## 验证
- `npx vitest run` 全部通过
