# 任务 1.1：创建 subtract.mjs + 测试

## 依赖
无

## 目标
TDD 方式创建 `subtract.mjs` 模块，导出 `subtract(a, b)`。

## 实现步骤

### RED（测试先行）
1. 创建 `tests/subtract.test.mjs`
2. 写测试：`subtract(3, 1) === 2`
3. 运行 `npx vitest run` → 确认失败（模块不存在）

### GREEN（最小实现）
1. 创建 `subtract.mjs`，导出 `subtract(a, b) { return a - b }`
2. 运行 `npx vitest run` → 确认通过

### REFACTOR
1. 代码已最简，无需重构

## 推荐技能
- RED 阶段：`/test-driven-development`
- 打 [x] 前：`/verification-before-completion`

## 验证
- `npx vitest run` 通过
- `subtract.mjs` 存在
