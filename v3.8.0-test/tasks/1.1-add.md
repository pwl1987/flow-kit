# 任务 1.1：创建 add.mjs + 测试

## 依赖
无

## 目标
TDD 方式创建 `add.mjs` 模块。

## 实现步骤

### RED（测试先行）
1. 创建 `tests/add.test.mjs`
2. 写测试：`add(1, 2) === 3`
3. 运行 → 确认失败（模块不存在）

### GREEN（最小实现）
1. 创建 `add.mjs`，导出 `add(a, b) { return a + b }`
2. 运行 → 确认通过

### REFACTOR
1. 清理

## 推荐技能
- RED 阶段：`/test-driven-development`
- 实现后：`/requesting-code-review`

## 验证
- `npx vitest run` 通过
- `add.mjs` 存在
