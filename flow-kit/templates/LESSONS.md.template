> 【CLAUDE CODE INSTRUCTION 强制约束】
> 本文件为 flow-kit 工作流模板。所有变更必须通过 8 阶段工作流管理。

--- BEGIN flow-kit/templates/LESSONS.md.template ---

# TIL — Today I Learned

> flow-kit 经验沉淀模板，使用 YAML 结构化格式，便于检索和自动处理。

## 文档信息

```yaml
id: {{TIL_ID}}           # 格式: til-{phase}-{YYYYMMDD}-{n}
type: {{TIL_TYPE}}        # success | failure | pattern | decision
severity: {{SEVERITY}}    # critical | major | minor | info
phase: {{PHASE_NAME}}
date: {{DATE}}
author: {{AUTHOR}}
tags: [{{TAG_1}}, {{TAG_2}}, {{TAG_3}}]
```

---

## 经验条目

### TIL-{{TIL_ID}}

```yaml
id: til-{{PHASE}}-{{YYYYMMDD}}-{{N}}
type: {{success|failure|pattern|decision}}
severity: {{critical|major|minor|info}}

context:
  phase: {{PHASE_NAME}}
  task: {{TASK_DESCRIPTION}}
  project_type: {{brownfield|greenfield|light-brownfield}}

problem: |
  {{PROBLEM_DESCRIPTION}}
  多行描述问题现象。

root_cause: |
  {{ROOT_CAUSE_ANALYSIS}}
  分析根本原因。

solution: |
  {{SOLUTION_DESCRIPTION}}
  具体修复措施。

prevention: |
  {{PREVENTION_MEASURES}}
  如何避免同类问题。

references:
  - {{RELATED_DOC_1}}
  - {{RELATED_DOC_2}}

timestamp: "{{TIMESTAMP}}"
```

---

## 使用说明

1. 每条 TIL 使用 YAML 格式，包含 id/type/severity/context/problem/root_cause/solution/prevention/tags/timestamp
2. 按 phase 分类存储，便于后续 L2 知识注入检索
3. 关键经验标记 severity: critical，便于优先查阅
4. context.project_type 字段支持按项目类型筛选适用经验

---

## 提名条件（满足任一即入库）

- 调试耗时 > 30 分钟
- 非微细节（涉及架构/设计模式/流程）
- 6 个月内可能重试
- 丢失则存在重复踩坑风险

**禁止入库**：
- 一次性笔误
- 业务独有规则（应入 CONTEXT.md）
- 已在 ADR 中详述的架构权衡

---

## 复核剪枝机制

- **栈变更检查**：技术栈升级时，复核相关 TIL 是否仍然有效
- **superseded 标记**：若新方案完全替代旧方案，标记旧 TIL 为 superseded
- **季度剪枝**：每季度清理已superseded或过时的TIL

---

## 参考来源

- [rihebty/flow-kit](https://github.com/rihebty/flow-kit) — LESSONS 提名条件与复核剪枝机制
- [bytedance/deer-flow](https://github.com/bytedance/deer-flow) — 结构化记忆 TIL 格式

--- END flow-kit/templates/LESSONS.md.template ---