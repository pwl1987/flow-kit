---
description: flow-kit resume 命令
category: dev
reference: flow-kit/commands/resume.md
execute: bash -c 'source flow-kit/lib/session-state.sh && session_resume_prompt'
---

/flow-kit:resume: 恢复上次会话状态。读取 session-state.json 输出当前变更、阶段、任务进度和下一步。
