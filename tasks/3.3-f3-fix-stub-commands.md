# 任务 3.3：F3: 修复 stub 命令

**文件**：
- `flow-kit/flow-kit.sh`，`flow-kit/scripts/`（相关脚本）

**类型**：修改
**依赖**：1.1

## 功能描述
修复 7 个 stub 命令（health/scan/cost-report/estimate-tokens/check-expiry/update-context/p0），将 echo 占位替换为实际脚本调用或逻辑实现。新增 7 测试。

## 验证标准
- [ ] 7 个命令执行实际逻辑而非 echo、参数正确传递、退出码正确
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- 脚本文件不存在时的降级、参数透传、退出码传播
