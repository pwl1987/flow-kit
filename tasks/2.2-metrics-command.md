# 任务 2.2：/metrics 命令

**文件**：
- `flow-kit/scripts/metrics.sh`（新建），`flow-kit/tests/unit/metrics-cmd.test.js`（新建）

**类型**：新建
**依赖**：2.1

## 功能描述
实现 --summary/--detail/--clear CLI 子命令。source metrics-logger.sh 复用核心逻辑。新增 3 测试。

## 验证标准
- [ ] --summary 输出聚合摘要、--clear 清空日志文件、无数据时不报错
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- 无数据文件时友好提示、权限不足、超大文件性能
