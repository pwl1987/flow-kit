# 任务 1.2：F9: 临时文件清理

**文件**：
- `flow-kit/lib/cleanup.sh`，`flow-kit/lib/error-handler.sh`，`flow-kit/tests/unit/cleanup.test.js`

**类型**：修改
**依赖**：1.1

## 功能描述
cleanup.sh 添加 with_cleanup wrapper，通过 setup_trap 自动注册 EXIT 清理。迁移 14 个脚本的 mktemp 模式为统一调用。新增 5+ 测试。

## 验证标准
- [ ] with_cleanup 注册后 EXIT 时自动删除临时文件、mktemp 文件在脚本异常退出时被清理
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- trap EXIT 冲突（已有 trap 的脚本）、并发 mktemp 竞争、信号中断时清理
