# 任务 4.6：F12: 缩进统一

**文件**：
- 全部 `.sh` 文件

**类型**：修改
**依赖**：4.4

## 功能描述
shfmt -i 4 -w 统一全部 .sh 文件缩进为 4 空格。格式化专用 commit（不混合功能变更）。必须是最后一个功能性变更。

## 验证标准
- [ ] 所有 .sh 文件缩进一致、git diff 仅含空白变更
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- shfmt 未安装时降级为手动检查提示
