# 任务 2.3：指标埋点

**文件**：
- `flow-kit/scripts/phase-executor.sh`，`flow-kit/scripts/auto-pilot.sh`，`flow-kit/tests/unit/phase-executor.test.js`

**类型**：修改
**依赖**：2.1

## 功能描述
在 phase-executor 和 auto-pilot 主执行流程包装 metrics_log_event start/complete 调用。用 $SECONDS 计算耗时。新增 3 测试。

## 验证标准
- [ ] phase 执行前后各有一条 metrics 日志、metrics-logger 不可 source 时不影响正常执行
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- metrics-logger.sh 缺失时静默降级（不中断主流程）、长时间运行脚本的时间溢出
