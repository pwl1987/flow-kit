# 任务 4.1：F7: phase-executor 拆分

**文件**：
- `flow-kit/scripts/phase-executor.sh`

**类型**：修改
**依赖**：3.1

## 功能描述
提取 validate_phase_input、render_phase_prompt、load_workflow_file、persist_phase 四个函数。每个函数 ≤30 行。main 函数 ≤20 行。拆分后所有现有测试仍通过。

## 验证标准
- [ ] 函数提取后测试全通过、main ≤20 行、无全局变量泄漏
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- 拆分后行为不变（依赖 3.1 测试覆盖保障）
