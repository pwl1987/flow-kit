# 任务 2.1：指标日志库

**文件**：
- `flow-kit/lib/metrics-logger.sh`（新建），`flow-kit/tests/unit/metrics-logger.test.js`（新建）

**类型**：新建
**依赖**：1.1

## 功能描述
metrics_log_event 以 JSONL 格式追加写入 `.flow-kit/logs/metrics.jsonl`。metrics_query/metrics_summary 提供按类型/时间范围的聚合查询。新增 8 测试。

## 验证标准
- [ ] metrics_log_event 写入合法 JSONL 行、metrics_summary 输出聚合结果、日志目录不存在时自动创建
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- 磁盘满时写入失败处理、JSONL 文件损坏时的查询降级、并发写入竞争、空文件查询
