# 任务 1.4：F4: 验证 set -euo pipefail

**文件**：
- `flow-kit/hooks/*.sh`，`flow-kit/tests/unit/hooks.test.js`

**类型**：验证
**依赖**：无

## 功能描述
验证所有 hooks 已有 `set -euo pipefail`（v1.12.8 已添加）。添加 grep 校验测试。可能无代码变更。

## 验证标准
- [ ] grep -L "set -euo pipefail" 返回空、测试断言所有 .sh 含该行
- [ ] ShellCheck 零 error
- [ ] 测试通过

## 边界条件
- 无 hooks 目录、空文件
