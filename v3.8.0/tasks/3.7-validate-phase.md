# 任务 3.7：validate-phase 模块重写

## 依赖
2.3（front-matter），2.1（session-state）

## 目标
将 `scripts/validate-phase.sh`（650行）重写为 `src/lib/validate-phase.mjs`。实现阶段产物验证、证据记录和 schema 校验。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| validateArtifacts | `validateArtifacts(phase)` | 验证阶段产物 |
| recordEvidence | `recordEvidence(phase, data)` | 记录验证证据 |
| validateSchema | `validateSchema(phase, output)` | 校验产物 schema |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/validate-phase.test.mjs`
2. 创建测试夹具：各 phase 的 front matter 文件，含 expected_artifacts 定义
3. 测试 `validateArtifacts`：所有产物存在时返回 `{valid: true, missing: []}`
4. 测试 `validateArtifacts`：部分产物缺失时返回 `{valid: false, missing: ['file.md']}`
5. 测试 `validateArtifacts`：从 front matter 读取 expected_artifacts 列表
6. 测试 `validateArtifacts`：无 expected_artifacts 时默认通过
7. 测试 `recordEvidence`：将验证结果写入 session state
8. 测试 `recordEvidence`：记录时间戳和验证详情
9. 测试 `validateSchema`：产物内容符合 schema 定义
10. 测试 `validateSchema`：schema 不匹配时返回具体错误字段

### GREEN（最小实现）
1. 创建 `src/lib/validate-phase.mjs`
2. 实现 `validateArtifacts(phase)`：读取 front matter → 检查文件存在性 → 返回结果
3. 实现 `recordEvidence(phase, data)`：写入 session state 的 evidence 数组
4. 实现 `validateSchema(phase, output)`：基础 schema 校验（必要字段检查）
5. 定义各 phase 的默认 schema

### REFACTOR
1. Schema 定义外部化为 JSON 文件
2. 提取文件存在性检查为通用函数
3. 添加 schema 版本兼容处理

## 推荐技能
- 实现前：`/writing-plans`（650 行 Shell，9 个 schema 验证）
- 实现后：`/requesting-code-review`
- 打 [x] 前：`/smart-commit`（规范 git commit）

## 验证
- `npx vitest run tests/unit/validate-phase.test.mjs` 通过
- 各 phase 产物验证逻辑正确
- 缺失产物准确报告

## 涉及文件
- 创建：`src/lib/validate-phase.mjs`
- 创建：`tests/unit/validate-phase.test.mjs`
- 参考：`scripts/validate-phase.sh`
