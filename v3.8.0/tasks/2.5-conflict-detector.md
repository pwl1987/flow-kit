# 任务 2.5：conflict-detector 模块重写

## 依赖
2.1（session-state）

## 目标
将 `lib/conflict-detector.sh`（221行）重写为 `src/lib/conflict-detector.mjs`。实现需求分类、冲突检测、文件冲突检测和冲突决策处理。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| classifyRequirement | `classifyRequirement(req)` | 分类需求类型 |
| detectConflict | `detectConflict(newReq)` | 检测新需求与现有需求的冲突 |
| detectFileConflict | `detectFileConflict(targetModule)` | 检测文件级冲突 |
| handleConflictDecision | `handleConflictDecision(choice)` | 处理冲突决策 |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/conflict-detector.test.mjs`
2. 测试 `classifyRequirement`：正确分类为 feature/bugfix/refactor/docs
3. 测试 `classifyRequirement`：边界输入（空字符串、未知类型）
4. 测试 `detectConflict`：检测功能重叠冲突
5. 测试 `detectConflict`：检测范围冲突（同一文件不同需求）
6. 测试 `detectConflict`：无冲突时返回 null
7. 测试 `detectFileConflict`：检测目标模块已被占用
8. 测试 `detectFileConflict`：未占用时返回 null
9. 测试 `handleConflictDecision`：处理 merge/override/defer 决策
10. 测试与 session-state 集成：读写 session 中的冲突记录

### GREEN（最小实现）
1. 创建 `src/lib/conflict-detector.mjs`
2. 实现需求分类：关键词匹配 → 类型映射
3. 实现 `detectConflict`：读取 session 中已记录的需求，比对范围
4. 实现 `detectFileConflict`：扫描当前活跃需求涉及的文件列表
5. 实现 `handleConflictDecision`：根据决策更新 session state

### REFACTOR
1. 提取分类规则为可配置映射
2. 冲突检测算法优化：从 O(n²) 降至 O(n)
3. 统一冲突报告格式

## 验证
- `npx vitest run tests/unit/conflict-detector.test.mjs` 通过
- 需求分类准确率覆盖主要类型
- 冲突检测无遗漏

## 涉及文件
- 创建：`src/lib/conflict-detector.mjs`
- 创建：`tests/unit/conflict-detector.test.mjs`
- 参考：`lib/conflict-detector.sh`
