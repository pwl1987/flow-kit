# 任务 3.5：command-gen 模块重写

## 依赖
1.1（项目基础设施），2.3（front-matter）

## 目标
将 `scripts/generate-commands.sh`（293行）重写为 `src/lib/command-gen.mjs`。实现斜杠命令生成和注册。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| generateEntry | `generateEntry(cmdName, desc, ref, outputFile)` | 生成单个命令条目 |
| generatePhaseEntry | `generatePhaseEntry(phase, name, phaseDir, outputFile)` | 生成阶段命令条目 |
| registerAllCommands | `registerAllCommands()` | 注册所有命令 |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/command-gen.test.mjs`
2. 测试 `generateEntry`：生成正确格式的命令条目
3. 测试 `generateEntry`：追加到输出文件末尾
4. 测试 `generateEntry`：outputFile 不存在时自动创建
5. 测试 `generatePhaseEntry`：从 phase 文件读取 front matter 描述
6. 测试 `generatePhaseEntry`：phase 文件无 front matter 时使用默认描述
7. 测试 `registerAllCommands`：生成所有核心命令 + 阶段命令
8. 测试 `registerAllCommands`：输出文件格式符合 Claude Code 斜杠命令规范
9. 测试 `registerAllCommands`：重复注册时覆盖而非追加

### GREEN（最小实现）
1. 创建 `src/lib/command-gen.mjs`
2. 实现命令模板：核心命令和阶段命令的模板字符串
3. 实现 `generateEntry(cmdName, desc, ref, outputFile)`：格式化 + 写入
4. 实现 `generatePhaseEntry(phase, name, phaseDir, outputFile)`：读取 front matter + 格式化 + 写入
5. 实现 `registerAllCommands()`：遍历所有命令定义 → 逐个生成 → 写入输出文件

### REFACTOR
1. 命令模板外部化为 JSON 配置
2. 提取文件写入逻辑，支持原子替换
3. 添加命令冲突检测

## 验证
- `npx vitest run tests/unit/command-gen.test.mjs` 通过
- 生成的命令文件格式正确
- 所有 22 个命令均可生成

## 涉及文件
- 创建：`src/lib/command-gen.mjs`
- 创建：`tests/unit/command-gen.test.mjs`
- 参考：`scripts/generate-commands.sh`
