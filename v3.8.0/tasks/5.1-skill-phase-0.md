# 任务 5.1: 迁移 Phase 0 为 Skill 格式

## 依赖
4.19

## 目标
迁移 `phases/0-init.md` → `skills/phase-0/SKILL.md`。添加 YAML frontmatter：`name=phase-0`、`description=初始化阶段`、`disable-model-invocation=true`。保留原始 phase 内容为 `reference.md`。

## 实现步骤

### RED（测试先行）
1. 编写测试验证 `skills/phase-0/SKILL.md` 存在且包含 YAML frontmatter
2. 验证 frontmatter 中 `name: phase-0`、`description: 初始化阶段`、`disable-model-invocation: true` 字段存在
3. 验证 `skills/phase-0/reference.md` 存在且内容与原 `phases/0-init.md` 一致

### GREEN（最小实现）
1. 创建 `skills/phase-0/` 目录
2. 从 `phases/0-init.md` 读取原始内容
3. 创建 `skills/phase-0/SKILL.md`，顶部添加 YAML frontmatter：
   ```yaml
   ---
   name: phase-0
   description: 初始化阶段
   disable-model-invocation: true
   ---
   ```
4. 将原始 phase 内容写入 `skills/phase-0/reference.md`

### REFACTOR
1. 检查 frontmatter 格式与其他 skill 一致
2. 确认引用路径正确

## 验证
- `npx vitest run` 通过
- `cat skills/phase-0/SKILL.md | head -10` 显示正确 frontmatter
- `diff phases/0-init.md skills/phase-0/reference.md` 无差异

## 涉及文件
- `skills/phase-0/SKILL.md`（新建）
- `skills/phase-0/reference.md`（新建）
