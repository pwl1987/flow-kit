# 任务 5.7: 创建 recall Skill

## 依赖
4.19

## 目标
创建 `skills/recall/SKILL.md`。使用 `!`command`` 动态注入 git log 输出。引用 `flow_kit_recall` MCP 工具。

## 实现步骤

### RED（测试先行）
1. 编写测试验证 `skills/recall/SKILL.md` 存在
2. 验证 frontmatter 包含 `name: recall`、`description` 字段
3. 验证 SKILL.md body 中包含 `!`command`` 动态注入语法
4. 验证引用了 `flow_kit_recall` MCP 工具

### GREEN（最小实现）
1. 创建 `skills/recall/` 目录
2. 创建 `skills/recall/SKILL.md`，包含：
   ```yaml
   ---
   name: recall
   description: 会话记忆召回
   ---
   ```
3. Body 中添加动态注入：
   ```
   !`git log --oneline -20 --format="%h %s (%cr)"`
   ```
4. 添加 MCP 工具调用说明：使用 `flow_kit_recall` 工具检索历史会话状态

### REFACTOR
1. 确认动态注入语法符合 skill 规范
2. 验证 git log 输出格式可读性

## 验证
- `npx vitest run` 通过
- `cat skills/recall/SKILL.md` 包含动态注入语法
- `/flow-kit:recall` 命令能正常加载并执行

## 涉及文件
- `skills/recall/SKILL.md`（新建）
