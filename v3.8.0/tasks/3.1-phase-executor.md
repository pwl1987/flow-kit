# 任务 3.1：phase-executor 模块重写

## 依赖
2.1（session-state），2.3（front-matter）

## 目标
将 `scripts/phase-executor.sh`（113行）重写为 `src/lib/phase-executor.mjs`。实现阶段加载、执行和状态记录。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| runPhase | `runPhase(phaseNum)` | 执行指定阶段 |
| loadWorkflow | `loadWorkflow(phaseFile)` | 加载工作流文件 |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/phase-executor.test.mjs`
2. 创建测试夹具：模拟 phase 文件（含 front matter）和 workflow 文件
3. 测试 `loadWorkflow`：解析含 front matter 的 phase 文件，返回工作流配置
4. 测试 `loadWorkflow`：文件不存在时抛出明确错误
5. 测试 `runPhase`：读取 session state，更新当前 phase
6. 测试 `runPhase`：记录 phase 变更到 history
7. 测试 `runPhase`：加载对应 workflow 文件内容
8. 测试 `runPhase`：无效 phase 编号时抛出错误

### GREEN（最小实现）
1. 创建 `src/lib/phase-executor.mjs`
2. 实现 `loadWorkflow(phaseFile)`：读取文件 → parseFrontMatter → 返回结构化配置
3. 实现 `runPhase(phaseNum)`：sessionGet → 验证 phase → sessionSet → sessionHistoryAdd → loadWorkflow
4. 集成 front-matter 解析和 session-state 操作

### REFACTOR
1. 提取 phase 编号验证为独立函数
2. 统一 workflow 文件路径解析逻辑
3. 添加 phase 转换合法性校验

## 验证
- `npx vitest run tests/unit/phase-executor.test.mjs` 通过
- phase 0-8 均可正确加载
- session state 正确记录 phase 变更

## 涉及文件
- 创建：`src/lib/phase-executor.mjs`
- 创建：`tests/unit/phase-executor.test.mjs`
- 参考：`scripts/phase-executor.sh`
