# 任务 5.10: 迁移 plan-generate → skills/plan-generate/SKILL.md

## 依赖
4.19

## 目标
将 `scripts/plan-generate.sh`(442行) 迁移为 `skills/plan-generate/SKILL.md` 目录结构格式。

## 实现步骤

### RED（测试先行）
1. 验证 `skills/plan-generate/SKILL.md` 不存在
2. 验证 `skills/plan-generate/` 目录不存在

### GREEN（最小实现）
1. 创建 `skills/plan-generate/` 目录
2. 从 `scripts/plan-generate.sh` 提取核心工作流逻辑
3. 创建 `skills/plan-generate/SKILL.md`，包含：
   - frontmatter: `name: plan-generate`, `description: 解析 REVIEW.md 生成分模块开发计划`
   - 工作流步骤（从 Shell 脚本提取）
   - `disable-model-invocation: true`（只允许用户手动触发）
4. 创建 `skills/plan-generate/reference.md`（可选，放详细指引）

### REFACTOR
1. 对比 Shell 版本和 Skill 版本，确认功能等价
2. 清理 Shell 特有的路径处理（Skill 中由 MCP 工具处理）

## 推荐技能
- 实现前：`/writing-skills`（442 行 Shell → SKILL.md 迁移方法论）
- 实现后：`/requesting-code-review`
- 打 [x] 前：`/smart-commit`（规范 git commit）

## 验证
- `ls skills/plan-generate/SKILL.md` 存在
- SKILL.md frontmatter 格式正确
- `/flow-kit:plan-generate` 可被发现

## 涉及文件
- `skills/plan-generate/SKILL.md`（新建）
- `skills/plan-generate/reference.md`（新建，可选）
