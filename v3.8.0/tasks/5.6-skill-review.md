# 任务 5.6: 迁移 code-review 为 Skill 格式

## 依赖
4.19

## 目标
迁移 `skills/code-review.md` → `skills/code-review/SKILL.md`（目录化）。添加 YAML frontmatter：`context: fork`、`agent: reviewer`。

## 实现步骤

### RED（测试先行）
1. 编写测试验证 `skills/code-review/SKILL.md` 存在
2. 验证 frontmatter 包含 `name: code-review`、`context: fork`、`agent: reviewer`
3. 验证内容与原 `skills/code-review.md` 等价

### GREEN（最小实现）
1. 创建 `skills/code-review/` 目录
2. 读取 `skills/code-review.md` 原有内容
3. 创建 `skills/code-review/SKILL.md`，添加 frontmatter：
   ```yaml
   ---
   name: code-review
   description: 代码审查
   context: fork
   agent: reviewer
   ---
   ```
4. 将原有内容整合进 SKILL.md body
5. 删除旧 `skills/code-review.md` 单文件

### REFACTOR
1. 确认引用路径更新（如有其他文件引用旧路径）
2. 验证 `/flow-kit:code-review` 命令仍能正确加载

## 推荐技能
- 实现前：`/writing-skills`（435 行 Shell → SKILL.md 迁移方法论）
- 实现后：`/requesting-code-review`
- 打 [x] 前：`/smart-commit`（规范 git commit）

## 验证
- `npx vitest run` 通过
- `cat skills/code-review/SKILL.md | head -10` 显示正确 frontmatter
- 旧文件 `skills/code-review.md` 已删除
- 斜杠命令 `/flow-kit:code-review` 正常工作

## 涉及文件
- `skills/code-review/SKILL.md`（新建）
- `skills/code-review.md`（删除）
