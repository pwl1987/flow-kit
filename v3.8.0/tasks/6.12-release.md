# 任务 6.12: 最终发布

## 依赖
6.10

## 目标
最终发布。更新 VERSION → v3.8.0。更新 CHANGELOG.md（新增 [3.8.0] 段）。更新 CLAUDE.md 版本号。运行 npm run build 最终验证。运行 npm test 全量验证。确认 dist/mcp-server.cjs < 2MB。

## 实现步骤

### RED（测试先行）
1. 编写版本检查脚本或测试用例：
   - 验证 `VERSION` 文件内容为 `v3.8.0`
   - 验证 `CHANGELOG.md` 包含 `[3.8.0]` 段
   - 验证 `CLAUDE.md` 版本号为 `v3.8.0`
   - 验证 `package.json` version 字段为 `3.8.0`

### GREEN（最小实现）
1. 更新 `VERSION` 文件：`v3.8.0`
2. 更新 `CHANGELOG.md`：在文件顶部新增 `[3.8.0]` 段
   - 列出所有新功能：MCP Server、12 个工具、5 个 hook、会话记忆
   - 列出破坏性变更（如有）
   - 列出修复项（如有）
3. 更新 `CLAUDE.md`：版本号引用从 `v3.7.0` → `v3.8.0`
4. 更新 `package.json`：`version: "3.8.0"`
5. 运行 `npm run build`：确认构建成功，无警告
6. 检查产物大小：`ls -lh dist/mcp-server.cjs` 确认 < 2MB
7. 运行 `npm test`：全量测试通过

## 推荐技能
- 开始前：`/finishing-a-development-branch`（分支收尾 + 发布流程）
- 完成后：`/smart-commit`（规范 git commit）

## 验证
- `npm run build` 成功，无错误/警告
- `npm test` 全部通过（单元 + 集成 + E2E）
- `cat VERSION` → `v3.8.0`
- `dist/mcp-server.cjs` 文件大小 < 2MB
- `CHANGELOG.md` 包含 `[3.8.0]` 段且格式正确
- `CLAUDE.md` 版本号已更新为 `v3.8.0`
- `git diff --stat` 确认变更文件清单合理

## 涉及文件
- `VERSION`（修改）
- `CHANGELOG.md`（修改）
- `CLAUDE.md`（修改）
- `package.json`（修改）
- `dist/mcp-server.cjs`（构建产物，验证大小）
