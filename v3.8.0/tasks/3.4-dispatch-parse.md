# 任务 3.4：dispatch-parse 模块重写

## 依赖
1.2（基础设施）

## 目标
将 `scripts/dispatch-parse.sh`（90行）重写为 `src/lib/dispatch-parse.mjs`。实现 JSON manifest 解析。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| parseManifest | `parseManifest(data)` | 解析任务 manifest |
| parseSubtask | `parseSubtask(data)` | 解析单个子任务 |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/dispatch-parse.test.mjs`
2. 测试 `parseManifest`：解析有效的 JSON manifest，返回结构化对象
3. 测试 `parseManifest`：包含 tasks 数组、metadata、config
4. 测试 `parseManifest`：无效 JSON 时抛出解析错误
5. 测试 `parseManifest`：缺少必要字段时抛出校验错误
6. 测试 `parseSubtask`：解析子任务定义，包含 id、command、args
7. 测试 `parseSubtask`：缺少 id 时使用自动生成的序号
8. 测试 `parseSubtask`：空 args 默认为空数组

### GREEN（最小实现）
1. 创建 `src/lib/dispatch-parse.mjs`
2. 实现 `parseManifest(data)`：JSON.parse → 结构校验 → 返回
3. 实现 `parseSubtask(data)`：解析 + 默认值填充 → 返回
4. 添加必要的字段校验和类型检查

### REFACTOR
1. 提取校验逻辑为独立函数
2. 添加 manifest schema 版本兼容
3. 统一错误消息格式

## 验证
- `npx vitest run tests/unit/dispatch-parse.test.mjs` 通过
- 有效 manifest 正确解析
- 无效输入有明确错误提示

## 涉及文件
- 创建：`src/lib/dispatch-parse.mjs`
- 创建：`tests/unit/dispatch-parse.test.mjs`
- 参考：`scripts/dispatch-parse.sh`
