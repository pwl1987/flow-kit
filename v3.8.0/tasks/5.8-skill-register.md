# 任务 5.8: 创建 register-commands Skill

## 依赖
4.19

## 目标
创建 `skills/register-commands/SKILL.md`。调用 `flow_kit_register` MCP 工具注册斜杠命令。`disable-model-invocation=true`。

## 实现步骤

### RED（测试先行）
1. 编写测试验证 `skills/register-commands/SKILL.md` 存在
2. 验证 frontmatter 包含 `name: register-commands`、`disable-model-invocation: true`
3. 验证 body 中引用 `flow_kit_register` MCP 工具

### GREEN（最小实现）
1. 创建 `skills/register-commands/` 目录
2. 创建 `skills/register-commands/SKILL.md`，包含：
   ```yaml
   ---
   name: register-commands
   description: 注册斜杠命令
   disable-model-invocation: true
   ---
   ```
3. Body 中添加 MCP 工具调用说明：使用 `flow_kit_register` 工具完成一键注册所有斜杠命令
4. 列出待注册的命令清单（从 `scripts/generate-commands.sh` 获取）

### REFACTOR
1. 确认注册逻辑与现有 `generate-commands.sh` 一致
2. 验证注册后斜杠命令可正常调用

## 验证
- `npx vitest run` 通过
- `cat skills/register-commands/SKILL.md` 显示正确 frontmatter
- `/flow-kit:register-commands` 执行后所有命令可用

## 涉及文件
- `skills/register-commands/SKILL.md`（新建）
