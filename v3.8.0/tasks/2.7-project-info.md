# 任务 2.7：project-info 模块重写

## 依赖
1.1（项目基础设施）

## 目标
将 `lib/project-info.sh`（78行）重写为 `src/lib/project-info.mjs`。使用 `child_process.execSync` 调用 git 命令获取项目信息。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| getGitInfo | `getGitInfo()` | 获取当前 git 仓库信息 |
| getVersion | `getVersion()` | 获取项目版本号 |
| getProjectName | `getProjectName()` | 获取项目名称 |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/project-info.test.mjs`
2. 测试 `getGitInfo`：返回包含 branch、commit、remote 的对象
3. 测试 `getGitInfo`：非 git 仓库时返回默认值
4. 测试 `getVersion`：从 VERSION 文件读取版本号
5. 测试 `getVersion`：VERSION 文件不存在时返回 'unknown'
6. 测试 `getProjectName`：从 package.json 读取项目名
7. 测试 `getProjectName`：package.json 不存在时从目录名推断

### GREEN（最小实现）
1. 创建 `src/lib/project-info.mjs`
2. 实现 `getGitInfo()`：execSync 调用 `git rev-parse`、`git branch`、`git remote`
3. 实现 `getVersion()`：读取 VERSION 文件
4. 实现 `getProjectName()`：读取 package.json 的 name 字段，回退到目录名
5. 添加 execSync 错误处理（非 git 仓库场景）

### REFACTOR
1. 提取 execSync 调用为安全封装函数
2. 缓存结果避免重复调用 git
3. 统一默认值格式

## 验证
- `npx vitest run tests/unit/project-info.test.mjs` 通过
- git 仓库和非 git 仓库场景均正常
- VERSION 文件不存在时优雅降级

## 涉及文件
- 创建：`src/lib/project-info.mjs`
- 创建：`tests/unit/project-info.test.mjs`
- 参考：`lib/project-info.sh`
