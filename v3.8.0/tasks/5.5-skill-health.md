# 任务 5.5: 迁移 health 命令为 Skill 格式

## 依赖
4.19

## 目标
迁移 `commands/M-health.md` → `skills/health/SKILL.md`。添加 YAML frontmatter：`name=health`、`disable-model-invocation=true`。

## 实现步骤

### RED（测试先行）
1. 编写测试验证 `skills/health/SKILL.md` 存在
2. 验证 frontmatter 包含 `name: health`、`disable-model-invocation: true`
3. 验证内容涵盖原有健康检查逻辑（版本检查、依赖检查、配置校验）

### GREEN（最小实现）
1. 创建 `skills/health/` 目录
2. 读取 `commands/M-health.md` 内容
3. 创建 `skills/health/SKILL.md`，添加 frontmatter：
   ```yaml
   ---
   name: health
   description: 健康检查
   disable-model-invocation: true
   ---
   ```
4. 将原有命令内容整合进 SKILL.md body

### REFACTOR
1. 去除 M- 前缀相关逻辑
2. 确认 skill 格式与其他 skill 一致

## 验证
- `npx vitest run` 通过
- `cat skills/health/SKILL.md | head -10` 显示正确 frontmatter
- 功能与原 `commands/M-health.md` 等价

## 涉及文件
- `skills/health/SKILL.md`（新建）
