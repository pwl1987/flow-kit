# 任务 5.3: 迁移 Phase 2 为 Skill 格式

## 依赖
4.19

## 目标
迁移 `phases/2-design.md` → `skills/phase-2/SKILL.md`。添加 YAML frontmatter：`name=phase-2`、`description=设计阶段`、`disable-model-invocation=true`。保留原始 phase 内容为 `reference.md`。

## 实现步骤

### RED（测试先行）
1. 编写测试验证 `skills/phase-2/SKILL.md` 存在且包含 YAML frontmatter
2. 验证 frontmatter 中 `name: phase-2`、`description: 设计阶段`、`disable-model-invocation: true` 字段存在
3. 验证 `skills/phase-2/reference.md` 存在且内容与原 `phases/2-design.md` 一致

### GREEN（最小实现）
1. 创建 `skills/phase-2/` 目录
2. 从 `phases/2-design.md` 读取原始内容
3. 创建 `skills/phase-2/SKILL.md`，顶部添加 YAML frontmatter：
   ```yaml
   ---
   name: phase-2
   description: 设计阶段
   disable-model-invocation: true
   ---
   ```
4. 将原始 phase 内容写入 `skills/phase-2/reference.md`

### REFACTOR
1. 检查 frontmatter 格式与 5.1、5.2 一致
2. 确认目录结构统一

## 验证
- `npx vitest run` 通过
- `cat skills/phase-2/SKILL.md | head -10` 显示正确 frontmatter
- `diff phases/2-design.md skills/phase-2/reference.md` 无差异

## 涉及文件
- `skills/phase-2/SKILL.md`（新建）
- `skills/phase-2/reference.md`（新建）
