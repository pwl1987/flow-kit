# 任务 0.6: 更新 Vitest 配置

## 依赖
0.1（需要目录骨架）

## 目标
更新 `vitest.config.ts`，在 include 中添加新模块的测试文件模式（含 unit/tools/hooks/build/e2e），保持原有 Shell 时代测试模式不变。

## 实现步骤

### RED（测试先行）
1. 创建占位测试文件 `tests/unit/placeholder.test.mjs`、`tests/tools/placeholder.test.mjs`、`tests/hooks/placeholder.test.mjs`
2. 运行 `npx vitest run`，确认新测试文件被发现并执行

### GREEN（最小实现）
1. 读取当前 `vitest.config.ts`
2. 在 include 数组中追加：
   - `tests/unit/**/*.test.mjs`
   - `tests/tools/**/*.test.mjs`
   - `tests/hooks/**/*.test.mjs`
   - `tests/e2e/**/*.test.mjs`
   - `tests/build/**/*.test.mjs`
3. 保持原有 `tests/**/*.test.js` 不变
4. 最终 include 应为：
   ```typescript
   include: [
     'tests/**/*.test.js',
     'tests/unit/**/*.test.mjs',
     'tests/tools/**/*.test.mjs',
     'tests/hooks/**/*.test.mjs',
     'tests/e2e/**/*.test.mjs',
     'tests/build/**/*.test.mjs',
   ]
   ```

### REFACTOR
1. 删除占位测试文件
2. 确认原有测试仍全部通过

## 验证
- `npx vitest run --dry-run` 显示新旧测试文件均被发现
- 原有 `tests/**/*.test.js` 测试全部通过
- `npx vitest run tests/unit/` 和 `npx vitest run tests/tools/` 不报 "no test files found"

## 涉及文件
- `vitest.config.ts`（修改 — 添加 include 模式）
