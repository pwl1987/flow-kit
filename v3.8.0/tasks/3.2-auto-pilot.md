# 任务 3.2：auto-pilot 模块重写

## 依赖
2.1（session-state），3.1（phase-executor）

## 目标
将 `scripts/auto-pilot.sh`（264行）重写为 `src/lib/auto-pilot.mjs`。实现自动导航状态机，双文件驱动（auto-plan.json + session-state.json）。

## 核心函数

| 函数 | 签名 | 说明 |
|------|------|------|
| doNext | `doNext()` | 执行下一步 |
| doStatus | `doStatus()` | 获取当前状态 |
| recommendNextStep | `recommendNextStep()` | 推荐下一步操作 |
| validateCurrentPhase | `validateCurrentPhase()` | 验证当前阶段 |
| createPlan | `createPlan()` | 创建执行计划 |

## 实现步骤

### RED（测试先行）
1. 创建 `tests/unit/auto-pilot.test.mjs`
2. 测试 `doStatus`：返回当前 phase、task、block 状态
3. 测试 `doStatus`：无 session 时返回初始状态
4. 测试 `recommendNextStep`：根据当前 phase 推荐下一步
5. 测试 `recommendNextStep`：有 block 时推荐解除阻塞
6. 测试 `validateCurrentPhase`：phase 有效时返回 true
7. 测试 `validateCurrentPhase`：phase 无效时返回错误信息
8. 测试 `createPlan`：生成 auto-plan.json 结构
9. 测试 `doNext`：执行下一步并更新 session state
10. 测试双文件同步：auto-plan.json 和 session-state.json 保持一致

### GREEN（最小实现）
1. 创建 `src/lib/auto-pilot.mjs`
2. 实现 `doStatus()`：读取 session state，汇总当前状态
3. 实现 `recommendNextStep()`：基于 phase 状态机的转移逻辑
4. 实现 `validateCurrentPhase()`：检查 phase 编号和前置条件
5. 实现 `createPlan()`：生成 auto-plan.json 模板
6. 实现 `doNext()`：读取推荐 → 验证 → 执行 → 更新 session

### REFACTOR
1. 提取状态机定义为声明式配置
2. 双文件读写提取为同步工具函数
3. 添加 plan 执行进度跟踪

## 验证
- `npx vitest run tests/unit/auto-pilot.test.mjs` 通过
- 状态机转移逻辑正确
- 双文件一致性保证

## 涉及文件
- 创建：`src/lib/auto-pilot.mjs`
- 创建：`tests/unit/auto-pilot.test.mjs`
- 参考：`scripts/auto-pilot.sh`
