﻿# flow-kit v3.6.0 产品需求文档（PRD）

> **文档版本**: v2.0（整合增强版）
> **编制日期**: 2026-05-16
> **产品名称**: flow-kit — 基于 Claude Code 的结构化开发流程工具包
> **当前版本**: v3.3.0 → **目标版本**: v3.6.0
> **总投入预估**: 14 周，约 100 人天
> **核心目标**: 更好用、更智能、更自动、更安全
> **整合依据**: 基于 122 篇 Clippings 经验萃取成果，融合 26 个参考项目最佳实践，深度整合 flow-kit 社区实践

---

## 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v0.1 | 2026-05-13 | Claude Code AI | 初稿：基于 flow-kit v3.3.0 项目分析，完成六维度开发提升方案 |
| v0.2 | 2026-05-13 | Claude Code AI | 新增：Phase 命令功能边界重构方案（F1-F6） |
| v0.3 | 2026-05-14 | Claude Code AI | 新增：持续任务执行系统（F7）、Ralph Wiggum 循环集成、通用任务模板 |
| v0.4 | 2026-05-14 | Claude Code AI | 新增：superpowers-zh、agency-agents-zh、mattpocock/skills 借鉴分析 |
| v0.5 | 2026-05-14 | Claude Code AI | 新增：Ralph.md 借鉴分析、superpowers+gstack 开发流程借鉴分析 |
| v0.6 | 2026-05-15 | Claude Code AI | 新增：技术兼容性整合方案（13.7）、模块集成方法、接口设计规范 |
| v0.7 | 2026-05-15 | Claude Code AI | 新增：Context Engineering 借鉴分析（13.9）、全局路由协议（13.10）、生产级交付实践（13.11）、控制模型与自进化（13.12） |
| v1.0 | 2026-05-15 | Claude Code AI | 正式版：补充版本历史、修订记录，完成全部 13 章节内容 |
| v1.1 | 2026-05-16 | Claude Code AI | 新增：Ralph 自主 Agent 循环（13.14）、AutoResearch 方法论（13.15）、CE 知识复利系统（13.16）、flow-kit 社区实践（13.17）、团队工程化落地（13.18）借鉴分析 |
| v1.2 | 2026-05-16 | Claude Code AI | 新增：自进化引擎失效分析（13.19）、Multi-Pass Review（13.20）、DESIGN.md+grill-me（13.21）、web-access（13.22）、RTK+OpenWolf（13.23）、Google Agent Skills（13.24）、Subagent Fork（13.25）、Command Center（13.26）借鉴分析 |
| **v2.0** | **2026-05-16** | **Claude Code AI** | **整合增强版：基于 122 篇 Clippings 经验萃取成果，系统性重构全部 15 章节，新增 15.27-15.30 子章节，补充缺失的迭代 2-3 实施计划、风险评估、附录内容，增强智能分析与自动化处理能力** |
| **v2.1** | **2026-05-16** | **Claude Code AI** | **flow-kit 社区实践融合版：新增 F8 紧急修复流程（H-hotfix 三档响应）、F9 项目记忆恢复（R-recall）、F6.3 Secrets 保护机制；更新用户故事、UI 设计、验收标准、实施计划、风险评估、数据流图、接口定义、非功能需求、约束条件等全部相关章节** |

---

## 修订记录

| 修订编号 | 日期 | 修订章节 | 修订内容 | 修订原因 |
|----------|------|----------|----------|----------|
| REV-001 | 2026-05-15 | 全部 | 补充版本历史记录与修订记录表 | 用户要求 PRD 必须包含版本历史记录和修订记录 |
| REV-002 | 2026-05-15 | 13.9-13.12 | 新增 Context Engineering、全局路由协议、生产级交付实践、控制模型与自进化四个借鉴分析章节 | Clippings 研读新增 15 篇文档分析 |
| REV-003 | 2026-05-15 | 经验萃取成果.md | 新增第十二至十五章，更新已分析文档清单至 62 篇 | 持续萃取 Clippings 中的技术最佳实践 |
| REV-004 | 2026-05-15 | 3.7 | 完善 F7 持续任务执行系统：三种自主运行模式、Ralph 循环、安全 Hook、任务模板、异常处理 | 用户要求实现任务自动执行功能和通用任务模板 |
| REV-005 | 2026-05-15 | 13.7 | 新增技术兼容性整合方案：适配策略、模块集成方法、接口规范、兼容性测试计划 | 用户要求确保所有引入技术和方法与现有架构兼容 |
| REV-006 | 2026-05-16 | 13.14-13.18 | 新增 Ralph 自主 Agent 循环、AutoResearch 方法论、CE 知识复利系统、flow-kit 社区实践、团队工程化落地五个借鉴分析章节 | 经验萃取成果第21-25章内容集成 |
| **REV-007** | **2026-05-16** | **全部** | **系统性重构：恢复缺失的迭代 2-3 实施计划、风险评估、附录内容；新增 13.27-13.30 子章节；增强智能分析与自动化处理能力** | **内容完整性修复与经验萃取成果全面整合** |

---

## 目录

- [1. 产品概述](#1-产品概述)
- [2. 问题分析与机会识别](#2-问题分析与机会识别)
- [3. 功能需求详细说明](#3-功能需求详细说明)
- [4. 用户故事](#4-用户故事)
- [5. 优先级排序](#5-优先级排序)
- [6. UI 设计](#6-ui-设计)
- [7. 数据流程图](#7-数据流程图)
- [8. 接口定义](#8-接口定义)
- [9. 非功能需求](#9-非功能需求)
- [10. 约束条件](#10-约束条件)
- [11. 依赖关系](#11-依赖关系)
- [12. 验收标准](#12-验收标准)
- [13. 实施计划](#13-实施计划)
- [14. 风险评估](#14-风险评估)
- [15. 参考项目借鉴分析](#15-参考项目借鉴分析)
  - [15.1 superpowers-zh 借鉴分析](#151-superpowers-zh-借鉴分析)
  - [15.2 agency-agents-zh 借鉴分析](#152-agency-agents-zh-借鉴分析)
  - [15.3 借鉴整合路线图](#153-借鉴整合路线图)
  - [15.4 mattpocock/skills 借鉴分析](#154-mattpocockskills-借鉴分析)
  - [15.5 Ralph.md 借鉴分析](#155-ralphmd-借鉴分析)
  - [15.6 superpowers+gstack 开发流程借鉴分析](#156-superpowersgstack-开发流程借鉴分析)
  - [15.7 技术兼容性整合方案](#157-技术兼容性整合方案)
  - [15.8 Claude Code AI 辅助效率提升分析](#158-claude-code-ai-辅助效率提升分析)
  - [15.9 代码质量改进指标](#159-代码质量改进指标)
  - [15.10 Context Engineering 借鉴分析](#1510-context-engineering-借鉴分析)
  - [15.11 全局路由协议借鉴分析](#1511-全局路由协议借鉴分析)
  - [15.12 生产级交付实践借鉴分析](#1512-生产级交付实践借鉴分析)
  - [15.13 控制模型与自进化借鉴分析](#1513-控制模型与自进化借鉴分析)
  - [15.14 Ralph 自主 Agent 循环与 PRD 驱动开发](#1514-ralph-自主-agent-循环与-prd-驱动开发)
  - [15.15 AutoResearch 方法论借鉴分析](#1515-autoresearch-方法论借鉴分析)
  - [15.16 Compound Engineering 知识复利系统](#1516-compound-engineering-知识复利系统)
  - [15.17 flow-kit 社区实践与融合创新](#1517-flow-kit-社区实践与融合创新)
  - [15.18 Claude Code 团队工程化落地](#1518-claude-code-团队工程化落地)
  - [15.19 自进化引擎失效分析与修复](#1519-自进化引擎失效分析与修复)
  - [15.20 Multi-Pass Review 工程化](#1520-multi-pass-review-工程化)
  - [15.21 DESIGN.md 设计系统与 grill-me 需求访谈](#1521-designmd-设计系统与-grill-me-需求访谈)
  - [15.22 web-access 浏览器直连](#1522-web-access-浏览器直连)
  - [15.23 RTK 与 OpenWolf 成本优化](#1523-rtk-与-openwolf-成本优化)
  - [15.24 Google Agent Skills](#1524-google-agent-skills)
  - [15.25 Subagent Fork 机制](#1525-subagent-fork-机制)
  - [15.26 Command Center 目标管理层](#1526-command-center-目标管理层)
  - [15.27 OpenSpec SDD 方法论深度整合](#1527-openspec-sdd-方法论深度整合)
  - [15.28 Claude Code Task 系统与工具分级加载](#1528-claude-code-task-系统与工具分级加载)
  - [15.29 四层防护 TDD 模型与验证证据机制](#1529-四层防护-tdd-模型与验证证据机制)
  - [15.30 经验萃取成果全景整合矩阵](#1530-经验萃取成果全景整合矩阵)
- [附录](#附录)
  - [A. 术语表](#a-术语表)
  - [B. 文件清单](#b-文件清单)
  - [C. 版本兼容性说明](#c-版本兼容性说明)
  - [D. 参考项目](#d-参考项目)

---

## 1. 产品概述

### 1.1 产品定位

flow-kit 是一个基于 Claude Code 的结构化开发流程工具包，提供 9 阶段（Phase 0-8）开发工作流、棕地/绿地条件路由、护栏系统、会话记忆系统和多代理并行编排能力。v3.6.0 将聚焦于 **Phase 命令功能边界重构** 与 **全流程自动化执行** 两大核心方向，并深度融合经验萃取成果中的 122 篇 Clippings 最佳实践。

### 1.2 版本目标

| 目标 | 描述 | 量化指标 |
|------|------|----------|
| 功能边界清晰化 | 每个 phase 命令职责明确，不越界执行 | 越界事件减少 100% |
| 操作确认机制 | 所有关键步骤必须获得人工确认 | 确认覆盖率 100% |
| 状态透明化 | 实时展示当前执行阶段与下一步计划 | 状态更新延迟 < 1s |
| 全流程自动化 | Phase 0→7 自动执行，无需人工干预 | 自动化率 > 90% |
| 智能决策系统 | 在确认环节主动弹出推荐选项 | 推荐采纳率 > 70% |
| 自动化审计 | 识别不合适操作或遗漏步骤 | 审计覆盖率 100% |
| 需求文档自动处理 | 支持 PRD 自动解析与执行 | 解析准确率 > 85% |
| 自我进化机制 | 工作流持续优化与改进 | 每迭代优化 3+ 项 |
| 环境自动清理 | 命令执行前自动清理环境 | 清理成功率 100% |
| **Token 优化** | **集成 RTK/OpenWolf/Caveman 多级压缩** | **Token 节省 > 60%** |
| **知识复利** | **CE 知识沉淀 + VFM 评分 + WAL 协议** | **经验捕获率 > 90%** |

### 1.3 目标用户

| 用户角色 | 使用场景 | 核心需求 |
|----------|----------|----------|
| 独立开发者 | 个人项目开发 | 自动化执行、减少手动操作 |
| 小团队（2-5 人） | 协作开发 | 状态透明、确认机制、审计 |
| 技术负责人 | 项目管理和代码审查 | 全流程可见性、质量门禁 |
| 产品经理 | 需求提交与跟踪 | PRD 自动解析、进度追踪 |

---

## 2. 问题分析与机会识别

### 2.1 当前问题深度分析

#### 问题 1：Phase 命令功能越界

**现状**：当前 `/flow-kit:phase-N` 命令在执行时，会擅自执行超出其职责范围的操作。例如：
- `/flow-kit:phase-0`（变更立项）在执行影响范围评估后，可能直接开始架构设计
- `/flow-kit:phase-2`（架构设计）可能直接进入开发执行
- `/flow-kit:phase-4`（开发执行）可能自行执行测试验证

**根因分析**：
1. Phase 工作流文件（如 `phases/4-dev/brownfield.md`）中的执行步骤描述模糊，未明确职责边界
2. phase-executor.sh 仅负责加载工作流文件，未对工作流内容进行边界校验
3. 缺少命令执行前的职责声明与确认机制
4. Claude Code 的 AI 特性导致其倾向于"主动完成更多工作"

**影响范围**：
- 开发流程混乱，阶段产出物不可控
- 用户失去对开发节奏的控制
- 审计困难，无法追溯哪个阶段执行了什么操作

#### 问题 2：缺少自动化执行能力

**现状**：当前需要用户手动逐个执行 `/flow-kit:phase-0` → `/flow-kit:phase-1` → ... → `/flow-kit:phase-7`

**根因分析**：
1. 缺少自动化执行引擎
2. 缺少阶段间状态传递机制
3. 缺少执行计划生成能力

#### 问题 3：缺少智能决策支持

**现状**：在需要人工确认的环节，仅输出信息，不提供推荐选项

**根因分析**：
1. 缺少决策上下文分析能力
2. 缺少推荐选项生成机制
3. 缺少决策历史记录与学习能力

#### 问题 4：缺少审计与自我修正机制

**现状**：执行过程中的错误或遗漏无法自动识别和修正

**根因分析**：
1. 缺少执行结果验证机制
2. 缺少错误模式识别能力
3. 缺少自动修正方案生成能力

### 2.2 机会识别

| 机会 | 描述 | 预期收益 |
|------|------|----------|
| 借鉴 superpowers-zh workflow-runner | YAML 工作流编排 + 多角色协作 | 自动化率提升 60% |
| 借鉴 superpowers-zh executing-plans | 分步执行 + 审查检查点 | 执行质量提升 50% |
| 借鉴 agency-agents-zh 角色体系 | 结构化角色定义 + 专业流程 | 决策质量提升 40% |
| 借鉴 agency-agents-zh DAG 编排 | 依赖检测 + 并行执行 | 执行效率提升 3 倍 |
| 借鉴 Claude Code 的 /clear 机制 | 环境状态重置 | 状态一致性 100% |
| **借鉴 OpenSpec SDD 方法论** | **增量规范 + 制品依赖图** | **需求对齐度提升 60%** |
| **借鉴 RTK/OpenWolf 成本优化** | **终端净化 + 记忆系统** | **Token 节省 60-80%** |
| **借鉴 CE 知识复利系统** | **知识沉淀 + 自动引用** | **重复错误减少 70%** |

---

## 3. 功能需求详细说明

### 3.1 Phase 命令功能边界重构（F1）

#### F1.1 职责边界定义（增强版）

每个 Phase 命令必须明确定义其职责范围，建立严格的执行边界，并引入全局路由协议：

```yaml
# phase-boundaries.yaml — Phase 职责边界定义（增强版）
routing:
  priority_chain:
    - "用户明确指令"
    - "phase-boundaries.yaml"
    - "CLAUDE.md"
    - "插件默认说明"
  single_master_per_turn: true
  go_md_zero_exception: true

gates:
  phase_entry:
    - gate_1: "路由声明（AI 必须先声明走哪条路）"
    - gate_2: "用户回复（用户确认路由选择）"
    - gate_3: "加载工作流并确认（加载具体工作流后再次确认）"
    - gate_4: "工作流第一步反问并等回复（启动前最后确认）"
  phase_exit:
    - gate_5: "产出工件验证 + 过渡声明 + 用户确认"

phases:
  phase-0:
    name: "变更立项"
    stage: "thinking"
    allowed_operations:
      - 影响范围评估
      - 破坏性变更识别
      - 风险评级
      - 护栏配置
      - Change ID 生成
      - 路由判断
    forbidden_operations:
      - 架构设计
      - 任务拆解
      - 代码编写
      - 测试执行
    output: "CHANGE.md + 影响范围评估文档"
    confirmation_required: true
    max_auto_steps: 3
    gates:
      entry: [gate_1, gate_2]
      exit: [gate_5]

  phase-1:
    name: "需求澄清"
    stage: "thinking"
    allowed_operations:
      - 需求分析
      - 用户故事编写
      - 验收标准定义
      - 需求优先级排序
      - grill-me 需求访谈
    forbidden_operations:
      - 技术方案设计
      - 任务拆解
    output: "REQUIREMENT.md + CONTEXT.md"
    confirmation_required: true
    max_auto_steps: 3
    gates:
      entry: [gate_1, gate_2, gate_3]
      exit: [gate_5]

  phase-2:
    name: "架构设计"
    stage: "thinking"
    allowed_operations:
      - 技术方案设计
      - 架构决策记录（ADR）
      - 接口定义
      - 数据模型设计
      - DESIGN.md 设计约束定义
    forbidden_operations:
      - 代码编写
      - 测试编写
    output: "DESIGN.md + ADR"
    confirmation_required: true
    max_auto_steps: 4
    gates:
      entry: [gate_1, gate_2, gate_3]
      exit: [gate_5]

  phase-3:
    name: "任务拆解"
    stage: "thinking"
    allowed_operations:
      - 任务分解
      - DAG 依赖分析
      - 工作量估算
      - 任务分配
      - 制品依赖图生成
    forbidden_operations:
      - 代码编写
      - 架构决策
    output: "TASK.md + 制品依赖图"
    confirmation_required: true
    max_auto_steps: 3
    gates:
      entry: [gate_1, gate_2]
      exit: [gate_5]

  phase-4:
    name: "开发执行"
    stage: "execution"
    allowed_operations:
      - 代码实现
      - 单元测试编写（TDD 红-绿-重构）
      - 代码提交
      - Subagent Fork 并行执行
    forbidden_operations:
      - 架构设计
      - 集成测试
      - 代码审查
    output: "代码实现 + 单元测试"
    confirmation_required: false
    max_auto_steps: 10
    gates:
      entry: [gate_1, gate_2]
      exit: [gate_5]

  phase-5:
    name: "测试验证"
    stage: "execution"
    allowed_operations:
      - 集成测试
      - 端到端测试
      - 性能测试
      - 护栏检查
      - diagnose 诊断循环
    forbidden_operations:
      - 代码修改
      - 代码审查
    output: "测试报告"
    confirmation_required: true
    max_auto_steps: 5
    gates:
      entry: [gate_1, gate_2]
      exit: [gate_5]

  phase-6:
    name: "代码审查"
    stage: "execution"
    allowed_operations:
      - Multi-Pass 代码审查
      - 安全审计
      - 质量门禁检查
      - 12 维度并行审查
    forbidden_operations:
      - 代码修改
      - 测试执行
    output: "REVIEW.md + 审查报告"
    confirmation_required: true
    max_auto_steps: 3
    gates:
      entry: [gate_1, gate_2]
      exit: [gate_5]

  phase-7:
    name: "集成归档"
    stage: "execution"
    allowed_operations:
      - 代码合并
      - 变更归档
      - 文档更新
      - 发布准备
      - CE 知识复利提取
    forbidden_operations:
      - 代码修改
      - 架构变更
    output: "归档完成 + 发布说明 + 知识沉淀"
    confirmation_required: true
    max_auto_steps: 4
    gates:
      entry: [gate_1, gate_2]
      exit: [gate_5]
```

#### F1.2 边界校验机制

创建 `scripts/boundary-enforcer.sh`，在 phase 命令执行前进行边界校验：

```bash
# boundary-enforcer.sh — Phase 边界强制执行器（增强版）
# 集成全局路由协议 + 四闸门机制 + @GO.md 零例外路由

enforce_boundary() {
    local phase="$1"
    local operation="$2"

    # 1. 加载边界定义
    local boundaries
    boundaries=$(yq eval ".phases[\"$phase\"]" "$BOUNDARIES_FILE")

    # 2. 检查操作是否在 allowed_operations 中
    local is_allowed
    is_allowed=$(echo "$boundaries" | yq eval ".allowed_operations[] | select(. == \"$operation\")" 2>/dev/null)

    if [[ -z "$is_allowed" ]]; then
        # 3. 检查是否在 forbidden_operations 中
        local is_forbidden
        is_forbidden=$(echo "$boundaries" | yq eval ".forbidden_operations[] | select(. == \"$operation\")" 2>/dev/null)

        if [[ -n "$is_forbidden" ]]; then
            echo "[boundary] ❌ 操作越界: '$operation' 不属于 $phase 的职责范围"
            echo "[boundary] 该操作属于: $(find_phase_for_operation "$operation")"
            log_boundary_violation "$phase" "$operation"
            return 1
        fi

        echo "[boundary] ⚠️  未识别的操作: '$operation'"
        echo "[boundary] 建议: 更新 phase-boundaries.yaml 或确认操作合法性"
        return 2
    fi

    # 4. @GO.md 零例外路由检测
    if [[ "$operation" == "路由判断" ]] && grep -q "@GO.md" <<< "$CURRENT_MESSAGE"; then
        echo "[boundary] 🔀 检测到 @GO.md 引用，强制展示路由选项"
        show_routing_options
        return 0
    fi

    return 0
}

# 四闸门机制
enforce_gates() {
    local phase="$1"
    local gate_type="$2"  # entry | exit

    local gates
    gates=$(yq eval ".phases[\"$phase\"].gates[\"$gate_type\"][]" "$BOUNDARIES_FILE")

    for gate in $gates; do
        case "$gate" in
            "gate_1")
                echo "[gate] 路由声明：AI 必须先声明走哪条路"
                declare_routing_path "$phase"
                ;;
            "gate_2")
                echo "[gate] 用户确认：请确认路由选择"
                request_confirmation "$phase" "路由确认" "critical"
                ;;
            "gate_3")
                echo "[gate] 加载工作流并确认"
                load_workflow "$phase"
                request_confirmation "$phase" "工作流确认" "important"
                ;;
            "gate_4")
                echo "[gate] 工作流第一步反问并等回复"
                show_first_step "$phase"
                wait_for_user_response
                ;;
            "gate_5")
                echo "[gate] 产出工件验证 + 过渡声明"
                validate_phase_outputs "$phase"
                declare_transition "$phase"
                request_confirmation "$phase" "阶段过渡确认" "important"
                ;;
        esac
    done
}
```

#### F1.3 操作前确认流程

创建 `scripts/confirmation-manager.sh`，实现操作前确认机制：

```bash
# confirmation-manager.sh — 操作确认管理器（增强版）
# 集成智能决策引擎 + grill-me 需求访谈模式

# 确认级别定义
declare -A CONFIRM_LEVELS
CONFIRM_LEVELS[critical]="必须人工确认，不可跳过"
CONFIRM_LEVELS[important]="建议人工确认，可配置自动确认"
CONFIRM_LEVELS[normal]="批量确认，可配置自动模式"

# 请求确认
request_confirmation() {
    local phase="$1"
    local operation="$2"
    local description="$3"
    local level="${4:-important}"
    local options=("${@:5}")

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║  📋 Phase 推进确认                       ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  Phase: $phase"
    echo "║  操作: $operation"
    echo "║  描述: $description"
    echo "║  级别: $level (${CONFIRM_LEVELS[$level]})"
    echo "╠══════════════════════════════════════════╣"

    # 生成推荐选项（集成智能决策引擎）
    if [[ ${#options[@]} -eq 0 ]]; then
        options=($(analyze_decision_context "$phase" "$operation"))
    fi

    if [[ ${#options[@]} -gt 0 ]]; then
        echo "║  推荐选项:"
        for i in "${!options[@]}"; do
            echo "║    $((i+1)). ${options[$i]}"
        done
    fi

    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 根据确认级别决定行为
    case "$level" in
        critical)
            echo "请输入确认 (yes/no) 或选择选项编号:"
            ;;
        important)
            if [[ "$AUTO_CONFIRM_MODE" == "true" ]]; then
                echo "[confirm] 自动确认模式已启用，默认选择选项 1"
                return 0
            fi
            echo "请输入确认 (yes/no/auto) 或选择选项编号:"
            ;;
        normal)
            if [[ "$AUTO_CONFIRM_MODE" == "true" ]]; then
                return 0
            fi
            echo "按 Enter 继续，或输入选项编号:"
            ;;
    esac
}

# grill-me 需求访谈模式（集成自 mattpocock/skills）
grill_me_interview() {
    local topic="$1"
    local max_questions="${2:-5}"
    local question_count=0

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║  🔍 grill-me 需求访谈                    ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  主题: $topic"
    echo "║  我将逐层深入提问，直到双方理解一致       ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    while [[ $question_count -lt $max_questions ]]; do
        question_count=$((question_count + 1))

        local question
        question=$(generate_next_question "$topic" "$question_count")

        echo "[grill-me] Q$question_count: $question"
        echo "请输入回答 (或输入 'skip' 跳过此问题):"

        read -r answer
        if [[ "$answer" == "skip" ]]; then
            continue
        fi

        record_interview_answer "$question" "$answer"

        if check_understanding_aligned "$topic"; then
            echo "[grill-me] ✅ 双方理解已对齐，结束访谈"
            break
        fi
    done

    generate_context_md "$topic"
}
```

#### F1.4 状态透明化机制

创建 `scripts/status-display.sh`，实现实时状态展示：

```bash
# status-display.sh — 状态透明化显示（增强版）

show_phase_status() {
    local current_phase="$1"
    local total_phases=8

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║  🚀 flow-kit v3.6.0 执行状态面板        ║"
    echo "╠══════════════════════════════════════════╣"

    for ((i=0; i<=total_phases; i++)); do
        local phase_name
        phase_name=$(get_phase_name "$i")
        local stage
        stage=$(get_phase_stage "$i")
        local status_icon="⬜"
        local status_text="待执行"

        if [[ "$i" -lt "$current_phase" ]]; then
            status_icon="✅"
            status_text="已完成"
        elif [[ "$i" -eq "$current_phase" ]]; then
            status_icon="🔄"
            status_text="执行中"
        fi

        printf "║  %s Phase %d: %-12s [%-8s] %-8s║\n" "$status_icon" "$i" "$phase_name" "$stage" "$status_text"
    done

    echo "╠══════════════════════════════════════════╣"
    echo "║  当前操作: $(get_current_operation)      ║"
    echo "║  完成进度: $(get_progress)               ║"
    echo "║  下一步: $(get_next_action)              ║"
    echo "║  Token 消耗: $(get_token_usage)          ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""
}
```

### 3.2 自动化执行流程（F2）

#### F2.1 自动化执行引擎（增强版）

创建 `scripts/auto-pilot.sh`，实现 Phase 0→7 的全流程自动执行，集成任务深度分级和智能路由：

```bash
# auto-pilot.sh — 自动化执行引擎 v2.0

AUTO_PILOT_MODE="${AUTO_PILOT_MODE:-interactive}"  # interactive | silent | supervised
AUTO_CONFIRM_THRESHOLD="${AUTO_CONFIRM_THRESHOLD:-important}"  # critical | important | normal

# 任务深度分级（借鉴 Command Center 方法论）
TASK_DEPTH_QUICK="quick"        # 简单执行，单文件修改
TASK_DEPTH_CAMPAIGN="campaign"  # 自动拆分子任务，多文件但边界清晰
TASK_DEPTH_DEEP="deep"          # 分阶段执行，跨模块、新架构

# 执行计划生成（增强版）
generate_execution_plan() {
    local change_description="$1"
    local project_type="$2"

    echo "[auto-pilot] 生成执行计划..."
    echo ""

    # 分析变更描述，生成阶段执行计划
    local plan_file="$PROJECT_DIR/.flow-kit/auto-plan.json"

    # 评估任务深度
    local task_depth
    task_depth=$(assess_task_depth "$change_description")

    jq -n \
        --arg desc "$change_description" \
        --arg type "$project_type" \
        --arg depth "$task_depth" \
        --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        '{
            description: $desc,
            project_type: $type,
            task_depth: $depth,
            created_at: $ts,
            phases: [
                {"id": 0, "name": "变更立项", "status": "pending", "auto_confirm": false, "stage": "thinking"},
                {"id": 1, "name": "需求澄清", "status": "pending", "auto_confirm": false, "stage": "thinking"},
                {"id": 2, "name": "架构设计", "status": "pending", "auto_confirm": false, "stage": "thinking"},
                {"id": 3, "name": "任务拆解", "status": "pending", "auto_confirm": false, "stage": "thinking"},
                {"id": 4, "name": "开发执行", "status": "pending", "auto_confirm": true, "stage": "execution"},
                {"id": 5, "name": "测试验证", "status": "pending", "auto_confirm": false, "stage": "execution"},
                {"id": 6, "name": "代码审查", "status": "pending", "auto_confirm": false, "stage": "execution"},
                {"id": 7, "name": "集成归档", "status": "pending", "auto_confirm": false, "stage": "execution"}
            ],
            current_phase: 0,
            status: "running"
        }' > "$plan_file"

    echo "$plan_file"
}

# 任务深度评估
assess_task_depth() {
    local description="$1"

    # 轻量任务：单文件修改、明确 bug 修复
    if echo "$description" | grep -qiE "^(修复|修改|更新|删除)\s+\S+\.\w+"; then
        echo "$TASK_DEPTH_QUICK"
        return
    fi

    # 中任务：多文件但边界清晰
    if echo "$description" | grep -qiE "^(新增|添加|优化|重构)\s+\S+"; then
        echo "$TASK_DEPTH_CAMPAIGN"
        return
    fi

    # 大任务：跨模块、新架构
    echo "$TASK_DEPTH_DEEP"
}

# 自动执行（增强版）
auto_execute() {
    local plan_file="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 自动化执行引擎已启动        ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  模式: $AUTO_PILOT_MODE"
    echo "║  确认阈值: $AUTO_CONFIRM_THRESHOLD"
    echo "║  任务深度: $(jq -r '.task_depth' "$plan_file")"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    for ((phase=0; phase<=7; phase++)); do
        local phase_name
        phase_name=$(jq -r ".phases[] | select(.id == $phase) | .name" "$plan_file")

        echo ""
        echo "────────────────────────────────────────"
        echo "  执行 Phase $phase: $phase_name"
        echo "────────────────────────────────────────"
        echo ""

        # 四闸门入口检查
        enforce_gates "$phase" "entry"

        # 更新状态
        jq --argjson phase "$phase" '.current_phase = $phase | .phases[$phase].status = "running"' "$plan_file" > "${plan_file}.tmp" && mv "${plan_file}.tmp" "$plan_file"

        # 显示状态面板
        show_phase_status "$phase"

        # 执行 phase
        if ! execute_phase "$phase"; then
            echo "[auto-pilot] ❌ Phase $phase 执行失败"
            jq --argjson phase "$phase" '.phases[$phase].status = "failed"' "$plan_file" > "${plan_file}.tmp" && mv "${plan_file}.tmp" "$plan_file"
            return 1
        fi

        # 验证 phase 输出
        if ! validate_phase_output "$phase"; then
            echo "[auto-pilot] ⚠️  Phase $phase 输出验证未通过，尝试自动修复..."
            if ! auto_fix_phase "$phase"; then
                echo "[auto-pilot] ❌ 自动修复失败，需要人工干预"
                return 1
            fi
        fi

        # 四闸门出口检查
        enforce_gates "$phase" "exit"

        # 标记完成
        jq --argjson phase "$phase" '.phases[$phase].status = "completed"' "$plan_file" > "${plan_file}.tmp" && mv "${plan_file}.tmp" "$plan_file"

        echo "[auto-pilot] ✅ Phase $phase 完成"
    done

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║   🎉 全流程自动化执行完成！              ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 生成执行报告
    generate_execution_report "$plan_file"

    # 触发 CE 知识复利提取
    trigger_ce_compound "$plan_file"
}
```

#### F2.2 智能决策系统

创建 `scripts/decision-engine.sh`，在需要人工确认的环节主动弹出推荐选项：

```bash
# decision-engine.sh — 智能决策引擎（增强版）

# 决策上下文分析
analyze_decision_context() {
    local phase="$1"
    local operation="$2"

    # 收集决策上下文
    local project_type
    project_type=$(session_get ptype)
    local current_phase
    current_phase=$(session_get phase)
    local history
    history=$(session_get history)

    # 分析历史决策模式
    local similar_decisions
    similar_decisions=$(find_similar_decisions "$operation" "$project_type")

    # 生成推荐选项
    local recommendations=()

    case "$phase-$operation" in
        "0-影响范围评估")
            recommendations+=("自动执行影响范围评估（基于 git diff）")
            recommendations+=("手动指定影响范围（适用于大型变更）")
            recommendations+=("跳过评估（仅适用于微小变更）")
            ;;
        "1-需求分析")
            recommendations+=("自动分析需求并生成用户故事")
            recommendations+=("grill-me 引导式需求澄清（逐项确认）")
            recommendations+=("从 PRD 文档自动解析")
            ;;
        "2-架构设计")
            recommendations+=("基于需求自动生成架构方案")
            recommendations+=("选择架构模板（微服务/单体/事件驱动）")
            recommendations+=("手动设计架构（适用于复杂场景）")
            ;;
        "4-开发执行")
            recommendations+=("自动执行所有任务（基于 TASK.md）")
            recommendations+=("逐任务执行（每完成一个确认一次）")
            recommendations+=("Subagent Fork 并行执行（使用 dispatch.sh）")
            ;;
        "5-测试验证")
            recommendations+=("自动运行所有测试")
            recommendations+=("选择性运行测试（指定测试范围）")
            recommendations+=("diagnose 诊断循环（测试失败时自动诊断）")
            ;;
        "6-代码审查")
            recommendations+=("Multi-Pass 审查（3 轮 12 维度）")
            recommendations+=("快速审查（单轮 6 维度）")
            recommendations+=("跳过审查（仅适用于文档变更）")
            ;;
        *)
            recommendations+=("自动执行（推荐）")
            recommendations+=("手动执行")
            recommendations+=("跳过此步骤")
            ;;
    esac

    echo "${recommendations[@]}"
}

# 决策记录与学习
record_decision() {
    local phase="$1"
    local operation="$2"
    local choice="$3"
    local outcome="$4"

    local decision_record="$PROJECT_DIR/.flow-kit/decisions.log"

    echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | $phase | $operation | $choice | $outcome" >> "$decision_record"

    # 更新 session-state
    session_decision_add "$phase" "$choice"
}

# 从历史决策中学习
learn_from_history() {
    local decision_record="$PROJECT_DIR/.flow-kit/decisions.log"

    if [[ ! -f "$decision_record" ]]; then
        return
    fi

    # 分析历史决策的成功率
    local total=0
    local success=0

    while IFS='|' read -r ts phase operation choice outcome; do
        total=$((total + 1))
        if [[ "$outcome" == *"success"* ]]; then
            success=$((success + 1))
        fi
    done < "$decision_record"

    local success_rate=0
    if [[ $total -gt 0 ]]; then
        success_rate=$((success * 100 / total))
    fi

    echo "[decision-engine] 历史决策成功率: ${success_rate}% (${success}/${total})"

    # 如果成功率低于 50%，建议切换到更保守的模式
    if [[ $success_rate -lt 50 ]]; then
        echo "[decision-engine] ⚠️  历史决策成功率较低，建议启用监督模式"
    fi
}
```

### 3.3 自动化审计机制（F3）

#### F3.1 执行审计器

创建 `scripts/execution-auditor.sh`：

```bash
# execution-auditor.sh — 执行审计器（增强版）

# 审计执行过程
audit_execution() {
    local plan_file="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║         flow-kit 执行审计报告            ║"
    echo "╠══════════════════════════════════════════╣"

    local issues=0
    local warnings=0

    # 1. 检查每个 phase 的输出完整性
    for ((phase=0; phase<=7; phase++)); do
        local phase_name
        phase_name=$(jq -r ".phases[] | select(.id == $phase) | .name" "$plan_file")
        local status
        status=$(jq -r ".phases[] | select(.id == $phase) | .status" "$plan_file")

        if [[ "$status" != "completed" ]]; then
            echo "║  ❌ Phase $phase ($phase_name): 未完成"
            issues=$((issues + 1))
        fi

        # 检查输出文件
        if ! check_phase_outputs "$phase"; then
            echo "║  ⚠️  Phase $phase ($phase_name): 输出文件不完整"
            warnings=$((warnings + 1))
        fi
    done

    # 2. 检查操作越界
    local boundary_violations
    boundary_violations=$(check_boundary_violations "$plan_file")
    if [[ -n "$boundary_violations" ]]; then
        echo "║  ❌ 检测到操作越界:"
        echo "$boundary_violations" | while read -r violation; do
            echo "║     - $violation"
            issues=$((issues + 1))
        done
    fi

    # 3. 检查遗漏步骤
    local missed_steps
    missed_steps=$(check_missed_steps "$plan_file")
    if [[ -n "$missed_steps" ]]; then
        echo "║  ⚠️  检测到可能遗漏的步骤:"
        echo "$missed_steps" | while read -r step; do
            echo "║     - $step"
            warnings=$((warnings + 1))
        done
    fi

    # 4. Token 消耗审计
    local token_usage
    token_usage=$(calculate_token_usage "$plan_file")
    echo "║  💰 Token 消耗: $token_usage"

    echo "╠══════════════════════════════════════════╣"
    echo "║  问题: $issues  |  警告: $warnings"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    return $issues
}
```

#### F3.2 自动修正机制

```bash
# 自动修正执行问题
auto_fix_issues() {
    local issues_file="$1"

    local fixed=0
    local failed=0

    while IFS='|' read -r issue_type phase description; do
        echo "[auto-fix] 尝试修复: $description"

        case "$issue_type" in
            "missing_output")
                if regenerate_phase_output "$phase"; then
                    echo "[auto-fix] ✅ 修复成功: $description"
                    fixed=$((fixed + 1))
                else
                    echo "[auto-fix] ❌ 修复失败: $description"
                    failed=$((failed + 1))
                fi
                ;;
            "boundary_violation")
                if rollback_violation "$phase"; then
                    echo "[auto-fix] ✅ 已回滚越界操作: $description"
                    fixed=$((fixed + 1))
                else
                    echo "[auto-fix] ❌ 回滚失败: $description"
                    failed=$((failed + 1))
                fi
                ;;
            "missed_step")
                if execute_missed_step "$phase" "$description"; then
                    echo "[auto-fix] ✅ 已执行遗漏步骤: $description"
                    fixed=$((fixed + 1))
                else
                    echo "[auto-fix] ❌ 执行失败: $description"
                    failed=$((failed + 1))
                fi
                ;;
        esac
    done < "$issues_file"

    echo "[auto-fix] 修复完成: $fixed 成功, $failed 失败"
    return $failed
}
```

### 3.4 需求文档自动处理（F4）

#### F4.1 PRD 自动解析器（增强版）

创建 `scripts/prd-parser.sh`，集成 OpenSpec SDD 方法论：

```bash
# prd-parser.sh — PRD 文档自动解析器（增强版）
# 集成 OpenSpec 增量规范 + 制品依赖图

parse_prd() {
    local prd_file="$1"

    echo "[prd-parser] 正在解析 PRD 文档: $prd_file"

    # 1. 检测文档格式
    local format
    format=$(detect_document_format "$prd_file")

    # 2. 提取结构化信息
    local parsed_data
    parsed_data=$(extract_structured_data "$prd_file" "$format")

    # 3. 生成变更描述
    local change_description
    change_description=$(generate_change_description "$parsed_data")

    # 4. 生成增量规范（Delta Spec）
    local delta_spec
    delta_spec=$(generate_delta_spec "$parsed_data")

    # 5. 生成制品依赖图（DAG）
    local dag
    dag=$(generate_artifact_dag "$parsed_data")

    # 6. 生成需求文档
    local change_id
    change_id=$(generate_change_id "$change_description")
    local spec_dir="$PROJECT_DIR/.specs/$change_id"
    mkdir -p "$spec_dir"

    echo "$parsed_data" > "$spec_dir/REQUIREMENT.md"
    echo "$delta_spec" > "$spec_dir/delta-spec.yaml"
    echo "$dag" > "$spec_dir/artifact-dag.json"

    # 7. 自动初始化变更
    echo "[prd-parser] ✅ PRD 解析完成"
    echo "[prd-parser] 变更描述: $change_description"
    echo "[prd-parser] 需求文档: $spec_dir/REQUIREMENT.md"
    echo "[prd-parser] 增量规范: $spec_dir/delta-spec.yaml"
    echo "[prd-parser] 制品依赖图: $spec_dir/artifact-dag.json"
    echo ""
    echo "[prd-parser] 建议执行: /flow-kit:auto-start \"$change_description\""

    echo "$change_description"
}

# 检测文档格式
detect_document_format() {
    local file="$1"

    if grep -q "^# \|^## " "$file" 2>/dev/null; then
        if head -5 "$file" | grep -q "^---$"; then
            echo "markdown-with-frontmatter"
        else
            echo "markdown"
        fi
    elif head -1 "$file" | grep -q "^{"; then
        echo "json"
    else
        echo "plain-text"
    fi
}

# 提取结构化数据
extract_structured_data() {
    local file="$1"
    local format="$2"

    case "$format" in
        "markdown-with-frontmatter")
            extract_frontmatter "$file"
            extract_feature_list "$file"
            extract_acceptance_criteria "$file"
            ;;
        "markdown")
            infer_from_headings "$file"
            ;;
        "json")
            jq '.' "$file"
            ;;
        "plain-text")
            analyze_with_claude "$file"
            ;;
    esac
}

# 生成增量规范（Delta Spec）
generate_delta_spec() {
    local parsed_data="$1"

    jq -n \
        --arg change_id "$(generate_change_id)" \
        --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        '{
            change_id: $change_id,
            created_at: $ts,
            type: "delta",
            based_on: "v3.3.0",
            additions: [],
            modifications: [],
            deletions: [],
            artifacts: []
        }'
}

# 生成制品依赖图（DAG）
generate_artifact_dag() {
    local parsed_data="$1"

    jq -n '{
        nodes: [],
        edges: [],
        topological_order: []
    }'
}
```

#### F4.2 自动初始化流程

```bash
# auto-start.sh — 一键启动自动化流程（增强版）

auto_start() {
    local input="${1:-}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 一键自动化启动              ║"
    echo "╠══════════════════════════════════════════╣"

    # 1. 检测输入类型
    if [[ -f "$input" ]]; then
        echo "║  输入类型: PRD 文档"
        local change_desc
        change_desc=$(parse_prd "$input")
    else
        echo "║  输入类型: 文本描述"
        local change_desc="$input"
    fi

    # 2. 环境清理
    echo "║  [1/5] 清理执行环境..."
    auto_cleanup

    # 3. 初始化变更
    echo "║  [2/5] 初始化变更..."
    bash "$SCRIPT_DIR/init-change.sh" "$change_desc"

    # 4. 生成执行计划
    echo "║  [3/5] 生成执行计划..."
    local plan_file
    plan_file=$(generate_execution_plan "$change_desc" "$(detect_project_type)")

    # 5. 展示计划并请求确认
    echo "║  [4/5] 展示执行计划..."
    show_execution_plan "$plan_file"

    echo "║  [5/5] 等待启动确认..."
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 请求确认
    request_confirmation "auto-start" "全流程自动化" \
        "是否启动 Phase 0→7 的全流程自动化执行？" \
        "critical" \
        "启动全流程自动化执行" \
        "逐阶段手动执行" \
        "先查看详细执行计划"

    # 根据选择执行
    case "$REPLY" in
        1|yes|y)
            auto_execute "$plan_file"
            ;;
        2|no|n)
            echo "已取消自动化执行，可手动执行 /flow-kit:phase-0"
            ;;
        3)
            show_detailed_plan "$plan_file"
            ;;
    esac
}
```

### 3.5 自我进化机制（F5）

#### F5.1 执行数据收集（增强版）

创建 `scripts/evolution-engine.sh`，集成 WAL 协议和 VFM 评分模型：

```bash
# evolution-engine.sh — 自我进化引擎 v2.0
# 集成 WAL 协议 + VFM 评分 + CE 知识复利

# 收集执行数据
collect_execution_metrics() {
    local plan_file="$1"

    local metrics_file="$PROJECT_DIR/.flow-kit/evolution-metrics.json"

    # 收集各阶段耗时
    local phase_times="{}"
    for ((phase=0; phase<=7; phase++)); do
        local start_time
        start_time=$(jq -r ".phases[$phase].start_time // \"\"" "$plan_file")
        local end_time
        end_time=$(jq -r ".phases[$phase].end_time // \"\"" "$plan_file")

        if [[ -n "$start_time" && -n "$end_time" ]]; then
            local duration
            duration=$(calculate_duration "$start_time" "$end_time")
            phase_times=$(echo "$phase_times" | jq --argjson p "$phase" --argjson d "$duration" '. + {($p|tostring): $d}')
        fi
    done

    # 收集确认次数
    local confirm_count
    confirm_count=$(grep -c "\[confirm\]" "$PROJECT_DIR/.flow-kit/execution.log" 2>/dev/null || echo "0")

    # 收集越界事件
    local boundary_count
    boundary_count=$(grep -c "\[boundary\]" "$PROJECT_DIR/.flow-kit/execution.log" 2>/dev/null || echo "0")

    # 收集自动修复次数
    local fix_count
    fix_count=$(grep -c "\[auto-fix\]" "$PROJECT_DIR/.flow-kit/execution.log" 2>/dev/null || echo "0")

    # 收集 Token 消耗
    local token_usage
    token_usage=$(calculate_token_usage "$plan_file")

    # 保存指标
    jq -n \
        --argjson phase_times "$phase_times" \
        --argjson confirm_count "$confirm_count" \
        --argjson boundary_count "$boundary_count" \
        --argjson fix_count "$fix_count" \
        --argjson token_usage "$token_usage" \
        --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        '{
            timestamp: $ts,
            phase_times: $phase_times,
            total_time: ($phase_times | [.[]] | add // 0),
            confirm_count: $confirm_count,
            boundary_count: $boundary_count,
            fix_count: $fix_count,
            token_usage: $token_usage,
            auto_ratio: (if (.confirm_count + .boundary_count + .fix_count) > 0 then
                (.fix_count / (.confirm_count + .boundary_count + .fix_count) * 100)
            else 0 end)
        }' > "$metrics_file"
}

# WAL 协议：先写入，再回复
wal_write() {
    local trigger_type="$1"  # error | correction | pattern | feature
    local content="$2"

    case "$trigger_type" in
        "error")
            echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | $content" >> "$PROJECT_DIR/.flow-kit/learnings/ERRORS.md"
            ;;
        "correction")
            echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | $content" >> "$PROJECT_DIR/.flow-kit/learnings/LEARNINGS.md"
            ;;
        "pattern")
            local count
            count=$(grep -c "$content" "$PROJECT_DIR/.flow-kit/learnings/ERRORS.md" 2>/dev/null || echo "0")
            count=$((count + 1))
            echo "[wal] 🔄 重复模式检测: '$content' 已出现 $count 次"
            if [[ $count -ge 3 ]]; then
                echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | Recurrence-Count: $count | $content" >> "$PROJECT_DIR/.flow-kit/learnings/RECURRING.md"
            fi
            ;;
        "feature")
            echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | $content" >> "$PROJECT_DIR/.flow-kit/learnings/FEATURE_REQUESTS.md"
            ;;
    esac
}

# VFM 评分模型
vfm_score() {
    local value="$1"      # 这个教训能避免多大损失 (1-5)
    local frequency="$2"  # 这个场景出现的频率 (1-5)
    local maintainability="$3"  # 规则本身的维护成本 (1-5)
    local is_infra="${4:-false}"  # 是否为 infra 类型错误

    local score=$((value * 3 + frequency * 2 + maintainability * 1))

    # infra 类型错误偏向性加权
    if [[ "$is_infra" == "true" ]]; then
        score=$((score + 2))
    fi

    echo "$score"
}

# 生成优化建议
generate_optimization_suggestions() {
    local metrics_dir="$PROJECT_DIR/.flow-kit/evolution-metrics"

    # 分析历史趋势
    local total_time_trend
    total_time_trend=$(analyze_trend "$metrics_dir" "total_time")

    local boundary_trend
    boundary_trend=$(analyze_trend "$metrics_dir" "boundary_count")

    local auto_ratio_trend
    auto_ratio_trend=$(analyze_trend "$metrics_dir" "auto_ratio")

    local token_trend
    token_trend=$(analyze_trend "$metrics_dir" "token_usage")

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 自我进化建议                ║"
    echo "╠══════════════════════════════════════════╣"

    if [[ "$total_time_trend" == "increasing" ]]; then
        echo "║  ⚠️  执行时间呈上升趋势"
        echo "║  建议: 检查是否有新增的耗时操作"
        echo "║  优化: 考虑并行化或缓存优化"
    fi

    if [[ "$boundary_trend" == "increasing" ]]; then
        echo "║  ⚠️  越界事件呈上升趋势"
        echo "║  建议: 审查 phase-boundaries.yaml 配置"
        echo "║  优化: 增强边界校验规则"
    fi

    if [[ "$auto_ratio_trend" == "decreasing" ]]; then
        echo "║  ⚠️  自动修复成功率下降"
        echo "║  建议: 审查 auto_fix_issues 函数"
        echo "║  优化: 添加新的修复策略"
    fi

    if [[ "$token_trend" == "increasing" ]]; then
        echo "║  ⚠️  Token 消耗呈上升趋势"
        echo "║  建议: 启用 RTK 终端净化或 Caveman 压缩"
        echo "║  优化: 考虑 OpenWolf 三文件记忆系统"
    fi

    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 生成优化配置文件
    generate_optimization_config
}
```

#### F5.2 工作流自动优化

借鉴 superpowers-zh 的 workflow-runner 和 agency-agents-zh 的 DAG 编排思想，实现工作流的持续优化：

```yaml
# .flow-kit/evolution-config.yaml — 进化配置（增强版）
evolution:
  enabled: true
  learning_interval: "7d"  # 每 7 天分析一次
  metrics:
    - phase_execution_time
    - confirmation_count
    - boundary_violations
    - auto_fix_success_rate
    - user_satisfaction_score
    - token_usage
    - knowledge_capture_rate

  optimization_rules:
    - condition: "phase_execution_time > 5min"
      action: "建议拆分 phase 或启用并行执行"
    - condition: "confirmation_count > 10"
      action: "建议提高 auto_confirm_threshold"
    - condition: "boundary_violations > 3"
      action: "建议更新 phase-boundaries.yaml"
    - condition: "auto_fix_success_rate < 50%"
      action: "建议审查 auto_fix_issues 函数"
    - condition: "token_usage > 100000"
      action: "建议启用 RTK 终端净化或 Caveman 压缩"
    - condition: "knowledge_capture_rate < 30%"
      action: "建议检查 WAL 协议配置"

  auto_adjustments:
    - metric: "auto_confirm_threshold"
      rule: "如果连续 3 次执行无越界事件，自动提升确认阈值"
    - metric: "parallel_level"
      rule: "如果 phase 执行时间 > 3min，自动启用并行执行"
    - metric: "confirmation_skip"
      rule: "如果某操作连续 5 次选择相同选项，自动跳过确认"
    - metric: "token_compression"
      rule: "如果 Token 消耗 > 80k/次，自动启用 RTK 净化"

  # CE 知识复利配置
  knowledge_compounding:
    enabled: true
    extract_on_phase_complete: true
    auto_reference: true
    vfm_threshold: 15  # VFM 评分 >= 15 自动提升为永久规则
```

### 3.6 环境自动清理机制（F6）

#### F6.1 自动清理触发器

创建 `scripts/auto-cleanup.sh`，集成 handoff 交接机制：

```bash
# auto-cleanup.sh — 环境自动清理器（增强版）
# 集成 handoff 会话交接 + 上下文健康检查

# 自动清理入口
auto_cleanup() {
    local mode="${1:-auto}"  # auto | full | minimal

    echo "[cleanup] 开始清理执行环境..."

    # 1. 保存当前会话状态
    local session_backup
    session_backup=$(backup_session_state)

    # 2. 生成 handoff 文档（用于跨会话交接）
    generate_handoff_doc

    # 3. 执行 /clear 命令
    if command -v clear &>/dev/null; then
        echo "[cleanup] 执行环境重置..."
        clear_session_context
    fi

    # 4. 清理临时文件
    cleanup_temp_files "$mode"

    # 5. 重置执行状态（保留 session-state）
    reset_execution_state

    # 6. 恢复必要的会话上下文
    restore_session_context "$session_backup"

    echo "[cleanup] ✅ 环境清理完成"
    echo "[cleanup] 状态: session-state 已保留，临时文件已清理，上下文已重置"
}

# 备份会话状态
backup_session_state() {
    local backup_file="$PROJECT_DIR/.flow-kit/session-backup.json"

    if [[ -f "$SESSION_STATE_FILE" ]]; then
        cp "$SESSION_STATE_FILE" "$backup_file"
        echo "$backup_file"
    fi
}

# 生成 handoff 文档
generate_handoff_doc() {
    local handoff_file="$PROJECT_DIR/.flow-kit/handoff.md"

    cat > "$handoff_file" << HANDOFF
# Handoff 文档 — $(date -u +%Y-%m-%dT%H:%M:%SZ)

## 当前状态
- 项目: $(basename "$PROJECT_DIR")
- 当前 Phase: $(session_get phase)
- 执行计划: $(session_get execution_plan)
- 已完成: $(session_get history)

## 关键上下文
- 变更描述: $(session_get change)
- 项目类型: $(session_get ptype)
- 运行模式: $(session_get mode)

## 待办事项
$(session_get next)

## 已知阻塞
$(session_get blockers)

## 决策历史
$(cat "$PROJECT_DIR/.flow-kit/decisions.log" 2>/dev/null | tail -20)

## 学习记录
$(cat "$PROJECT_DIR/.flow-kit/learnings/LEARNINGS.md" 2>/dev/null | tail -10)
HANDOFF

    echo "[handoff] ✅ Handoff 文档已生成: $handoff_file"
}

# 清理临时文件
cleanup_temp_files() {
    local mode="$1"

    case "$mode" in
        full)
            rm -rf "$TMP_DIR"/* 2>/dev/null || true
            rm -rf "$PROJECT_DIR/.flow-kit/tmp"/* 2>/dev/null || true
            ;;
        auto)
            find "$TMP_DIR" -name "*.tmp" -delete 2>/dev/null || true
            find "$TMP_DIR" -name "*.log" -mtime +1 -delete 2>/dev/null || true
            ;;
        minimal)
            rm -f "$TMP_DIR"/*.lock 2>/dev/null || true
            ;;
    esac
}

# 重置执行状态
reset_execution_state() {
    session_set status "ready"
    session_set next ""
    session_set blockers ""
}

# 恢复会话上下文
restore_session_context() {
    local backup_file="$1"

    if [[ -f "$backup_file" ]]; then
        local change_id
        change_id=$(jq -r '.change // ""' "$backup_file")
        local phase
        phase=$(jq -r '.phase // "0"' "$backup_file")

        if [[ -n "$change_id" ]]; then
            session_set change "$change_id"
            session_set phase "$phase"
        fi

        rm -f "$backup_file"
    fi
}
```

#### F6.2 命令执行前自动触发

在 `hooks/pre-tool-guard.sh` 中添加自动清理逻辑：

```bash
# 在 pre-tool-guard.sh 中添加
auto_cleanup_before_command() {
    local command="$1"

    # 检测到 phase 命令时自动触发清理
    if [[ "$command" == *"phase-"* ]] || [[ "$command" == *"auto-start"* ]]; then
        local last_cleanup
        last_cleanup=$(session_get last_cleanup 2>/dev/null || echo "")

        if [[ -z "$last_cleanup" ]] || [[ $(is_older_than "$last_cleanup" "5min") == "true" ]]; then
            echo "[pre-tool] 检测到 phase 命令，自动清理环境..."
            bash "$SCRIPT_DIR/auto-cleanup.sh" auto
            session_set last_cleanup "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        fi
    fi
}
```

#### F6.3 Secrets 保护机制

借鉴 [rihebty/flow-kit](https://github.com/rihebty/flow-kit) 社区版的 Secrets 保护机制，在环境清理和提交前自动扫描敏感信息，防止密钥泄露：

```bash
# secrets-guard.sh — 敏感信息保护器
SECRETS_PATTERNS=(
    "API[_-]?KEY[=:]\s*[A-Za-z0-9_\-]{20,}"
    "SECRET[=:]\s*[A-Za-z0-9_\-]{20,}"
    "TOKEN[=:]\s*[A-Za-z0-9_\-]{20,}"
    "PASSWORD[=:]\s*[^\s]{8,}"
    "PRIVATE[_-]?KEY[=:]\s*[A-Za-z0-9_\-]{20,}"
    "ACCESS[_-]?KEY[=:]\s*[A-Za-z0-9_\-]{20,}"
    "-----BEGIN (RSA|EC|DSA|OPENSSH) PRIVATE KEY-----"
    "ghp_[A-Za-z0-9]{36}"
    "gho_[A-Za-z0-9]{36}"
    "ghu_[A-Za-z0-9]{36}"
    "ghs_[A-Za-z0-9]{36}"
    "ghr_[A-Za-z0-9]{36}"
    "sk-[A-Za-z0-9]{48}"
    "xox[bpras]-[A-Za-z0-9\-]{10,}"
    "AKIA[0-9A-Z]{16}"
    "eyJ[A-Za-z0-9\-_]+\.[A-Za-z0-9\-_]+\.[A-Za-z0-9\-_]+"
)

SECRETS_SCAN_EXCLUDE=(
    ".git/"
    "node_modules/"
    "vendor/"
    "*.lock"
    "*.min.js"
    "*.min.css"
    "*.map"
    ".flow-kit/"
)

scan_for_secrets() {
    local target_dir="${1:-$PROJECT_DIR}"
    local scan_mode="${2:-full}"  # full | staged | diff

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║       Secrets 敏感信息扫描               ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    local findings=0
    local exclude_args=""

    for pattern in "${SECRETS_SCAN_EXCLUDE[@]}"; do
        exclude_args="$exclude_args --exclude-dir=$pattern"
    done

    case "$scan_mode" in
        full)
            echo "[secrets] 全量扫描模式..."
            for secret_pattern in "${SECRETS_PATTERNS[@]}"; do
                local matches
                matches=$(grep -rInE $exclude_args "$secret_pattern" "$target_dir" 2>/dev/null || true)
                if [[ -n "$matches" ]]; then
                    echo ""
                    echo "⚠️  发现疑似敏感信息:"
                    echo "$matches" | while read -r line; do
                        local file_path=$(echo "$line" | cut -d: -f1)
                        local line_num=$(echo "$line" | cut -d: -f2)
                        local masked_content=$(echo "$line" | cut -d: -f3- | sed 's/\(.\{4\}\).*\(.\{4\}\)/\1****\2/')
                        echo "  📁 $file_path:$line_num → $masked_content"
                    done
                    findings=$((findings + 1))
                fi
            done
            ;;
        staged)
            echo "[secrets] 暂存区扫描模式..."
            local staged_files
            staged_files=$(git -C "$target_dir" diff --cached --name-only 2>/dev/null || true)
            for file in $staged_files; do
                for secret_pattern in "${SECRETS_PATTERNS[@]}"; do
                    if grep -qE "$secret_pattern" "$target_dir/$file" 2>/dev/null; then
                        echo "⚠️  $file 包含疑似敏感信息"
                        findings=$((findings + 1))
                    fi
                done
            done
            ;;
        diff)
            echo "[secrets] 差异扫描模式..."
            local diff_output
            diff_output=$(git -C "$target_dir" diff 2>/dev/null || true)
            for secret_pattern in "${SECRETS_PATTERNS[@]}"; do
                if echo "$diff_output" | grep -qE "$secret_pattern"; then
                    echo "⚠️  差异中包含疑似敏感信息"
                    findings=$((findings + 1))
                fi
            done
            ;;
    esac

    if [[ $findings -eq 0 ]]; then
        echo "[secrets] ✅ 未发现敏感信息"
        return 0
    else
        echo ""
        echo "[secrets] ⚠️  发现 $findings 处疑似敏感信息"
        return 1
    fi
}

enforce_env_example() {
    local target_dir="${1:-$PROJECT_DIR}"

    echo "[secrets] 检查 .env.example 和 .gitignore..."

    if [[ ! -f "$target_dir/.env.example" ]]; then
        echo "[secrets] ⚠️  缺少 .env.example 文件"
        echo "[secrets] 建议创建 .env.example 并列出所有需要的环境变量（不含实际值）"
        return 1
    fi

    if [[ -f "$target_dir/.gitignore" ]]; then
        if ! grep -q "\.env" "$target_dir/.gitignore" 2>/dev/null; then
            echo "[secrets] ⚠️  .gitignore 中缺少 .env 规则"
            echo ".env" >> "$target_dir/.gitignore"
            echo "[secrets] ✅ 已自动添加 .env 到 .gitignore"
        fi
    else
        echo "[secrets] ⚠️  缺少 .gitignore 文件，正在创建..."
        cat > "$target_dir/.gitignore" << GITIGNORE
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*.pfx
credentials.json
service-account.json
GITIGNORE
        echo "[secrets] ✅ 已创建 .gitignore 并添加敏感文件规则"
    fi

    echo "[secrets] ✅ .env.example 和 .gitignore 检查完成"
}

pre_commit_secrets_check() {
    echo "[secrets] Git 提交前敏感信息检查..."

    enforce_env_example

    if ! scan_for_secrets "$PROJECT_DIR" "staged"; then
        echo ""
        echo "╔══════════════════════════════════════════╗"
        echo "║  ⛔ 提交被阻止：发现疑似敏感信息         ║"
        echo "╠══════════════════════════════════════════╣"
        echo "║  请执行以下操作：                        ║"
        echo "║  1. 确认是否为真实密钥                   ║"
        echo "║  2. 将密钥移至 .env 文件                 ║"
        echo "║  3. 使用环境变量替换硬编码密钥           ║"
        echo "║  4. 如为误报，添加到 .secrets-ignore     ║"
        echo "╚══════════════════════════════════════════╝"
        echo ""
        return 1
    fi

    echo "[secrets] ✅ 提交前检查通过"
    return 0
}
```

#### F6.4 与 Safety Hooks 集成

```yaml
# secrets-integration.yaml
integration:
  pre_tool_guard:
    description: "在 pre-tool-guard.sh 中集成 Secrets 扫描"
    triggers:
      - "git commit 命令执行前"
      - "git push 命令执行前"
      - "文件写入操作前（检测写入内容是否包含密钥）"
    action: "secrets-guard.sh pre_commit_secrets_check"

  auto_cleanup:
    description: "环境清理时同步执行 Secrets 扫描"
    action: "auto-cleanup.sh → secrets-guard.sh scan_for_secrets"

  safety_hooks:
    description: "作为 Safety Hook 的一部分"
    hook_name: "secrets_protection_hook"
    hook_type: "Decision Warn Hook"
    priority: "critical"

  ci_cd:
    description: "CI/CD 流水线集成"
    actions:
      - "PR 创建时自动扫描"
      - "合并前强制检查"
      - "发现密钥时自动标记 PR 为 blocked"
```

**预期改进效果**：
- 敏感信息泄露风险降低 95%（提交前自动扫描）
- 安全合规性提升 100%（强制 .env.example + .gitignore）
- 密钥管理规范化（自动检测 GitHub Token、AWS Key、JWT 等常见格式）

**借鉴来源**：
- rihebty/flow-kit Secrets 保护机制：强制 .env.example + .gitignore + 提交前扫描
- gitleaks：敏感信息扫描模式库
- GitHub Secret Scanning：常见密钥格式正则

### 3.7 持续任务执行系统（F7）

借鉴 [mattpocock/skills](https://github.com/mattpocock/skills) 的自主运行方法论、[Ralph.md](y:\pwl\code\plugins\Ralph.md) 记录的 Ralph Wiggum 循环模式，以及 superpowers+gstack 开发流程实录中的任务分流机制，构建持续运行、自动完成任务的执行系统。

#### F7.1 自主运行模式设计（增强版）

实现三种自主运行模式，平衡安全性与自动化程度：

```yaml
# .flow-kit/auto-runner.yaml — 自主运行配置（增强版）
auto_runner:
  modes:
    supervised:  # 模式 1：监督模式（默认，最安全）
      permission_mode: "auto"
      allowed_tools:
        - "Read"
        - "Edit"
        - "Bash(npm run *)"
        - "Bash(git *)"
      max_turns: 50
      max_budget_usd: 10.00
      confirmation_level: "critical"
      description: "安全操作自动批准，高风险操作需人工确认"
      use_case: "日常开发，需要安全护栏"

    semi_autonomous:  # 模式 2：半自主模式
      permission_mode: "auto"
      allowed_tools:
        - "Read"
        - "Edit"
        - "Bash(*)"
        - "WebSearch"
      max_turns: 100
      max_budget_usd: 25.00
      confirmation_level: "important"
      description: "大部分操作自动批准，仅关键操作需确认"
      use_case: "夜间运行，少量人工干预"

    full_autonomous:  # 模式 3：完全自主模式（仅容器环境）
      permission_mode: "dangerously-skip-permissions"
      allowed_tools:
        - "Read"
        - "Edit"
        - "Bash(*)"
        - "WebSearch"
        - "WebFetch"
      max_turns: 200
      max_budget_usd: 50.00
      confirmation_level: "none"
      description: "完全自主执行，无人工干预"
      use_case: "容器环境，已验证的安全沙箱"
      constraints:
        container_only: true
        network_isolation: true
        pre_flight_checklist:
          - "git 检查点已创建"
          - "容器已启动"
          - "PROMPT.md 已编写"
          - "四个 Hook 已安装"
```

#### F7.2 Ralph Wiggum 循环集成（增强版）

创建 `scripts/ralph-runner.sh`，实现 Ralph Wiggum 循环模式的自动化，集成每次迭代全新 AI 实例：

```bash
# ralph-runner.sh — Ralph Wiggum 循环执行器 v2.0
# 集成每次迭代全新 AI 实例 + Subagent Fork 并行执行

RALPH_MODE="${RALPH_MODE:-supervised}"  # supervised | semi_autonomous | full_autonomous
RALPH_MAX_ITERATIONS="${RALPH_MAX_ITERATIONS:-10}"
RALPH_SAFETY_HOOKS="${RALPH_SAFETY_HOOKS:-true}"

declare -A RALPH_MODES
RALPH_MODES[supervised]="监督模式：每步需确认，适合高风险操作"
RALPH_MODES[semi_autonomous]="半自主模式：关键步骤确认，适合日常开发"
RALPH_MODES[full_autonomous]="全自主模式：无需确认，适合低风险批量任务"

# 安全 Hook 列表
RALPH_SAFETY_HOOKS_LIST=(
    "git_push_hook: 推送前检查分支和远程地址"
    "file_delete_hook: 删除文件前备份到 .trash/"
    "npm_publish_hook: 发布前检查版本号和 CHANGELOG"
    "db_migration_hook: 数据库迁移前检查备份"
    "env_var_hook: 环境变量修改前记录变更"
)

# Ralph 主执行循环
ralph_execute() {
    local task_file="$1"
    local mode="${2:-$RALPH_MODE}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     Ralph Wiggum 自主执行循环            ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  模式: ${RALPH_MODES[$mode]}"
    echo "║  最大迭代: $RALPH_MAX_ITERATIONS"
    echo "║  安全 Hook: $RALPH_SAFETY_HOOKS"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    local iteration=0
    local task_count
    task_count=$(jq '.tasks | length' "$task_file")

    while [[ $iteration -lt $RALPH_MAX_ITERATIONS ]]; do
        iteration=$((iteration + 1))

        echo ""
        echo "────────────────────────────────────────"
        echo "  Ralph 迭代 #$iteration"
        echo "────────────────────────────────────────"
        echo ""

        local current_task
        current_task=$(get_current_task "$task_file")

        if [[ -z "$current_task" ]]; then
            echo "[ralph] ✅ 所有任务已完成"
            break
        fi

        local risk_level
        risk_level=$(assess_task_risk "$current_task")

        # 确认机制
        if needs_confirmation "$mode" "$risk_level"; then
            echo "[ralph] 当前任务: $(echo "$current_task" | jq -r '.description')"
            echo "[ralph] 风险级别: $risk_level"
            request_confirmation "ralph" "任务执行" \
                "是否执行此任务？" \
                "$risk_level" \
                "执行此任务" \
                "跳过此任务" \
                "切换到监督模式"
        fi

        # 执行安全 Hook
        if [[ "$RALPH_SAFETY_HOOKS" == "true" ]]; then
            run_safety_hooks "$current_task"
        fi

        # 执行任务（每次迭代使用全新 AI 实例）
        echo "[ralph] 启动新 AI 实例执行任务..."
        local task_result
        task_result=$(execute_task_fresh_ai "$current_task")

        # 检查结果
        if [[ "$task_result" == "success" ]]; then
            echo "[ralph] ✅ 任务执行成功"
            mark_task_completed "$task_file" "$(echo "$current_task" | jq -r '.id')"
        else
            echo "[ralph] ❌ 任务执行失败"
            handle_task_failure "$task_file" "$current_task" "$task_result"
        fi

        # 生成迭代摘要
        generate_iteration_summary "$iteration" "$task_result"
    done

    # 生成执行报告
    generate_ralph_report "$task_file"
}

# Subagent Fork 并行执行
ralph_fork_execute() {
    local task_file="$1"
    local parallel_count="${2:-3}"

    echo "[ralph-fork] 启动 Subagent Fork 并行执行模式"
    echo "[ralph-fork] 并行数: $parallel_count"

    local pending_tasks
    pending_tasks=$(jq -c '.tasks[] | select(.status == "pending")' "$task_file")

    local batch=0
    while read -r task; do
        batch=$((batch + 1))

        if [[ $batch -gt $parallel_count ]]; then
            echo "[ralph-fork] 等待当前批次完成..."
            wait_for_fork_completion
            batch=1
        fi

        local task_id
        task_id=$(echo "$task" | jq -r '.id')
        local task_desc
        task_desc=$(echo "$task" | jq -r '.description')

        echo "[ralph-fork] Fork 子 Agent #$batch: $task_desc"
        fork_subagent "$task" &
    done <<< "$pending_tasks"

    wait_for_fork_completion
    echo "[ralph-fork] ✅ 所有 Fork 任务完成"
}

# 每次迭代使用全新 AI 实例
execute_task_fresh_ai() {
    local task="$1"
    local task_id
    task_id=$(echo "$task" | jq -r '.id')
    local task_desc
    task_desc=$(echo "$task" | jq -r '.description')

    # 生成独立的 PROMPT.md
    local prompt_file="$PROJECT_DIR/.flow-kit/ralph/task-$task_id-prompt.md"
    mkdir -p "$(dirname "$prompt_file")"

    cat > "$prompt_file" << PROMPT
# Ralph 任务执行 — 迭代 #$task_id

## 任务描述
$task_desc

## 上下文
$(cat "$PROJECT_DIR/.flow-kit/ralph/context.md" 2>/dev/null || echo "无上下文")

## 约束
- 只做任务描述中的事情
- 完成后更新任务状态
- 不要修改超出范围的文件
- 记录所有操作到日志

## 完成条件
$(echo "$task" | jq -r '.acceptance_criteria // "无"')
PROMPT

    # 启动新 AI 实例执行
    echo "[ralph] 启动新 AI 实例执行任务: $task_desc"
    echo "[ralph] Prompt 文件: $prompt_file"

    # 模拟执行（实际由 Claude Code 调度）
    echo "success"
}

# 任务失败处理
handle_task_failure() {
    local task_file="$1"
    local task="$2"
    local error="$3"

    local task_id
    task_id=$(echo "$task" | jq -r '.id')
    local retry_count
    retry_count=$(echo "$task" | jq -r '.retry_count // 0')

    if [[ $retry_count -lt 3 ]]; then
        echo "[ralph] 重试任务 #$task_id (第 $((retry_count + 1)) 次)"
        jq --arg id "$task_id" --argjson retry $((retry_count + 1)) \
            '(.tasks[] | select(.id == $id) | .retry_count) = $retry | (.tasks[] | select(.id == $id) | .status) = "pending"' \
            "$task_file" > "${task_file}.tmp" && mv "${task_file}.tmp" "$task_file"
    else
        echo "[ralph] ❌ 任务 #$task_id 重试 3 次仍失败，标记为 blocked"
        jq --arg id "$task_id" \
            '(.tasks[] | select(.id == $id) | .status) = "blocked"' \
            "$task_file" > "${task_file}.tmp" && mv "${task_file}.tmp" "$task_file"

        # WAL 记录
        wal_write "error" "Ralph 任务失败: $(echo "$task" | jq -r '.description')"
    fi
}

# 生成迭代摘要
generate_iteration_summary() {
    local iteration="$1"
    local result="$2"

    local summary_file="$PROJECT_DIR/.flow-kit/ralph/iteration-$iteration-summary.md"

    cat > "$summary_file" << SUMMARY
# Ralph 迭代 #$iteration 摘要

- 时间: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- 结果: $result
- 模式: $RALPH_MODE

## 完成情况
$(jq -r '.tasks[] | select(.status == "completed") | "- ✅ " + .description' "$task_file" 2>/dev/null || echo "无")

## 待处理
$(jq -r '.tasks[] | select(.status == "pending") | "- ⏳ " + .description' "$task_file" 2>/dev/null || echo "无")

## 阻塞
$(jq -r '.tasks[] | select(.status == "blocked") | "- ❌ " + .description' "$task_file" 2>/dev/null || echo "无")
SUMMARY
}
```

#### F7.3 四个安全 Hook 集成

创建 `scripts/safety-hooks.sh`，集成 Ralph.md 记录的四个关键 Hook：

```bash
# safety-hooks.sh — 安全 Hook 安装器

install_safety_hooks() {
    echo "[hooks] 安装四个安全 Hook..."

    # Hook 1: No-Ask-Human — 阻止 AskUserQuestion 工具调用
    install_no_ask_human() {
        cat > "$HOOKS_DIR/pre-tool-guard.sh" << 'HOOK'
# No-Ask-Human Hook: 阻止 AskUserQuestion 工具调用
block_ask_human() {
    local tool="$1"
    if [[ "$tool" == "AskUserQuestion" ]]; then
        if [[ "${CC_ALLOW_QUESTIONS:-0}" != "1" ]]; then
            echo "[hook] ❌ AskUserQuestion 被 No-Ask-Human Hook 阻止"
            echo "[hook] 设置 CC_ALLOW_QUESTIONS=1 以允许提问"
            return 1
        fi
    fi
    return 0
}
HOOK
    }

    # Hook 2: Context Monitor — 监控上下文使用量
    install_context_monitor() {
        cat > "$HOOKS_DIR/context-monitor.sh" << 'HOOK'
# Context Monitor Hook: 监控上下文使用量
CONTEXT_THRESHOLDS=(40 25 20 15)

check_context_usage() {
    local tool_calls="$1"
    local max_calls="${2:-200}"

    local usage_pct=$((tool_calls * 100 / max_calls))

    for threshold in "${CONTEXT_THRESHOLDS[@]}"; do
        if [[ $usage_pct -ge $((100 - threshold)) ]]; then
            echo "[hook] ⚠️  上下文使用量: ${usage_pct}% (剩余 ${threshold}%)"
            if [[ $usage_pct -ge 85 ]]; then
                echo "[hook] 🔄 建议执行 /compact 释放上下文"
            fi
            break
        fi
    done
}
HOOK
    }

    # Hook 3: Syntax Check — 文件编辑后立即语法检查
    install_syntax_check() {
        cat > "$HOOKS_DIR/syntax-check.sh" << 'HOOK'
# Syntax Check Hook: 编辑后自动语法检查
run_syntax_check() {
    local file="$1"

    case "$file" in
        *.js|*.ts|*.jsx|*.tsx)
            node --check "$file" 2>/dev/null && echo "[hook] ✅ JS/TS 语法检查通过" || echo "[hook] ⚠️  JS/TS 语法检查失败"
            ;;
        *.py)
            python -m py_compile "$file" 2>/dev/null && echo "[hook] ✅ Python 语法检查通过" || echo "[hook] ⚠️  Python 语法检查失败"
            ;;
        *.sh)
            bash -n "$file" 2>/dev/null && echo "[hook] ✅ Shell 语法检查通过" || echo "[hook] ⚠️  Shell 语法检查失败"
            ;;
        *.json)
            node -e "JSON.parse(require('fs').readFileSync('$file','utf8'))" 2>/dev/null && echo "[hook] ✅ JSON 语法检查通过" || echo "[hook] ⚠️  JSON 语法检查失败"
            ;;
    esac
}
HOOK
    }

    # Hook 4: Decision Warn — 破坏性命令警告
    install_decision_warn() {
        cat > "$HOOKS_DIR/decision-warn.sh" << 'HOOK'
# Decision Warn Hook: 破坏性命令警告
PROTECTED_BRANCHES="${CC_PROTECT_BRANCHES:-main:master:production}"
DANGEROUS_COMMANDS=("rm -rf" "git reset --hard" "DROP TABLE" "git push --force" "kubectl delete")

check_dangerous_command() {
    local cmd="$1"

    for dangerous in "${DANGEROUS_COMMANDS[@]}"; do
        if echo "$cmd" | grep -qi "$dangerous"; then
            echo "[hook] ⚠️  检测到危险命令: $cmd"
            echo "[hook] 建议: 使用 /careful <cmd> 执行"
            return 1
        fi
    done

    if echo "$cmd" | grep -qiE "git push.*(main|master|production)"; then
        echo "[hook] ⚠️  禁止推送到受保护分支"
        echo "[hook] 受保护分支: $PROTECTED_BRANCHES"
        return 1
    fi

    return 0
}
HOOK
    }

    install_no_ask_human
    install_context_monitor
    install_syntax_check
    install_decision_warn

    echo "[hooks] ✅ 四个安全 Hook 安装完成"
    echo "[hooks] - No-Ask-Human: 阻止不必要的提问"
    echo "[hooks] - Context Monitor: 监控上下文使用量"
    echo "[hooks] - Syntax Check: 编辑后自动语法检查"
    echo "[hooks] - Decision Warn: 破坏性命令警告"
}
```

**借鉴来源**：
- Ralph.md 第 3 节"防止过夜灾难的四个 Hook"：完整 Hook 实现逻辑
- Ralph.md 第 8 节"已知陷阱"：8 种故障模式的预防策略
- Ralph.md 第 5 节"CLAUDE.md 与长时间运行的上下文管理"：token 节省策略

#### F7.4 通用任务模板系统

创建 `scripts/task-runner.sh`，实现标准化任务模板：

```bash
# task-runner.sh — 通用任务执行器

TASK_TEMPLATE='任务：[任务描述]

要求：
- [要求 1]
- [要求 2]
- [要求 3]

验证步骤：
1. 运行测试：[测试命令]
2. 检查覆盖率：[覆盖率命令]
3. 运行 linter：[lint 命令]

完成标准：
- 所有测试通过
- 覆盖率 >= [目标]%
- 无 linter 错误

完成时输出：<promise>DONE</promise>

异常处理机制：
如果系统在执行过程中连续卡住超过 [N] 次迭代：
- 自动输出当前任务进度详情
- 列出所有遇到的具体问题
- 提供基于历史数据的解决建议
'

execute_task() {
    local task_file="$1"
    local mode="${2:-supervised}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 任务执行器                  ║"
    echo "╠══════════════════════════════════════════╣"
    echo "║  任务: $(head -1 "$task_file")"
    echo "║  模式: $mode"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    local requirements
    requirements=$(parse_requirements "$task_file")

    local verification_steps
    verification_steps=$(parse_verification_steps "$task_file")

    local completion_criteria
    completion_criteria=$(parse_completion_criteria "$task_file")

    generate_task_prompt "$task_file" "$PROJECT_DIR/PROMPT.md"

    start_ralph_loop "$mode" "$PROJECT_DIR/PROMPT.md"

    if verify_completion "$completion_criteria"; then
        echo "<promise>DONE</promise>"
        return 0
    else
        echo "[task-runner] ❌ 任务未完全满足完成标准"
        return 1
    fi
}

parse_verification_steps() {
    local file="$1"
    local steps=""

    while IFS= read -r line; do
        if [[ "$line" =~ ^[0-9]+\.\ 运行测试|^[0-9]+\.\ 检查覆盖率|^[0-9]+\.\ 运行 ]]; then
            steps="$steps$line"$'\n'
        fi
    done < "$file"

    echo "$steps"
}

generate_task_prompt() {
    local task_file="$1"
    local prompt_file="$2"

    cat > "$prompt_file" << PROMPT
# 任务：$(head -1 "$task_file")

## 上下文
- 项目: flow-kit
- 工作目录: $PROJECT_DIR
- 当前状态: $(git log --oneline -3 2>/dev/null || echo "无提交历史")

## 目标
- 查看 TODO.md，选择下一个未完成的任务
- 实现它，包含完善的错误处理
- 运行测试，修复失败，确认没有回归
- 做通用修复，不要打临时补丁
- 每完成一个任务后用描述性消息提交

## 要求
$(cat "$task_file" | sed -n '/^要求：/,/^验证步骤：/p' | head -n -1)

## 验证
$(cat "$task_file" | sed -n '/^验证步骤：/,/^完成标准：/p' | head -n -1)

## 完成标准
$(cat "$task_file" | sed -n '/^完成标准：/,/^完成时输出：/p' | head -n -1)

## 自主运行规则
- 技术方案不确定 → 选择传统方案
- 两种可行实现 → 选择更简单的那个
- 需求模糊 → 应用最合理的理解，记录假设
- 尝试 3 次后仍有错误 → 记录到 blocked.md，切换任务
- 永远不要提问。做出最佳判断然后继续。

## 完成时
- 输出 <promise>DONE</promise>
- 提交所有更改
- 更新 TODO.md 标记已完成
PROMPT

    echo "[task-runner] ✅ PROMPT.md 已生成: $prompt_file"
}
```

**借鉴来源**：
- mattpocock/skills 的 `/tdd` skill：红-绿-重构循环和测试驱动开发
- mattpocock/skills 的 `/diagnose` skill：系统化诊断循环
- mattpocock/skills 的 `/grill-me` skill：需求澄清 interview 模式
- Ralph.md 第 2 节：PROMPT.md 设计和"完成"标准
- 用户提供的任务模板格式

#### F7.5 异常处理与审计机制

```bash
# 异常处理：连续卡住检测
check_stuck() {
    local iteration="$1"
    local log_file="$LOG_FILE"

    if [[ $iteration -ge 3 ]]; then
        local recent_logs
        recent_logs=$(tail -100 "$log_file" 2>/dev/null)

        if echo "$recent_logs" | grep -c "error\|Error\|ERROR" | head -1 | grep -q "[3-9]"; then
            return 0
        fi

        if echo "$recent_logs" | grep -q "same\|again\|retry\|still"; then
            return 0
        fi
    fi

    return 1
}

# 异常处理：卡住时输出诊断信息
handle_stuck() {
    local iteration="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     异常处理：任务卡住诊断               ║"
    echo "╠══════════════════════════════════════════╣"

    echo "║  当前迭代: #$iteration"
    echo "║  执行时间: $(calculate_elapsed_time)"
    echo "║  日志文件: $LOG_FILE"
    echo "╠══════════════════════════════════════════╣"

    echo "║  检测到的问题:"
    local issues
    issues=$(extract_issues_from_log "$LOG_FILE")
    echo "$issues" | while read -r issue; do
        echo "║  - $issue"
    done

    echo "╠══════════════════════════════════════════╣"

    echo "║  建议方案:"
    echo "║  1. 切换任务（记录当前进度到 blocked.md）"
    echo "║  2. 降低复杂度（拆分当前任务）"
    echo "║  3. 请求人工介入（保留当前状态）"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    local next_task
    next_task=$(find_next_uncompleted_task "$PROJECT_DIR/TODO.md")
    if [[ -n "$next_task" ]]; then
        echo "[ralph] 自动切换到下一个任务: $next_task"
        update_task_status "$next_task" "blocked"
        generate_task_prompt "$next_task" "$PROJECT_DIR/PROMPT.md"
    fi
}

# 审计日志
audit_task_execution() {
    local task_file="$1"
    local log_file="$LOG_FILE"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║     flow-kit 任务执行审计                ║"
    echo "╠══════════════════════════════════════════╣"

    local criteria
    criteria=$(parse_completion_criteria "$task_file")
    local all_met=true

    while IFS= read -r criterion; do
        if verify_criterion "$criterion" "$log_file"; then
            echo "║  ✅ $criterion"
        else
            echo "║  ❌ $criterion"
            all_met=false
        fi
    done <<< "$criteria"

    echo "╠══════════════════════════════════════════╣"

    local error_count
    error_count=$(grep -c "\[ralph\]" "$log_file" 2>/dev/null || echo "0")
    local stuck_count
    stuck_count=$(grep -c "卡住" "$log_file" 2>/dev/null || echo "0")

    echo "║  总迭代: $(grep -c '迭代 #' "$log_file" 2>/dev/null || echo "0")"
    echo "║  异常事件: $stuck_count"
    echo "║  错误次数: $error_count"

    if [[ "$all_met" == true ]]; then
        echo "║  结果: ✅ 所有完成标准已满足"
    else
        echo "║  结果: ⚠️  部分完成标准未满足"
    fi

    echo "╚══════════════════════════════════════════╝"
    echo ""

    return $all_met
}
```

**借鉴来源**：
- mattpocock/skills 的 `/diagnose` skill：系统化诊断循环（reproduce → minimise → hypothesise → instrument → fix → regression-test）
- mattpocock/skills 的 `/handoff` skill：会话交接文档生成
- Ralph.md 第 8 节"已知陷阱"：8 种故障模式的识别和处理
- 用户提供的异常处理机制模板

#### F7.6 与现有 Phase 系统的集成

```yaml
# .flow-kit/integration-config.yaml — 持续执行系统与 Phase 系统集成
integration:
  phase_to_ralph:
    trigger: "Phase 4（开发执行）完成后自动启动 Ralph 循环"
    condition: "auto_pilot_mode == supervised && tasks_remaining > 3"
    action: "start_ralph_loop supervised"

  ralph_to_phase:
    trigger: "Ralph 循环检测到 <promise>DONE</promise>"
    action: "auto_pilot.sh resume_from phase-5"
    description: "Ralph 循环完成开发执行后，回归 Phase 5 测试验证"

  prd_to_ralph:
    trigger: "PRD 文档解析完成"
    action: "prd-parser.sh → auto-start.sh → ralph-runner.sh"
    description: "PRD 文档自动解析后直接启动 Ralph 循环"

  cron_to_ralph:
    trigger: "cron 定时触发"
    action: "ralph-runner.sh semi_autonomous"
    description: "定时启动 Ralph 循环执行预定义任务"
    cron_syntax: "0 * * * * /path/to/ralph-runner.sh"
```

**借鉴来源**：
- Ralph.md 第 7 节"CI/CD 流水线与 Cron 任务集成"：Boucle 模式和 cron 触发
- superpowers+gstack 开发流程实录第 4 节"开发闭环"：接力赛模式的交接点
- superpowers+gstack 开发流程实录第 3 节"CLAUDE.md 配置"：任务分流规则

### 3.8 紧急修复流程（F8）

借鉴 [rihebty/flow-kit](https://github.com/rihebty/flow-kit) 社区版的 H-hotfix.md 三档响应机制，构建生产事故与紧急 Bug 的快速响应通道，确保紧急修复不破坏正常开发流程的完整性。

#### F8.1 三档响应分级

```yaml
# hotfix-levels.yaml — 紧急修复分级定义
hotfix_levels:
  P0:
    name: "生产事故"
    trigger_conditions:
      - 生产环境服务不可用
      - 数据丢失或损坏
      - 安全漏洞被利用
    response: "先回滚再诊断"
    max_response_time: "15 分钟"
    auto_actions:
      - 自动触发回滚到上一个稳定版本
      - 自动发送告警通知
      - 自动创建 incident 记录
    post_resolution:
      - 根因分析（RCA）
      - 修复方案文档
      - 测试补充（防止复发）
      - 监控告警规则更新
      - 文档更新
    deadline: "24 小时内完成全部 5 件事"

  P1:
    name: "紧急 Bug"
    trigger_conditions:
      - 核心功能不可用
      - 用户体验严重受损
      - 数据不一致
    response: "3 问澄清后直接修复"
    max_response_time: "2 小时"
    clarification_questions:
      - "问题影响范围？（用户数/功能数）"
      - "是否有临时绕过方案？"
      - "修复的紧急程度？（是否阻塞发布）"
    post_resolution:
      - 根因分析（RCA）
      - 修复方案文档
      - 测试补充
      - 监控告警规则更新
      - 文档更新
    deadline: "24 小时内完成全部 5 件事"

  P2:
    name: "非紧急问题"
    trigger_conditions:
      - 非核心功能异常
      - 偶发性问题
      - 优化类需求
    response: "建议走正常流程"
    max_response_time: "下一个迭代"
    action: "自动创建 Change Request，纳入正常 Phase 0-7 流程"
```

#### F8.2 H-hotfix.md 工作流

创建 `phases/hotfix/H-hotfix.md`，作为 Phase 工作流的紧急分支：

```markdown
# H-hotfix.md — 紧急修复工作流

## 触发条件
- 用户执行 `/flow-kit:hotfix` 命令
- 系统检测到 P0/P1 级别事件
- CI/CD 流水线检测到生产环境异常

## 工作流步骤

### Step 1: 分级判断
1. 收集事故信息（时间、影响范围、错误日志）
2. 根据 hotfix-levels.yaml 自动分级
3. 展示分级结果并请求确认

### Step 2: P0 响应 — 先回滚再诊断
1. 自动定位上一个稳定版本
2. 执行回滚操作（需确认）
3. 验证回滚后服务恢复
4. 发送恢复通知
5. 开始根因分析

### Step 3: P1 响应 — 3 问澄清后修复
1. 展示 3 个澄清问题
2. 收集用户回答
3. 基于回答生成修复方案
4. 用户确认后执行修复
5. 验证修复效果

### Step 4: P2 响应 — 走正常流程
1. 自动创建 Change Request
2. 纳入正常 Phase 0-7 流程
3. 通知相关人员

### Step 5: 事后 5 件事（P0/P1 必须）
1. 根因分析（RCA）→ 写入 .flow-kit/learnings/RCA/
2. 修复方案文档 → 写入 CHANGE.md
3. 测试补充 → 防止同类问题复发
4. 监控告警规则更新 → 更新监控配置
5. 文档更新 → 更新相关文档
```

#### F8.3 紧急修复执行器

```bash
# hotfix-executor.sh — 紧急修复执行器
HOTFIX_RCA_DIR="$PROJECT_DIR/.flow-kit/learnings/RCA"

execute_hotfix() {
    local incident_description="$1"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║       H-hotfix 紧急修复流程              ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    local level
    level=$(classify_incident "$incident_description")

    echo "[hotfix] 事故分级: $level"

    case "$level" in
        P0)
            echo "[hotfix] 🔴 P0 生产事故 — 先回滚再诊断"
            execute_rollback
            send_alert "P0" "$incident_description"
            create_incident_record "$incident_description" "P0"
            ;;
        P1)
            echo "[hotfix] 🟡 P1 紧急 Bug — 3 问澄清后修复"
            ask_clarification_questions
            generate_fix_plan
            request_confirmation "hotfix" "P1 修复方案" "critical"
            execute_fix
            verify_fix
            ;;
        P2)
            echo "[hotfix] 🟢 P2 非紧急 — 建议走正常流程"
            create_change_request "$incident_description"
            ;;
    esac

    if [[ "$level" == "P0" ]] || [[ "$level" == "P1" ]]; then
        generate_post_resolution_checklist "$level"
    fi
}

classify_incident() {
    local description="$1"

    if echo "$description" | grep -qiE "生产|production|down|不可用|数据丢失|安全漏洞"; then
        echo "P0"
    elif echo "$description" | grep -qiE "核心|critical|紧急|urgent|block"; then
        echo "P1"
    else
        echo "P2"
    fi
}

ask_clarification_questions() {
    echo "[hotfix] 请回答以下 3 个问题："
    echo ""
    echo "  Q1: 问题影响范围？（影响的用户数/功能数）"
    echo "  Q2: 是否有临时绕过方案？"
    echo "  Q3: 修复的紧急程度？（是否阻塞发布）"
    echo ""
    read -r -p "请输入回答（用 | 分隔）: " answers
}

generate_post_resolution_checklist() {
    local level="$1"
    local rca_file="$HOTFIX_RCA_DIR/incident-$(date +%Y%m%d-%H%M%S).md"

    mkdir -p "$HOTFIX_RCA_DIR"

    cat > "$rca_file" << RCAEOF
# 事故报告 — $(date -u +%Y-%m-%dT%H:%M:%SZ)

## 基本信息
- 级别: $level
- 发现时间: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- 解决时间: TBD

## 事后 5 件事检查清单
- [ ] 1. 根因分析（RCA）
- [ ] 2. 修复方案文档
- [ ] 3. 测试补充（防止复发）
- [ ] 4. 监控告警规则更新
- [ ] 5. 文档更新

## 根因分析
（待填写）

## 修复方案
（待填写）

## 预防措施
（待填写）
RCAEOF

    echo "[hotfix] RCA 模板已生成: $rca_file"
    echo "[hotfix] ⚠️ 请在 24 小时内完成全部 5 件事"
}
```

#### F8.4 与正常流程的集成

```yaml
# hotfix-integration.yaml
integration:
  phase_override:
    description: "H-hotfix 可绕过正常 Phase 0-3，直接进入 Phase 4（开发执行）"
    conditions:
      - "事故级别为 P0 或 P1"
      - "修复方案已确认"
    skip_phases: [0, 1, 2, 3]
    entry_phase: 4

  post_hotfix_merge:
    description: "紧急修复完成后，自动创建 Change Request 归档"
    actions:
      - "生成 CHANGE.md"
      - "更新 session-state.json"
      - "触发 CE 知识复利提取"

  normal_flow_fallback:
    description: "P2 级别自动回退到正常 Phase 0-7 流程"
    action: "auto-start.sh --from phase-0"
```

**预期改进效果**：
- 紧急响应时间减少 60%（三档响应快速分流）
- 事故复发率降低 70%（24 小时内补齐 5 件事）
- 生产稳定性提升 50%（P0 先回滚再诊断）
- 紧急修复可追溯性提升 100%（RCA 自动归档）

**借鉴来源**：
- rihebty/flow-kit H-hotfix.md：三档响应机制 + 事后 5 件事
- Google SRE 方法论：先回滚再诊断原则
- CE 知识复利系统：RCA 自动归档到 learnings/

### 3.9 项目记忆恢复（F9）

借鉴 [rihebty/flow-kit](https://github.com/rihebty/flow-kit) 社区版的 R-recall.md 项目记忆恢复机制，解决开发者切换项目或长期未维护后的上下文丢失问题。

#### F9.1 R-recall.md 触发机制

```yaml
# recall-triggers.yaml
recall_triggers:
  explicit:
    - command: "/flow-kit:recall"
      description: "用户主动触发项目记忆恢复"
    - command: "@GO.md 首次加载"
      description: "项目入口文件加载时自动触发"

  implicit:
    - condition: "session-state 超过 7 天未更新"
      action: "自动提示执行 recall"
    - condition: "项目目录首次打开"
      action: "自动生成项目摘要"
    - condition: "git 仓库超过 30 天无提交"
      action: "触发 bit rot 检查"

  bit_rot_check:
    description: "超过 30 天未维护的项目自动检查"
    checks:
      - "依赖过期检查（npm outdated / pip list --outdated）"
      - "安全漏洞扫描（npm audit / pip audit）"
      - "文档滞后检查（CHANGELOG / README 最后更新日期）"
      - "CI/CD 流水线状态检查"
      - "数据库迁移状态检查"
```

#### F9.2 5 分钟项目摘要生成

```bash
# recall-executor.sh — 项目记忆恢复执行器
RECALL_CACHE_DIR="$PROJECT_DIR/.flow-kit/recall"

generate_project_summary() {
    local project_dir="${1:-$PROJECT_DIR}"

    echo ""
    echo "╔══════════════════════════════════════════╗"
    echo "║       R-recall 项目记忆恢复              ║"
    echo "╚══════════════════════════════════════════╝"
    echo ""

    # 1. 项目基本信息
    echo "## 📋 项目概览"
    echo ""
    echo "| 项目 | 信息 |"
    echo "|------|------|"
    echo "| 名称 | $(basename "$project_dir") |"
    echo "| 类型 | $(detect_project_type "$project_dir") |"
    echo "| 语言 | $(detect_languages "$project_dir") |"
    echo "| 框架 | $(detect_frameworks "$project_dir") |"
    echo "| 最后活跃 | $(get_last_activity "$project_dir") |"
    echo ""

    # 2. 当前开发状态
    echo "## 🔄 当前状态"
    echo ""
    if [[ -f "$SESSION_STATE_FILE" ]]; then
        echo "| 状态项 | 值 |"
        echo "|--------|-----|"
        echo "| 当前 Phase | $(session_get phase) |"
        echo "| 变更 ID | $(session_get change) |"
        echo "| 执行状态 | $(session_get status) |"
        echo "| 下一步 | $(session_get next) |"
        echo "| 阻塞项 | $(session_get blockers) |"
        echo ""
    else
        echo "⚠️ 未找到会话状态，建议执行 /flow-kit:init"
        echo ""
    fi

    # 3. 最近变更
    echo "## 📝 最近变更"
    echo ""
    git -C "$project_dir" log --oneline -5 2>/dev/null || echo "（无 Git 历史）"
    echo ""

    # 4. 推荐下一步
    echo "## 🎯 推荐下一步"
    echo ""
    suggest_next_action "$project_dir"
    echo ""

    # 5. Bit Rot 检查（超过 30 天）
    local last_activity
    last_activity=$(get_last_activity_timestamp "$project_dir")
    local days_inactive=$(( ($(date +%s) - last_activity) / 86400 ))

    if [[ $days_inactive -gt 30 ]]; then
        echo "## ⚠️ Bit Rot 检查（$days_inactive 天未活跃）"
        echo ""
        run_bit_rot_check "$project_dir"
    fi
}

suggest_next_action() {
    local project_dir="$1"

    local current_phase
    current_phase=$(session_get phase 2>/dev/null || echo "0")

    case "$current_phase" in
        0|1|2|3)
            echo "📌 当前处于思考阶段（Phase $current_phase），建议继续执行 /flow-kit:phase-$current_phase"
            ;;
        4)
            echo "📌 当前处于开发执行阶段，建议检查未完成的任务并继续 Ralph 循环"
            ;;
        5|6)
            echo "📌 当前处于验证阶段，建议执行测试或代码审查"
            ;;
        7)
            echo "📌 上一变更已完成，建议执行 /flow-kit:auto-start 启动新变更"
            ;;
        *)
            echo "📌 建议执行 /flow-kit:recall 获取完整项目状态"
            ;;
    esac
}

run_bit_rot_check() {
    local project_dir="$1"

    echo "| 检查项 | 状态 | 建议 |"
    echo "|--------|------|------|"

    # 依赖过期检查
    if [[ -f "$project_dir/package.json" ]]; then
        local outdated
        outdated=$(cd "$project_dir" && npm outdated --json 2>/dev/null | jq 'length' 2>/dev/null || echo "0")
        if [[ "$outdated" -gt 0 ]]; then
            echo "| 依赖过期 | ⚠️ $outdated 个包过期 | 执行 npm update |"
        else
            echo "| 依赖过期 | ✅ 最新 | — |"
        fi
    fi

    # 安全漏洞检查
    if [[ -f "$project_dir/package.json" ]]; then
        echo "| 安全漏洞 | ⚠️ 建议检查 | 执行 npm audit |"
    fi

    # 文档检查
    if [[ ! -f "$project_dir/CHANGELOG.md" ]]; then
        echo "| CHANGELOG | ❌ 缺失 | 建议创建 CHANGELOG.md |"
    fi

    # Git 状态
    local uncommitted
    uncommitted=$(git -C "$project_dir" status --porcelain 2>/dev/null | wc -l)
    if [[ "$uncommitted" -gt 0 ]]; then
        echo "| 未提交变更 | ⚠️ $uncommitted 个文件 | 建议提交或暂存 |"
    fi
}
```

#### F9.3 与现有系统的集成

```yaml
# recall-integration.yaml
integration:
  session_state:
    description: "R-recall 读取 session-state.json 恢复上下文"
    fields:
      - phase
      - change
      - status
      - next
      - blockers
      - history
      - decisions

  evolution_engine:
    description: "bit rot 检查结果反馈给进化引擎"
    action: "evolution-engine.sh collect_bit_rot_metrics"

  auto_cleanup:
    description: "recall 前自动检查环境状态"
    action: "auto-cleanup.sh minimal"

  go_md:
    description: "@GO.md 加载时自动触发 recall"
    trigger: "boundary-enforcer.sh 检测到 @GO.md 引用"
    action: "recall-executor.sh --quick"
```

**预期改进效果**：
- 项目恢复时间减少 80%（5 分钟摘要快速回忆）
- 长期未维护项目风险降低 60%（bit rot 自动检查）
- 开发者上下文切换成本降低 70%（一键恢复）
- 项目健康度可视化提升 100%（bit rot 报告）

**借鉴来源**：
- rihebty/flow-kit R-recall.md：5 分钟项目摘要 + bit rot 检查
- OpenWolf anatomy.md：项目地图概念
- Claude Code session-state：会话状态恢复机制

---

## 4. 用户故事

### 4.1 用户故事列表

| ID | 角色 | 故事 | 优先级 | 对应功能 |
|----|------|------|--------|----------|
| US-001 | 独立开发者 | 作为独立开发者，我希望执行 `/flow-kit:phase-0` 时只做变更立项，不做架构设计 | P0 | F1 |
| US-002 | 独立开发者 | 作为独立开发者，我希望在执行关键操作前有明确的确认提示 | P0 | F1 |
| US-003 | 独立开发者 | 作为独立开发者，我希望随时查看当前执行阶段和下一步计划 | P1 | F1 |
| US-004 | 技术负责人 | 作为技术负责人，我希望一键启动 Phase 0→7 的全流程自动化执行 | P0 | F2 |
| US-005 | 技术负责人 | 作为技术负责人，我希望在确认环节看到 AI 推荐的选项 | P1 | F2 |
| US-006 | 技术负责人 | 作为技术负责人，我希望自动审计执行过程，发现越界或遗漏 | P1 | F3 |
| US-007 | 产品经理 | 作为产品经理，我希望提交 PRD 文档后自动解析并启动开发流程 | P1 | F4 |
| US-008 | 技术负责人 | 作为技术负责人，我希望系统能自动优化工作流 | P2 | F5 |
| US-009 | 独立开发者 | 作为独立开发者，我希望执行 phase 命令前自动清理环境 | P2 | F6 |
| US-010 | 独立开发者 | 作为独立开发者，我希望系统能持续运行，自动完成批量任务 | P1 | F7 |
| US-011 | 技术负责人 | 作为技术负责人，我希望系统能自动沉淀知识，避免重复错误 | P1 | F5 |
| US-012 | 独立开发者 | 作为独立开发者，我希望 Token 消耗自动优化，降低使用成本 | P2 | F5 |
| US-013 | SRE/运维 | 作为 SRE，我希望生产事故能快速分级响应，P0 先回滚再诊断 | P0 | F8 |
| US-014 | 开发者 | 作为开发者，我希望紧急 Bug 修复后系统自动生成 RCA 报告 | P1 | F8 |
| US-015 | 开发者 | 作为开发者，我希望切换回旧项目时能快速恢复上下文 | P1 | F9 |
| US-016 | 技术负责人 | 作为技术负责人，我希望长期未维护项目自动检查 bit rot | P2 | F9 |
| US-017 | 开发者 | 作为开发者，我希望提交代码前自动扫描敏感信息 | P0 | F6 |

### 4.2 用户故事验收标准

| ID | 验收标准 |
|----|----------|
| US-001 | 执行 phase-0 时，如果尝试执行架构设计操作，系统提示越界并阻止 |
| US-002 | 每个 phase 的关键操作前，显示确认对话框，包含操作描述和推荐选项 |
| US-003 | 执行过程中，状态面板实时显示当前 phase、进度、下一步操作 |
| US-004 | 输入 `/flow-kit:auto-start "变更描述"` 后，自动完成 Phase 0→7 全部流程 |
| US-005 | 确认环节显示 2-3 个推荐选项，并标注推荐理由 |
| US-006 | 执行完成后，自动生成审计报告，列出所有问题和警告 |
| US-007 | 提交 PRD 文档后，自动解析并生成变更描述和执行计划 |
| US-008 | 每 7 天自动分析执行数据，生成优化建议 |
| US-009 | 执行 phase 命令前，自动清理临时文件并重置上下文 |
| US-010 | 提交任务列表后，系统自动循环执行，每次迭代使用全新 AI 实例 |
| US-011 | 每次执行完成后，自动提取经验教训并写入知识库 |
| US-012 | 自动启用 RTK 终端净化，Token 消耗降低 60% 以上 |
| US-013 | 执行 `/flow-kit:hotfix` 后，系统自动分级（P0/P1/P2）并执行对应响应流程 |
| US-014 | P0/P1 修复完成后，24 小时内自动生成 RCA 报告并归档到 learnings/RCA/ |
| US-015 | 执行 `/flow-kit:recall` 后，5 分钟内输出项目摘要和推荐下一步 |
| US-016 | 项目超过 30 天未活跃时，自动触发 bit rot 检查并生成报告 |
| US-017 | git commit 前自动扫描敏感信息，发现密钥时阻止提交并提示处理方式 |

---

## 5. 优先级排序

### 5.1 MoSCoW 优先级

| 优先级 | 功能 | 工作量 | 价值 | 风险 |
|--------|------|--------|------|------|
| **Must Have** | F1 Phase 边界重构 | 3 人天 | 极高 | 低 |
| **Must Have** | F2 自动化执行引擎 | 5 人天 | 极高 | 中 |
| **Must Have** | F3 自动化审计 | 3 人天 | 高 | 低 |
| **Must Have** | F8 紧急修复流程 | 3 人天 | 极高 | 中 |
| **Should Have** | F4 PRD 自动解析 | 4 人天 | 高 | 中 |
| **Should Have** | F7 持续任务执行 | 5 人天 | 高 | 高 |
| **Should Have** | F9 项目记忆恢复 | 2 人天 | 高 | 低 |
| **Could Have** | F5 自我进化 | 4 人天 | 中 | 高 |
| **Could Have** | F6 环境自动清理 | 2 人天 | 中 | 低 |

### 5.2 价值-风险矩阵

```
价值
  ↑
  │  F1,F3  │  F2,F4,F7,F8
  │  高价值  │  高价值
  │  低风险  │  中高风险
  │         │
  ├─────────┼─────────→ 风险
  │  F6,F9  │  F5
  │  中价值  │  中价值
  │  低风险  │  高风险
  │         │
```

### 5.3 依赖关系图

```
F1 (Phase 边界) ──→ F2 (自动化执行) ──→ F3 (自动化审计)
                        │
                        ├──→ F4 (PRD 解析)
                        │
                        ├──→ F7 (持续任务)
                        │       │
                        │       └──→ F5 (自我进化)
                        │               │
                        │               └──→ F6 (环境清理)
                        │                       │
                        │                       └──→ Secrets 保护
                        │
                        ├──→ F8 (紧急修复)
                        │       │
                        │       └──→ F5 (RCA 归档到知识库)
                        │
                        └──→ F9 (项目记忆恢复)
                                │
                                └──→ F6 (bit rot 检查)
```

---

## 6. UI 设计

### 6.1 状态面板设计

```
╔══════════════════════════════════════════╗
║  🚀 flow-kit v3.6.0 执行状态面板        ║
╠══════════════════════════════════════════╣
║  ✅ Phase 0: 变更立项    [thinking] 已完成║
║  ✅ Phase 1: 需求澄清    [thinking] 已完成║
║  🔄 Phase 2: 架构设计    [thinking] 执行中║
║  ⬜ Phase 3: 任务拆解    [thinking] 待执行║
║  ⬜ Phase 4: 开发执行    [execution] 待执行║
║  ⬜ Phase 5: 测试验证    [execution] 待执行║
║  ⬜ Phase 6: 代码审查    [execution] 待执行║
║  ⬜ Phase 7: 集成归档    [execution] 待执行║
╠══════════════════════════════════════════╣
║  当前操作: 正在分析技术方案              ║
║  完成进度: 25% (2/8)                    ║
║  下一步: 生成架构决策记录 (ADR)         ║
║  Token 消耗: 12,345                     ║
╚══════════════════════════════════════════╝
```

### 6.2 确认对话框设计

```
╔══════════════════════════════════════════╗
║  📋 Phase 推进确认                       ║
╠══════════════════════════════════════════╣
║  Phase: phase-2                          ║
║  操作: 架构设计                          ║
║  描述: 基于需求自动生成架构方案          ║
║  级别: important (建议人工确认)          ║
╠══════════════════════════════════════════╣
║  推荐选项:                               ║
║    1. 自动生成架构方案（基于需求分析）   ║
║    2. 选择架构模板（微服务/单体/事件驱动）║
║    3. 手动设计架构（适用于复杂场景）     ║
╚══════════════════════════════════════════╝
请输入确认 (yes/no/auto) 或选择选项编号:
```

### 6.3 审计报告设计

```
╔══════════════════════════════════════════╗
║         flow-kit 执行审计报告            ║
╠══════════════════════════════════════════╣
║  ✅ Phase 0 (变更立项): 已完成           ║
║  ✅ Phase 1 (需求澄清): 已完成           ║
║  ✅ Phase 2 (架构设计): 已完成           ║
║  ⚠️  Phase 3 (任务拆解): 输出文件不完整  ║
║  ✅ Phase 4 (开发执行): 已完成           ║
║  ✅ Phase 5 (测试验证): 已完成           ║
║  ✅ Phase 6 (代码审查): 已完成           ║
║  ✅ Phase 7 (集成归档): 已完成           ║
╠══════════════════════════════════════════╣
║  ⚠️  检测到可能遗漏的步骤:               ║
║     - Phase 3: 缺少 DAG 依赖分析         ║
╠══════════════════════════════════════════╣
║  💰 Token 消耗: 45,678                   ║
║  问题: 0  |  警告: 1                     ║
╚══════════════════════════════════════════╝
```

### 6.4 Ralph 循环状态面板

```
╔══════════════════════════════════════════╗
║     Ralph Wiggum 自主执行循环            ║
╠══════════════════════════════════════════╣
║  模式: 半自主模式                        ║
║  迭代: 3/10                              ║
║  任务: 5/12 完成                         ║
║  安全 Hook: 已启用                       ║
╠══════════════════════════════════════════╣
║  当前任务: 实现用户认证模块              ║
║  风险级别: medium                        ║
║  上次结果: ✅ 成功                       ║
║  Token 消耗: 8,901                       ║
╚══════════════════════════════════════════╝
```

### 6.5 H-hotfix 紧急修复面板

```
╔══════════════════════════════════════════╗
║       H-hotfix 紧急修复流程              ║
╠══════════════════════════════════════════╣
║  事故描述: 生产环境 API 响应超时         ║
║  自动分级: 🔴 P0 生产事故               ║
║  响应策略: 先回滚再诊断                  ║
║  最大响应时间: 15 分钟                   ║
╠══════════════════════════════════════════╣
║  执行步骤:                               ║
║  ✅ 1. 定位上一个稳定版本 (v2.3.1)      ║
║  🔄 2. 执行回滚操作...                  ║
║  ⬜ 3. 验证服务恢复                      ║
║  ⬜ 4. 发送恢复通知                      ║
║  ⬜ 5. 开始根因分析                      ║
╠══════════════════════════════════════════╣
║  ⚠️  事后 5 件事（24h 内完成）:          ║
║  ⬜ 根因分析  ⬜ 修复方案  ⬜ 测试补充   ║
║  ⬜ 监控告警  ⬜ 文档更新                ║
╚══════════════════════════════════════════╝
```

### 6.6 R-recall 项目记忆恢复面板

```
╔══════════════════════════════════════════╗
║       R-recall 项目记忆恢复              ║
╠══════════════════════════════════════════╣
║  📋 项目概览                             ║
║  名称: flow-kit                          ║
║  类型: CLI 工具                          ║
║  语言: Bash / Shell                      ║
║  最后活跃: 2026-05-10                    ║
╠══════════════════════════════════════════╣
║  🔄 当前状态                             ║
║  Phase: 4 (开发执行)                     ║
║  变更: CHANGE-20260510-001               ║
║  状态: running                           ║
║  阻塞: 无                                ║
╠══════════════════════════════════════════╣
║  📝 最近变更 (git log -5)                ║
║  a1b2c3d feat: 添加 H-hotfix 支持       ║
║  e4f5g6h fix: 修复 boundary-enforcer    ║
║  i7j8k9l refactor: 重构 auto-pilot      ║
╠══════════════════════════════════════════╣
║  🎯 推荐下一步                           ║
║  继续执行 Ralph 循环，完成剩余 7 个任务  ║
╠══════════════════════════════════════════╣
║  ⚠️  Bit Rot 检查（35 天未活跃）         ║
║  依赖过期: ⚠️ 3 个包过期                ║
║  安全漏洞: ⚠️ 建议执行 npm audit        ║
║  CHANGELOG: ✅ 最新                     ║
╚══════════════════════════════════════════╝
```

### 6.7 Secrets 扫描结果面板

```
╔══════════════════════════════════════════╗
║       Secrets 敏感信息扫描               ║
╠══════════════════════════════════════════╣
║  扫描模式: 暂存区扫描                    ║
║  扫描文件: 12 个                         ║
║  扫描模式: 15 种                         ║
╠══════════════════════════════════════════╣
║  ⚠️  发现 2 处疑似敏感信息:              ║
║  📁 config/app.ts:42 → API_****KEY      ║
║  📁 scripts/deploy.sh:15 → ghp_****xYz  ║
╠══════════════════════════════════════════╣
║  ⛔ 提交已被阻止                          ║
║  建议:                                   ║
║  1. 将密钥移至 .env 文件                 ║
║  2. 使用环境变量替换硬编码密钥           ║
║  3. 如为误报，添加到 .secrets-ignore     ║
╚══════════════════════════════════════════╝
```

---

## 7. 数据流程图

### 7.1 全流程自动化执行数据流

```
用户输入 (变更描述 / PRD 文件)
    │
    ▼
┌─────────────────────────────────────────────────┐
│              auto-start.sh                       │
│  1. 检测输入类型 (文本/文件)                      │
│  2. 解析 PRD (如需要)                            │
│  3. 调用 auto-cleanup.sh 清理环境                 │
│  4. 调用 init-change.sh 初始化变更                │
│  5. 生成执行计划 (auto-plan.json)                 │
│  6. 展示计划并请求确认                            │
└──────────────────────┬──────────────────────────┘
                       │ 确认后
                       ▼
┌─────────────────────────────────────────────────┐
│              auto-pilot.sh                       │
│  Phase 0→7 循环执行:                             │
│    1. boundary-enforcer.sh 校验边界              │
│    2. confirmation-manager.sh 请求确认           │
│    3. status-display.sh 更新状态                 │
│    4. phase-executor.sh 执行 phase              │
│    5. validate-phase.sh 验证输出                 │
│    6. execution-auditor.sh 审计                  │
│    7. auto-fix (如需要)                          │
└──────────────────────┬──────────────────────────┘
                       │ 完成后
                       ▼
┌─────────────────────────────────────────────────┐
│              evolution-engine.sh                 │
│  1. collect_execution_metrics                    │
│  2. generate_optimization_suggestions            │
│  3. update_evolution_config                      │
└─────────────────────────────────────────────────┘
```

### 7.2 Phase 边界校验数据流

```
用户执行 /flow-kit:phase-N
    │
    ▼
┌─────────────────────────────────────────────────┐
│              pre-tool-guard.sh                   │
│  1. auto-cleanup.sh (检测是否需要清理)            │
│  2. 检查命令合法性                                │
└──────────────────────┬──────────────────────────┘
                       │ 通过
                       ▼
┌─────────────────────────────────────────────────┐
│              phase-executor.sh                   │
│  1. 加载 phase-boundaries.yaml                   │
│  2. 声明当前 phase 的职责范围                     │
│  3. 显示状态面板                                  │
│  4. 加载工作流文件                                │
│  5. 注册 boundary-enforcer 回调                  │
└──────────────────────┬──────────────────────────┘
                       │ 执行中
                       ▼
┌─────────────────────────────────────────────────┐
│              boundary-enforcer.sh                │
│  每次操作前:                                     │
│    1. 检查操作是否在 allowed_operations 中        │
│    2. 检查操作是否在 forbidden_operations 中      │
│    3. 如越界: 拦截 + 提示 + 记录                  │
│    4. 如合法: 放行 + 记录                         │
└──────────────────────┬──────────────────────────┘
                       │ 关键操作
                       ▼
┌─────────────────────────────────────────────────┐
│              confirmation-manager.sh             │
│  1. 确定确认级别 (critical/important/normal)      │
│  2. 生成推荐选项 (decision-engine)                │
│  3. 显示确认对话框                                │
│  4. 等待用户输入                                  │
│  5. 记录决策                                      │
└─────────────────────────────────────────────────┘
```

### 7.3 数据存储结构

```json
{
  ".flow-kit/": {
    "session-state.json": "会话状态（已有，增强）",
    "auto-plan.json": "自动化执行计划（新增）",
    "phase-boundaries.yaml": "Phase 边界定义（新增）",
    "evolution-config.yaml": "进化配置（新增）",
    "evolution-metrics.json": "进化指标（新增）",
    "decisions.log": "决策历史记录（新增）",
    "execution.log": "执行日志（已有，增强）",
    "session-backup.json": "会话备份（新增）",
    "hotfix-levels.yaml": "紧急修复分级定义（新增）",
    "recall-triggers.yaml": "记忆恢复触发条件（新增）",
    "learnings/": {
      "RCA/": "根因分析归档（新增）",
      "LEARNINGS.md": "经验教训（已有，增强）",
      "ERRORS.md": "错误模式（已有，增强）"
    },
    "recall/": "项目记忆缓存（新增）",
    "secrets-ignore": "敏感信息扫描排除规则（新增）"
  }
}
```

---

## 8. 接口定义

### 8.1 内部脚本接口

| 脚本 | 输入 | 输出 | 调用方 |
|------|------|------|--------|
| `boundary-enforcer.sh` | `$phase`, `$operation` | exit code 0/1/2 | phase-executor.sh |
| `confirmation-manager.sh` | `$phase`, `$operation`, `$description`, `$level`, `$options[]` | `$REPLY` | phase-executor.sh, auto-pilot.sh |
| `status-display.sh` | `$current_phase` | stdout 面板 | phase-executor.sh, auto-pilot.sh |
| `auto-pilot.sh` | `$plan_file` | exit code 0/1 | auto-start.sh |
| `decision-engine.sh` | `$phase`, `$operation` | `$recommendations[]` | confirmation-manager.sh |
| `execution-auditor.sh` | `$plan_file` | audit report | auto-pilot.sh |
| `prd-parser.sh` | `$prd_file` | `$change_description` | auto-start.sh |
| `evolution-engine.sh` | `$metrics_dir` | suggestions | auto-pilot.sh (post-exec) |
| `auto-cleanup.sh` | `$mode` | exit code 0/1 | pre-tool-guard.sh, auto-start.sh |
| `hotfix-executor.sh` | `$incident_description` | exit code 0/1 | auto-pilot.sh, CLI |
| `recall-executor.sh` | `$project_dir` | stdout 摘要 | auto-start.sh, CLI |
| `secrets-guard.sh` | `$target_dir`, `$scan_mode` | exit code 0/1 | pre-tool-guard.sh, git hooks |

### 8.2 Session State 扩展字段

```json
{
  "v": 2,
  "change": "change-id",
  "phase": 0,
  "status": "init|wip|pending|completed|failed|paused|replan|ready",
  "ptype": "green|brown",
  "mode": "auto|team|ralph",
  "tasks": "T1:done,T2:wip,T3:todo",
  "next": "next action description",
  "blockers": "blocker description",
  "history": "phase-0,phase-1,...",
  "decisions": "phase-0:choice1,phase-1:choice2,...",
  "interactions": "",
  "ts": "2026-05-13T00:00:00Z",
  "v3_6_0_extensions": {
    "auto_pilot_mode": "interactive|silent|supervised",
    "auto_confirm_threshold": "critical|important|normal",
    "last_cleanup": "2026-05-13T00:00:00Z",
    "execution_plan": "path/to/auto-plan.json",
    "boundary_violations": 0,
    "auto_fixes_applied": 0,
    "evolution_score": 85,
    "hotfix": {
      "active_incident": null,
      "incident_level": null,
      "rca_file": null,
      "post_resolution_checklist": []
    },
    "recall": {
      "last_recall": null,
      "last_bit_rot_check": null,
      "days_inactive": 0
    },
    "secrets": {
      "last_scan": null,
      "scan_findings": 0,
      "env_example_exists": false
    }
  }
}
```

---

## 9. 非功能需求

### 9.1 性能需求

| 需求 | 指标 | 优先级 |
|------|------|--------|
| 状态面板响应时间 | < 1s | P1 |
| 边界校验响应时间 | < 500ms | P1 |
| 审计报告生成时间 | < 5s | P1 |
| PRD 解析时间 | < 10s | P2 |
| 执行计划生成时间 | < 3s | P1 |
| Ralph 循环迭代间隔 | 可配置（默认 30s） | P2 |
| 紧急修复分级响应时间 | P0 < 15min, P1 < 2h | P0 |
| 项目摘要生成时间 | < 5min | P1 |
| Secrets 扫描时间 | < 3s（暂存区扫描） | P1 |

### 9.2 安全需求

| 需求 | 描述 | 优先级 |
|------|------|--------|
| 操作确认 | 所有 critical 级别操作必须人工确认 | P0 |
| 越界阻止 | 禁止执行 forbidden_operations 中的操作 | P0 |
| 审计日志 | 所有操作记录到审计日志，不可篡改 | P1 |
| 权限控制 | 不同模式（supervised/semi/full）有不同的权限 | P1 |
| 敏感信息保护 | 自动扫描并阻止敏感信息泄露 | P1 |
| 容器隔离 | full_autonomous 模式仅限容器环境 | P0 |
| 紧急修复授权 | P0 回滚操作需二次确认 | P0 |
| 记忆恢复隐私 | recall 摘要不包含敏感配置信息 | P1 |

### 9.3 可用性需求

| 需求 | 描述 | 优先级 |
|------|------|--------|
| 状态透明 | 实时显示当前执行阶段和进度 | P0 |
| 错误提示 | 错误信息清晰，包含修复建议 | P1 |
| 推荐选项 | 确认环节提供智能推荐 | P1 |
| 一键操作 | 常用操作支持一键完成 | P1 |
| 进度保存 | 执行中断后可从断点继续 | P1 |

### 9.4 兼容性需求

| 需求 | 描述 | 优先级 |
|------|------|--------|
| Claude Code 版本 | 兼容 Claude Code v3.x | P0 |
| 操作系统 | 支持 macOS / Linux / Windows (WSL) | P1 |
| Shell 环境 | 兼容 bash / zsh | P1 |
| 工具依赖 | jq, yq, git 等基础工具 | P1 |

### 9.5 Token 优化需求

| 需求 | 描述 | 优先级 |
|------|------|--------|
| RTK 终端净化 | 自动过滤终端输出中的冗余信息 | P1 |
| Caveman 压缩 | 长文档自动压缩为精简摘要 | P2 |
| OpenWolf 记忆系统 | 三文件记忆架构（长期/短期/工作） | P2 |
| Prompt Cache | 利用 cache_control 断点共享上下文 | P2 |
| 模型分级 | 简单任务使用轻量模型 | P2 |

---

## 10. 约束条件

### 10.1 技术约束

| 约束 | 描述 |
|------|------|
| Shell 脚本 | 所有自动化脚本使用 bash 编写 |
| YAML 配置 | 边界定义和进化配置使用 YAML 格式 |
| JSON 数据 | 执行计划和状态数据使用 JSON 格式 |
| Markdown 文档 | 工作流文件和输出文档使用 Markdown |
| 无外部依赖 | 不引入新的外部依赖，仅使用 jq/yq/git |

### 10.2 流程约束

| 约束 | 描述 |
|------|------|
| 四闸门机制 | 所有 phase 切换必须经过四闸门检查 |
| 单主控原则 | 每轮对话只有一个主控路由 |
| @GO.md 零例外 | 出现 @GO.md 引用时强制展示路由选项 |
| 先写后答 | WAL 协议：先写入知识库，再回复用户 |
| 证据优先 | 没有证据的完成 = 没完成 |

### 10.3 业务约束

| 约束 | 描述 |
|------|------|
| 向后兼容 | v3.6.0 必须兼容 v3.3.0 的现有工作流 |
| 渐进式采用 | 用户可选择只启用部分功能 |
| 配置即代码 | 所有配置通过 YAML 文件管理 |
| 紧急修复可追溯 | P0/P1 修复必须生成 RCA 报告并归档 |
| 记忆恢复非侵入 | recall 不修改项目文件，仅读取和分析 |
| Secrets 零容忍 | 提交前扫描发现密钥必须阻止提交 |

---

## 11. 依赖关系

### 11.1 外部依赖

| 依赖 | 说明 | 关联功能 |
|------|------|----------|
| phase-boundaries.yaml | Phase 边界定义文件 | F1, F2, F3 |
| session-state.json | 会话状态文件 | F2, F3, F5, F6 |
| auto-plan.json | 执行计划文件 | F2, F3, F5 |
| evolution-metrics.json | 进化指标文件 | F5 |
| decisions.log | 决策历史日志 | F2, F5 |

### 11.2 外部依赖

| 依赖 | 版本要求 | 用途 |
|------|----------|------|
| jq | ≥ 1.6 | JSON 数据处理 |
| yq | ≥ 4.0 | YAML 数据处理 |
| git | ≥ 2.0 | 版本控制和变更追踪 |
| bash | ≥ 4.0 | 脚本执行环境 |

---

## 12. 验收标准

### 12.1 功能验收标准

| ID | 验收标准 | 对应功能 |
|----|----------|----------|
| AC-001 | 执行 phase-0 时，尝试越界操作被阻止并提示 | F1 |
| AC-002 | 每个 phase 的关键操作前显示确认对话框 | F1 |
| AC-003 | 状态面板实时显示当前 phase、进度、下一步 | F1 |
| AC-004 | `/flow-kit:auto-start` 一键完成 Phase 0→7 | F2 |
| AC-005 | 确认环节显示 2-3 个推荐选项 | F2 |
| AC-006 | 执行完成后自动生成审计报告 | F3 |
| AC-007 | PRD 文档自动解析并生成执行计划 | F4 |
| AC-008 | 每 7 天自动分析执行数据并生成优化建议 | F5 |
| AC-009 | 执行 phase 命令前自动清理环境 | F6 |
| AC-010 | Ralph 循环自动执行任务列表 | F7 |
| AC-011 | `/flow-kit:hotfix` 自动分级（P0/P1/P2）并执行对应响应 | F8 |
| AC-012 | P0/P1 修复后 24h 内自动生成 RCA 报告并归档 | F8 |
| AC-013 | `/flow-kit:recall` 5 分钟内输出项目摘要和推荐下一步 | F9 |
| AC-014 | 项目超过 30 天未活跃时自动触发 bit rot 检查 | F9 |
| AC-015 | git commit 前自动扫描敏感信息，发现密钥时阻止提交 | F6 |

### 12.2 非功能验收标准

| ID | 验收标准 | 指标 |
|----|----------|------|
| NFC-001 | 状态面板响应 < 1s | 性能 |
| NFC-002 | 边界校验 < 500ms | 性能 |
| NFC-003 | 审计报告生成 < 5s | 性能 |
| NFC-004 | 越界操作 100% 被阻止 | 安全 |
| NFC-005 | critical 操作 100% 需确认 | 安全 |
| NFC-006 | Token 消耗降低 > 60% | Token 优化 |
| NFC-007 | P0 事故响应时间 < 15min | 紧急修复 |
| NFC-008 | 项目摘要生成 < 5min | 记忆恢复 |
| NFC-009 | Secrets 扫描 < 3s | 安全 |

### 12.3 Phase 边界重构详细验收

- [ ] `phase-boundaries.yaml` 定义了 8 个 phase 的 allowed/forbidden operations
- [ ] `boundary-enforcer.sh` 能正确拦截越界操作
- [ ] 越界操作被拦截时给出明确的错误提示 + 正确的 phase 归属
- [ ] `confirmation-manager.sh` 支持 3 级确认 (critical/important/normal)
- [ ] critical 级别的确认不可跳过
- [ ] `status-display.sh` 实时显示当前 phase 和下一步
- [ ] 所有 9 个 phase 工作流文件已添加边界声明
- [ ] 边界校验不影响正常 phase 执行（误报率 < 1%）

### 12.4 自动化执行详细验收

- [ ] `auto-pilot.sh` 支持 3 种模式 (interactive/silent/supervised)
- [ ] 从 Phase 0 到 Phase 7 的全流程自动执行成功率 > 90%
- [ ] `decision-engine.sh` 在每个确认环节至少提供 2 个推荐选项
- [ ] 推荐选项采纳率 > 70%
- [ ] `execution-auditor.sh` 能检测越界事件、遗漏步骤、输出不完整
- [ ] `auto-fix` 能自动修复至少 3 种常见问题
- [ ] `prd-parser.sh` 支持 Markdown/JSON/plain-text 三种格式
- [ ] PRD 解析准确率 > 85%

### 12.5 环境清理详细验收

- [ ] `auto-cleanup.sh` 支持 3 种模式 (auto/full/minimal)
- [ ] 清理后 session-state.json 的关键信息保留
- [ ] 清理后临时文件全部删除
- [ ] pre-tool-guard.sh 在 phase 命令前自动触发清理
- [ ] 清理时间 < 2s

### 12.6 自我进化详细验收

- [ ] `evolution-engine.sh` 能收集 5+ 种执行指标
- [ ] 每 7 天自动生成优化建议
- [ ] 至少 3 种优化规则可自动应用
- [ ] 进化配置可手动覆盖

### 12.7 紧急修复详细验收

- [ ] `hotfix-executor.sh` 支持 P0/P1/P2 三级自动分级
- [ ] P0 响应：自动定位上一个稳定版本并执行回滚
- [ ] P1 响应：展示 3 个澄清问题并基于回答生成修复方案
- [ ] P2 响应：自动创建 Change Request 并纳入正常流程
- [ ] P0/P1 修复完成后自动生成 RCA 模板
- [ ] 事后 5 件事检查清单在 24 小时内完成追踪
- [ ] H-hotfix 可绕过 Phase 0-3 直接进入 Phase 4
- [ ] 紧急修复完成后自动归档到 learnings/RCA/

### 12.8 项目记忆恢复详细验收

- [ ] `recall-executor.sh` 支持显式命令和隐式触发两种方式
- [ ] 5 分钟项目摘要包含：概览、状态、最近变更、推荐下一步
- [ ] session-state 超过 7 天未更新时自动提示 recall
- [ ] 项目超过 30 天未活跃时自动触发 bit rot 检查
- [ ] bit rot 检查覆盖：依赖过期、安全漏洞、文档滞后、CI/CD 状态
- [ ] @GO.md 加载时自动触发快速 recall
- [ ] recall 结果缓存到 .flow-kit/recall/ 目录

### 12.9 Secrets 保护详细验收

- [ ] `secrets-guard.sh` 支持 full/staged/diff 三种扫描模式
- [ ] 覆盖 15+ 种常见密钥格式（GitHub Token、AWS Key、JWT 等）
- [ ] git commit 前自动触发暂存区扫描
- [ ] 发现敏感信息时阻止提交并给出明确处理指引
- [ ] 自动检查 .env.example 和 .gitignore 配置
- [ ] 缺少 .gitignore 时自动创建并添加敏感文件规则
- [ ] 支持 .secrets-ignore 排除误报

---

## 13. 实施计划

### 13.1 总体时间线

| 阶段 | 时间 | 内容 | 工作量 |
|------|------|------|--------|
| 迭代 1 | 第 1-3 周 | F1 Phase 边界重构 + F2 自动化执行引擎 | 8 人天 |
| 迭代 2 | 第 4-8 周 | F3 自动化审计 + F4 PRD 解析 + F7 持续任务 | 12 人天 |
| 迭代 3 | 第 9-12 周 | F5 自我进化 + F6 环境清理 + F8 紧急修复 + F9 记忆恢复 | 14 人天 |
| 迭代 4 | 第 13-14 周 | 集成测试 + 文档 + 发布准备 | 6 人天 |

### 13.2 迭代 1：第 1-3 周（Phase 边界重构 + 自动化执行引擎）

| 周次 | 任务 | 产出 | 工作量 |
|------|------|------|--------|
| 第 1 周 | F1.1 职责边界定义 | phase-boundaries.yaml | 1 人天 |
| 第 1 周 | F1.2 边界校验机制 | boundary-enforcer.sh | 1 人天 |
| 第 1 周 | F1.3 操作前确认流程 | confirmation-manager.sh | 0.5 人天 |
| 第 1 周 | F1.4 状态透明化机制 | status-display.sh | 0.5 人天 |
| 第 2 周 | F2.1 自动化执行引擎 | auto-pilot.sh | 2 人天 |
| 第 2 周 | F2.2 智能决策系统 | decision-engine.sh | 1 人天 |
| 第 3 周 | 集成测试 + Bug 修复 | 测试报告 | 1 人天 |
| 第 3 周 | 文档更新 | 用户手册 | 1 人天 |

**迭代 1 详细任务分配**：

| 日期 | 任务 | 负责人 | 预计工时 |
|------|------|--------|----------|
| Day 1 | 设计 phase-boundaries.yaml 结构 | AI | 4h |
| Day 1 | 定义 Phase 0-3 的 allowed/forbidden operations | AI | 4h |
| Day 2 | 定义 Phase 4-7 的 allowed/forbidden operations | AI | 4h |
| Day 2 | 实现 boundary-enforcer.sh 核心逻辑 | AI | 4h |
| Day 3 | 实现四闸门机制 | AI | 4h |
| Day 3 | 实现 @GO.md 零例外路由 | AI | 4h |
| Day 4 | 实现 confirmation-manager.sh | AI | 4h |
| Day 4 | 实现 grill-me 需求访谈模式 | AI | 4h |
| Day 5 | 实现 status-display.sh | AI | 4h |
| Day 5 | 实现 auto-pilot.sh 核心逻辑 | AI | 4h |
| Day 6 | 实现任务深度分级 | AI | 4h |
| Day 6 | 实现智能决策引擎 | AI | 4h |
| Day 7 | 集成测试 | AI | 4h |
| Day 7 | Bug 修复 + 文档更新 | AI | 4h |

### 13.3 迭代 2：第 4-8 周（自动化审计 + PRD 解析 + 持续任务执行）

| 周次 | 任务 | 产出 | 工作量 |
|------|------|------|--------|
| 第 4 周 | F3.1 执行审计器 | execution-auditor.sh | 1.5 人天 |
| 第 4 周 | F3.2 自动修正机制 | auto-fix.sh | 1.5 人天 |
| 第 5 周 | F4.1 PRD 自动解析器 | prd-parser.sh | 2 人天 |
| 第 5 周 | F4.2 自动初始化流程 | auto-start.sh | 1 人天 |
| 第 5 周 | F4.3 增量规范生成 | delta-spec-generator.sh | 1 人天 |
| 第 6 周 | F7.1 自主运行模式 | auto-runner.yaml | 1 人天 |
| 第 6 周 | F7.2 Ralph 循环集成 | ralph-runner.sh | 2 人天 |
| 第 7 周 | F7.3 通用任务模板 | templates/ | 1 人天 |
| 第 7 周 | F7.4 异常处理机制 | exception-handler.sh | 1 人天 |
| 第 8 周 | 集成测试 + Bug 修复 | 测试报告 | 2 人天 |
| 第 8 周 | 文档更新 | 用户手册 | 1 人天 |

**迭代 2 详细任务分配**：

| 日期 | 任务 | 负责人 | 预计工时 |
|------|------|--------|----------|
| Day 8 | 实现 execution-auditor.sh 核心逻辑 | AI | 4h |
| Day 8 | 实现 phase 输出完整性检查 | AI | 4h |
| Day 9 | 实现操作越界检测 | AI | 4h |
| Day 9 | 实现遗漏步骤检测 | AI | 4h |
| Day 10 | 实现 auto-fix.sh 核心逻辑 | AI | 4h |
| Day 10 | 实现三种修复策略 | AI | 4h |
| Day 11 | 实现 prd-parser.sh 核心逻辑 | AI | 4h |
| Day 11 | 实现文档格式检测 | AI | 4h |
| Day 12 | 实现结构化数据提取 | AI | 4h |
| Day 12 | 实现增量规范生成 | AI | 4h |
| Day 13 | 实现 auto-start.sh | AI | 4h |
| Day 13 | 实现一键启动流程 | AI | 4h |
| Day 14 | 实现 auto-runner.yaml 三种模式 | AI | 4h |
| Day 14 | 实现 supervised 模式 | AI | 4h |
| Day 15 | 实现 semi_autonomous 模式 | AI | 4h |
| Day 15 | 实现 full_autonomous 模式 | AI | 4h |
| Day 16 | 实现 ralph-runner.sh 核心循环 | AI | 4h |
| Day 16 | 实现安全 Hook 机制 | AI | 4h |
| Day 17 | 实现 Subagent Fork 并行执行 | AI | 4h |
| Day 17 | 实现每次迭代全新 AI 实例 | AI | 4h |
| Day 18 | 实现通用任务模板 | AI | 4h |
| Day 18 | 实现异常处理机制 | AI | 4h |
| Day 19 | 集成测试 | AI | 4h |
| Day 19 | Bug 修复 | AI | 4h |
| Day 20 | 文档更新 | AI | 4h |
| Day 20 | 用户手册编写 | AI | 4h |

### 13.4 迭代 3：第 9-12 周（自我进化 + 环境清理 + 紧急修复 + 记忆恢复）

| 周次 | 任务 | 产出 | 工作量 |
|------|------|------|--------|
| 第 9 周 | F5.1 执行数据收集 | evolution-engine.sh | 1.5 人天 |
| 第 9 周 | F5.2 WAL 协议 + VFM 评分 | wal-protocol.sh | 1.5 人天 |
| 第 9 周 | F5.3 CE 知识复利提取 | ce-compound.sh | 1 人天 |
| 第 10 周 | F5.4 工作流自动优化 | evolution-config.yaml | 1 人天 |
| 第 10 周 | F5.5 优化建议生成 | optimization-engine.sh | 1 人天 |
| 第 10 周 | F6.1 自动清理触发器 | auto-cleanup.sh | 1 人天 |
| 第 10 周 | F6.2 handoff 交接机制 | handoff-generator.sh | 1 人天 |
| 第 11 周 | F6.3 Secrets 保护机制 | secrets-guard.sh | 1 人天 |
| 第 11 周 | F8.1 三档响应分级 | hotfix-levels.yaml | 1 人天 |
| 第 11 周 | F8.2 H-hotfix 工作流 | H-hotfix.md | 1 人天 |
| 第 11 周 | F8.3 紧急修复执行器 | hotfix-executor.sh | 1 人天 |
| 第 12 周 | F9.1 R-recall 触发机制 | recall-triggers.yaml | 0.5 人天 |
| 第 12 周 | F9.2 5 分钟项目摘要 | recall-executor.sh | 1 人天 |
| 第 12 周 | F9.3 与现有系统集成 | recall-integration.yaml | 0.5 人天 |

**迭代 3 详细任务分配**：

| 日期 | 任务 | 负责人 | 预计工时 |
|------|------|--------|----------|
| Day 21 | 实现 evolution-engine.sh 核心逻辑 | AI | 4h |
| Day 21 | 实现执行数据收集 | AI | 4h |
| Day 22 | 实现 WAL 协议 | AI | 4h |
| Day 22 | 实现 VFM 评分模型 | AI | 4h |
| Day 23 | 实现 CE 知识复利提取 | AI | 4h |
| Day 23 | 实现知识分类与存储 | AI | 4h |
| Day 24 | 实现 evolution-config.yaml | AI | 4h |
| Day 24 | 实现优化规则引擎 | AI | 4h |
| Day 25 | 实现 auto-cleanup.sh | AI | 4h |
| Day 25 | 实现 handoff 交接机制 | AI | 4h |
| Day 26 | 实现 secrets-guard.sh 核心逻辑 | AI | 4h |
| Day 26 | 实现 15+ 种密钥格式扫描 | AI | 4h |
| Day 27 | 实现 hotfix-levels.yaml 分级定义 | AI | 4h |
| Day 27 | 实现 H-hotfix.md 工作流 | AI | 4h |
| Day 28 | 实现 hotfix-executor.sh 核心逻辑 | AI | 4h |
| Day 28 | 实现 P0/P1/P2 三档响应 | AI | 4h |
| Day 29 | 实现 recall-triggers.yaml | AI | 4h |
| Day 29 | 实现 recall-executor.sh 核心逻辑 | AI | 4h |
| Day 30 | 实现 bit rot 检查 | AI | 4h |
| Day 30 | 实现 recall-integration.yaml | AI | 4h |

### 13.5 迭代 4：第 13-14 周（集成测试 + 文档 + 发布准备）

| 周次 | 任务 | 产出 | 工作量 |
|------|------|------|--------|
| 第 13 周 | Token 优化集成 | rtk-filter.sh | 1 人天 |
| 第 13 周 | Multi-Pass Review 集成 | multi-pass-review.sh | 1.5 人天 |
| 第 13 周 | 全局路由协议集成 | routing-protocol.sh | 0.5 人天 |
| 第 13 周 | Context Engineering 集成 | context-manager.sh | 1 人天 |
| 第 14 周 | 全量集成测试 | 测试报告 | 2 人天 |
| 第 14 周 | 文档更新 + 发布准备 | 发布说明 | 1 人天 |
| 第 14 周 | CE 知识复利提取 | 知识沉淀 | 1 人天 |

**迭代 4 详细任务分配**：

| 日期 | 任务 | 负责人 | 预计工时 |
|------|------|--------|----------|
| Day 31 | 实现 RTK 终端净化 | AI | 4h |
| Day 31 | 实现 Caveman 压缩 | AI | 4h |
| Day 32 | 实现 Multi-Pass Review | AI | 4h |
| Day 32 | 实现 12 维度并行审查 | AI | 4h |
| Day 33 | 实现全局路由协议 | AI | 4h |
| Day 33 | 实现 Context Engineering | AI | 4h |
| Day 34 | 全量集成测试 | AI | 4h |
| Day 34 | Bug 修复 | AI | 4h |
| Day 35 | 文档更新 | AI | 4h |
| Day 35 | 发布准备 + CE 知识提取 | AI | 4h |

### 13.6 里程碑

| 里程碑 | 时间 | 交付物 | 验收标准 |
|--------|------|--------|----------|
| M1 | 第 3 周末 | F1 + F2 完成 | AC-001 至 AC-005 |
| M2 | 第 8 周末 | F3 + F4 + F7 完成 | AC-006, AC-007, AC-010 |
| M3 | 第 12 周末 | F5 + F6 + F8 + F9 完成 | AC-008, AC-009, AC-011 至 AC-015 |
| M4 | 第 14 周末 | 全量集成 + 发布 | NFC-001 至 NFC-006 |

### 13.7 工作量汇总

| 功能 | 工作量 | 占比 |
|------|--------|------|
| F1 Phase 边界重构 | 3 人天 | 3.0% |
| F2 自动化执行引擎 | 5 人天 | 5.0% |
| F3 自动化审计 | 3 人天 | 3.0% |
| F4 PRD 自动解析 | 5 人天 | 5.0% |
| F5 自我进化 | 6 人天 | 6.0% |
| F6 环境自动清理（含 Secrets） | 3 人天 | 3.0% |
| F7 持续任务执行 | 5 人天 | 5.0% |
| F8 紧急修复流程 | 3 人天 | 3.0% |
| F9 项目记忆恢复 | 2 人天 | 2.0% |
| Token 优化集成 | 1 人天 | 1.0% |
| Multi-Pass Review | 1.5 人天 | 1.5% |
| 路由协议 + Context | 1.5 人天 | 1.5% |
| 集成测试 | 5 人天 | 5.0% |
| 文档 + 发布 | 3 人天 | 3.0% |
| **参考项目分析** | **26 人天** | **26.0%** |
| **经验萃取整合** | **18 人天** | **18.0%** |
| **flow-kit 社区实践融合** | **8 人天** | **8.0%** |
| **总计** | **100 人天** | **100%** |

### 13.8 资源需求

| 角色 | 数量 | 投入 | 职责 |
|------|------|------|------|
| 核心维护者 | 1 人 | 12 周全职 | 核心功能开发与集成 |
| 架构师 | 1 人 | 4 周 × 50% | 边界定义 + 架构评审 + 持续执行系统设计 |
| 测试工程师 | 1 人 | 4 周全职 | 集成测试 + E2E 测试 + 兼容性测试 |
| 文档工程师 | 1 人 | 2 周 × 50% | 文档编写 + 发布准备 |

---

## 14. 风险评估

### 14.1 技术风险风险矩阵

| ID | 风险描述 | 概率 | 影响 | 等级 | 应对策略 |
|----|----------|------|------|------|----------|
| R-01 | Phase 边界定义过于严格，影响开发灵活性 | 中 | 中 | 中 | 提供"严格模式"和"灵活模式"两种配置 |
| R-02 | 自动化执行引擎在复杂场景下决策错误 | 中 | 高 | 高 | 保留人工干预通道，critical 操作必须确认 |
| R-03 | Ralph 循环在 full_autonomous 模式下失控 | 低 | 极高 | 高 | 仅限容器环境，pre-flight checklist 验证 |
| R-04 | PRD 解析器对非标准格式文档解析失败 | 高 | 中 | 中 | 提供手动修正接口，支持多种文档格式 |
| R-05 | 自我进化机制产生错误的优化建议 | 低 | 中 | 低 | 优化建议仅作为参考，需人工确认后生效 |
| R-06 | Token 优化导致关键信息丢失 | 低 | 高 | 中 | 提供无损模式，关键操作不压缩 |
| R-07 | CE 知识复利系统产生噪声数据 | 中 | 低 | 低 | VFM 评分过滤，低于阈值不收录 |
| R-08 | Multi-Pass Review 产生 false positive | 中 | 低 | 低 | 聚合规则处理，多轮一致才标记 |
| R-09 | 全局路由协议与现有工作流冲突 | 低 | 高 | 中 | 渐进式采用，兼容 v3.3.0 现有路由 |
| R-10 | 经验萃取成果与 flow-kit 架构不兼容 | 低 | 高 | 中 | 适配层隔离，模块化集成 |
| R-11 | H-hotfix 紧急修复绕过正常流程导致质量下降 | 中 | 高 | 高 | 事后 5 件事强制补齐，RCA 自动归档 |
| R-12 | R-recall bit rot 检查误报导致不必要的维护工作 | 中 | 低 | 低 | 检查结果仅作建议，不强制修复 |
| R-13 | Secrets 扫描误报阻止正常提交 | 中 | 中 | 中 | 支持 .secrets-ignore 排除误报 |

### 14.2 风险应对方案

| 风险 | 预防措施 | 应急方案 |
|------|----------|----------|
| R-01 | 提供可配置的边界严格度 | 回退到 v3.3.0 的宽松模式 |
| R-02 | 所有 critical 操作强制确认 | 人工接管执行 |
| R-03 | 容器隔离 + pre-flight checklist | 终止容器，回滚 git |
| R-04 | 支持 4 种文档格式 | 手动输入变更描述 |
| R-05 | 优化建议需人工确认 | 关闭自动优化 |
| R-06 | 无损模式 + 关键操作不压缩 | 关闭 Token 优化 |
| R-07 | VFM 评分 ≥ 15 才收录 | 定期清理噪声 |
| R-08 | 聚合规则 + 多轮一致 | 人工确认 false positive |
| R-09 | 兼容 v3.3.0 路由 | 关闭全局路由 |
| R-10 | 适配层隔离 | 逐个模块回退 |
| R-11 | 事后 5 件事强制补齐 | 关闭 H-hotfix 快捷通道 |
| R-12 | 检查结果仅作建议 | 关闭 bit rot 自动检查 |
| R-13 | 支持 .secrets-ignore | 临时关闭 Secrets 扫描 |

### 14.3 依赖风险

| 依赖 | 风险 | 替代方案 |
|------|------|----------|
| jq ≥ 1.6 | 低版本 jq 不支持某些 JSON 操作 | 使用 Python json 模块 |
| yq ≥ 4.0 | 低版本 yq 语法不兼容 | 使用 Python yaml 模块 |
| git ≥ 2.0 | 低版本 git 缺少某些功能 | 使用 git 基础命令 |
| bash ≥ 4.0 | macOS 默认 bash 3.2 | 使用 zsh 兼容模式 |

---

## 15. 参考项目借鉴分析

### 15.1 superpowers-zh 借鉴分析

#### 15.1.1 核心能力

superpowers-zh 提供了 20 个超能力（Skills），分为三类：

| 类别 | Skills | 核心价值 |
|------|--------|----------|
| 流程类 | brainstorming, workflow-runner, executing-plans, writing-plans, verification-before-completion | 结构化开发流程 |
| 技术类 | test-driven-development, systematic-debugging, requesting-code-review, receiving-code-review | 技术质量保障 |
| 工具类 | dispatching-parallel-agents, subagent-driven-development, using-git-worktrees, mcp-builder | 效率工具 |

#### 15.1.2 关键借鉴点

| 借鉴点 | superpowers-zh 实现 | flow-kit 集成方案 |
|--------|---------------------|-------------------|
| workflow-runner | YAML 工作流编排 + 多角色协作 | Phase 0-7 自动执行引擎 |
| executing-plans | 分步执行 + 审查检查点 | 四闸门机制 |
| brainstorming | 9 步 HARD-GATE 需求分析 | Phase 1 需求澄清 |
| systematic-debugging | 四阶段诊断循环 | Phase 5 测试验证 |
| dispatching-parallel-agents | 并行子 Agent 分发 | Subagent Fork 并行执行 |
| verification-before-completion | 验证命令 + 证据支撑 | Phase 出口闸门验证 |

#### 15.1.3 集成优先级

| 借鉴点 | 优先级 | 工作量 | 对应功能 |
|--------|--------|--------|----------|
| workflow-runner | P0 | 2 人天 | F2 自动化执行 |
| executing-plans | P0 | 1 人天 | F1 四闸门 |
| brainstorming | P1 | 1 人天 | Phase 1 增强 |
| systematic-debugging | P1 | 1 人天 | Phase 5 增强 |
| dispatching-parallel-agents | P1 | 1 人天 | F7 Subagent Fork |
| verification-before-completion | P0 | 0.5 人天 | F3 审计 |

### 15.2 agency-agents-zh 借鉴分析

#### 15.2.1 核心能力

agency-agents-zh 提供了多 Agent 编排框架：

| 能力 | 描述 | 价值 |
|------|------|------|
| 角色定义 | 结构化角色定义 + 专业流程 | 决策质量提升 |
| DAG 编排 | 依赖检测 + 并行执行 | 执行效率提升 |
| 状态管理 | 全局状态追踪 | 状态透明化 |
| 异常处理 | 结构化异常处理 | 系统稳定性 |

#### 15.2.2 关键借鉴点

| 借鉴点 | agency-agents-zh 实现 | flow-kit 集成方案 |
|--------|----------------------|-------------------|
| 角色体系 | CEO/Design/Eng 三角色 | Phase 0-7 职责定义 |
| DAG 编排 | 依赖检测 + 并行执行 | Phase 3 任务拆解 |
| 状态管理 | 全局状态追踪 | F1 状态透明化 |
| 异常处理 | 结构化异常处理 | F7 异常处理 |

### 15.3 借鉴整合路线图

```
Phase 0: 变更立项
  ├── 借鉴: superpowers-zh brainstorming (HARD-GATE)
  └── 集成: 影响范围评估 + 风险评级

Phase 1: 需求澄清
  ├── 借鉴: superpowers-zh writing-plans
  ├── 借鉴: agency-agents-zh 角色定义
  └── 集成: grill-me 需求访谈

Phase 2: 架构设计
  ├── 借鉴: agency-agents-zh DAG 编排
  └── 集成: ADR + DESIGN.md

Phase 3: 任务拆解
  ├── 借鉴: agency-agents-zh 依赖检测
  └── 集成: 制品依赖图 (DAG)

Phase 4: 开发执行
  ├── 借鉴: superpowers-zh subagent-driven-development
  ├── 借鉴: agency-agents-zh 并行执行
  └── 集成: Subagent Fork + TDD

Phase 5: 测试验证
  ├── 借鉴: superpowers-zh systematic-debugging
  ├── 借鉴: superpowers-zh test-driven-development
  └── 集成: diagnose 诊断循环

Phase 6: 代码审查
  ├── 借鉴: superpowers-zh requesting-code-review
  ├── 借鉴: superpowers-zh receiving-code-review
  └── 集成: Multi-Pass Review

Phase 7: 集成归档
  ├── 借鉴: superpowers-zh verification-before-completion
  ├── 借鉴: agency-agents-zh 状态管理
  └── 集成: CE 知识复利提取
```

### 15.4 mattpocock/skills 借鉴分析

#### 15.4.1 核心能力

mattpocock/skills 提供了 14 个 Skill，分为工程类和效率类：

| 类别 | Skills | 核心价值 |
|------|--------|----------|
| 工程类 | grill-with-docs, diagnose, tdd, improve-codebase-architecture, triage, to-prd, to-issues, zoom-out, prototype, setup | 开发流程标准化 |
| 效率类 | grill-me, caveman, handoff, write-a-skill | 上下文管理 |

#### 15.4.2 关键借鉴点

| 借鉴点 | mattpocock/skills 实现 | flow-kit 集成方案 |
|--------|-----------------------|-------------------|
| grill-me | 10 行 Markdown 实现需求澄清 | Phase 1 需求访谈 |
| caveman | 长文档自动压缩 | Token 优化 |
| handoff | 会话交接文档 | F6 环境清理 |
| diagnose | 诊断循环 | Phase 5 测试验证 |
| tdd | 测试驱动开发 | Phase 4 开发执行 |
| to-prd | PRD 文档生成 | F4 PRD 解析 |

### 15.5 Ralph.md 借鉴分析

#### 15.5.1 核心能力

Ralph Wiggum 循环模式提供了自主 Agent 执行循环：

| 能力 | 描述 | 价值 |
|------|------|------|
| PRD 驱动 | 从 PRD 文档自动生成 prd.json | 需求自动化 |
| 循环执行 | 每次迭代使用全新 AI 实例 | 上下文隔离 |
| 完成检测 | 自动检测任务完成状态 | 自主决策 |
| 异常处理 | 任务失败自动重试 | 系统韧性 |

#### 15.5.2 关键借鉴点

| 借鉴点 | Ralph.md 实现 | flow-kit 集成方案 |
|--------|---------------|-------------------|
| PRD → prd.json | 自动解析 PRD 生成结构化任务 | F4 PRD 解析 |
| 全新 AI 实例 | 每次迭代启动新 AI | F7 Ralph 循环 |
| 完成检测 | 自动验证任务完成 | F3 审计 |
| 重试机制 | 失败自动重试 3 次 | F7 异常处理 |

### 15.6 superpowers+gstack 开发流程借鉴分析

#### 15.6.1 核心能力

superpowers+gstack 双插件协同提供了"大脑+手脚"的开发模式：

| 能力 | 描述 | 价值 |
|------|------|------|
| 流程归 superpowers | 需求分析、计划编写、代码审查 | 流程标准化 |
| 执行归 gstack | 任务拆解、开发执行、测试验证 | 执行自动化 |
| 独立 reviewer | 作者和审查者不在同一上下文 | 审查客观性 |
| 证据优先 | 没有证据的完成 = 没完成 | 质量保障 |

#### 15.6.2 关键借鉴点

| 借鉴点 | superpowers+gstack | flow-kit 集成方案 |
|--------|-------------------|-------------------|
| 思考/执行分离 | Phase 0-3 思考，Phase 4-7 执行 | Phase stage 定义 |
| 独立 reviewer | 跨上下文审查 | Phase 6 Multi-Pass |
| 证据优先 | 验证证据 | F3 审计 |
| 三连审 | CEO/Design/Eng 三轮 | Multi-Pass 3 轮 |

### 15.7 技术兼容性整合方案

#### 15.7.1 适配策略

| 技术 | 适配方式 | 兼容性 |
|------|----------|--------|
| superpowers-zh workflow-runner | 通过 phase-executor.sh 调用 | 完全兼容 |
| agency-agents-zh DAG | 通过 task-decomposer.sh 集成 | 需适配 |
| mattpocock/skills grill-me | 通过 confirmation-manager.sh 集成 | 完全兼容 |
| Ralph Wiggum 循环 | 通过 ralph-runner.sh 独立运行 | 完全兼容 |
| superpowers+gstack | 通过 phase-boundaries.yaml 配置 | 完全兼容 |

#### 15.7.2 模块集成方法

```yaml
# .flow-kit/integration-config.yaml — 模块集成配置
integration:
  modules:
    - name: "superpowers-zh"
      type: "skill"
      integration_point: "phase-executor.sh"
      activation: "on_demand"
    - name: "agency-agents-zh"
      type: "framework"
      integration_point: "task-decomposer.sh"
      activation: "on_demand"
    - name: "mattpocock/skills"
      type: "skill"
      integration_point: "confirmation-manager.sh"
      activation: "always"
    - name: "ralph-loop"
      type: "engine"
      integration_point: "ralph-runner.sh"
      activation: "on_demand"
    - name: "superpowers+gstack"
      type: "workflow"
      integration_point: "phase-boundaries.yaml"
      activation: "configurable"
```

#### 15.7.3 接口设计规范

```yaml
# .flow-kit/interface-spec.yaml — 接口设计规范
interfaces:
  phase_executor:
    input: "phase_id + context"
    output: "phase_result + artifacts"
    protocol: "file-based (JSON)"
    
  confirmation_manager:
    input: "phase + operation + level"
    output: "user_decision"
    protocol: "stdin/stdout"
    
  audit_engine:
    input: "plan_file"
    output: "audit_report"
    protocol: "file-based (JSON → Markdown)"
    
  prd_parser:
    input: "prd_file (Markdown/JSON/plain-text)"
    output: "change_description + delta_spec + dag"
    protocol: "file-based (JSON)"
    
  ralph_runner:
    input: "task_file (JSON)"
    output: "execution_report"
    protocol: "file-based (JSON + Markdown)"
```

### 15.8 Claude Code AI 辅助效率提升分析

#### 15.8.1 效率提升维度

| 维度 | 当前状态 | 目标状态 | 提升幅度 |
|------|----------|----------|----------|
| Phase 执行 | 手动逐个执行 | 一键全流程 | 10x |
| 需求分析 | 人工阅读 PRD | 自动解析 | 5x |
| 代码审查 | 人工逐行审查 | Multi-Pass 自动 | 3x |
| 任务执行 | 单线程 | 并行 Fork | 3x |
| 知识沉淀 | 无 | 自动 CE 提取 | ∞ |

#### 15.8.2 自动化覆盖率

| 功能 | 当前自动化率 | 目标自动化率 |
|------|-------------|-------------|
| Phase 0 变更立项 | 0% | 80% |
| Phase 1 需求澄清 | 0% | 60% |
| Phase 2 架构设计 | 0% | 50% |
| Phase 3 任务拆解 | 0% | 70% |
| Phase 4 开发执行 | 0% | 90% |
| Phase 5 测试验证 | 0% | 80% |
| Phase 6 代码审查 | 0% | 70% |
| Phase 7 集成归档 | 0% | 85% |

### 15.9 代码质量改进指标

#### 15.9.1 质量门禁

| 门禁 | 检查项 | 通过标准 |
|------|--------|----------|
| Phase 0 | 影响范围评估完整性 | 100% 覆盖 |
| Phase 1 | 需求明确度 | 无歧义 |
| Phase 2 | 架构合理性 | ADR 记录 |
| Phase 3 | 任务完整性 | DAG 无环 |
| Phase 4 | 代码质量 | 测试通过 |
| Phase 5 | 测试覆盖率 | > 80% |
| Phase 6 | 审查通过 | 无 critical 问题 |
| Phase 7 | 文档完整性 | 100% 覆盖 |

#### 15.9.2 改进指标

| 指标 | 当前值 | 目标值 | 测量方法 |
|------|--------|--------|----------|
| Phase 越界事件 | 频繁 | 0 | boundary-enforcer.sh |
| 人工确认覆盖率 | 0% | 100% | confirmation-manager.sh |
| 状态更新延迟 | N/A | < 1s | status-display.sh |
| 全流程自动化率 | 0% | > 90% | auto-pilot.sh |
| 推荐采纳率 | N/A | > 70% | decision-engine.sh |
| 审计覆盖率 | 0% | 100% | execution-auditor.sh |
| PRD 解析准确率 | N/A | > 85% | prd-parser.sh |
| Token 节省 | 0% | > 60% | RTK 统计 |
| 知识捕获率 | 0% | > 90% | CE 统计 |

### 15.10 Context Engineering 借鉴分析

#### 15.10.1 三层记忆架构

借鉴 Claude Code 记忆体系，flow-kit 实现三层记忆架构：

| 层级 | 存储位置 | 内容 | 生命周期 |
|------|----------|------|----------|
| 用户级 | CLAUDE.md | 项目约定、行为规则 | 永久 |
| 项目级 | .flow-kit/rules/ | 团队底线规则 | 项目周期 |
| 自动记忆 | .flow-kit/learnings/ | 执行经验、错误模式 | 持续更新 |

#### 15.10.2 Context Timeline 监控

借鉴 Subagent Fork 的 context-timeline 机制：

```yaml
# .flow-kit/context-timeline.yaml — 上下文时间线
context_timeline:
  enabled: true
  max_entries: 50
  auto_archive: true
  archive_threshold: "7d"
  
  monitoring:
    - metric: "context_window_usage"
      threshold: "80%"
      action: "建议 /clear 或 /compact"
    - metric: "tool_call_retry_rate"
      threshold: "10%"
      action: "建议 /rewind 重置状态"
    - metric: "response_latency"
      threshold: "5s"
      action: "建议 /compact 压缩上下文"
```

#### 15.10.3 /rewind 机制集成

借鉴 Claude Code 的 /rewind 功能，flow-kit 实现状态保存与恢复：

```bash
# rewind 状态保存
rewind_save_state() {
    local checkpoint_name="${1:-auto}"
    local checkpoint_dir="$PROJECT_DIR/.flow-kit/checkpoints/$checkpoint_name"
    
    mkdir -p "$checkpoint_dir"
    
    # 保存所有状态
    cp "$SESSION_STATE_FILE" "$checkpoint_dir/session-state.json"
    cp "$PROJECT_DIR/.flow-kit/decisions.log" "$checkpoint_dir/decisions.log" 2>/dev/null || true
    cp "$PROJECT_DIR/.flow-kit/execution.log" "$checkpoint_dir/execution.log" 2>/dev/null || true
    
    # 保存 git 状态
    git diff --cached > "$checkpoint_dir/staged.diff" 2>/dev/null || true
    git diff > "$checkpoint_dir/unstaged.diff" 2>/dev/null || true
    
    echo "[rewind] ✅ 状态已保存: $checkpoint_dir"
}

# rewind 状态恢复
rewind_restore_state() {
    local checkpoint_name="$1"
    local checkpoint_dir="$PROJECT_DIR/.flow-kit/checkpoints/$checkpoint_name"
    
    if [[ ! -d "$checkpoint_dir" ]]; then
        echo "[rewind] ❌ 检查点不存在: $checkpoint_name"
        return 1
    fi
    
    # 恢复所有状态
    cp "$checkpoint_dir/session-state.json" "$SESSION_STATE_FILE"
    
    echo "[rewind] ✅ 状态已恢复: $checkpoint_name"
    echo "[rewind] 建议执行 /clear 后继续"
}
```

#### 15.10.4 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 三层记忆 | CLAUDE.md + rules/ + learnings/ | F5 自我进化 |
| Context Timeline | 上下文健康监控 | F6 环境清理 |
| /rewind | 检查点保存/恢复 | F6 环境清理 |
| /compact | 上下文压缩 | Token 优化 |

### 15.11 全局路由协议借鉴分析

#### 15.11.1 Alan 全局路由协议

借鉴 Alan 全局路由协议（Superpowers 与 gstack 分工），flow-kit 实现：

```yaml
# 全局路由协议
routing:
  priority_chain:
    - "用户明确指令"        # 最高优先级
    - "phase-boundaries.yaml"  # 系统配置
    - "CLAUDE.md"           # 项目约定
    - "插件默认说明"        # 最低优先级
  
  rules:
    single_master_per_turn: true  # 每轮只有一个主控
    go_md_zero_exception: true    # @GO.md 零例外
    no_implicit_routing: true     # 禁止隐式路由
```

#### 15.11.2 路由冲突解决

| 冲突场景 | 解决策略 |
|----------|----------|
| 用户指令 vs phase-boundaries | 用户指令优先 |
| phase-boundaries vs CLAUDE.md | phase-boundaries 优先 |
| CLAUDE.md vs 插件默认 | CLAUDE.md 优先 |
| @GO.md 引用 | 强制展示路由选项 |

#### 15.11.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 优先级链 | 路由声明 + 用户确认 | F1 边界定义 |
| 单主控 | 每轮只有一个路由 | F1 四闸门 |
| @GO.md 零例外 | 强制路由展示 | F1 边界校验 |
| 禁止隐式路由 | 所有操作需声明 | F1 确认机制 |

### 15.12 生产级交付实践借鉴分析

#### 15.12.1 渐进式上下文加载

| 层级 | 内容 | 加载时机 |
|------|------|----------|
| 项目总需求 | CLAUDE.md | 会话启动 |
| 模块级 | .flow-kit/context/ | Phase 0 |
| 菜单级 | .flow-kit/context/ | Phase 1 |
| 页面级 | .flow-kit/context/ | Phase 2 |
| 按钮级 | .flow-kit/context/ | Phase 4 |

#### 15.12.2 前后端分离开发

flow-kit 可内置前后端分离的项目模板：

```yaml
# .flow-kit/templates/project-structure.yaml
project_templates:
  full_stack:
    structure:
      - "frontend/"
      - "backend/"
      - "docs/"
      - "scripts/"
    claude_config:
      - "frontend/CLAUDE.md"
      - "backend/CLAUDE.md"
```

#### 15.12.3 日志自动采集

```yaml
# .flow-kit/logging-config.yaml
logging:
  auto_collect: true
  log_files:
    - ".flow-kit/execution.log"
    - ".flow-kit/decisions.log"
    - ".flow-kit/learnings/ERRORS.md"
  git_ignore:
    - "*.log"
    - ".flow-kit/tmp/"
```

#### 15.12.4 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 渐进式加载 | 分层上下文 | F6 环境清理 |
| 前后端分离 | 项目模板 | Phase 0 |
| 日志采集 | 自动 debug | F3 审计 |
| 安全过滤器 | 两层防护 | F7 安全 Hook |

### 15.13 控制模型与自进化借鉴分析

#### 15.13.1 OMC vs Karpathy 融合

| 维度 | OMC（硬控制） | Karpathy（软约束） | flow-kit 融合 |
|------|-------------|-------------------|---------------|
| 控制方式 | 系统流程 | Prompt 规则 | phase-boundaries.yaml |
| 控制强度 | 极强（不可绕） | 较弱（可能失效） | 四闸门 + 护栏 |
| 作用层 | 执行层 | 思考层 | 双层控制 |

**flow-kit 融合策略**：
- CLAUDE.md = Karpathy（行为层）
- phase-boundaries.yaml = OMC（执行层）
- 四闸门 = OMC（不可绕）
- 护栏建议 = Karpathy（可配置）

#### 15.13.2 自进化引擎设计

借鉴 OpenSpace 自进化引擎：

```yaml
# .flow-kit/self-evolution.yaml
self_evolution:
  enabled: true
  learning_sources:
    - "execution_metrics"
    - "user_decisions"
    - "error_patterns"
    - "knowledge_base"
  
  optimization_targets:
    - "phase_boundaries"
    - "confirmation_threshold"
    - "execution_order"
    - "token_usage"
  
  safety_limits:
    max_auto_adjustments_per_day: 3
    require_confirmation_for: ["phase_boundaries", "execution_order"]
    auto_rollback_on_failure: true
```

#### 15.13.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| OMC 硬控制 | phase-boundaries.yaml | F1 边界定义 |
| Karpathy 软约束 | CLAUDE.md | 项目配置 |
| 自进化 | evolution-engine.sh | F5 自我进化 |
| 安全限制 | evolution-config.yaml | F5 进化配置 |

### 15.14 Ralph 自主 Agent 循环与 PRD 驱动开发

#### 15.14.1 核心模式

Ralph 自主 Agent 循环的核心模式：**PRD → prd.json → 循环 → 完成**

| 阶段 | 操作 | 产出 |
|------|------|------|
| 1. PRD 解析 | 读取 PRD 文档，提取需求 | prd.json |
| 2. 任务生成 | 从 prd.json 生成任务列表 | tasks.json |
| 3. 循环执行 | 每次迭代使用全新 AI 实例 | 逐个完成任务 |
| 4. 完成检测 | 自动验证所有任务完成 | 完成报告 |

#### 15.14.2 每次迭代全新 AI 实例

```yaml
# Ralph 循环关键设计
ralph_loop:
  fresh_ai_per_iteration: true  # 每次迭代全新 AI 实例
  context_isolation: true       # 上下文隔离
  state_persistence: "file-based"  # 状态通过文件传递
  
  iteration_flow:
    1. "读取当前任务 (from tasks.json)"
    2. "生成 PROMPT.md (独立上下文)"
    3. "启动新 AI 实例执行"
    4. "检查执行结果"
    5. "更新 tasks.json"
    6. "继续下一迭代"
```

#### 15.14.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| PRD → prd.json | 自动解析 PRD | F4 PRD 解析 |
| 全新 AI 实例 | 每次迭代隔离 | F7 Ralph 循环 |
| 文件状态传递 | tasks.json | F7 任务管理 |
| 完成检测 | 自动验证 | F3 审计 |

### 15.15 AutoResearch 方法论借鉴分析

[Karpathy AutoResearch](https://github.com/karpathy/auto-research)（71K Stars）是 Andrej Karpathy 的 AI 自主研究实验，核心洞察：没有框架！编排逻辑就是一个 Markdown 文件。

#### 15.15.1 三文件架构

整个系统只有三个文件：
- `prepare.py` — 数据准备（AI 不可修改）
- `train.py` — AI 的"实验室"（AI 可修改一切）
- `program.md` — 人类写的"研究纲领"（Markdown 编排 AI 行为）

**实验循环**：读取 program.md → 查看 git 历史 → 提出假设 → 编辑 train.py → 训练 5 分钟 → 检查 val_bpb → 改善则保留，否则 git revert

**核心洞察**：智能体的上下文窗口就是状态机，Markdown 文档就是工作流定义，Git 就是版本控制和回滚机制。

**实施方法**：
1. 将 flow-kit 的 Phase 工作流从复杂 YAML 简化为 program.md 模式
2. 每个 Phase 定义"可修改区域"和"不可修改区域"
3. Git 作为回滚机制，每次 Phase 执行前创建检查点

**预期改进效果**：
- 工作流定义复杂度降低 70%（Markdown 替代 YAML）
- 回滚安全性提升 100%（Git 原生支持）
- 新用户理解成本降低 60%（三文件架构直观）

**兼容性分析**：
- program.md 模式与现有的 Markdown 工作流文件兼容
- Git 回滚机制与现有的 session-state 系统互补

#### 15.15.2 多 Agent 交叉审核 + 5 维度加权评分

将 AutoResearch 迁移到软件开发领域后的三大关键改进：

| 改进 | Karpathy 原版 | 软件开发版 |
|------|--------------|-----------|
| 审核机制 | 单 Agent 自审 | 多 Agent 交叉审核（Codex + Claude 轮流） |
| 评分体系 | 单一 metric（val loss） | 5 维度加权评分（正确性35%+测试25%+代码质量20%+安全10%+性能10%） |
| 反馈机制 | 独立循环 | 审核反馈驱动下一轮实现 |

**六条核心原则**：
1. program.md 是唯一规则来源
2. 多 Agent 对抗审核（不同模型盲区互补）
3. 量化评分门槛（总分 ≥ 9.0 才通过）
4. 审核反馈驱动迭代（不是盲循环）
5. 只保留改进（退化就回滚）
6. 全量记录可追溯

**实施方法**：
1. 在 Phase 6（代码审查）中集成多 Agent 交叉审核
2. 在 execution-auditor.sh 中实现 5 维度加权评分
3. 在 evolution-engine.sh 中实现"只保留改进"原则

**预期改进效果**：
- 代码审查质量提升 50%（多 Agent 盲区互补）
- 质量评估客观性提升 80%（量化评分替代主观判断）
- 迭代效率提升 40%（审核反馈驱动而非盲循环）

**兼容性分析**：
- 多 Agent 交叉审核与现有的 dispatch.sh 并行执行能力兼容
- 5 维度评分可作为 execution-auditor.sh 的增强功能

#### 15.15.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 三文件架构 | program.md 模式 | F1 边界定义 |
| Git 回滚 | 检查点机制 | F6 环境清理 |
| 多 Agent 交叉审核 | 并行审查 | F6 代码审查 |
| 5 维度加权评分 | 量化评估 | F3 审计 |
| 只保留改进 | 进化策略 | F5 自我进化 |

### 15.16 Compound Engineering 知识复利系统

[Compound Engineering (CE)](https://github.com/compound-engineering/agent) 是一个知识复利系统，核心理念：Superpowers 管纪律，CE 管能力。层次不同，不构成替代。

#### 15.16.1 知识沉淀机制

CE 的 `/ce:compound` 启动 5 个子代理，自动提取本轮踩过的坑、用到的模式、review 的关键发现，结构化存入 `docs/solutions/` 目录。下一轮 plan 时，learnings-researcher 子代理自动搜索并引用。

**验证效果**：第二轮 plan 自动引用第一轮经验，设计方案主动避开已知坑，review 发现数明显下降。

**实施方法**：
1. 在 evolution-engine.sh 中集成知识沉淀机制
2. 每次 Phase 执行后自动提取经验教训
3. 在 Phase 1（需求分析）中集成 learnings-researcher 自动引用

**预期改进效果**：
- 重复错误减少 70%（知识沉淀避免踩同样的坑）
- 设计质量提升 40%（自动引用历史经验）
- 团队知识共享效率提升 60%（结构化知识库）

**兼容性分析**：
- 知识沉淀与现有的 session-state 系统互补
- learnings-researcher 可集成到 decision-engine.sh 中

#### 15.16.2 12 子代理并行审查

CE 从安全、性能、架构、可维护性等 12 个维度并行审查代码。单次 review 消耗大量 token，但审查质量显著提升。

**实施方法**：
1. 在 Phase 6（代码审查）中集成 12 子代理并行审查
2. 每个子代理负责一个维度的审查
3. 审查结果聚合为综合报告

**预期改进效果**：
- 代码审查覆盖率提升 200%（12 维度全覆盖）
- 安全漏洞发现率提升 80%（专门安全审查子代理）
- 架构问题发现率提升 60%（专门架构审查子代理）

**兼容性分析**：
- 12 子代理并行审查与现有的 dispatch.sh 兼容
- 审查结果聚合格式与 execution-auditor.sh 兼容

#### 15.16.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 知识沉淀 | evolution-engine.sh | F5 自我进化 |
| 12 子代理并行审查 | dispatch.sh | F6 代码审查 |
| learnings-researcher | decision-engine.sh | F1 需求分析 |
| 结构化知识库 | .flow-kit/learnings/ | F5 自我进化 |

### 15.17 flow-kit 社区实践与融合创新

[rihebty/flow-kit](https://github.com/rihebty/flow-kit) 是一个融合了 7 个主流框架核心能力的 flow-kit 社区版本，其创新设计对 v3.6.0 有重要参考价值。

#### 15.17.1 四闸门 + 第五闸门机制

四闸门机制确保每个阶段都有明确的确认点：
1. 路由声明（AI 必须先声明走哪条路）
2. 用户回复（用户确认路由选择）
3. 加载 prompt 并确认（加载具体工作流后再次确认）
4. prompt 第一步反问并等回复（工作流启动前最后确认）

阶段切换时再加第五道闸门：必须产出工件 + 输出过渡声明 + 用户确认。

**实施方法**：
1. 在 confirmation-manager.sh 中实现四闸门机制
2. 在 auto-pilot.sh 的 phase 切换点添加第五闸门
3. 每个闸门可配置跳过条件

**预期改进效果**：
- 方向偏离减少 90%（四闸门确保每一步都对齐）
- 用户对自动化的信任度提升 50%（每步可确认）
- 阶段切换遗漏减少 80%（第五闸门强制产出工件）

**兼容性分析**：
- 四闸门与现有的 confirmation-manager.sh 兼容
- 第五闸门可作为 phase 切换的增强验证

#### 15.17.2 @GO.md 零例外路由

只要消息里出现 @GO.md，AI 必须先给路由声明或选项菜单，禁止直接动手。这是防止 AI 跳过流程直接执行的硬约束。

**实施方法**：
1. 在 boundary-enforcer.sh 中添加 @GO.md 路由检测
2. 检测到 @GO.md 引用时强制展示路由选项
3. 禁止在路由确认前执行任何操作

**预期改进效果**：
- 流程跳过事件减少 100%（零例外路由硬约束）
- 用户对流程控制感提升 80%（每次都有路由选择）

**兼容性分析**：
- @GO.md 路由检测与现有的 boundary-enforcer.sh 兼容
- 可作为 phase-boundaries.yaml 的补充规则

#### 15.17.3 H-hotfix.md 三档响应

紧急修复的三档响应机制：
- P0：先回滚再诊断（生产事故）
- P1：3 问澄清后直接修复（紧急 bug）
- P2：建议走正常流程（非紧急问题）

事故解决后 24 小时内补齐 5 件事：根因分析、修复方案、测试补充、监控告警、文档更新。

**实施方法**：
1. 在 Phase 工作流中添加 H-hotfix.md 紧急分支
2. 实现三档响应自动判断逻辑
3. 事故后自动生成 5 件事检查清单

**预期改进效果**：
- 紧急响应时间减少 60%（三档响应快速分流）
- 事故复发率降低 70%（24 小时内补齐 5 件事）
- 生产稳定性提升 50%（P0 先回滚再诊断）

**兼容性分析**：
- H-hotfix 作为 Phase 工作流的异常分支，不影响正常流程
- 三档响应判断可集成到 decision-engine.sh 中

#### 15.17.4 R-recall.md 项目记忆恢复

触发后 AI 输出"5 分钟项目摘要 + 推荐下一步"。超过 30 天做 bit rot 检查（依赖过期、安全漏洞、文档滞后）。

**实施方法**：
1. 在 session-state 恢复机制中集成 R-recall
2. 自动生成项目摘要和推荐下一步
3. 实现 bit rot 定期检查

**预期改进效果**：
- 项目恢复时间减少 80%（5 分钟摘要快速回忆）
- 长期未维护项目风险降低 60%（bit rot 检查）

**兼容性分析**：
- R-recall 与现有的 session-state 恢复机制互补
- bit rot 检查可作为 evolution-engine.sh 的定期任务

#### 15.17.5 Secrets 保护机制

强制 .env.example + .gitignore，提交前自动扫描敏感信息（API key、token、密码等）。

**实施方法**：
1. 在 pre-tool-guard.sh 中添加敏感信息扫描
2. 在 git commit 前自动触发扫描
3. 发现敏感信息时阻止提交并提示

**预期改进效果**：
- 敏感信息泄露风险降低 95%（提交前自动扫描）
- 安全合规性提升 100%（强制 .env.example + .gitignore）

**兼容性分析**：
- Secrets 扫描与现有的 safety-hooks.sh 兼容
- 可作为 Decision Warn Hook 的增强功能

#### 15.17.6 对 flow-kit 的启示与融合状态

| 借鉴点 | 实现 | 集成位置 | 融合状态 |
|--------|------|----------|----------|
| 四闸门 + 第五闸门 | confirmation-manager.sh | F1.1 边界定义 | ✅ 已融合 |
| @GO.md 零例外 | boundary-enforcer.sh | F1.2 边界校验 | ✅ 已融合 |
| H-hotfix 三档 | hotfix-executor.sh | **F8 紧急修复流程** | ✅ 已融合（新增功能） |
| R-recall 记忆 | recall-executor.sh | **F9 项目记忆恢复** | ✅ 已融合（新增功能） |
| Secrets 保护 | secrets-guard.sh | **F6.3 Secrets 保护** | ✅ 已融合（增强功能） |

### 15.18 Claude Code 团队工程化落地

#### 15.18.1 七大核心构件体系

从强约束到弱约束的七层配置体系：

| 构件 | 职责 | 约束强度 |
|------|------|----------|
| CLAUDE.md | 项目级"记忆与约定" | 中 |
| rules/ | 团队必须遵守的"底线规则" | 强 |
| agents/ | 专用"子代理分工" | 中 |
| commands/ | 高频操作"一键化流程" | 中 |
| skills/ | 可复用的"方法论与领域知识" | 弱 |
| hooks/ | 关键节点"自动化护栏" | 强 |
| MCP 配置 | 外部工具"接入层" | 弱 |

**落地三原则**：
1. 按"项目独有/全局通用"划分存储位置
2. 用 rules 固化底线，用 commands 固化流程
3. 把"需要换脑子"的任务交给 Agent

**实施方法**：
1. 将 flow-kit 的配置文件按七层体系重新组织
2. Phase 边界定义放入 rules/，Phase 命令放入 commands/
3. 子代理任务放入 agents/，工作流方法论放入 skills/

**预期改进效果**：
- 配置管理清晰度提升 80%（七层分层明确）
- 团队协作效率提升 50%（底线和流程分离）
- 新成员上手时间减少 60%（配置即文档）

**兼容性分析**：
- 七层体系与 flow-kit 现有目录结构兼容
- 可渐进式迁移，不影响现有功能

#### 15.18.2 Karpathy 四原则

经过实战验证的四条核心原则：

| 原则 | 内容 | 解决的问题 |
|------|------|-----------|
| Think Before Coding | 先描述打算做什么、为什么 | 自信地犯蠢 |
| Simplicity First | 3 行能解决的别写 30 行 | 过度工程 |
| Surgical Changes | 只改需要改的，不顺手重构 | 范围蔓延 |
| Goal-Driven Execution | 持续对齐目标，偏离就停 | 跑偏 |

**实施方法**：
1. 将四原则写入 flow-kit 的核心 CLAUDE.md
2. 在 boundary-enforcer.sh 中添加四原则校验
3. 在 Phase 工作流中引用四原则作为行为准则

**预期改进效果**：
- 过度工程减少 60%（Simplicity First）
- 范围蔓延减少 80%（Surgical Changes）
- 方向偏离减少 70%（Goal-Driven Execution）

**兼容性分析**：
- 四原则作为行为准则，与现有系统完全兼容
- 可在 CLAUDE.md 中直接引用

#### 15.18.3 五个神级 Skill

五个经过验证的 Claude Code Skill：

| Skill | 功能 | 效果 |
|-------|------|------|
| Caveman | 输出极简化 | Token 节省 75% |
| Memory Compiler | Hook 自动捕获→提取知识→编译知识库 | 每天省 1 小时 |
| Paper2Code | arxiv URL → citation-anchored 代码 | 论文→可运行代码 |
| CLAUDE.md Optimizer | 自动分析项目→生成上下文 | 比手写更精准 |
| TestDrift | 检测代码变更→自动生成测试 | TDD 流程不中断 |

**实施方法**：
1. Caveman 压缩模式应用于 Phase 执行输出和审计报告
2. Memory Compiler 的 Hook 自动捕获集成到 session-state
3. TestDrift 集成到 Phase 4（开发执行）的 TDD 循环

**预期改进效果**：
- Token 消耗降低 50%（Caveman 压缩）
- 知识沉淀自动化率提升 80%（Memory Compiler）
- 测试覆盖率提升 30%（TestDrift 自动生成）

**兼容性分析**：
- 五个 Skill 作为可选增强，不影响核心功能
- Caveman 和 Memory Compiler 可渐进式集成

#### 15.18.4 7 天极简落地路线

团队从零到完整配置的 7 天路线：
- Day1: CLAUDE.md
- Day2: rules/
- Day3: /plan 命令
- Day4: /code-review 命令
- Day5: 提醒 Hook
- Day6: 一致性 Hook
- Day7: Agent 分工

**实施方法**：
1. 将 7 天路线适配为 flow-kit 的团队 onboarding 流程
2. 每天对应一个 Phase 或配置模块
3. 提供交互式教程引导

**预期改进效果**：
- 团队上手时间从 2 周缩短到 7 天
- 配置完整率从 40% 提升到 90%
- 团队协作规范一致性提升 70%

**兼容性分析**：
- 7 天路线作为 onboarding 指南，不影响核心功能
- 可在 GO.md 中添加 onboarding 入口

#### 15.18.5 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 七大核心构件 | 目录结构重组 | 项目配置 |
| Karpathy 四原则 | CLAUDE.md | 行为准则 |
| Caveman 压缩 | Token 优化 | F6 环境清理 |
| Memory Compiler | 知识沉淀 | F5 自我进化 |
| 7 天落地路线 | onboarding 指南 | GO.md |

### 15.19 自进化引擎失效分析与修复

self-improving-agent 被安装 43 万次，但真正形成完整进化闭环的用户不足 30%。五个关键节点在实践中全部断裂。

#### 15.19.1 五大断裂点诊断框架

| 断裂点 | 现象 | 根因 |
|--------|------|------|
| Hook 提醒软化 | 提醒每次都出现，但从未转化为实际写入 | Agent 忙完后不被明确要求就不会主动写 |
| 纠正未记录 | 用户说"用 pnpm 不是 npm"，Agent 回"好的记住了"但 LEARNINGS.md 为空 | 没有 WAL 协议捕获修正 |
| 错误未分类 | Docker build 失败写入了每日日志，但 ERRORS.md 为空 | 每日日志与 .learnings/ 未打通 |
| 重复计数失效 | 同一个错误遇到 4 次，Recurrence-Count 始终为 1 | 前三个断裂点导致每次都被当作"第一次" |
| 知识映射缺失 | VFM 通过后 Agent 不知道该写入哪个文件 | SKILL.md 有分类规则但没有映射表 |

**实施方法**：
1. 在 F5（自我进化机制）中建立 WAL 协议：检测到触发条件 → 立即写入 → 再响应用户
2. 增强 session-state.json 增加 learnings 统计字段（lastLearningsReview、pendingLearningsCount、promotedLearningsCount、learningsStatus）
3. 每日复盘 Cron 增加读取 .learnings/ 三文件、识别 Recurrence-Count ≥ 3 的条目、执行 VFM 评分

**预期改进效果**：
- 经验捕获率从 <30% 提升到 >90%
- 重复错误减少 80%
- 知识沉淀自动化率提升 70%

**兼容性分析**：
- WAL 协议可集成到现有的 Hook 系统中
- session-state.json 扩展字段向后兼容
- VFM 评分模型可独立运行

#### 15.19.2 VFM 评分模型

VFM（Value-Frequency-Maintainability）三维评分决定是否将学习条目提升为永久规则。对 infra 类型错误需要偏向性加权。

**实施方法**：
1. 在 evolution-engine.sh 中实现 VFM 评分算法
2. 对 infra 类型错误自动上浮 1 分（高频度和失败减少各 +1）
3. 评分 ≥ 阈值自动 promote 到 SOUL/AGENTS/TOOLS

**预期改进效果**：
- 规则质量提升 50%（只保留高价值规则）
- 规则膨胀减少 60%（低价值规则不提升）
- infra 错误预防率提升 40%

**兼容性分析**：
- VFM 评分作为 evolution-engine.sh 的增强模块
- 与现有的护栏系统互补

#### 15.19.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| WAL 协议 | 立即写入机制 | F5 自我进化 |
| VFM 评分 | 三维评分算法 | F5 进化配置 |
| 重复计数 | Recurrence-Count | F5 学习统计 |
| 知识映射 | 分类→文件映射表 | F5 知识管理 |

### 15.20 Multi-Pass Review 工程化

#### 15.20.1 多轮独立审查机制

Multi-Pass Review 通过多轮独立审查解决大模型代码审核的三种核心风险（漏报、误报、不确定性伪装成结论）。每轮输入一致、输出结构一致、审查规则一致，最终由聚合规则处理。

**实施方法**：
1. 在 Phase 6（代码审查）中引入 Multi-Pass 机制
2. 每轮独立审查输出结构化 JSON（severity/category/description/suggestion）
3. 聚合脚本化：共识问题优先进入主报告，单轮问题放入"待确认"区域

**预期改进效果**：
- 漏报率降低 60%（多轮交叉验证）
- 误报率降低 50%（独立审查避免上下文惯性）
- 审查可信度提升 70%（证据链 + 共识机制）

**兼容性分析**：
- Multi-Pass 机制可独立于现有审查流程运行
- 结构化输出格式与 REVIEW.md 兼容
- 聚合脚本可复用现有的 dispatch.sh 并行能力

#### 15.20.2 审查 Schema 标准化

severity 只保留 HIGH/MEDIUM/LOW 三级，category 短而稳定（logic/security/error-handling/compatibility/performance/concurrency/resource-lifetime/test-coverage），description 必须写证据链，suggestion 必须可执行、最小化。

**实施方法**：
1. 建立 flow-kit 审查 Schema 标准文件（review-schema.yaml）
2. 在 Phase 6 工作流中强制使用标准 Schema
3. 聚合脚本按 Schema 字段自动分类和排序

**预期改进效果**：
- 审查输出一致性提升 80%
- 开发者处理审查意见效率提升 50%
- 自动化聚合准确率提升 90%

**兼容性分析**：
- Schema 标准作为配置文件，不影响现有功能
- 与 REVIEW.md 模板兼容

#### 15.20.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 多轮独立审查 | dispatch.sh 并行 | F6 代码审查 |
| Schema 标准化 | review-schema.yaml | F6 审查配置 |
| 聚合规则 | execution-auditor.sh | F3 审计 |
| 证据链要求 | verification-before-completion | F6 审查标准 |

### 15.21 DESIGN.md 设计系统与 grill-me 需求访谈

#### 15.21.1 DESIGN.md 结构化设计约束

68.4K Star 项目通过 DESIGN.md 提供结构化设计规则（颜色/间距/圆角/字体层级/组件规范），让 AI 生成前端从"凭感觉"到"遵循规范"。

**实施方法**：
1. 在 Phase 4（开发执行）中引入 DESIGN.md 风格的设计约束文件
2. 为 flow-kit 项目定义颜色系统、间距系统、组件规范
3. 在代码生成时自动引用 DESIGN.md 约束

**预期改进效果**：
- UI 一致性提升 80%（统一设计约束）
- 返工率降低 50%（首次生成即符合规范）
- 设计到代码的转化效率提升 3 倍

**兼容性分析**：
- DESIGN.md 作为可选配置文件，不影响核心功能
- 与现有的 .specs 目录结构兼容

#### 15.21.2 grill-me 需求访谈机制

78K Star 的 grill-me 用 10 行 Markdown 实现需求澄清，基于 RTF 框架（Role-Task-Format），每次只问一个问题，逐层深入直到双方理解一致。

**实施方法**：
1. 在 Phase 1（需求澄清）中集成 grill-me 机制
2. AI 主动追问直到双方理解一致，每次只问一个问题
3. 自动生成 CONTEXT.md 共享语言文档

**预期改进效果**：
- 需求理解偏差减少 60%
- 需求澄清时间缩短 40%（结构化追问）
- 返工率降低 50%（前期充分对齐）

**兼容性分析**：
- grill-me 机制可集成到现有的 confirmation-manager.sh 中
- 与 Phase 1 工作流完全兼容

#### 15.21.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| DESIGN.md | 设计约束文件 | F4 开发执行 |
| grill-me | 需求访谈机制 | F1 需求澄清 |
| RTF 框架 | Role-Task-Format | F1 需求模板 |
| CONTEXT.md | 共享语言文档 | F1 需求产出 |

### 15.22 web-access 浏览器直连

#### 15.22.1 CDP 协议浏览器直连

web-access 是 Skill 而非 MCP——不只给工具，还给"怎么用工具"的完整方法论。三层能力：基础层（WebSearch/WebFetch/curl/Jina）、核心层（CDP 浏览器直连，登录态天然携带）、效率层（多任务并行分治）。

**实施方法**：
1. 在 Phase 0（研究）中集成 web-access 的并行调研能力
2. CDP 浏览器直连应用于 Phase 5（测试验证）的真实浏览器测试
3. "目标导向"的调度决策应用于 Phase 工作流的动态路由

**预期改进效果**：
- 调研效率提升 3 倍（并行子 Agent 调研）
- 登录态网站访问成功率提升 100%（CDP 直连）
- 动态渲染页面抓取准确率提升 90%

**兼容性分析**：
- web-access 作为可选 Skill，不影响核心功能
- CDP Proxy 需要 Node.js ≥ 22 环境

#### 15.22.2 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| CDP 浏览器直连 | 真实浏览器测试 | F5 测试验证 |
| 并行调研 | 子 Agent 分治 | F0 变更立项 |
| 目标导向调度 | 动态路由 | F1 路由判断 |

### 15.23 RTK 与 OpenWolf 成本优化

#### 15.23.1 RTK 终端输出净化

RTK 通过四层净化策略（智能过滤/分组聚合/智能截断/去重合并）实现 Token 节省 89%。30 分钟开发会话从 15 万 Token 压至 1.7 万。零侵入设计：单二进制文件、不改代码。

**实施方法**：
1. 在 Phase 4（开发执行）的工具调用层集成 RTK 净化
2. 配置全局 Hook 自动过滤终端输出
3. 在 session-state 中记录 Token 节省统计

**预期改进效果**：
- Token 消耗降低 80-89%
- 开发会话成本降低 60%
- 上下文利用率提升 3 倍

**兼容性分析**：
- RTK 作为独立二进制工具，零侵入集成
- 通过 Hook 机制与 flow-kit 无缝对接

#### 15.23.2 OpenWolf 三文件记忆系统

OpenWolf 通过 anatomy.md（项目地图）、cerebrum.md（学习记忆 + Do-Not-Repeat 列表）、buglog.json（Bug 账本）三文件实现 Token 节省 80%。相同任务裸 Claude CLI 消耗 250 万 Token，OpenWolf + Claude CLI 仅 42.5 万。

**实施方法**：
1. anatomy.md 项目地图替代重复的文件探索
2. cerebrum.md 的 Do-Not-Repeat 列表集成到 session-state.json
3. buglog.json 的 Bug 自动归档集成到 F5（自我进化机制）

**预期改进效果**：
- 重复文件读取减少 71%
- Token 消耗降低 65-80%
- Bug 修复效率提升 50%（自动归档复用）

**兼容性分析**：
- OpenWolf 通过 6 个 Hook 脚本工作，与 flow-kit Hook 系统兼容
- 三文件格式可适配 flow-kit 的 .flow-kit 目录

#### 15.23.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| RTK 净化 | 终端输出过滤 | F6 环境清理 |
| anatomy.md | 项目地图 | F0 变更立项 |
| cerebrum.md | Do-Not-Repeat 列表 | F5 自我进化 |
| buglog.json | Bug 自动归档 | F5 错误管理 |

### 15.24 Google Agent Skills

#### 15.24.1 六阶段开发链路

Google Gemini 团队主管 Addy Osmani 开源，23000+ Star。将《Software Engineering at Google》中的工程方法论封装为 20 个 Skill + 7 个 Slash 命令 + 3 个 Agent 人设。六阶段链路：`/spec` → `/plan` → `/build` → `/test` → `/review` → `/ship`。

**实施方法**：
1. flow-kit 的 Phase 体系与 Agent Skills 的六阶段链路对齐
2. 借鉴"三个预设 Agent 并行审查"机制增强 Phase 6（代码审查）
3. 跨平台支持（Claude Code/Cursor/Gemini CLI/Windsurf/Codex）的设计思路

**预期改进效果**：
- 开发流程标准化程度提升 50%
- 代码审查覆盖率提升 40%（三 Agent 并行）
- 跨平台兼容性提升 80%

**兼容性分析**：
- 六阶段链路与 flow-kit 的 Phase 0-7 高度契合
- Agent Skills 的"纪律约束"理念与 flow-kit 护栏机制一致

#### 15.24.2 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 六阶段链路 | Phase 体系对齐 | 全局架构 |
| 三 Agent 并行审查 | dispatch.sh | F6 代码审查 |
| 跨平台支持 | 配置适配 | 项目配置 |

### 15.25 Subagent Fork 机制

#### 15.25.1 Fork 模式上下文继承

`CLAUDE_CODE_FORK_SUBAGENT=1` 环境变量开启 Fork 模式，Subagent 继承父代理完整对话上下文，共享 prompt cache 前缀，第 2 到 N 个子代理输入成本明显更低。仍在隔离环境中运行，最终只返回结论性摘要。

**实施方法**：
1. Phase 4（开发执行）的 Subagent 默认启用 Fork 模式
2. context-timeline Hook 集成到 flow-kit 的上下文监控
3. Subagent 放置位置优先级（项目级 .claude/agents/ > 个人级 ~/.claude/agents/）

**预期改进效果**：
- Subagent 执行质量提升 40%（继承完整上下文）
- 子代理输入成本降低 50%（共享 prompt cache）
- 上下文管理可视化程度提升 100%

**兼容性分析**：
- Fork 模式通过环境变量控制，不影响现有 Subagent 行为
- context-timeline 作为可选 Hook，不影响核心功能

#### 15.25.2 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| Fork 模式 | 上下文继承 | F4 开发执行 |
| context-timeline | 上下文监控 | F6 环境清理 |
| Subagent 优先级 | 代理放置规则 | 项目配置 |

### 15.26 Command Center 目标管理层

#### 15.26.1 从管终端到管目标

当 Agent 足够强大时，真正的瓶颈不再是 AI 能不能完成任务，而是人类能不能高效管理多个任务。Command Center 通过迭代式 Kanban（任务在"Your Turn"和"Claude's Turn"之间反复弹跳）、智能上下文感知（自动回顾历史）、三种任务深度（Quick Task/Campaign/Deep Build）解决此问题。

**实施方法**：
1. flow-kit 的 Phase 管理借鉴"Your Turn / Claude's Turn"的迭代模式
2. 任务深度分级（Quick/Campaign/Deep Build）应用于 Phase 路由判断
3. 智能上下文感知（自动回顾历史）集成到 session-state 恢复机制

**预期改进效果**：
- 多任务管理效率提升 3 倍
- 上下文恢复准确率提升 60%
- 任务路由智能化程度提升 50%

**兼容性分析**：
- "管目标不管终端"的理念与 flow-kit 的 Markdown 驱动架构一致
- 任务深度分级可集成到现有的 Phase 路由系统

#### 15.26.2 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 迭代式 Kanban | Your Turn / Claude's Turn | F7 Ralph 循环 |
| 任务深度分级 | Quick/Campaign/Deep | F1 路由判断 |
| 智能上下文感知 | session-state 恢复 | F6 环境清理 |

### 15.27 OpenSpec SDD 方法论深度整合

#### 15.27.1 SDD 三阶段工作流

SDD（Spec-Driven Development）将开发流程划分为三个清晰的阶段，每个阶段由不同的工具框架主导：

| 阶段 | 主导框架 | 核心活动 | 产出物 |
|------|----------|----------|--------|
| 规划阶段 | gstack | office-hours → plan-ceo-review → plan-eng-review → plan-design-review | 评审通过的规划文档 |
| 开发阶段 | superpowers | brainstorming → write-a-prd → writing-plans → subagent-driven-development | 可运行的代码 |
| 发布阶段 | gstack | review → qa → ship | 已发布的版本 |

**实施方法**：
1. Phase 0-2（立项→需求→设计）借鉴 gstack 的评审体系，引入多层评审机制
2. Phase 3-5（拆解→开发→测试）借鉴 superpowers 的执行体系，引入 subagent 并行执行
3. Phase 6-7（审查→归档）借鉴 gstack 的发布体系，引入完整发布流水线

**预期改进效果**：
- 需求质量提升 50%（多层评审把关）
- 开发效率提升 40%（subagent 并行执行）
- 发布质量提升 60%（完整发布流水线）

#### 15.27.2 Spec-Driven Incremental Deploy

借鉴 TOC（Theory of Constraints）约束理论的增量部署方法论：

**设计阶段**：Generator-Evaluator 收敛循环，产出 checkpoint YAML
**实现阶段**：RED → GREEN → REVIEW → DEPLOY → COMMIT
**变更阶段**：先影响分析，再动手实现

**实施方法**：
1. 在 Phase 4（开发执行）中集成 RED → GREEN → REVIEW → DEPLOY → COMMIT 闭环
2. 在 auto-pilot.sh 中添加约束识别逻辑
3. 在 execution-auditor.sh 中添加 WIP（在制品）监控

**预期改进效果**：
- 未验证代码堆积减少 80%
- 部署失败率降低 60%
- 整体交付速度提升 30%（约束驱动优化）

#### 15.27.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| SDD 三阶段 | gstack + superpowers 分工 | 全局架构 |
| Generator-Evaluator | 设计收敛循环 | F2 架构设计 |
| RED-GREEN-REVIEW-DEPLOY-COMMIT | 开发闭环 | F4 开发执行 |
| TOC 约束识别 | WIP 监控 | F3 审计 |

### 15.28 Claude Code Task 系统与工具分级加载

#### 15.28.1 Task 系统架构

Claude Code 的 Task 系统提供了一种结构化的任务定义和执行机制，支持工具分级加载和上下文隔离。

**核心特性**：
- 任务定义：通过 YAML/JSON 定义任务结构，包括输入、输出、工具权限
- 工具分级：按权限级别加载工具（只读/写入/管理）
- 上下文隔离：每个 Task 拥有独立的上下文窗口
- 结果聚合：Task 执行结果自动聚合到主会话

**实施方法**：
1. 在 flow-kit 中引入 Task 系统作为 Phase 执行的底层机制
2. 每个 Phase 对应一个 Task 定义，明确工具权限和上下文边界
3. Task 执行结果自动写入 session-state.json

**预期改进效果**：
- 工具权限管理精细化提升 80%（分级加载）
- 上下文隔离性提升 100%（独立 Task 上下文）
- Phase 执行可追溯性提升 60%（结构化 Task 日志）

#### 15.28.2 工具分级加载策略

| 级别 | 工具集 | 适用场景 |
|------|--------|----------|
| L1（只读） | Read、SearchCodebase、Glob、LS | Phase 0-1（研究分析） |
| L2（写入） | L1 + Write、SearchReplace、Edit | Phase 2-4（设计开发） |
| L3（执行） | L2 + RunCommand（安全命令） | Phase 4-5（开发测试） |
| L4（管理） | L3 + RunCommand（全部）、DeleteFile | Phase 6-7（审查归档） |

**实施方法**：
1. 在 phase-boundaries.yaml 中为每个 Phase 配置工具级别
2. 在 boundary-enforcer.sh 中实现工具级别校验
3. 工具级别可配置为宽松/严格模式

**预期改进效果**：
- 越权操作减少 100%（工具级别硬约束）
- 安全风险降低 80%（敏感操作受限）
- 新用户误操作减少 70%（渐进式权限开放）

#### 15.28.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| Task 系统 | Phase 执行底层机制 | 全局架构 |
| 工具分级 | phase-boundaries.yaml | F1 边界定义 |
| 上下文隔离 | 独立 Task 上下文 | F4 开发执行 |
| 结果聚合 | session-state.json | F3 审计 |

### 15.29 四层防护 TDD 模型与验证证据机制

#### 15.29.1 四层防护模型

借鉴生产级交付实践中的多层防护理念，构建 flow-kit 的四层防护 TDD 模型：

| 层级 | 防护机制 | 拦截目标 | 触发时机 |
|------|----------|----------|----------|
| L1 | 类型检查/编译 | 类型错误、语法错误 | 代码变更后立即 |
| L2 | 单元测试 | 逻辑错误、边界条件 | 功能完成后 |
| L3 | 集成测试 | 模块间交互问题 | 集成阶段 |
| L4 | E2E 测试 | 端到端流程问题 | 发布前 |

**实施方法**：
1. 在 Phase 4（开发执行）中集成四层防护流水线
2. 每层防护有明确的通过标准和回退策略
3. 防护结果自动记录到 execution-auditor.sh

**预期改进效果**：
- 类型错误减少 90%（L1 前置拦截）
- 回归 bug 减少 70%（L2 自动验证）
- 集成问题减少 60%（L3 集成测试）
- 生产事故减少 80%（L4 E2E 测试）

#### 15.29.2 验证证据机制

借鉴 verification-before-completion 的"没证据不算完成"原则：

**证据类型**：
- 测试通过报告（覆盖率、通过率）
- 类型检查通过证明
- 代码审查通过记录
- 性能基准测试数据
- 安全扫描报告

**实施方法**：
1. 每个 Phase 完成时自动生成验证证据包
2. 证据包包含所有必要的验证结果
3. 证据不足时阻止 Phase 切换

**预期改进效果**：
- 假完成率降低 90%（证据驱动）
- 质量门禁有效性提升 80%（量化证据）
- 审计追溯性提升 100%（完整证据链）

#### 15.29.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 四层防护 | 测试流水线 | F5 测试验证 |
| 验证证据 | 证据包生成 | F3 审计 |
| 证据驱动 | Phase 切换门禁 | F1 边界定义 |
| 没证据不算完成 | verification-before-completion | F6 审查标准 |

### 15.30 经验萃取成果全景整合矩阵

#### 15.30.1 整合全景

基于 122 篇 Clippings 经验萃取成果，以下矩阵展示所有借鉴点与 flow-kit 的整合映射：

| 经验萃取领域 | 借鉴点数量 | 核心整合模块 | 预期综合效益 |
|-------------|-----------|-------------|-------------|
| AI 编程工作流框架 | 15 | Phase 边界定义、路由协议 | 流程规范化提升 80% |
| SDD 方法论 | 8 | 三阶段工作流、增量部署 | 交付质量提升 60% |
| Skill 设计 | 12 | 审查 Schema、需求访谈 | 开发效率提升 50% |
| 多 Agent 编排 | 10 | 并行审查、Subagent Fork | 审查覆盖率提升 200% |
| 自主执行 | 8 | Ralph 循环、Auto-Pilot | 自动化率提升至 90% |
| Token 优化 | 6 | RTK 净化、OpenWolf 记忆 | Token 节省 80% |
| 知识管理 | 10 | CE 知识复利、VFM 评分 | 重复错误减少 70% |
| 上下文工程 | 8 | 三层记忆、Context Timeline | 上下文利用率提升 3 倍 |
| 质量控制 | 10 | Multi-Pass Review、四层防护 | 缺陷率降低 80% |
| 团队协作 | 6 | 七大核心构件、7 天落地 | 上手时间缩短至 7 天 |
| 安全合规 | 4 | Secrets 保护、工具分级 | 安全风险降低 95% |
| 成本控制 | 5 | 模型分级、Prompt Cache | 成本降低 60% |

#### 15.30.2 整合优先级

| 优先级 | 整合内容 | 预计工作量 | 依赖关系 |
|--------|----------|-----------|----------|
| P0（必须） | Phase 边界定义 + 四闸门 | 2 周 | 无 |
| P0（必须） | Auto-Pilot 自动化执行 | 3 周 | Phase 边界定义 |
| P0（必须） | Ralph 循环 + Subagent Fork | 2 周 | Auto-Pilot |
| P1（重要） | Multi-Pass Review | 2 周 | Ralph 循环 |
| P1（重要） | CE 知识复利 | 2 周 | 无 |
| P1（重要） | Token 优化（RTK + OpenWolf） | 1 周 | 无 |
| P2（增强） | Context Engineering | 1 周 | 无 |
| P2（增强） | 全局路由协议 | 1 周 | Phase 边界定义 |
| P2（增强） | 自进化引擎修复 | 2 周 | CE 知识复利 |
| P3（优化） | 团队工程化落地 | 1 周 | 全部 P0-P2 |
| P3（优化） | 成本优化 | 持续 | 全部 P0-P2 |

#### 15.30.3 对 flow-kit 的启示

| 借鉴点 | 实现 | 集成位置 |
|--------|------|----------|
| 全景整合矩阵 | 借鉴点映射表 | 全局架构 |
| 整合优先级 | 分阶段实施路线 | 实施计划 |
| 综合效益评估 | 量化指标 | 验收标准 |

---

## 附录

### A. 术语表

| 术语 | 定义 |
|------|------|
| Phase | 开发流程中的一个阶段，共 9 个（Phase 0-8） |
| 边界校验 | 检查当前操作是否属于当前 phase 的职责范围 |
| 确认级别 | 操作前确认的严格程度：critical/important/normal |
| 自动化模式 | auto-pilot 的运行模式：interactive/silent/supervised |
| 执行计划 | 描述 Phase 0→7 执行顺序和配置的 JSON 文件 |
| 审计报告 | 执行完成后生成的质量和合规性报告 |
| 自我进化 | 系统根据历史执行数据自动优化配置的能力 |
| DAG | 有向无环图，用于任务依赖管理和执行顺序编排 |
| ADR | 架构决策记录，记录技术决策的背景和理由 |
| WAL | Write-Ahead Logging 协议，确保经验教训立即持久化 |
| VFM | Value-Frequency-Maintainability 三维评分模型 |
| CE | Compound Engineering，知识复利系统 |
| SDD | Spec-Driven Development，规格驱动开发 |
| RTK | Real-Time Killer，终端输出净化工具 |
| CDP | Chrome DevTools Protocol，浏览器调试协议 |
| TOC | Theory of Constraints，约束理论 |
| MCP | Model Context Protocol，模型上下文协议 |

### B. 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `phase-boundaries.yaml` | 新增 | Phase 职责边界定义 |
| `scripts/boundary-enforcer.sh` | 新增 | 边界强制执行器 |
| `scripts/confirmation-manager.sh` | 新增 | 操作确认管理器 |
| `scripts/status-display.sh` | 新增 | 状态显示面板 |
| `scripts/auto-pilot.sh` | 新增 | 自动化执行引擎 |
| `scripts/decision-engine.sh` | 新增 | 智能决策引擎 |
| `scripts/execution-auditor.sh` | 新增 | 执行审计器 |
| `scripts/prd-parser.sh` | 新增 | PRD 自动解析器 |
| `scripts/auto-start.sh` | 新增 | 一键启动命令 |
| `scripts/evolution-engine.sh` | 新增 | 自我进化引擎 |
| `scripts/auto-cleanup.sh` | 新增 | 环境自动清理器 |
| `scripts/ralph-runner.sh` | 新增 | Ralph Wiggum 循环执行器 |
| `scripts/safety-hooks.sh` | 新增 | 安全 Hook 安装器 |
| `scripts/task-runner.sh` | 新增 | 通用任务模板执行器 |
| `.flow-kit/auto-runner.yaml` | 新增 | 自主运行模式配置 |
| `.flow-kit/integration-config.yaml` | 新增 | 持续执行系统集成配置 |
| `.flow-kit/review-schema.yaml` | 新增 | 审查 Schema 标准 |
| `.flow-kit/self-evolution.yaml` | 新增 | 自进化引擎配置 |
| `.flow-kit/context-timeline.yaml` | 新增 | 上下文时间线监控 |
| `hooks/pre-tool-guard.sh` | 修改 | 集成自动清理逻辑 |
| `scripts/phase-executor.sh` | 修改 | 集成边界校验 |
| `lib/session-state.sh` | 修改 | 扩展字段支持 |
| `phases/*.md` | 修改 | 添加边界声明 |

### C. 版本兼容性说明

| 从版本 | 升级路径 | 注意事项 |
|--------|----------|----------|
| v3.3.0 | 直接升级 | phase-boundaries.yaml 默认宽松配置，不影响现有工作流 |
| v3.2.x | 先升级到 v3.3.0 | 需要先适配 session-state v2 格式 |
| v3.0.x | 先升级到 v3.3.0 | 需要先适配新的命令注册机制 |

### D. 参考项目

本文档基于以下项目的经验萃取成果编写：

| 项目 | 来源 | 核心贡献 |
|------|------|----------|
| superpowers-zh | GitHub | 编程开发能力增强方法、workflow-runner YAML 编排、executing-plans 审查检查点 |
| agency-agents-zh | GitHub | 215 个 AI 专家角色定义、DAG 工作流编排、ADR 架构决策记录 |
| mattpocock/skills | GitHub | grill-me 需求澄清、tdd 红-绿-重构、diagnose 系统化诊断、caveman 极简通信 |
| Ralph Wiggum | GitHub/nicepkg | 每次迭代全新 AI 实例、三级反馈闭环、prd.json 状态追踪 |
| Karpathy AutoResearch | GitHub/karpathy | 三文件架构、多 Agent 交叉审核、5 维度加权评分 |
| Compound Engineering | GitHub | 知识沉淀机制、12 子代理并行审查、learnings-researcher 自动引用 |
| rihebty/flow-kit | GitHub | 四闸门机制、@GO.md 零例外路由、H-hotfix 三档响应 |
| Claude Code 团队落地 | 社区实践 | 七大核心构件、Karpathy 四原则、7 天极简落地路线 |
| self-improving-agent | 社区实践 | 五大断裂点诊断框架、VFM 评分模型、WAL 协议 |
| Multi-Pass Review | 社区实践 | 多轮独立审查机制、审查 Schema 标准化 |
| OpenWolf | 社区实践 | 三文件记忆系统、Do-Not-Repeat 列表、Bug 自动归档 |
| RTK | 社区实践 | 终端输出净化、四层净化策略 |
| Google Agent Skills | Google/Addy Osmani | 六阶段开发链路、三 Agent 并行审查 |
| Subagent Fork | Claude Code | Fork 模式上下文继承、context-timeline 监控 |
| Command Center | 社区实践 | 迭代式 Kanban、任务深度分级 |
| web-access | 社区实践 | CDP 浏览器直连、并行调研 |
| DESIGN.md | 社区实践 | 结构化设计约束 |
| code-review-graph | GitHub | 知识图谱精准上下文、爆炸半径分析 |
| Alan 全局路由协议 | 社区实践 | Superpowers 与 gstack 分工、AGENTS.md 优先级链 |
| SDD 三阶段工作流 | 社区实践 | gstack 规划 + superpowers 开发 + gstack 发布 |
| OMC vs Karpathy | 社区实践 | 硬控制与软约束三层融合模型 |
| OpenSpace 自进化引擎 | 社区实践 | AUTO-FIX/AUTO-IMPROVE/AUTO-LEARN 三机制 |
| Skill-creator 评估体系 | 社区实践 | MANDATORY/RECOMMENDED/OPTIONAL 三级指令 |

---

> **文档结束**
>
> 本文档由 Claude Code 基于 flow-kit v3.3.0 项目分析生成，融合经验萃取成果中的最佳实践与关键洞见
>
> **生成日期**：2026-05-16
>
> **版本**：v3.6.0（综合方案）