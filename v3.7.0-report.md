# v3.7.0 开发报告

## 1. 执行摘要

| 指标 | 值 |
|------|-----|
| 版本 | v3.6.0 → v3.7.0 |
| 任务数 | 22（全部完成） |
| 测试 | 138 → 213（+75, +54%） |
| 新增文件 | 15 |
| 修改文件 | 8 |
| ShellCheck | 零 error |
| 开发方式 | Ralph Loop 自动执行 + flow-kit 自身能力 |

## 2. ralph-driven-dev 评估

### 优势
- **PRD→原子任务拆分**精准，22 个任务依赖关系无死锁
- **并行波次设计**合理，Wave 1（1.1/2.5/1.4/4.2-4.5）确实无依赖可并行
- **TODO.md 状态机**简单可靠，打钩即完成
- **PROMPT.md 宪法**有效约束了每轮只做一个任务

### 不足
- **任务文件名不一致**：实际文件名与 TODO.md 中的路径有偏差（如 `1.3-lock-timeout.md` vs `1.3-f10-lock-timeout.md`），打钩时需 grep 确认
- **环境预检遗漏 ShellCheck**：未检测到 shellcheck 未安装，但 shellcheck-all.sh 任务可降级处理
- **max-iterations 估算偏高**：74 次上限实际用了约 5 轮迭代
- **PROMPT.md 技能路由未生效**：声明了"打钩前用 stop-quality-gate"但 Ralph Loop prompt 中没有对应调用

## 3. flow-kit 自我验证评估

### 有效的能力
- **error-handler.sh 自举验证**：setup_trap 被自己的测试覆盖，证明库可以用自己测试自己
- **TDD 红绿灯**：先写测试→确认 RED→实现→确认 GREEN，节奏稳定
- **指标日志自用**：metrics_log_event 写入 phase_complete 事件，flow-kit 自己消费自己的指标

### 暴露的问题
- **hooks 测试缺失**：1.4 任务才发现 hooks 目录下从无测试
- **dispatch.test.js 是 CommonJS**：与其他 ES Module 测试文件风格不一致
- **auto-pilot.sh 的 metrics 埋点**在非 set -e 环境下 `_METRICS_AVAILABLE` 变量可能未初始化

### "双重验证"实际执行情况

| 环节 | 计划 | 实际 |
|------|------|------|
| Ralph Loop 执行 | ✅ 循环推进 22 任务 | ✅ 有效 |
| flow-kit Phase 工作流指导开发 | 计划中 | ❌ 未触发 |
| flow-kit hooks 自动格式化/质量门禁 | 计划中 | ❌ 未参与验证 |
| auto-pilot 追踪进度 | 计划中 | ❌ 未使用 |
| /verification-before-completion | 计划中 | ❌ 未调用 |
| /requesting-code-review | 计划中 | ❌ 未调用 |

**完成度：约 50%**。ralph-driven-dev 执行力验证通过，但 flow-kit 自身工作流未被真正用来辅助开发。

**根因**：
1. Ralph Loop 单线程独占——循环每次 turn 做一个任务立即继续，无间隙让 flow-kit 审查
2. PROMPT.md 技能路由只写了声明，没在 Ralph Loop prompt 中嵌入实际调用
3. hooks 是被动触发——post-edit-format/stop-quality-gate 只做格式化和提醒，没有拦截质量问题
4. "双方互相测试"需要两个独立进程——实际只有一个 Claude Code 会话

## 4. 踩坑记录

### 4.1 error-handler.sh 的 `set -e` 感染
**问题**：error-handler.sh 头部有 `set -euo pipefail`，source 后强制开启 `set -e`，测试中预期返回非零的命令直接杀掉脚本。
**解决**：测试 runShell 模板在 source 后加 `set +e`。
**教训**：库文件不应设 `set -e`，应由调用者决定。这是 Shell 库设计的基本原则——库是工具，不是执行者。

### 4.2 LIB_DIR 双重路径
**问题**：`vitest` 的 `process.cwd()` 是 `flow-kit/`，`join(cwd, 'flow-kit/lib')` 产生双重路径。
**解决**：`join(cwd, 'lib')`。
**教训**：vitest 配置中 cwd 已经是项目根。每次新测试文件复制旧文件时必须检查路径假设。

### 4.3 bash -c 引号嵌套
**问题**：`execSync('bash -c \'...\')` 在复杂 shell 脚本中引号嵌套崩溃。
**解决**：写临时 .sh 文件再 `bash script.sh`。
**教训**：Shell 测试永远写文件，不要用 `-c`。这是 v3.5.0 就踩过的坑，v3.7.0 再次确认。

### 4.4 is_lock_stale 的进程检查优先
**问题**：测试写 PID:99999（不存在），is_lock_stale 因 `kill -0 99999` 失败返回 stale=true，但测试期望未超时=false。
**解决**：改用 `$$`（当前 shell PID，存活）。
**教训**：锁超时逻辑中进程存活检查应先于时间检查。这是设计决策——一个死进程的锁永远应该被视为过期。测试必须理解这个语义。

### 4.5 cleanup.sh 的 trap 冲突
**问题**：error-handler.sh 的 setup_trap 也注册 EXIT trap，与 cleanup.sh 的 with_cleanup 冲突。Bash 的 trap EXIT 只有最后一个 `trap ... EXIT` 生效，前面的会被覆盖。
**解决**：用 `_CLEANUP_TRAP_SET` 标志防重复注册。acquire_lock/release_lock 独立于 cleanup 机制。
**教训**：多个 EXIT trap 注册在 Bash 中不叠加。解决方案是统一一个 trap dispatcher，或用标志位互斥。

### 4.6 generate-commands.sh 命令文件名映射
**问题**：commands/ 目录下 health → M-health.md、scan → I-intel-scan.md、p0 → p0-approval.md，文件名和命令名不直接对应。
**解决**：flow-kit.sh 中硬编码 case 映射。
**教训**：命令文件命名应建立规范——要么统一前缀，要么维护映射表。当前混合命名是历史遗留。

### 4.7 auto-pilot.sh metrics 埋点变量作用域
**问题**：`_METRICS_AVAILABLE` 在 source 链中可能未被正确初始化（set -u 环境下引用未定义变量会报错）。
**解决**：source 时加 `2>/dev/null` 降级，变量用 `${:-0}` 默认值。
**教训**：可选依赖的初始化必须是防御性的——不能假设 source 成功。

### 4.8 Ralph Loop 与 hooks 的冲突
**问题**：Ralph Loop 的 stop hook 在每轮结束时触发，但 flow-kit 的 stop-quality-gate hook 也在 Stop 事件触发。两个 stop hook 可能互相干扰。
**解决**：实际开发中 Ralph Loop 的 stop hook 先触发，flow-kit 的 hook 没有产生实质拦截。
**教训**：多个 Stop hook 注册时需要明确优先级和交互方式。建议合并为单一 hook。

## 5. 经验清单

### 可复用模式

1. **临时 .sh 文件测试模式**
   ```js
   writeFileSync(scriptFile, `#!/bin/bash\nset -uo pipefail\nsource "lib/error-handler.sh"\nset +e\n${body}`);
   execSync(`bash ${scriptFile} 2>&1`);
   ```
   避免 bash -c 嵌套，支持复杂 shell 脚本测试。

2. **全局变量 trap 回调**
   ```bash
   _ERROR_HANDLER_MODULE="unknown"  # 全局
   setup_trap() { _ERROR_HANDLER_MODULE="$1"; trap '_handler' ERR; }
   ```
   trap 回调中的变量必须是 global，local 会出作用域。

3. **`_LOADED` 防重复 source**
   ```bash
   if [[ -z "${_MODULE_LOADED:-}" ]]; then
       readonly _MODULE_LOADED=1
       # 定义函数和变量
   fi
   ```
   防止重复定义 readonly 常量报错。

4. **JSONL 日志格式**
   - 追加写入（`>>`），行级原子
   - grep 可查、jq 可解析
   - flock 防并发交叉写入
   - 损坏行降级跳过（`[[ -z "$etype" ]] && continue`）

5. **锁文件 PID+TIMESTAMP 双检查**
   ```bash
   lock_file: PID:12345\nTIMESTAMP:1715865600\n
   # 1. kill -0 $PID → 进程死→过期
   # 2. now - timestamp > timeout → 超时→过期
   ```
   两个维度独立检查，任一满足即过期。

6. **静默降级模式**
   ```bash
   _METRICS_AVAILABLE=0
   if [[ -f "$LIB_DIR/metrics-logger.sh" ]]; then
       source "$LIB_DIR/metrics-logger.sh" 2>/dev/null && _METRICS_AVAILABLE=1 || _METRICS_AVAILABLE=0
   fi
   ```
   可选依赖永远不阻断主流程。

7. **原子文件写入（缓存）**
   ```bash
   tmp=$(mktemp "${cache}.XXXXXX")
   echo "$content" > "$tmp"
   mv "$tmp" "$cache"
   ```
   先写临时文件再 mv，防止并发读到半写状态。

### 需避免的反模式

1. **bash -c 复杂脚本**：引号地狱，永远写文件
2. **trap EXIT 多处注册**：Bash 只有最后一个生效，需要协调
3. **local 变量在 trap 回调中**：作用域消失，必须用全局变量
4. **库文件头设 set -e**：调用者不应被库强制退出策略
5. **测试中用不存在 PID 测"未过期"**：kill -0 失败=进程死=stale
6. **vitest 测试中 cwd 假设**：cwd 是 vitest.config.ts 所在目录，不是项目根
7. **PROMPT.md 声明但不执行**：技能路由写了但不嵌入 Ralph prompt 等于没写
8. **复制旧测试文件不改路径假设**：LIB_DIR/cwd 等路径必须每次检查

### 架构层面教训

1. **单进程循环无法做"双重验证"**：Ralph Loop 占用全部 turn，flow-kit 的交互式命令无法并行介入。需要双进程或异步 review 机制。
2. **PRD 拆分的粒度要匹配单轮上下文**：v3.7.0 每个任务≈50-100 行代码变更，适合单轮完成。如果单个任务超过 200 行，应进一步拆分。
3. **测试文件风格要统一**：ES Module vs CommonJS 混用增加维护负担。新文件必须用 ES Module。
4. **命令注册 vs 实现解耦**：generate-commands.sh 注册了 /metrics 和 /recall，但它们实际是独立脚本。命令注册中心不应假设实现方式。

## 6. 双重验证矩阵复盘

| 维度 | ralph-driven-dev | flow-kit | 差距原因 |
|------|-----------------|----------|----------|
| 任务拆分 | ✅ PRD→22 原子任务 | — | — |
| 开发指导 | ✅ PROMPT.md TDD 规则 | ❌ Phase 工作流未触发 | 单进程独占 |
| 代码质量 | ✅ ShellCheck + 测试 | ❌ hooks 未拦截 | hooks 是被动的 |
| 进度追踪 | ✅ TODO.md 打钩 | ❌ auto-pilot 未用 | 没有集成点 |
| 验证 | ✅ 每任务测试通过 | ❌ /verification 未调用 | prompt 未嵌入 |
| 评审 | — | ❌ /code-review 未调用 | prompt 未嵌入 |
| 归档 | — | ❌ /archive 未调用 | 最后一步未触发 |

**评分**：ralph-driven-dev 8/10，flow-kit 自验证 2/10。

## 7. v3.8.0 建议

### 范围建议
1. **scripts/ 子目录迁移**：按 4.3 的 README 规划，dispatch/quality/workflow/utils 四个分组，旧路径 symlink 过渡
2. **E2E 测试完善**：3 个骨架替换为真实端到端测试
3. **dispatch.test.js ES Module 迁移**：统一测试风格
4. **error-handler.sh 移除 set -e**：库文件不应强制
5. **metrics.jsonl 轮转**：集成 paths.sh 的 rotate_logs
6. **ralph-loop 插件深度对接**：从 /ralph 命令直接启动循环
7. **test 辅助函数库**：抽取 runShell/writeMetricsLine 等到 tests/helpers/

### 如何真正实现双重验证

1. **Ralph Loop 每轮末尾调用 `/flow-kit:phase-6` 做代码审查**——审查结果写文件，下一轮读取反馈
2. **auto-pilot 状态追踪集成到 PROMPT.md**——每轮更新 auto-plan.json，Ralph 读取推荐下一步
3. **post-edit-format hook 实际验证**——检查缩进、set -euo pipefail、函数签名格式
4. **stop-quality-gate hook 拦截打钩**——验证测试覆盖后才允许 `[x]`
5. **双进程架构**：Ralph Loop + 一个独立 review agent 并行运行，共享 TODO.md 协调

### flow-kit 改进建议
1. **CI 集成 shellcheck-all.sh**：作为 lint 步骤
2. **自动版本号更新**：commit hook 检查 VERSION/CHANGELOG/CLAUDE.md 一致性
3. **命令文件命名规范化**：统一为 `{command}.md`，去除 M-/I- 前缀
4. **trap dispatcher 机制**：统一管理多个 EXIT trap 回调，避免覆盖

### ralph-driven-dev 技能改进建议
1. **任务文件名标准化**：TODO.md 和 tasks/ 文件名必须精确匹配，生成时校验
2. **环境预检增强**：检测 shellcheck、node 版本、npm 可用性
3. **自适应 max-iterations**：基于任务数动态计算，当前 N×3+5 过于保守
4. **PROMPT.md 技能路由可执行化**：不只是声明，要生成对应的 shell 命令嵌入 Ralph prompt
5. **每轮 review 检查点**：在打钩前强制调用一次代码审查，审查失败则回退

## 8. 数字摘要

```
v3.6.0 → v3.7.0
─────────────────
任务:       22/22 (100%)
测试:       138 → 213 (+54%)
新增文件:   15
修改文件:   8
新增代码行: ~800 (lib+scripts)
新增测试行: ~600
Ralph 轮次: ~5 有效轮
双验证完成: 50%
最大踩坑:   set -e 感染 + trap 冲突
最有效模式: TDD 红绿灯 + 临时 .sh 文件测试
最无效模式: PROMPT.md 技能路由声明
```
